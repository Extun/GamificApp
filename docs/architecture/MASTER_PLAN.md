# MASTER_PLAN

# Objetivo

Roadmap de GamificApp: fases, backlog priorizado, dependencias y riesgos. El estado detallado de cada módulo vive en `CURRENT_STATE.md`.

# Última actualización

2026-07-30 (**SE CIERRA EL BLOQUE P2 DE LA FASE RELEASE CANDIDATE: 4 DE LOS 8 ÍTEMS, TRAS REEVALUAR EL BACKLOG A DOS DÍAS DE LA ENTREGA.** Implementados **P2-8** (estados de carga/error del Home del docente), **P2-2** (WCAG 2.2 SC 2.4.11), **P2-3** (`aria-expanded` del disclosure del PIN) y **P2-5 en su variante segura** (la racha 🔥 gana `role="img"` + `aria-label`, **sin texto visible nuevo ni cambio de layout**, por restricción expresa de Fabrizio). **P2-1, P2-4 y P2-7 pasan oficialmente a Post v1.0** por beneficio nulo para la 1.0 o riesgo desproporcionado; **P2-6 queda aprobado solo si sobra tiempo y NO está implementado.** Cinco archivos, cero arquitectura, BD, migraciones, permisos, XP, ranking, misiones o contratos de juegos.

**CORRECCIÓN AL PROPIO PLAN DE LA FASE: P2-2 nombraba dos contenedores y solo uno era real.** `.sidebar-nav` con `.sidebar-footer` es **premisa falsa, medida**: el footer es **hermano** del contenedor con scroll, no hijo, y sus cajas se tocan sin solaparse (**0 px de solape**); un `scroll-padding` ahí habría sido un no-op y **no se añadió**. El caso real —la barra de publicar del editor sobre `.contenido`— **sí se reprodujo y se corrigió**: recorriendo con foco los 24 controles que preceden a la barra, **4 tapados → 0 a 1280** y **3 tapados → 0 a 375**, donde el peor («Vista previa») quedaba **162,4 px bajo una barra de 159 px, oculto por completo**. **Nota de método que conviene no perder:** la primera medición dio 0 tapados incluso sin la regla porque reiniciaba el scroll antes de cada foco; **el fallo solo aparece tabulando en secuencia**, que es como se navega de verdad.

**P2-8 es el mismo defecto de clase que P1-1, en el otro panel:** el Home del docente afirmaba «Hoy tienes 0 materias y 0 estudiantes» y «Aún no tienes materias asignadas» mientras las peticiones viajaban, y `propagarError` —el opt-in que nació en P1— es otra vez lo que hace **alcanzable** el estado de error. Alcance real menor que el del plan: la «materia del estudiante» que listaba ya la había cubierto P1. Build limpio y **lint 29 (26+3) = línea base exacta**. Detalle, mediciones y límites de lo probado en `CURRENT_STATE.md`.)

2026-07-30 (**ENTRA LA FASE RELEASE CANDIDATE Y SE CIERRA SU BLOQUE P1** — commit `b5f1dc3`. Se terminan las auditorías y se trabaja contra un **backlog único** (`docs/audit/RC-Plan-Limpieza-Y-Backlog-v1.0.md`, insumo `docs/audit/Auditoria-UX-UI-Release-Candidate-v1.md`). **13 mejoras de presentación**, cero arquitectura, BD, migraciones, permisos, XP, ranking, misiones o contratos de juegos. **Cierra los ítems 29, 35, 36 y 37.** Lo más relevante: **modal de cambio de PIN** en sustitución de dos `window.prompt` encadenados —el proyecto queda a **cero `window.prompt`**— autorizado por Fabrizio **sin spec** porque el cambio es de piel y no de comportamiento; **tipografía autoalojada** (Inter + Poppins, 4 archivos, 76 kB en `public/fonts/`), que era requisito de facto del instalador Windows porque sin internet la identidad visual cambiaba; **estados de carga/error reales** en el panel del estudiante, que antes afirmaba «Todavía no hay juegos» mientras la petición viajaba y para siempre si fallaba la red; y **tres correcciones de contraste AA** (podio del ranking oro 2,15 → 8,31 y plata 2,56 → 6,96; `--color-text-muted` 4,34 → 5,01).

**LA VERIFICACIÓN FUNCIONAL PAGÓ SOLA: DOS BUGS QUE LA LECTURA DE CÓDIGO NO HABÍA VISTO.** (1) **Crítico y preexistente:** equivocarse al teclear el PIN actual **cerraba la sesión del estudiante en silencio** (401 de `cambiar-pin` + `authFetch` interpretándolo como sesión caducada). Corregido en el cliente con `conservarSesionEn401` opt-in; el arreglo semánticamente limpio —400 en el servidor— queda como ítem **63**, porque toca §10 y el contrato de la API. (2) **Introducido en P1:** el estado de error era **inalcanzable** porque `retosService` y `materialesService` capturaban el fallo y devolvían `[]`; resuelto con `propagarError` opt-in, sin cambiar el comportamiento de ningún otro consumidor.

**CORRECCIÓN DE LÍNEA BASE DE LINT: SIGUE SIENDO 29 (26+3), Y SOLO SE MIDE SOBRE `src` Y `server`.** Durante P1 se reportó **58** y se atribuyó a una actualización de plugin: **ambas cosas eran falsas**. El utillaje es idéntico al lock (`eslint 10.4.1`, `react-hooks 7.1.1`, `react-refresh 0.5.2`). La causa real es que `npm run lint` ejecuta `eslint .` y **recorre `release/GamificApp/`, la copia completa del código que genera el empaquetador** —`.gitignore` no aplica al flat config—, contando cada problema dos veces: desglose por regla exactamente al doble y 14 rutas de la fuente + 14 de `release/`. **`npx eslint src server` = 29 (26 errores + 3 warnings).** La conclusión de P1 no cambia (cero problemas nuevos: el ×2 afectaba igual a las dos mediciones). Anotado como ítem **62**; **no se tocó `eslint.config.js`**.

**LIMPIEZA: CASI TODA LA LISTA ERA UN NO-OP.** De las 12 categorías «Eliminar» de `REPOSITORY_CLEANUP.md`, **10 no están versionadas** (ya las cubre `.gitignore`) y borrarlas del disco habría destruido el entorno justo antes de validar el instalador. **Se borraron solo 4 assets sin referencias**; `asistenteIA.jsx`/`respuestaIA.jsx` y la evidencia de tesis se conservan por decisión de Fabrizio. *(Nota posterior: en la limpieza final del Release Candidate se retiraron `tools/build_auditoria_docx.py` y el DOCX de auditoría de la raíz, y también el propio `REPOSITORY_CLEANUP.md` que se cita aquí; `asistenteIA.jsx`/`respuestaIA.jsx` siguen en el repositorio. Ver `CURRENT_STATE.md`, entrada del 2026-07-30.)*

**P2 NO IMPLEMENTADO**, pendiente de autorización: pausa de toasts en hover/foco, `scroll-padding` para WCAG 2.2 SC 2.4.11, `aria-expanded` en el disclosure del PIN, `<title>` por vista, la racha 🔥 que solo se explica en un `title=""`, bienvenida de primer uso del docente, densidad de filtros de la Biblioteca y estados de carga en las vistas restantes. Detalle completo en `CURRENT_STATE.md`.)

2026-07-29 (**SPEC-016 Y SPEC-017 VALIDADAS EN PRODUCCIÓN — LAS DOS SPECS DEL REVISOR, EN SOLO LECTURA Y CON UNA ÚNICA GENERACIÓN DE IA AUTORIZADA.** **SPEC-017 🟢:** el panel «Gestión de juegos» carga contra Aiven con los **3 estados exactos** de la spec y los **7 tipos en activo**, con conteos reales (30 actividades, 6 publicadas) que prueban que consultó la BD; **Verdadero o Falso, el juego de demostración de extensibilidad, está vivo en producción**. **SPEC-016 🟡→validada funcionalmente:** `GET /api/admin/ia/configuracion` da `gemini` / modelo automático / `origen: entorno` con **Gemini `configurado: true`** y **cero secretos**; y una **única** llamada a `POST /api/ia/generar` —el endpoint del modal del docente, que **no persiste**— devolvió **200 en 2.254 ms** un quiz de 3 preguntas que **pasó `validarConfig`**, sin guardarlo. **Cero escrituras, cero estados cambiados, cero configuración persistida, y el consumo de IA limitado a propósito a una generación.** **Migraciones 013/014: encadenadas al arranque y presumiblemente ejecutadas; existencia de las tablas en Aiven NO confirmada directamente** (ambas features degradan en silencio ante tabla ausente). Registrado el **ítem 61 [P3]**, deriva de contrato de `modelosSugeridos`, **sin corregir**. Detalle y límites en `CURRENT_STATE.md`.)

2026-07-29 (**AUDITORÍA DE PRODUCCIÓN DEL COMMIT `2718897`: DESPLIEGUE CONFIRMADO, CORS CORREGIDO POR CONFIGURACIÓN Y COSTE DE SPEC-019 MEDIDO EN AIVEN.** **Frontend `2718897` probado byte a byte** (mismo MD5 `5df100ff…41a9` que un build local de HEAD, en los dos dominios); **backend `2718897` por inferencia** de `Access-Control-Max-Age: 86400`, que solo existe desde este commit. **Incidencia P2 encontrada y cerrada: `gamificapp.vercel.app` no estaba en la allowlist de CORS** — diagnosticada con control positivo (`no-cors` sí respondía, `cors` no), **sin regresión de `2718897`** y **sin tocar código**, porque `server.js:75-78` ya soportaba varios orígenes separados por comas; Fabrizio corrigió `CORS_ORIGIN` en Render. Reverificado: los **dos** orígenes legítimos con ACAO propio y `maxAge` 86400, y **8 orígenes de control rechazados**, sin comodines. **Prueba autenticada (ejecutada por Fabrizio, sin compartir credenciales): 60/60 en 200, cero 401, cero 503**, con Admin Principal y Docente. **Coste, con los tres números separados:** delta end-to-end medido **+37,6 / +40,7 ms** (contiene el muro **y** el trabajo propio de `/api/materias`), estimación por proxy de un round-trip a Aiven **+19,8 / +20,0 ms** (reproducida antes y después del reinicio), y **la cuota exacta de SPEC-019 NO está aislada** (exigiría desplegar un build sin ella). **La revocación NO se probó en producción** (no autorizada): sigue validada solo en local. Detalle, limitaciones y evidencia en `CURRENT_STATE.md`.)

2026-07-29 (**SPEC-019 — REVOCACIÓN INMEDIATA DE ACCESO: aprobada, implementada y verificada.** Cierra el ítem **59**. `autenticar` revalida contra la BD; **401** y `authFetch` cierra sesión solo. **Un archivo (`server/middleware/auth.js`), sin migración, sin frontend.** **Fail-closed probado apagando MySQL**: 503 con cero datos, `/api/health` en 200, y el mismo token recuperado al volver la BD; única excepción tolerada `ER_BAD_FIELD_ERROR`, justificada con evidencia. **9/9 escenarios pasan y 0 × 401 en uso normal.** Coste +0,35 ms de mediana en local, **pendiente de remedir en Aiven**. Build limpio y lint 29 = baseline.)

2026-07-29 (**CIERRE DE LAS OBSERVACIONES ABIERTAS DEL EXPERTO.** De los 5 diferidos que quedaban: **55, 56 y 57 implementados y medidos** (3 peticiones de retos → 1 al abrir una materia; `cors` con `maxAge: 86400`; el sondeo del panel Admin acotado a la sección visible, 4 → 2 en Inicio y 4 → 1 en Materias con el admin restringido), el **58 cerrado como NO defecto** tras medir la barra de XP con la pestaña visible (30,0 % exacto; el síntoma era el reloj de animaciones congelado, igual que el falso «0/100»), y el **59 elevado a SPEC-019** —revocación inmediata de acceso— **pendiente de aprobación porque toca §10**. Tres archivos de código: `server/server.js`, `DashboardEstudiante.jsx` y `AdminDashboard.jsx`; **cero BD, cero migraciones, cero autenticación, cero XP, cero permisos, cero instalador**. Build limpio y **lint 29 = baseline exacto**. Del lado del revisor, **los ítems 24 y 27 siguen fuera por decisión de Fabrizio**, y el **33 se deja como está** tras medirlo (3,001:1, cumple el 3:1 que le aplica por ser un SVG). Detalle e incidente de la sesión en `CURRENT_STATE.md`.)

2026-07-29 (**FASE LOCAL 2.2 — MYSQL PORTABLE, INTEGRADO EN INSTALAR / INICIAR / DETENER.** **MySQL deja de ser un requisito previo.** Descripción exacta de lo que hay ahora: **«Instalación local automatizada para Windows que no exige ni Node ni MySQL instalados; la base de datos la pone la propia aplicación»**. Módulo nuevo `instalador/mysql.ps1` + `instalador/mysql-clave-root.mjs`; cambios acotados en `instalar/iniciar/detener.ps1` y `bd-verificar.mjs`; **cero archivos de la aplicación tocados**. **Separación clave: `runtime\mysql` = binarios reemplazables, `%LOCALAPPDATA%\GamificApp\` = datos permanentes** (el datadir nunca vive dentro del runtime). Puerto **3308** con reserva **3309-3315**; **3306 y 3307 quedan fuera de la lista a propósito**, y un puerto ocupado por un proceso ajeno se salta sin tocarlo nunca. `bind-address=127.0.0.1`, `mysqlx=0`, `utf8mb4_spanish_ci`. **root recibe una contraseña aleatoria de 24 caracteres en los segundos siguientes al `--initialize-insecure`**, verificada abriendo una conexión nueva, guardada con ACL restringida fuera del repositorio y transmitida siempre por variables de entorno (incluido `MYSQL_PWD` para `mysqladmin shutdown`), nunca por argumentos. **El hallazgo del doble proceso de mysqld se midió ANTES de escribir la lógica** (el dueño del socket es el proceso HIJO, no el que devuelve `Start-Process`): la identificación exige `ExecutablePath` exacto **y** mención de nuestro `my.ini`, más una prueba independiente por `@@datadir`. **Probado A-J**: primera instalación (42 s, 0→11→18 tablas, login real de los 3 roles), persistencia de un marcador creado por la API real, reinstalación sin reinicializar ni perder credenciales, fallback de puerto en ambos sentidos con el proceso ajeno vivo, doble `Iniciar` sin duplicar, `Detener` con cierre ordenado certificado por el `error.log`, Docker 3307 y `gamificapp_dev` de desarrollo **idénticos**, cero secretos en Git/logs/argumentos, build limpio y **lint 29 = baseline exacto**. **También se probó la reconstrucción tras reemplazar la carpeta del proyecto**: borrando `server/.env`, `.run/` y `CREDENCIALES.txt`, el instalador reutiliza el datadir sin inicializar (lo demuestra la fecha de creación de `ibdata1`) y regenera las credenciales de aplicación. **PENDIENTES EXPLÍCITOS: (1) prueba obligatoria en un Windows limpio SIN Visual C++ Redistributable — NO se declara resuelta; (2) reinicio real del sistema operativo no probado; (3) empaquetado, poda de los 925 MB de `runtime\mysql` y estrategia de distribución/integridad, que es la fase siguiente.** Sobre integridad: MySQL **no** publica un `SHASUMS256.txt` como Node, así que **el instalador NO descarga MySQL** (decisión aprobada) y exige `runtime\mysql` presente; el binario usado se verificó por **MD5 contra el publicado por Oracle**. Detalle en `CURRENT_STATE.md`.)

2026-07-29 (**FASE LOCAL 2.1 — NODE PORTABLE, CERRADA** y commiteada. **Node.js deja de ser un requisito previo.** Descripción exacta de lo que hay ahora: **«Instalación local automatizada para Windows que ya no exige Node instalado; MySQL 8 sigue siendo requisito previo»**. Prioridad de resolución fija: `runtime\node\node.exe` → Node del equipo si es compatible → descarga verificada → error. Versión **clavada en `v22.23.1`** (22 LTS, npm 10.9.8), descargada de nodejs.org y **verificada por SHA-256 contra el `SHASUMS256.txt` oficial**; si no coincide, el archivo se borra sin abrirlo. **Nada se instala en el sistema** (sin `setx`, registro, MSI, servicios ni admin) y **`runtime/` está en `.gitignore`: cero binarios en el repositorio**. Se confirmó y cerró el riesgo R6: el lanzador `node_modules\.bin\vite.cmd` cae a `node` por PATH — medido, falla con «"node" no se reconoce» sin la inyección de PATH y devuelve `vite/8.0.16 … node-v22.23.1` con ella, restaurando el PATH al salir y sin tocar el PATH persistente. **Probado sin Node global** (retirado del PATH y comprobado que `node -v` falla): `node`, `npm`, `npm ci` ×2, `npm run build`, backend con `/api/health` y `/api/institucion`, y frontend, con el `ExecutablePath` de ambos procesos apuntando a `runtime\node\node.exe`. Build limpio y **lint 29 = baseline exacto**. **Pendientes explícitos: (1) prueba obligatoria en un equipo/usuario Windows limpio, heredada de la Fase Local 1; (2) FASE LOCAL 2.2 — MySQL portable, NO integrada en ningún script.** Detalle en `CURRENT_STATE.md`.)

2026-07-29 (**FASE LOCAL 1 — CERRADA** y commiteada, aprobada por Fabrizio. Descripción exacta de lo entregado, sin exagerar: **«Instalación local automatizada para Windows con Node compatible y MySQL 8 previamente disponibles»** — **GamificApp NO tiene todavía instalación portable**. **Verificación de cierre:** la pregunta de datos demo se probó de forma **interactiva real** con pulsaciones de teclado en una consola de verdad (`n` → no siembra; **Enter a secas → No**; `s` → siembra, y solo sobre `gamificapp_dev`); las cuatro barreras de `seedDev.js` se ejercitaron en vivo **sin modificarlas** y abortan todas antes de escribir; se corrigió un hallazgo de codificación (los `.cmd` estaban en **LF** con un bloque `if` multilínea, el caso en que `cmd.exe` puede fallar) normalizando a **CRLF** y añadiendo **`.gitattributes`**; build limpio y **lint 29 = baseline exacto**; `gamificapp_dev` sin una sola fila alterada. **Pendientes explícitos: (1) prueba obligatoria en un equipo/usuario Windows limpio; (2) FASE LOCAL 2 — Node portable + MySQL portable, NO iniciada.** Detalle en `CURRENT_STATE.md`.)

2026-07-29 (**FASE LOCAL 1 — implementación del instalador base para Windows.** Responde al requisito del revisor de poder instalar y ejecutar GamificApp en local. Tres archivos de doble clic (`Instalar` / `Iniciar` / `Detener GamificApp.cmd`) sobre una carpeta `instalador/` nueva; **cero archivos de la aplicación modificados** (solo `.gitignore`, `README.md` y `START_HERE.md`, estos dos para corregir errores confirmados). **Tres hechos verificados contra el código que corrigen documentación equivocada:** el requisito real de Node es `^20.19.0 || >=22.12.0` (lo impone Vite 8; los docs decían «18+»); `initDb.js` **no** puede inicializar una base vacía (`ALTER TABLE materias` antes de crear las tablas); y —hallazgo nuevo— **el SQL base tampoco basta**: aporta 11 tablas y las 7 restantes solo las crean las migraciones de `initDb.js`, así que hacen falta ambos pasos en orden (medido: 0→11→18). Además se corrigió un fallo propio: `server.js` escucha antes de terminar `inicializarEsquema()`, así que `/api/health` no prueba que el esquema esté completo. Probado A–N (instalación desde cero en un clon limpio, segunda ejecución sin cambiar credenciales ni datos, start/stop/restart, puerto ocupado, fallo de backend, con y sin datos demo, login real de los 3 roles, build, lint = baseline 29, consola sin mensajes). **Pendiente: aprobación y prueba obligatoria en un Windows limpio. Fase Local 2 (Node y MySQL portables) no iniciada.** Detalle y evidencia en `CURRENT_STATE.md`.)

2026-07-29 (**MICROFASE DE CIERRE tras auditar las recomendaciones del experto.** Auditadas las 12 observaciones contra el código real (no contra informes): **7 ya estaban resueltas** por SPEC-018 y se re-verificaron en runtime (sidebar colapsable, alineación del menú, responsive del estudiante, contenido dinámico, caché, JWT, emojis), **2 estaban parcialmente resueltas** y se cerraron aquí, y **3 quedan como decisión consciente de no hacer** (Zod, reestructurar JWT, rediseñar el polling). **Implementados solo 2 hallazgos P1, con 3 reglas CSS aditivas en 3 archivos, cero JSX y cero backend:** **(N1)** `flex-wrap` en `.card-head` y `.section-head-extra` — antes había botones de acción **fuera del viewport** en Admin y Docente hasta los 768px inclusive; ahora los 10 botones quedan dentro y alcanzables en 320/375/768/1280, con el escritorio intacto (a 1280 las cabeceras siguen en una sola fila); **(N2)** los 3 `<select>` nativos del modal «Adaptar con IA» ampliando el selector existente de `.perfil-form` — ahora coinciden **exactamente** con los inputs vecinos. **Baseline de lint corregido a 29 (26 errores + 3 warnings)**: es determinista, no hay deriva de versiones y el commit `3f90372` no aportó ninguno (sus 2 archivos linteables dan exit 0 por separado) → el «28» de la Fase 8 fue un error de medición. Build limpio, consola sin errores, smoke test de los 3 roles, cero cambios de datos. **Ampliación aprobada dentro de la misma tanda: ítem 54 cerrado** — la tabla de Docente → Mis Estudiantes se envolvió en el `.tabla-scroll` ya existente (patrón de `TablaPro`), llevando su desbordamiento a **0 en los cuatro anchos** sin CSS nuevo ni columnas tocadas. **N8 (logo institucional) sigue pendiente de dato, no de código.** Detalle y evidencia en `CURRENT_STATE.md`.)

2026-07-29 (**SPEC-018 COMPLETA: las 8 fases cerradas y pusheadas.** La **Fase 8** (auditoría final) se ejecutó en dos etapas. *Etapa 1, solo lectura:* el **P0 «0/100» del triage (ítem 30) resultó ser un FALSO POSITIVO** — no hay fallo de calificación, es el contador visual de `ResultadoActividad.jsx` (`useContador`, `requestAnimationFrame`) congelado porque el preview mantiene la pestaña con `visibilityState = 'hidden'`; en la misma partida el backend devolvió `calificacion: 50` y el propio overlay mostró la retroalimentación de rango medio. **No se tocó calificación, XP, backend, BD ni Libro.** También se auditaron los requests (el `×2` de `GET /api/progreso/:id` es React StrictMode, solo desarrollo) y JWT/validación (**sin vulnerabilidad → no se introdujo Zod ni ninguna librería nueva ni se reestructuró JWT**, decisión consciente a 3 días de la entrega). *Etapa 2:* 7 archivos, +314/-17, **cero cambios en `server/`, migraciones, calificación, XP, recompensas, IA y permisos** — textareas con estilo del navegador tematizados (Editar materia, Introducción/Final de Misión), **fuga de caché entre usuarios cerrada** (`edu_xpTotal` y `edu_historialRetos` sobrevivían al logout; prueba A→B demuestra que el segundo estudiante ya no ve el XP/nivel del primero ni en el primer render), **sidebar colapsable en tablet/escritorio** con raíl de restauración (+209px de contenido a 768 y 1280, los 3 roles, sin tocar el menú móvil ≤760px), `aria-current="page"`, `:focus-visible` en `.completar-btn-siguiente` (compartido Completar + V/F) y **`prefers-reduced-motion` global** con excepción deliberada para los spinners de carga; más **2 microinteracciones** CSS del Home del estudiante. **Logo institucional: el soporte ya existe en `SidebarLayout` + `dashboard.css`; solo falta el dato (`logo_data` vacío en BD) → se carga desde Administración → Institución, sin cambio de código.** Se **cierran** los ítems 30 (falso positivo), 31, 34 y el `prefers-reduced-motion` del backlog; el resto del triage sigue abierto. **Baseline de lint corregido a 28 (25 errores + 3 warnings)**, verificado con `git stash` sobre `83708b0`. Detalle completo y evidencia en `CURRENT_STATE.md`.)

2026-07-28 (**SPEC-018: Fases 1 a 7 CERRADAS y pusheadas; solo queda la Fase 8.** Verificado contra Git, no contra informes: F6.1 Administrador `cf5ef6e`, F6.2 Docente `ccb18b6`, F6.3 Estudiante `43185ac`; F7.A+7.1 Quiz/infraestructura `ac8d6b6`, F7.2 Clasificador `844ede7`, F7.3 Misión Narrativa `25ae1c8`, F7.4 Completar Espacios `07ef88f`, F7.5 Verdadero o Falso `f818ade`, F7.6 Memorama `85d452b`, F7.7 Línea del tiempo `83d9ad1`. Corregidas cinco notas históricas de `CURRENT_STATE.md` que declaraban «Sin commit» fases que Git demuestra commiteadas. **Fase 8 (auditoría visual y funcional final) pendiente**, con el triage de 21 hallazgos diferidos de Fases 6 y 7 como entrada — detalle completo en `CURRENT_STATE.md` y resumen accionable en §3 de este documento. Ningún hallazgo implementado.)

2026-07-20 (Entorno local Docker+MySQL validado y versionado; SPEC-018 —pulido integral UI/UX— redactada y aprobada como base oficial tras auditoría runtime de los 3 roles. Sin implementación iniciada.)

2026-07-19 (SPEC-016 y SPEC-017 implementadas: proveedores de IA agnosticos y arquitectura extensible de juegos con septimo juego de demostracion. Ambas pendientes de validacion en produccion)

# Responsable

Fabrizio Zurita (Extun)

## 1. Roadmap por fases

| Fase | Nombre | Alcance | Estado |
|------|--------|---------|--------|
| 0 | MVP funcional | Auth, roles, materias, material, 3 juegos, XP/ranking, IA, deploy | ✅ Hecho (en producción) |
| 1 | Fundamentos v2 | Auditoría de navegación, inventario funcional, blueprint | ✅ Hecho |
| 2 | Dashboards reales | Reorganización de los 3 dashboards, componentes compartidos, cero datos ficticios | ✅ Hecho |
| 3 | Sistema documental | **Cerrado y simplificado** en la consolidación 2026-07-07 (START_HERE + 4 docs vivos) | ✅ Hecho |
| 3.5 | Centro de Administración (SPEC-002 + SPEC-003) | Materias/cursos/institución dinámicos, TablaPro, roles y permisos de admin, auditoría, papelera, sidebar agrupado | ✅ Hecho en código y **desplegado en Aiven** (migraciones 002-004 confirmadas en producción el 2026-07-10, ver `CURRENT_STATE.md`) |
| 3.6 | Centro de Trabajo Docente (SPEC-006) | 3 juegos nuevos (memorama, línea del tiempo, completar), IA genérica por registro, actividad sorpresa, adaptar con IA, Biblioteca IA con papelera/favoritas/estadísticas, Libro de Calificaciones editable | ✅ Hecho en código (2026-07-09) — **pendiente: migración 008 a Aiven + deploy + prueba end-to-end con BD** |
| 3.7 | Editor de actividades universal (SPEC-013) | Botón único "Agregar" con menú por acciones, lenguaje docente (sin "Banco"/"IA"/"manual"), toggles "Mezclar" del quiz, selección automática desde preguntas guardadas, autoguardado unificado, modal IA único | 🟡 **Parcial (estado verificado 2026-07-19): F1 ✅, F2 ✅, F3 ⚠️ parcial, F4-F7 ❌ pendientes.** La F8 queda absorbida por SPEC-017. Detalle por fase en `SPEC-013 §7` |
| 3.8 | Proveedores de IA (SPEC-016) | Arquitectura agnóstica al proveedor: adaptadores Gemini + OpenAI, selección de proveedor/modelo desde administración, "Probar conexión". API Keys solo en variables de entorno | 🟡 **Implementada — pendiente de validación funcional en producción.** *(Fila corregida el 2026-07-29: decía «pendiente de aprobación, ninguna fase iniciada», lo que contradecía a la línea de actualización del 2026-07-19 de este mismo documento y al código.)* Verificado en el repositorio: `server/lib/ia/` con `proveedorGemini.js`, `proveedorOpenAI.js`, `config.js`, `probar.js`, `registro.js` y `errores.js`; ruta `server/routes/adminIA.js`; servicio `src/services/iaConfigService.js`; migración **013** enganchada en la cadena de arranque (`migrarConfiguracionIA` en `initDb.js`), así que **se aplica sola en cada boot**. Responde a observación del revisor. **✅ VALIDADA FUNCIONALMENTE EN PRODUCCIÓN (2026-07-29)** con **una única generación real autorizada**: `POST /api/ia/generar` (el endpoint del modal del docente, que **no persiste**) devolvió **200 en 2.254 ms** un quiz de 3 preguntas que **pasó `quiz.validarConfig`**; la actividad no se guardó. `GET /api/admin/ia/configuracion` → `gemini` / modelo automático / `origen: entorno`, con **Gemini `configurado: true`**, OpenAI `false` y **cero secretos en la respuesta**. **Sin validar (exigen cuota o escritura):** catálogo dinámico de modelos, cambio de proveedor, «Probar conexión» y tipos distintos del quiz |
| 3.9 | Arquitectura extensible de juegos (SPEC-017) | Registro central de tipos de juego (backend + frontend), 3 estados (Activo / Solo jugar / Deshabilitado), módulo de administración, guía "Cómo añadir un juego" y séptimo juego de demostración | 🟢 **VALIDADA EN PRODUCCIÓN (2026-07-29), en solo lectura.** El panel carga contra Aiven con los **3 estados exactos** de la spec y los **7 tipos en activo**, con conteos reales (30 actividades, 6 publicadas) que prueban que consultó la BD: Quiz 6/1, Misión 6/1, Clasificador 7/1, Memorama 3/1, Línea del tiempo 4/1, Completar 3/1 y **Verdadero o Falso 1/0 — el juego de demostración, vivo en producción**. **Sin validar (exige escritura, no autorizada):** cambiar un estado y observar su efecto en docente y estudiante. *(Estado previo: implementada, pendiente de validación, 2026-07-19.)* Fases 1-7 en código, migración 014. Extensibilidad demostrada con Verdadero o Falso: 4 archivos propios, 4 líneas de registro, **0 módulos centrales con lógica del tipo y 0 cambios en los 6 juegos existentes** (`docs/COMO-AGREGAR-UN-JUEGO.md`). **NO cerrar** hasta validar en el despliegue |
| 3.10 | Pulido integral UI/UX (SPEC-018) | Design system completo (tokens semánticos, z-index, breakpoints), `ModalPanel` accesible, `ConfirmDialog`/`Toast`, sidebar móvil colapsable, pulido por rol y de los 7 juegos. Solo presentación; §10 y SPEC-013/016/017 congeladas | 🟢 **COMPLETA — 8/8 fases cerradas y pusheadas (2026-07-29), verificado contra Git.** F1 tokens/z-index/breakpoints, F2 ~60 hex → tokens, F3 `ModalPanel` accesible, F4 `ConfirmDialog`+`Toast`, F5 sidebar responsive, F6.1/6.2/6.3 pulido de los 3 paneles (`cf5ef6e`/`ccb18b6`/`43185ac`), F7.A-7.7 pulido de los 7 reproductores (`ac8d6b6`, `844ede7`, `25ae1c8`, `07ef88f`, `f818ade`, `85d452b`, `83d9ad1`). **F8 auditoría final** en dos etapas: la 1 (solo lectura) cerró el P0 «0/100» como **falso positivo** (contador rAF congelado con la pestaña oculta; el backend devolvió `calificacion: 50` en la misma partida) y descartó Zod/refactor de JWT tras auditar la validación; la 2 implementó textareas tematizados, cierre de la fuga de caché entre usuarios, sidebar colapsable en tablet/escritorio, `aria-current`, `:focus-visible` de `.completar-btn-siguiente` y `prefers-reduced-motion` global, más 2 microinteracciones. Cada fase verificada en el entorno local antes de su commit. Del triage de 21 diferidos se cierran los ítems 30, 31 y 34; el resto sigue abierto (§3) |
| Infra | Entorno local de desarrollo/QA (Docker + MySQL) | `docker-compose.dev.yml` + `seedDev.js` (triple barrera) + `initDb.js` real; los 3 roles y 7 juegos jugables en local. **No es SPEC** (infraestructura de dev) | 🟢 **Validado y versionado (2026-07-20).** `docs/DEV-ENTORNO-LOCAL.md` |
| Local 1 | Instalador local para Windows | `Instalar/Iniciar/Detener GamificApp.cmd` + `instalador/`: comprobación de requisitos, preparación de BD (SQL base + migraciones), `server/.env` con credenciales generadas e irrepetibles, build, arranque con health checks reales y datos demo opcionales. **Asume Node y MySQL 8 ya instalados. No es SPEC** (infraestructura, cero lógica de negocio) | 🟢 **CERRADA (2026-07-29, commiteada).** «Instalación local automatizada para Windows con Node compatible y MySQL 8 previamente disponibles» — **no portable**. **Pendiente: prueba obligatoria en un equipo/usuario Windows limpio** |
| Local 2.1 | **Node portable** | `instalador/runtime.ps1` + resolución portable-first en `comun.ps1`: `runtime\node\node.exe` → Node del equipo compatible → descarga verificada por SHA-256 → error. npm se invoca por `npm-cli.js`, nunca por `npm.cmd`; el PATH solo se toca en el proceso y sus hijos, para el lanzador `vite.cmd`. **No es SPEC** (infraestructura, cero lógica de negocio) | 🟢 **CERRADA (2026-07-29, commiteada).** Node v22.23.1 clavado y verificado; `runtime/` en `.gitignore`, **cero binarios en el repositorio**. Probado con el Node global retirado del PATH: `npm ci` ×2, `build`, backend y frontend, ambos procesos sobre `runtime\node\node.exe` |
| Local 2.2 | **MySQL portable** | `instalador/mysql.ps1` (ciclo de vida completo) + `mysql-clave-root.mjs`: MySQL 8.0.44 oficial x64 como proceso de usuario, sin servicio de Windows, sin Docker y sin admin; datadir persistente en `%LOCALAPPDATA%\GamificApp\`, puerto 3308 con reserva 3309–3315 y `bind-address=127.0.0.1`. **No es SPEC** (infraestructura, cero lógica de negocio) | 🟢 **INTEGRADA Y PROBADA (2026-07-29).** «No exige ni Node ni MySQL instalados». Ciclo Instalar/Iniciar/Detener completo, persistencia verificada, fallback de puerto sin tocar procesos ajenos, reconstrucción tras reemplazar la carpeta del proyecto, Docker 3307 intacto, lint = baseline. **Pendiente: Windows limpio SIN VC++ Redistributable (no demostrado) y reinicio real del SO** |
| Local 2.3 | **Empaquetado y distribución** | `instalador\empaquetar.ps1`: paquete reproducible en `release\` (ignorado por Git) con los runtimes portables, `node_modules` ya instalado (instalación **sin internet**), poda demostrada de `runtime\mysql` y auditoría que aborta si detecta secretos o datos. Runtime de Visual C++ **app-local** junto a `mysqld.exe`. **No es SPEC** (infraestructura, cero lógica de negocio) | 🟢 **IMPLEMENTADA Y PROBADA DESDE EL PAQUETE (2026-07-29).** «Se extrae y se instala con doble clic, sin Node, sin MySQL, sin npm, sin Docker y sin internet». **750,8 MB / 61.161 archivos**; MySQL podado **924,6 → 398,0 MB** solo con reglas demostradas (`*.pdb`, `*.lib`, `*-debug.dll`, `lib\plugin\debug`, `include`). Probado desde el paquete en ruta **con espacios y acentos**: instalación desde cero 51,1 s, login real de admin (200/401), Detener/Iniciar, persistencia y reemplazo de la carpeta sin reinicializar. Auditoría independiente de los 61.164 archivos byte a byte: **0 secretos**. ZIP **243,6 MB** en 393 s con el `tar.exe` de Windows. **Pendiente: la prueba en un Windows realmente limpio (sin VM disponible)** |
| 4 | **Épica 1: Experiencia del estudiante** | Rediseño completo del lado del niño en 5 specs (ver §2) | 🟡 En curso (auditoría y SPEC-001 redactadas; nada implementado) |
| 5 | Módulos incompletos | Libro de Calificaciones, 3 logros faltantes, UI de edición de docente | ⚪ Pendiente (antes de la defensa, si hay tiempo) |
| 6 | Post-tesis | Multi-institución, archivos fuera de la BD, fallback de IA, memoria del asistente | ⚪ Futuro |

## 2. Sprint actual — Épica 1

| Spec | Alcance | Estado |
|------|---------|--------|
| SPEC-001 | Student Shell: rutas anidadas, sin sidebar, nuevo Inicio, menú de avatar, ~~modal de PIN~~ | 🟡 Pendiente de aprobación · **el modal de PIN ya se entregó fuera de esta spec** (2026-07-30, bloque P1 de la fase RC, `ModalCambiarPin.jsx`): era la única pieza accionable sin rediseñar el shell. El resto del alcance sigue sin aprobar. |
| Spec 2 | Fusión de pestañas / vista de materia rediseñada | ⚪ Por redactar |
| Spec 3 | Motor único de retos | ⚪ Por redactar |
| Spec 4 | Login infantil (curso → estudiante → PIN numérico, sin teclear nombre) | ⚪ Por redactar |
| Spec 5 | Vitrina de premios (logros + ranking) | ⚪ Por redactar |

Insumos: `docs/audit/Auditoria-UX-Estudiante-v1.md`, `docs/specifications/SPEC-001-Student-Shell-Plan.md`.

## 3. Backlog priorizado (fuera de la Épica 1)

1. ~~Libro de Calificaciones~~ ✅ Hecho (2026-07-09, SPEC-006: detalle por intento, observación, revisado y ajuste manual de XP con auditoría).
2. Lógica de logros `racha-7`, `estrella-aula`, `explorador`.
3. UI de edición de docente (endpoint `PUT /api/admin/docentes/:id` ya existe).
4. Unificar fuente de materias (consumir `GET /api/materias` en vez de la constante).
5. Validador de configuración para retos tipo `quiz`.
6. Memoria de conversación en Asistente IA.

### Hallazgos de auditoría externa (2026-07-09) diferidos a post-sustentación

7. Material privado por docente: `materiales` no guarda `docente_id`, así que cualquier docente autenticado ve el material privado de cualquier materia. Requiere migración de BD (columna + backfill) → spec propia.
8. `POST /api/admin/materias` calcula `MAX(id)+1` sin bloqueo; con un solo admin real el riesgo es teórico. Ideal: AUTO_INCREMENT (migración).
9. El upsert de retos por `(materia_id, titulo)` no tiene índice único en BD; dos publicaciones simultáneas podrían duplicar. Requiere migración con deduplicación previa.
10. `useAutoRefresh` puede solapar peticiones si una tarda más que el intervalo (sin cancelación); riesgo bajo con los intervalos actuales.
11. Migraciones manuales 002 no idempotentes y `initDb` agrupa `color/icono/activa` bajo una sola comprobación de `color`; frágil ante migraciones parciales.
12. Lint: quedan 29 errores frontend (`react-hooks/set-state-in-effect`, `react-refresh/only-export-components`, algunos `no-empty`/`no-unused-vars` previos); corregirlos exige reestructurar componentes (los archivos nuevos de SPEC-006 repiten el patrón registro-de-constantes + componente).
13. ~~Accesibilidad de modales: sin focus trap, cierre con Escape ni restauración de foco.~~ ✅ Hecho (2026-07-20, SPEC-018 Fase 3: `ModalPanel` con foco inicial, focus trap, Escape, restauración y scroll-lock).
14. Rendimiento menor: `TablaPro` recibe `buscar`/`renderFila` inline (memo inútil); chunks grandes de Vite (`index` ~1.44 MB, `pdf.worker` ~1.29 MB) → code-splitting post-tesis.

### Diferidos de la auditoría de actividades (2026-07-19) — anotados, NO implementar sin decisión

20. **Sesiones/tokens de intento contra falsificación de resultados válidos.** El endurecimiento de `POST /api/progreso` cerró la manipulación trivial (crear retos, `xp_recompensa` arbitrario, `puntos_obtenidos` arbitrario, `total` incoherente), pero un usuario técnico puede seguir enviando un resultado **estructuralmente válido pero no jugado** (p. ej. `5/5` en un quiz que sí tiene 5 preguntas). Riesgo residual **aceptado explícitamente por Fabrizio** (2026-07-19) y documentado en SPEC-015. Cerrarlo exigiría que el servidor emita una sesión de intento firmada al iniciar la actividad y/o validar cada respuesta en backend — cambio de arquitectura mayor, fuera del alcance de la tesis.
21. **Decisión arquitectónica: acceso a actividades por `curso_id`.** Hoy `curso_id` **no** es frontera de acceso: `GET /api/retos` lista a cualquier estudiante las actividades publicadas de cualquier materia activa, sin filtrar por curso. `POST /api/progreso` replica deliberadamente ese criterio para que no exista nada visible que no sea enviable. Convertirlo en frontera real es una decisión de producto y debe cambiar **ambos endpoints a la vez** (si no, aparecerían actividades listadas pero rechazadas al enviarlas). Pendiente de decisión de Fabrizio.
22. **Posible duplicación de auditoría posterior al COMMIT.** En `POST /api/progreso`, `registrarAuditoria` se ejecuta **después** del commit y usa `pool` (no la conexión de la transacción). Un reintento de red del cliente podría generar dos entradas de auditoría para un mismo intento. No afecta XP, calificación ni progreso (la transacción es idempotente); es solo ruido en la bitácora. Pre-existente al cambio de 2026-07-19.
23. **Compatibilidad de notas históricas de Línea del tiempo.** El criterio de calificación pasó de posición absoluta (`n` posiciones) a concordancia de Kendall (`n(n-1)/2` pares). `GREATEST` garantiza que ningún estudiante pierda su mejor nota histórica, pero el Libro de Calificaciones mezcla dos criterios en actividades anteriores al cambio, así que las notas de esa actividad no son comparables entre sí. Pendiente: decidir si se menciona como limitación en la tesis, se recalcula o se deja como está.

### Diferidos de las specs del revisor (SPEC-016 / SPEC-017, anotados 2026-07-19)

24. **Fallback automático entre proveedores de IA.** Excluido de SPEC-016 por decisión de Fabrizio (2026-07-19) para acotar alcance. El contrato queda preparado: `clasificarError()` en cada adaptador y la columna `configuracion_ia.proveedor_respaldo`, que se **crea pero no se lee**. Incorporarlo después no requiere tocar los adaptadores.
25. **Mecanismo real de migraciones — documentado, no pendiente.** Ver §6 de este documento. Numeración resuelta por Fabrizio el 2026-07-19 (Opción B): SPEC-016 → `013`, SPEC-017 → `014`, sin archivo retroactivo para SPEC-015. Hueco consciente entre `012` y `013`.
27. **Evaluar un sistema formal de migraciones versionadas** (post-tesis, NO implementar ahora). Hoy la idempotencia se deriva del estado del esquema vía `faltaColumna()`, sin bitácora de migraciones aplicadas. Un sistema formal aportaría: tabla `migraciones_aplicadas` con marca de tiempo y checksum, ejecución de los `.sql` como artefactos reales en vez de reimplementarlos en JS, orden explícito y reversibilidad verificable. Motivo de diferirlo: el mecanismo actual **funciona y es idempotente**, y reescribirlo sería un refactor grande no pedido (regla §3) sobre la pieza más delicada del despliegue, a semanas de la sustentación.
26. ~~Séptimo juego de demostración~~ ✅ **Hecho (2026-07-19)**: Verdadero o Falso implementado solo con el contrato del registro. Evidencia medida en `docs/COMO-AGREGAR-UN-JUEGO.md` §Evidencia de extensibilidad. Pendiente de validación funcional en producción.

### Diferidos de SPEC-018 Fases 6 y 7 — triage 2026-07-28, entrada a Fase 8 — NO implementar sin decisión

Recopilación de TODO lo que las Fases 6.1-6.3 y 7.A-7.7 anotaron como diferido. Detalle y evidencia en `CURRENT_STATE.md` (entradas 2026-07-28 de cierre y 2026-07-29 de la Fase 8). **Actualización 2026-07-29 (Fase 8):** se resuelven los ítems **30** (falso positivo), **31** y **34**; **el resto sigue abierto y sin implementar.**

30. ~~**[P0] Overlay de fin de partida muestra `0/100` pese a respuestas correctas.**~~ ❌ **FALSO POSITIVO — cerrado el 2026-07-29 (Fase 8, Etapa 1). NO hay fallo de calificación.** La investigación extremo a extremo (reproductor → `POST /api/progreso` → backend → MySQL → respuesta → overlay → Libro de Calificaciones) en Quiz, V/F y Misión Narrativa demostró que backend, BD y Libro son **correctos**. La causa del síntoma es el contador visual de `ResultadoActividad.jsx` (hook `useContador`, basado en `requestAnimationFrame`): el entorno de preview mantiene la pestaña con `document.visibilityState === 'hidden'` y el reloj de animaciones no avanza, así que el número se queda en el 0 inicial. Evidencia decisiva: en una partida real de 1/2 aciertos el backend respondió `{"calificacion":50,"mejor_calificacion":50,"porcentaje":50}` mientras el overlay mostraba «0 / 100» **y, al lado, la retroalimentación de rango medio que corresponde a 50**; `getAnimations()[0].currentTime` medido clavado en 0 tras 1,6 s. **Es una limitación del entorno de preview, no un bug de la app** → no se tocó calificación, XP, backend, BD, Libro ni `ResultadoActividad`. **CONFIRMADO EN NAVEGADOR VISIBLE (2026-07-29, Microfase Final A) y cerrado del todo:** con `visibilityState:"visible"` y **rAF a 57 fps** (646 frames en 11,4 s, frente a **1 frame en 5,6 s** con el panel oculto), dos partidas reales dieron **backend 100 → overlay 100/100** y **backend 50 → overlay 50/100**, con la traza del contador subiendo `0→15→26→35→41→45→48→49→50`. **El contador arranca en 0 y sube por `requestAnimationFrame`: con el reloj congelado se queda en ese 0 inicial, que es exactamente el síntoma.** Sin cambios de código.
31. ~~**[P1] `SidebarLayout` no expone `aria-current`/`aria-selected`.**~~ ✅ **Hecho (2026-07-29, Fase 8):** `aria-current="page"` en el ítem activo, verificado en los 3 roles y también dentro del menú móvil.
32. ~~**[P1] Contraste del contenido bloqueado en Mis Premios.**~~ ✅ **Hecho (2026-07-29, Microfase Final A): 2.42:1 → 5.16:1.** Y con un hallazgo que **invalidaba la solución propuesta aquí**: subir la opacidad NO alcanza AA — medido, `0.75` da **3.04:1** y ni al 100% pasa de 4.84:1, porque el gris de origen (#6b7280) no tiene margen. Se corrigió por color (`--color-text` en `.mision-desc`, `.mision-progreso-txt` y `.mision-hint`) **dejando `opacity:.62` y `grayscale(.35)` intactos**, así que el estado «bloqueado» se ve igual. Evidencia en `CURRENT_STATE.md`.
33. **[P1] `--color-success` #16a34a global falla AA sobre blanco (3.30:1)** allí donde todavía se usa como color de texto. Corregido puntualmente en la Fase 7.4 (huecos correctos), 6.3 (✓ de misión) y **la Microfase Final A** (`.bib-preview-lista .is-correcta` de la Biblioteca: 3.15:1 → 4.79:1 sobre el `#f8fafc` real de la vista previa) con `--color-success-dark` #15803d; **falta la decisión global**, que se mantiene diferida a propósito. **Actualización 2026-07-29 (medido, sin tocar nada):** ya solo quedan **dos** usos como color de texto y ninguno es un fallo AA real — `.linea-evento-check` es **CSS muerto** (ítem 42) y `.opcion-correcta .opcion-icono` **no es texto sino un SVG de MUI**, así que le aplica SC 1.4.11 (**3:1**), no 4.5:1: mide **3,001:1** sobre `--color-success-soft` → cumple, con margen casi nulo (su contraparte roja mide 3,953:1). `--color-success-dark` lo llevaría a **4,57:1** cambiando un token. **Fabrizio decidió el 2026-07-29 dejarlo como está** (SPEC-018 congelada).
34. ~~**[P1] `.completar-btn-siguiente` sin `:focus-visible` temático.**~~ ✅ **Hecho (2026-07-29, Fase 8):** anillo `3px --color-primary-dark` con `outline-offset: 3px` (tono oscuro para que no se funda con el relleno `--color-primary` del propio botón); tratado como infraestructura compartida y **probado en los dos consumidores** — «Siguiente frase» de Completar Espacios y «Siguiente afirmación» de Verdadero o Falso. Mecánica sin tocar.
35. ~~**[P1] Estados vacíos inconsistentes.**~~ ✅ **Hecho (2026-07-30, bloque P1 de la fase RC, commit `b5f1dc3`).** Las 4 pestañas de materia del estudiante pasan de `<p class="vacio-msg">` a `EmptyState` con icono y texto propio, verificado en las cuatro en navegador (cero `vacio-msg` residual en el panel). **No fue transversal como se temía:** `.vacio-msg` se conserva intacta en los 6 reproductores y en la Biblioteca, porque allí no comunica «no hay datos» sino un **error técnico** («este juego no tiene configuración válida»), que es otra cosa. *(Enunciado original abajo.)* El Home del estudiante usa el componente `EmptyState`, pero las 4 pestañas de materia usan `<p class="vacio-msg">`; `.vacio-msg` es compartida con 6 reproductores y la Biblioteca del docente → unificar es transversal.
36. ~~**[P1] Formalizar `--color-text-soft`/`--color-text-muted` como tokens globales.**~~ ✅ **Resuelto en lo que era el fallo real (2026-07-30, bloque P1, commit `b5f1dc3`): `--color-text-muted` #64748b → #5c6a80.** Sobre `--color-bg` (#f1f5f9), que es el fondo real de `.contenido`, medía **4,34:1** y no alcanzaba el 4,5:1 de WCAG 1.4.3; ahora **5,01:1** (5,49:1 sobre blanco), conservando la jerarquía respecto a `--color-text` (~16:1). Afectaba al subtítulo bajo cada `h1` de los tres paneles. **`--color-text-soft` NO se creó**: no existía ningún consumidor que lo necesitase, así que añadirlo habría sido un token muerto.
37. ~~**[P2] `window.prompt` ×2 del cambio de PIN del estudiante.**~~ ✅ **Hecho (2026-07-30, bloque P1, commit `b5f1dc3`) — y sin spec, con autorización expresa de Fabrizio.** Ver ítem **29** para el detalle. La premisa «toca autenticación (§10, requiere spec)» **resultó no aplicar**: el reemplazo es de presentación (mismo `authService.cambiarPin(actual, nuevo)`, mismo `PUT /api/auth/cambiar-pin`, mismos parámetros, misma validación en el servidor), que es el principio que SPEC-018 §3 ya autoriza para áreas congeladas.
38. ~~**[P2] Títulos duplicados H1/H3 en Mis Estudiantes y Ranking, y jerarquía de headings en Mi Perfil**~~ ✅ **Hecho (2026-07-29, Microfase Final A)**, tras auditar los tres: **Mi Perfil** tenía un `<h2>` (el nombre) dentro de la sección `<h3>` → `<h4>`, con renderizado **idéntico medido** (223 × 29 px); **Mis Estudiantes** repetía el H1 literal → la tarjeta pasa a «Listado de estudiantes»; **Ranking se dejó intacto** porque «Ranking completo» sí distingue la tabla de la pantalla. **Queda abierto y NO implementado**: `SectionCard` renderiza `<h3>`, así que todas las pantallas saltan de H1 a H3 — cambiarlo toca los 3 roles y `.card-head h3` es selector por etiqueta (transversal, ver §3 regla de cambios pequeños).
39. **[P2] Faltan `aria-label` en los íconos del menú «Agregar preguntas»** (diferido en 6.2; área estabilizada por SPEC-013).
40. **[P2] Hex decorativos sueltos sin token:** ✕ `#ef4444` inline en la revisión de Completar Espacios y Verdadero o Falso, `#f0fdf4` en `.linea-evento.is-correcto`, y los violet/rose de las canastas del Clasificador (estos últimos son hex de identidad, decisión consciente de la Fase 2). Ninguno es fallo AA real: todos son decorativos y van acompañados de texto que sí cumple.
41. ~~**[P2] `.home-mundos-grid` cae a 1 sola columna a 320px**~~ ✅ **Hecho (2026-07-29, Microfase Final A)**: rejilla 754→**427px** y scroll 1231→**904px**, sin desbordes ni nombres cortados. La preocupación de la Fase 6.3 estaba justificada y **se midió**: bajar el `minmax` a 137px metía una **tercera** columna a 480px (tarjetas 217→140px), así que se resolvió con `@media (max-width: 359px) { repeat(2,1fr) }` — **375, 480, 768 y 1280 quedan byte a byte iguales**.
42. ~~**[P3] CSS muerto en los reproductores.**~~ ✅ **CERRADO del todo el 2026-07-30 (POLISH SPRINT v1.0.1).** Etapa D: `.linea-lista.is-sacudida` + `@keyframes linea-sacudida` + la regla que los neutralizaba en `prefers-reduced-motion` —se descartó revivirlos porque una sacudida comunica error y la emoción del juego es orden—. QW4: `.quiz-puntaje` y sus 3 reglas hijas, `.linea-evento.is-correcto` (que se llevó el `#f0fdf4` sin token del ítem 40) y `.linea-evento-check`. Comprobado antes de borrar que ninguna se construye dinámicamente: `.completar-hueco.is-correcto` sí se arma con `is-${estado}` y NO se tocó. Cero restos verificados en el CSSOM. *(Enunciado original abajo.)* `.quiz-puntaje` en `quizInteractivo.css` (el marcador migró a `ResultadoOverlay`) y, en Línea del tiempo, `.linea-evento.is-correcto`, `.linea-evento-check`, `.linea-lista.is-sacudida` + `@keyframes linea-sacudida` (verificado leyendo `LineaTiempo.jsx`: el JSX nunca los aplica).
43. **[P3] CSS muerto en el panel del estudiante** (~70 líneas de `dashboardEstudiante.css`): `.home-mundo-2/-4` (los colores llegan inline desde `uiMateria(...).estilo`, SPEC-002), `.logros-grid` + `.logro-card/-icon/-badge/-bloqueado` (retirados por SPEC-011 al sustituir los logros de `localStorage` por Misiones; contienen los **2 únicos `!important`** del archivo) y `.rank-item-yo`.
44. **[P3] `LogroToast` vive dentro del reproductor del Quiz.** Se define en `src/components/quiz/QuizInteractivo.jsx` y `juegosComunes.jsx` solo lo importa y re-exporta (`:11` y `:171`), de modo que los 7 juegos dependen del Quiz para su toast de logro. Deuda de ubicación; mover el componente a `juegosComunes` es refactor, no pulido.
45. **[P3] Divergencia Quiz ↔ `useRecompensa`.** Clasificador, Completar, Memorama, Línea del tiempo, Verdadero o Falso y Misión Narrativa usan el hook compartido; `QuizInteractivo.jsx` **no lo usa** (0 ocurrencias) y mantiene su propio camino de recompensa. Unificarlo toca XP (§10) → requiere spec. Probable causa a investigar del ítem 30.
46. **[P3] `GET /api/progreso/:id` se dispara varias veces al entrar al Home del estudiante** (StrictMode + dependencias `pagina`/`refrescar`). Ruido de red, sin impacto funcional ni de datos. **Reauditado el 2026-07-29 (Fase 8): el patrón medido es `×2` en el montaje inicial y la evidencia apunta a React StrictMode, que solo actúa en desarrollo; durante la navegación normal no hay refetches redundantes. NO se optimizó** — hacerlo exigiría tocar los efectos del panel sin beneficio en producción.
47. **[P3] Consolidar breakpoints históricos 560/720/900 y unificar las cabeceras de módulo compartidas** (diferido en 6.1).
48. **[P3] `style={{ pointerEvents: 'none' }}` en los `h1`/`p` de «Mis mundos» y «Mis premios»** — hack heredado sin causa documentada.
49. **[P3] `.mision-progreso` sigue siendo un nombre genérico en el panel de Premios.** Deuda de nomenclatura que queda tras el rename del reproductor a `.mision-capitulos` en la Fase 7.3; sin impacto (ya no hay colisión).
50. **[P3] `.memorama-carta-dorso` declara fallbacks hex divergentes del token real** (`var(--color-primary-soft, #e0f2fe)`). Inofensivo: los tokens existen, el fallback nunca se aplica.

### Añadidos por la Microfase Final A (2026-07-29) — decisión de diseño pendiente

60. **[P1] `.mision-xp` y `.mision-tier` de las misiones BLOQUEADAS siguen bajo AA — diferido con datos, requiere decisión de diseño de Fabrizio.** Medidos compositados (grayscale 0.35 → opacity 0.62) y por variante, con el fondo propio de cada elemento: `.mision-xp` (12px/700) va de **1.54:1** (categoría amarilla) a **2.50:1** (roja) —7 colores distintos—, y `.mision-tier` (11.52px/700, sobre su tinte al 14%) de **1.83:1** (Platino) a **2.59:1** (Diamante). **El barrido de soluciones locales muestra que toda vía a 4.5:1 rompe uno de los dos requisitos**: `color-mix(…, black)` solo llega a AA **al 20% de matiz**, dejando los 7 colores de categoría visualmente negros (adiós identidad); y conservar 40-50% del matiz obliga a subir la opacidad a **0.75-0.85**, debilitando la señal de «bloqueado». La información no se pierde mientras tanto: el tier lleva emoji (🥈🥇💠💎) y el XP vuelve a ser legible al desbloquear. **Opciones medidas para decidir:** (a) opacidad 0.62 + matiz 20% → 4.59:1 mínimo; (b) opacidad 0.75 + matiz 40% → 4.63:1; (c) opacidad 0.85 + matiz 50% → 4.79:1; (d) no tocar.

### Añadido por la validación en producción de SPEC-016 (2026-07-29) — registrado, NO corregir sin decisión

61. **[P3] Deriva de contrato: `modelosSugeridos` se mapea pero ningún adaptador lo define.** `registro.js:35` lo incluye en `proveedoresPublicos()` y la cabecera del propio archivo lo lista en el contrato del adaptador, pero **ni `proveedorGemini.js` ni `proveedorOpenAI.js` lo declaran**, así que sale `undefined` y JSON lo omite — comprobado en la respuesta real de producción, donde cada proveedor viaja solo con `id`, `etiqueta`, `variableEntorno` y `configurado`. **Sin impacto funcional:** SPEC-016 §4.5 decidió a propósito que el catálogo de modelos sea **dinámico** (`GET /api/admin/ia/modelos`) y no una lista fija que envejezca, así que el campo es un vestigio de una versión anterior del contrato. Arreglo posible en dos direcciones opuestas —quitarlo del mapeo y del contrato, o implementarlo en los adaptadores— y por eso **requiere decisión, no corrección automática**. Detectado durante la auditoría de producción; **no se tocó**.

### Añadidos por el bloque P1 de la fase RC (2026-07-30) — NO implementar sin decisión

62. ~~**[P3] `npm run lint` lintea también la copia empaquetada de `release/`, duplicando el recuento.**~~ ✅ **Hecho (2026-07-30, POLISH SPRINT v1.0.1 Etapa 0).** `globalIgnores(['dist'])` → `globalIgnores(['dist', 'release', 'runtime'])`. **Medido: `npm run lint` 58 → 29**, idéntico a `npx eslint src server`; los dos comandos coinciden por primera vez. *(Enunciado original abajo.)* `eslint .` recorre `release/GamificApp/`, que contiene una copia completa del código fuente generada por `instalador/empaquetar.ps1`; `.gitignore` **no** se aplica al flat config de ESLint, que solo obedece a sus propios `ignores` (hoy únicamente `dist`). Consecuencia medida el 2026-07-30: **58 problemas en vez de 29**, con el desglose por regla exactamente al doble y 14 rutas de la fuente + 14 de `release/`. `npx eslint src server` da los **29 (26+3)** correctos. **Sin impacto en el código** —es un artefacto del entorno: solo aparece si existe la carpeta `release/`— pero **envenena la comparación con la línea base**, que es el criterio de calidad de cada fase, y ya provocó un diagnóstico erróneo que hubo que corregir. Arreglo: añadir `release` (y probablemente `runtime`) a `globalIgnores` en `eslint.config.js`. Una línea. **No se hizo en P1 porque el bloque no toca configuración.**
63. **[P3] `PUT /api/auth/cambiar-pin` devuelve 401 para «PIN actual incorrecto», que semánticamente no es un fallo de sesión.** La petición **sí** está autenticada —`autenticar` la validó para llegar al handler—; lo que falla es la credencial que viaja en el cuerpo. Ese 401 es lo que hacía que `authFetch` cerrase la sesión del estudiante (ver ítem 29). En P1 se resolvió **en el cliente** (`conservarSesionEn401`) para no tocar el contrato de la API a días de la entrega. El arreglo más limpio sería **400** en el servidor, y entonces la excepción del cliente dejaría de ser necesaria. **Toca autenticación (§10) y cambia el contrato: requiere decisión explícita.**

### Añadidos por el POLISH SPRINT v1.0.1 (2026-07-30) — asignados a la Etapa 3 de SPEC-020

64. **[P3] `.contenido-materia*` es CSS muerto: 7 selectores, ~50 líneas de `dashboardWidgets.css`.** `.contenido-materias`, `.contenido-materia-btn` (+ `:hover`), `-icono`, `-nombre` y `-detalle` tienen **cero consumidores en todo `src/`** fuera del propio CSS. Detectado en la Etapa 1 al no encontrarlo en el navegador para verificarlo. Se descartó darle estado de pulsación por eso mismo.
65. **[P3] `QuickActionCard` se exporta pero no lo importa nadie**, así que `.quick-action-card/-icon/-meta/-btn` y `.quick-actions-grid` no se renderizan nunca. Se define en `DashboardWidgets.jsx:287`. **Contradice `CONTRIBUTING.md` §6.4**, que lo lista entre los componentes reutilizables a preferir: al borrarlo hay que corregir también esa regla. **Decidir antes de borrar:** el ítem **P2-6** del bloque RC —bienvenida de primer uso del docente, aprobado «solo si sobra tiempo» y no implementado— contemplaba justamente usarlo.

### Añadidos por la Fase 8 (2026-07-29)

51. **Cargar el logo institucional real.** El soporte **ya está completo en código y no hay nada que programar**: `SidebarLayout.jsx` lee `getInstitucionCache()?.logo_data` y lo pinta como `<img className="sidebar-logo">` (decorativo, `alt=""`, el nombre va en el `<h2>` contiguo), con CSS y variante móvil en `dashboard.css`; el dato llega de `GET /api/institucion` (público, SPEC-002) y el mismo campo alimenta el favicon. **Lo único que falta es el dato**: en la BD local `logo_data` y `favicon_data` están vacíos. Se sube desde **Administración → Institución**. No se inventó ningún asset ni se metió base64 temporal.
52. **Decisión registrada: NO se introdujo Zod ni ninguna librería de validación, ni se reestructuró JWT** (Fase 8, 2026-07-29). La auditoría de autenticación encontró secreto obligatorio, expiración, validación del token, relectura de permisos y estado del usuario desde BD, múltiples validaciones server-side y rechazo de payloads malformados: **ninguna vulnerabilidad nueva**. A tres días de la entrega, añadir una dependencia y reescribir el camino de autenticación sería riesgo sin beneficio demostrado. Si se retoma post-tesis, hacerlo con una spec propia.
53. **`prefers-reduced-motion` global implementado como red de seguridad** (Fase 8): un único bloque en `src/index.css` neutraliza animaciones y transiciones **completándolas al instante** (no cancelándolas, para que overlays y modales sigan apareciendo), con **excepción deliberada** para `.quiz-spinner` y `.editor-ia-spinner`, que siguen girando porque son la única señal de carga. Los 4 bloques específicos previos (login, Toast, overlays de resultado, `juegos.css`) se conservan intactos: ahora son redundantes pero correctos. **Pendiente menor, no urgente:** si algún día se limpian, hacerlo comprobando cada uno.

### Diferidos de la microfase de cierre (auditoría del experto, 2026-07-29) — NO implementar sin decisión

Auditados con evidencia medida y **conscientemente NO implementados** a 3 días de la entrega. Los P1 de esa auditoría **sí se implementaron y quedan cerrados**: acciones de cabecera sin `flex-wrap`, `<select>` nativos de «Adaptar con IA» y —como ampliación aprobada— la tabla sin `.tabla-scroll` del ítem 54; detalle en `CURRENT_STATE.md`.

**Actualización 2026-07-29 (sesión de cierre de observaciones):** se **cierran los ítems 55, 56 y 57** (implementados y medidos); el **58** se cierra como **no defecto** tras medirlo con la pestaña visible; el **59** queda elevado a **SPEC-019**, pendiente de aprobación porque toca §10. Detalle y números en `CURRENT_STATE.md`.

54. ~~**[P1] La tabla de Docente → Mis Estudiantes no está envuelta en `.tabla-scroll`.**~~ ✅ **Hecho (2026-07-29, ampliación aprobada de la microfase de cierre): se REUTILIZÓ `.tabla-scroll`**, el patrón que ya usan `TablaPro` (`DashboardWidgets.jsx:241`), `ImportarEstudiantes` y el Libro de Calificaciones — **sin CSS nuevo, sin un segundo sistema de scroll y sin tocar columnas ni datos**. Antes: desbordamiento del contenedor de contenido de 240px a 320, 185px a 375 y 80px a 768. Después: **0 en los cuatro anchos**, con la tabla completa y accesible por su propio scroll (238 de 519px visibles a 320), **las 5 columnas intactas**, **una sola barra horizontal** y **ninguna barra cuando no hace falta** (sidebar colapsado a 768 y escritorio a 1280, donde la tabla cabe entera). Evidencia en `CURRENT_STATE.md`.
55. ~~**[P2] Abrir una materia dispara 3 `GET /api/retos` a la misma materia**~~ ✅ **Hecho (2026-07-29): 3 peticiones → 1.** Verificado en el backend que `?materia_id=N` **sin `tipo` ya devuelve todos los tipos publicados**, y que `JUEGOS_UI` excluye `quiz` y `mision`, así que las 3 listas se derivan del mismo cuerpo sin solaparse. Los filtros nuevos son los de antes **más el `tipo` que ponía la query**. Equivalencia medida en dos materias con contenido distinto: Matemáticas **1 quiz / 1 juego / 0 misiones** y Lengua **0 / 1 / 1**, idénticas al baseline. Recorrido login→abrir materia: **16 → 14** peticiones. *(Enunciado original abajo.)* Abrir una materia disparaba 3 `GET /api/retos` a la misma materia (`?materia_id=N&tipo=quiz`, `?materia_id=N`, `?materia_id=N&tipo=mision`) cuando **el que va sin `tipo` ya devuelve todo**, más sus 3 preflight `OPTIONS`. El comentario del propio código ya dice «un solo listado»; el código hace tres. Tocarlo afecta la carga de contenido del estudiante (camino de demo) → diferido.
56. ~~**[P2] `cors()` sin `maxAge`** (`server/server.js`)~~ ✅ **Hecho (2026-07-29): `maxAge: 86400`,** una línea, sin cambiar qué orígenes se aceptan. Comprobado `Access-Control-Max-Age: 86400` en la respuesta real y, mejor aún, el comportamiento: antes `/api/retos` y `/api/misiones` pagaban **2 OPTIONS para sus 2 GET de StrictMode**; después, **1**. Navegando dos veces entre dos materias, los **8 GET siguientes generaron 0 preflight**. ~~**Exige redeploy para surtir efecto en producción.**~~ ✅ **CONFIRMADO VIVO EN PRODUCCIÓN (2026-07-29):** `Access-Control-Max-Age: 86400` presente en los preflight reales de `https://gamificapp.onrender.com` desde los dos orígenes legítimos, y también tras el reinicio del servicio por el cambio de `CORS_ORIGIN`. Es además la cabecera que sirve de huella para inferir qué commit corre en Render.
57. ~~**[P2] El panel Admin sondea cada 20s sin filtrar por la sección visible.**~~ ✅ **Hecho (2026-07-29).** `cargar()` acepta una lista de claves (`DATOS_POR_SECCION`); `null` = carga completa, que siguen haciendo **el montaje y toda escritura**. Solo el sondeo de 20 s se acota a la sección visible. Salvaguardas: `undefined` = «no tocada», el estado **no se vacía**; Misiones conserva `null` (no `[]`, que es *truthy* y rompería su módulo); el filtro **solo se aplica si llega un array**, así que `onCreado={cargar}`/`onImportado={cargar}` recargan todo; sección fuera del mapa → carga completa. **Medido con el admin restringido —el mismo perfil de la medición original—: Inicio 4 → 2 y Materias 4 → 1 por oleada**, con las oleadas a 20 s exactos. Para un Principal el mapa da 10 → 4 en Inicio y 10 → 1 en casi todas; **aritmética del mapa, no medido**. *(Enunciado original abajo.)* El panel Admin sondeaba cada 20s sin filtrar por la sección visible. Medido: 2 oleadas de 4 peticiones **exactamente a 20s** con el admin restringido; un Administrador Principal dispara **10 llamadas paralelas por oleada ≈ 30 req/min** sostenidas, refrescando auditoría/papelera/estudiantes aunque se esté mirando otra sección. **Mitigaciones que sí funcionan y quedan verificadas:** la guarda `document.hidden` (0 peticiones con la pestaña oculta) y el filtrado por permisos. Sin riesgo de saturación a escala de la tesis; rediseñarlo es reestructurar los efectos del panel → diferido.
58. ~~**[P2] La barra de XP anima `width`.**~~ ❌ **NO ES UN DEFECTO — cerrado el 2026-07-29 tras medirlo con la pestaña VISIBLE:** `width` en línea **30 %**, `getComputedStyle` **107,594 px de 358,672 px = 30,0 %** y `getAnimations()` **vacío**. El síntoma solo aparece con el reloj de animaciones congelado, **la misma forma de fallo que el falso «0/100»**. No se tocó nada. *(Enunciado original abajo.)* La barra de XP anima `width`. Con el reloj de animaciones congelado (pestaña no compositando) se renderiza **vacía** pese a tener el valor correcto en línea. El usuario real con pestaña visible la ve llenarse, y con `prefers-reduced-motion` se completa al instante. Es la misma forma de fallo que la Fase 8 diseñó deliberadamente *fuera* de la animación de entrada del Home (solo `transform`, nunca `opacity`); la barra de XP no recibió ese tratamiento. Sin impacto para el usuario final → diferido.
59. ~~**[P2] Un docente/estudiante desactivado o enviado a la Papelera conserva acceso hasta que expire su token (≤8h).**~~ ✅ **CERRADO por SPEC-019 (2026-07-29): aprobada por Fabrizio, implementada y verificada.** `autenticar` revalida `rol`/`activo`/`eliminado_en` contra la BD, como ya hacían `conPermiso` y `soloAdminPrincipal`; **401** y `authFetch` cierra sesión solo. **Un archivo, sin migración, sin frontend.** **Fail-closed:** con MySQL apagado, 4 rutas autenticadas dieron **503 con cero datos** mientras `/api/health` seguía en 200, y al volver la BD el mismo token recuperó el 200; la ÚNICA excepción tolerada es `ER_BAD_FIELD_ERROR`, justificada porque `activo`/`eliminado_en` los añaden las migraciones 003/004 y `inicializarEsquema()` corre dentro del callback de `app.listen` con el servidor sobreviviendo a su fallo. **9/9 escenarios pasan**; **0 × 401** en uso normal (Docente 45/45 y Estudiante 13/13 en 200); XP idempotente (`xp_abonado` 0/0 al reenviar). **Coste medido con baseline real: +0,35 ms de mediana en la ruta más liviana, dentro del ruido en rutas con trabajo real — pero es MySQL LOCAL; en Aiven será un round-trip de red más por petición, y hay que remedirlo tras el deploy.** ✅ **REMEDIDO EN PRODUCCIÓN (2026-07-29):** un round-trip a Aiven cuesta **~+19,8 / +20,0 ms** (estimación por proxy, `/api/institucion` menos `/api/health`), y una petición autenticada completa cuesta **+37,6 / +40,7 ms** sobre `/api/health` (medición end-to-end que incluye el trabajo propio de la ruta, **no aislable** sin desplegar un build sin SPEC-019). **60/60 peticiones autenticadas en 200, cero 401 y cero 503** con Admin Principal y Docente. **La revocación de una cuenta real NO se probó en producción** (Fabrizio no la autorizó): sigue validada solo en local. Enunciado original: `autenticar` solo verifica la firma y `soloDocente` solo mira el rol; **los caminos de admin sí revalidan contra BD** (`conPermiso`/`soloAdminPrincipal` comprueban `activo` y `eliminado_en`). Cerrarlo exige una consulta a BD por petición en `autenticar` → toca §10 (autenticación) y **requiere spec**. Documentado como limitación conocida.

**Deuda técnica explícitamente fuera del cierre de la tesis** (no reabrir sin spec): Zod o cualquier librería de validación, reestructuración de JWT, token en `localStorage`, code splitting (bundle de 1.6 MB / `pdf.worker` 1.29 MB), archivos base64 en MySQL, ausencia de pruebas automatizadas, Clean Architecture estricta y los **29** problemas de lint preexistentes.

### Diferidos de SPEC-018 Fase 4 (anotados 2026-07-20) — NO implementar sin decisión

27. ~~**Inconsistencia mensaje ↔ regla de backend al eliminar cursos (hallazgo PREEXISTENTE, detectado probando Fase 4).**~~ ✅ **Resuelto (2026-07-29, Microfase Final A). Decisión de Fabrizio: manda el backend, se corrige el mensaje.** Confirmado en vivo antes de tocar nada (`DELETE /api/admin/cursos/2` → **200** con 1 estudiante activado; dato restaurado desde la Papelera). El confirm, el botón y el toast pasan al patrón «Enviar a la papelera» de Materias y describen la regla real, incluido el 409 por pendientes de activación. **Backend sin tocar.** Añadido en la misma tanda: `SPEC-002 §2` documentaba la regla vieja **en sus dos filas `DELETE`** (cursos y materias) — corregidas, con nota fechada; `POLITICA-ELIMINACION.md` ganó la mención del 409.
28. **4 `window.confirm` no destructivos de editores siguen nativos** (`EditorClasificador` ×2, `GeneradorActividadIA`, `GeneradorMision` — avisan antes de sobrescribir contenido al generar con IA). Migrarlos a `ConfirmDialog` exige refactorizar el handler síncrono de generación a confirmación asíncrona en área estabilizada por SPEC-013; diferido.
29. ~~**`window.prompt` ×2 del cambio de PIN del estudiante.**~~ ✅ **Hecho (2026-07-30, bloque P1 de la fase RC, commit `b5f1dc3`).** Nuevo `src/pages/estudiante/ModalCambiarPin.jsx` sobre `ModalPanel`: tres campos (actual / nuevo / **repetir**), un solo ojo que revela los tres, contador «N de 6», aviso en vivo cuando los nuevos no coinciden y error inline con `role="alert"`. **Autenticación sin tocar** (mismo servicio, endpoint y parámetros), así que no hizo falta spec. **Con esto el proyecto queda a cero `window.prompt`.** El campo de confirmación cierra el riesgo real de los prompts: cambiar el PIN a algo que el niño no sabe. **Y la verificación funcional descubrió un bug crítico PREEXISTENTE que los prompts ocultaban:** `cambiar-pin` responde **401** con el PIN actual incorrecto y `authFetch` trataba cualquier 401 como sesión caducada → **el estudiante quedaba desconectado en silencio al teclear mal su PIN**. Corregido con `conservarSesionEn401` opt-in por llamada, que solo usa `cambiarPin`; el cierre automático por 401 sigue vigente en el resto de peticiones. *(Enunciado original abajo.)* Su reemplazo correcto no es un confirm sino el **modal de PIN** ya previsto en SPEC-001 (Student Shell); los `window.alert` de ese flujo ya son toasts (Fase 4).

### Diferidos de SPEC-006 (requieren migración propia)

15. Actividades **programadas** (fecha de publicación futura): no existe columna en BD.
16. Estadísticas por pregunta ("más fallada/acertada") y tiempo promedio: exigen `detalle_json` en `progreso_estudiante` + cambios en todos los reproductores.
17. El Libro de Calificaciones consulta el progreso estudiante por estudiante (N peticiones); con aulas grandes convendría un endpoint agregado por materia.

### Posibles extensiones del Banco de Preguntas (SPEC-010, anotadas 2026-07-16)

18. Banco para el **Clasificador**: analizado en SPEC-013 (2026-07-17) — la unidad viable es el **"grupo de clasificación" completo** (todas las categorías + elementos de una actividad como un solo ítem reutilizable, nunca categorías sueltas). Viable y coherente con el modelo snapshot+`_banco_id`, pero diferido a post-tesis por costo/beneficio.
19. "Banco de misiones" para **Misión Narrativa**: analizado y **descartado conscientemente** en SPEC-013 (2026-07-17) — reutilizar desafíos sueltos rompe la narrativa (su valor pedagógico central) y adaptarla con IA al insertar es costo desproporcionado. Si se retoma, el concepto correcto es reutilizar la historia COMPLETA vía duplicar/adaptar (`/api/ia/adaptar`), no ítems atómicos.

## 4. Dependencias entre items del backlog

- Specs 2–5 dependen de SPEC-001 (el shell con rutas es la base).
- **SPEC-017 y SPEC-013 ya NO están acopladas** (revisado 2026-07-19). La dependencia anotada antes partía de suponer que SPEC-013 no estaba implementada; en realidad sus fases 1-2 sí lo están y `BarraAccionesEditor` ya unifica la botonera. La Fase 3 de SPEC-017 se reduce a centralizar los mapas paralelos de `GeneradorActividadIA`, sin tocar `BarraAccionesEditor` ni rehacer SPEC-013.
- **SPEC-016 y SPEC-017 son independientes entre sí** y pueden implementarse en cualquier orden. Recomendado: SPEC-016 primero (menor, contenida, sin colisión con nada congelado).
- Multi-institución requiere spec propia obligatoria (cambios de BD).
- Detalle técnico de cada dependencia: ver la spec correspondiente en `docs/specifications/`.

## 5. Riesgos (prioridad de mitigación)

| Riesgo | Impacto | Mitigación |
|--------|---------|------------|
| Archivos base64 en MySQL (LONGTEXT) crecen sin límite | Lentitud/costos de BD | Post-tesis: mover a almacenamiento de objetos |
| Plan free de Render (cold start) | Latencia en primer uso | Keep-alive activo vía `/api/health` cada 14 min |
| Dependencia exclusiva de Gemini | Creación de contenido cae si Google falla | Reintentos multi-modelo ya implementados; **SPEC-016 la elimina** (proveedor conmutable Gemini/OpenAI). Fallback automático diferido: backlog §3 ítem 24 |
| Migración a rutas anidadas (SPEC-001) | Deep-links rotos, regresión CSS, romper docente/admin | Ver §5 de SPEC-001 antes de implementar |
| Logros del catálogo que nunca se otorgan | Promesa rota al niño | Backlog §3, ítem 2 |

## 6. Cómo funcionan realmente las migraciones (auditado 2026-07-19)

**Regla operativa:** los archivos de `database/migraciones/*.sql` son **referencia y versionado documental**. Las migraciones automáticas que se ejecutan en producción son **funciones idempotentes de `server/initDb.js`**.

Secuencia real en cada arranque del servidor (`inicializarEsquema()`, `initDb.js:17-59`):

1. Se abre una conexión de un solo uso con `multipleStatements` (el pool normal NO lo habilita, por defensa contra inyección).
2. Se ejecuta `database/produccion_defaultdb.sql` completo — idempotente (`CREATE TABLE IF NOT EXISTS` + upserts). **Ese script crea 11 tablas** (materias, cursos, institucion, estudiantes, retos, progreso_estudiante, usuarios, retroalimentaciones, materiales, docente_materia, invitaciones_estudiante); las **7 restantes** (auditoria, misiones, mision_estudiante, docente_curso, banco_preguntas, configuracion_ia, tipos_juego) nacen en el paso 3. Total: **18** (medido 2026-07-29 sobre una base vacía).
3. Se ejecutan en orden las funciones `migrarXxx(conn)` encadenadas en `initDb.js:36-52`, desde `migrarColumnasMaterias` hasta `migrarCalificacionAcademica`. Cada una se protege con `faltaColumna()` contra `information_schema` (MySQL 8 no soporta `ADD COLUMN IF NOT EXISTS`).
4. Se garantizan los invariantes de admin (`asegurarAdmin`, `asegurarAdminPrincipal`).

Consecuencias que hay que tener presentes al implementar una migración nueva:

- **El `.sql` por sí solo no hace nada.** Escribir el archivo y no añadir la función JS equivalente significa que la migración **nunca se aplica**. Este es el error más fácil de cometer en este repositorio.
- **Y a la inversa: sobre una base VACÍA el paso 3 no puede correr solo.** `migrarColumnasMaterias` (`initDb.js:36`) lanza un `ALTER TABLE materias` **antes** del `conn.query(sql)` de `:37`, así que una base recién creada falla con `Table 'materias' doesn't exist`. Una base nueva (deploy nuevo, restauración, instalación local) exige cargar antes `database/produccion_defaultdb.sql` a mano. Verificado 2026-07-29; `initDb.js` **no se modificó** (pieza crítica de producción).
- **`app.listen` empieza a aceptar peticiones antes de que terminen las migraciones**: el callback de `server.js:122` es asíncrono, así que `/api/health` puede responder con el esquema a medio crear. La señal fiable es `✅ Esquema verificado/creado en la base de datos.` o comprobar que existen las 7 tablas del paso 3.
- **No existe tabla de registro de migraciones aplicadas.** La idempotencia se deriva del estado del esquema, no de una bitácora.
- SPEC-015 está implementada solo como función (`migrarCalificacionAcademica`), sin archivo `.sql`. Es deliberado (decisión del 2026-07-19), no un olvido.
- Numeración actual: archivos hasta `012`; **`013` reservado a SPEC-016 y `014` a SPEC-017**.

Evolucionar esto a un sistema formal de migraciones versionadas está en §3 ítem 27, diferido a post-tesis.

# Pendientes

- **Probar EL PAQUETE en un equipo Windows realmente limpio** (requisito antes de enseñárselo al revisor). Lo que sigue **sin** ejercitarse, y solo se puede en una máquina sin Visual C++ Redistributable instalado: que las 3 DLL **app-local** basten por sí solas. En la prueba de aceptación del 2026-07-29 se demostró que `mysqld` **carga las copias del paquete** y no las de System32, pero eso no equivale a que en el equipo destino no exista otra copia. Protocolo manual redactado en la entrada de esa fecha de `CURRENT_STATE.md`. Sin VM disponible en el equipo de desarrollo.
- ~~**CREDENCIALES.txt pierde las cuentas de demostración al reinstalar** (P2 detectado el 2026-07-29).~~ **CERRADO el 2026-07-29.** El origen es ahora un marcador propio en `.run\datos-demo.txt` (no el `CREDENCIALES.txt` anterior, que es una salida y no una fuente de verdad), y antes de reescribirlo se comprueba contra la base de datos cuáles de esas cuentas permiten entrar hoy, con el mismo predicado que usa el login. Junto con él se cerraron los dos P3 (comentario obsoleto del `.cmd` y `README.md` fuera del paquete). Detalle y pruebas en la entrada del 2026-07-29 de `CURRENT_STATE.md`.
- ~~**Redesplegar el backend**: lo necesitan **SPEC-019** (§3 ítem 59) y el `maxAge` de CORS (§3 ítem 56); la corrección del ítem 55 va en el frontend (Vercel).~~ ✅ **HECHO Y VERIFICADO (2026-07-29).** Ambos despliegues son automáticos por push a `main` y ya ocurrieron. **Frontend probado byte a byte** (build local de HEAD = MD5 `5df100ff…41a9` idéntico al que sirven `www.gamificapp.com` y `gamificapp.vercel.app`); **backend por inferencia** de `Access-Control-Max-Age: 86400`, cabecera que solo existe desde `2718897`. Nota operativa descubierta en el proceso: **Vercel publica registros de despliegue en GitHub y Render no**, así que el estado del backend solo se puede establecer por comportamiento.
- ~~**Volver a medir el coste de SPEC-019 en producción.**~~ ✅ **MEDIDO (2026-07-29), con los tres números deliberadamente separados.** **(1) End-to-end medido:** una petición autenticada cuesta **+37,6 ms** (Admin) y **+40,7 ms** (Docente) de mediana sobre `/api/health` — pero ese delta **contiene también el trabajo propio de `/api/materias`**, no es de SPEC-019. **(2) Estimación por proxy de un round-trip a Aiven: +19,8 / +20,0 ms**, midiendo `/api/institucion` (un `SELECT` por clave primaria, sin autenticación) contra `/api/health` (no toca la BD); reproducido en dos tandas, antes y después del reinicio del servicio. **(3) NO aislado:** separar la cuota exacta de SPEC-019 exigiría desplegar un build sin ella como línea base, y no se hizo. Contexto: de los ~167 ms de una petición autenticada, **~125 ms son latencia base ajena a cualquier consulta**. Frente a los +0,35 ms de loopback local, en Aiven son ~20 ms absolutos, como la spec advirtió.
- ~~**Incidencia de CORS en producción: `https://gamificapp.vercel.app` no estaba autorizado** (P2, detectada el 2026-07-29 durante la auditoría de producción).~~ ✅ **CERRADA EL 2026-07-29 POR CONFIGURACIÓN, SIN TOCAR CÓDIGO.** El dominio público `https://www.gamificapp.com` sí funcionaba; el alias directo de Vercel no, y cualquier navegador en él recibía `TypeError: Failed to fetch`. Diagnosticada con **control positivo** (`fetch` en `no-cors` sí devolvía respuesta opaca, en `cors` no → rechazo de CORS, no problema de red). **No era regresión de `2718897`.** Se comprobó **antes de proponer nada** que `server.js:75-78` ya admite varios orígenes separados por comas (`trim()`, sin barra final, array a `cors()` con comparación `===`), así que **la solución mínima era la variable de entorno, no el código**. Fabrizio actualizó `CORS_ORIGIN` en Render conservando lo existente. Reverificado: ambos orígenes con su propio `Access-Control-Allow-Origin` y `Access-Control-Max-Age: 86400`, la app real funcionando en los dos con consola limpia, y **8 orígenes de control rechazados** (esquema `http`, sufijo, prefijo, apex, `localhost`, deploy de vista previa, `null`, dominio ajeno), **sin comodines**. El apex `gamificapp.com` **no** se añadió a propósito: hace 308 → `www` y nunca es origen de página. **Regla operativa para el futuro: el frontend puede desplegarse solo, pero estrenar un dominio nuevo exige actualizar `CORS_ORIGIN` en Render o el dominio nace roto.**
- ~~**Validar SPEC-016 y SPEC-017 en producción** antes de darlas por cerradas (checklists en `SPEC-016 §16` y en el informe de cierre de SPEC-017).~~ ✅ **HECHO EN SOLO LECTURA (2026-07-29), con los límites dichos en voz alta.** **SPEC-017:** panel operativo contra Aiven, 3 estados correctos, 7 tipos en activo con conteos reales; **falta** cambiar un estado y ver su efecto (exige escritura). **SPEC-016:** configuración correcta con Gemini `configurado: true` y sin fuga de secretos, más **una única generación real** (`POST /api/ia/generar`, 200 en 2.254 ms, quiz de 3 preguntas que pasó `validarConfig`, **no persistida**); **falta** el catálogo dinámico de modelos, el cambio de proveedor, «Probar conexión» y tipos distintos del quiz (exigen cuota). **El consumo de IA se limitó deliberadamente a una sola generación.** Evidencia completa en `CURRENT_STATE.md`.
- **Migraciones 013/014: encadenadas al arranque y presumiblemente ejecutadas; existencia de las tablas en Aiven NO confirmada directamente.** Las dos funcionalidades degradan en silencio ante tabla ausente (`estados.js:42`, `config.js:37`), así que ni la UI ni la API pueden distinguir «tabla vacía» de «tabla inexistente». Se cerraría **sin escribir nada** mirando los logs del último arranque en Render y buscando `✅ Esquema verificado/creado en la base de datos.` o un `⚠️ No se pudo inicializar el esquema`. Esta sesión no tiene acceso a esos logs (sin CLI ni credenciales de Render).
- **Subir el logo institucional real** desde Administración → Institución (§3 ítem 51: el código ya lo soporta, falta solo el dato).
- Aprobar SPEC-001 y arrancar su commit 1.
- Redactar Spec 2–5 a medida que se necesiten.
