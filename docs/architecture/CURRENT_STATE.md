# CURRENT_STATE — Tablero de estado

# Última actualización

2026-07-30 (**FASE RC — BLOQUE P2 CERRADO: 4 ÍTEMS DE LOS 8, TRAS REEVALUAR EL BACKLOG A DOS DÍAS DE LA ENTREGA.** Segundo y último bloque de la fase Release Candidate. Fabrizio aprobó **P2-8, P2-2, P2-3 y P2-5 (variante segura)**; **P2-1, P2-4 y P2-7 pasan oficialmente a Post v1.0** y **P2-6 queda aprobado solo si sobra tiempo — NO implementado**. **Cero cambios de arquitectura, BD, migraciones, permisos, XP, ranking, misiones o contratos de juegos.** Cinco archivos: `src/pages/admin/dashboard.jsx`, `src/pages/docente/docentePanel.css`, `src/pages/admin/dashboard.css`, `src/pages/admin/login.jsx` y `src/pages/estudiante/DashboardEstudiante.jsx`.

**MOTIVO DE LAS TRES RECLASIFICACIONES A POST v1.0.** **P2-4 (`<title>` por vista):** beneficio nulo para el usuario final —una sola pestaña, tanto el niño como el docente— y WCAG 2.4.2 ya se cumple formalmente con el título estático; a cambio obliga a tocar los **tres** paneles porque la navegación interna es `useState`, es decir paga la deuda de SPEC-001 sin resolverla. **P2-7 (densidad de filtros de la Biblioteca):** es un rediseño, no un pulido —colapsable nuevo con su responsive, 2-3 h y riesgo de regresión en el filtrado de una vista que hoy funciona—, contra §6.1 y §6.3. **P2-1 (pausa de toasts en hover/foco):** lógica de temporizadores sobre el componente de toasts, **infraestructura compartida por los 3 roles ya modificada en P1**, para un beneficio que ningún usuario real de esta app nota (el niño no hace hover y ningún toast lleva información irrecuperable).

**P2-8 · ESTADOS DE CARGA Y ERROR DEL HOME DEL DOCENTE.** Mismo defecto de clase que P1-1, en el otro panel: una lista vacía significaba a la vez «todavía no sé» y «no hay nada», y el Home siempre elegía la segunda. Con Render en frío el docente veía **«Hoy tienes 0 materias y 0 estudiantes»** y **«Aún no tienes materias asignadas»** mientras las peticiones viajaban. Ahora hay tres estados explícitos (`cargando`/`listo`/`error`) para materias, estudiantes y retos: **esqueletos** con la geometría de la tarjeta real, subtítulo que no afirma nada sin dato (`«Preparando tu aula…»`), y estado de error con **«Intentar de nuevo»** que recarga sin recargar la página. La tarjeta de materia deja de decir «Sin actividades · crea la primera» mientras cuenta: dice `«Contando actividades…»`. **`propagarError` (el opt-in que añadió P1) es lo que hace ALCANZABLE el estado de error**: por defecto `obtenerRetosPublicados` se traga el fallo y devuelve `[]`, que era exactamente el bug 2 de P1 repetido aquí. **El paso a `cargando` vive en el manejador de reintento y NO en los efectos**, para no introducir set-state en el cuerpo de un `useEffect` (mismo criterio que en el panel del estudiante). **Alcance real menor que el del plan:** el documento listaba también «materia del estudiante», pero P1 ya la había cubierto.

**P2-2 · WCAG 2.2 SC 2.4.11 «FOCUS NOT OBSCURED» — Y UNA CORRECCIÓN AL PLAN.** El plan nombraba **dos** contenedores; solo uno era real. **`.sidebar-nav` con `.sidebar-footer` es premisa falsa, medida:** el footer es **hermano** del contenedor con scroll, no hijo (`nav.contains(footer) === false`), y sus cajas se tocan sin solaparse (`nav.bottom` 659,4 = `footer.top` 659,4, **solape 0 px**); en móvil el footer pasa a `position: static` dentro del `.sidebar-menu` que scrollea. Un `scroll-padding` ahí habría sido un no-op, así que **no se añadió**. El caso real es `.contenido` con la barra de publicar del editor (`position: sticky; bottom: 0`), y **sí se reprodujo**. **Zona tapada medida:** 79 + 36 = **115 px** a 1280 y 159 + 24 = **183 px** a 375, donde la barra envuelve en dos filas; de ahí el valor único de **188 px**, acotado con `:has(.editor-publicar-barra)` para no reservar colchón en vistas que no lo necesitan (y que degrada al comportamiento de hoy si el navegador no soportara `:has()`). **Evidencia antes/después recorriendo con foco los 24 controles que preceden a la barra: 1280 → 4 tapados a 0** (incluido el campo de una pregunta real, «¿Cuánto es 10 - 4?», 35,8 px bajo la barra) **y 375 → 3 tapados a 0** (el peor, «Vista previa», **162,4 px bajo una barra de 159 px: oculto por completo**). **NOTA DE MÉTODO, PARA NO REDESCUBRIRLO COMO NO-DEFECTO:** la primera medición dio **0 tapados incluso sin la regla**, porque el bucle reiniciaba el scroll antes de cada foco. El fallo **solo aparece tabulando en secuencia**, que es como se navega de verdad; con saltos artificiales el navegador coloca el control muy por encima de la barra y no se reproduce.

**P2-3 · `aria-expanded` EN «¿OLVIDASTE TU PIN?».** Un atributo en `login.jsx`, cero lógica de autenticación — mismo principio que P1-8 (`aria-pressed` en las tarjetas de perfil), autorizado expresamente. `aria-controls` **solo viaja cuando la región existe**, porque el bloque de ayuda se desmonta al cerrarse y una referencia colgada es un fallo ARIA que las herramientas automáticas marcan. Verificado el ciclo completo en el navegador: cerrado `aria-expanded="false"` sin `aria-controls` y sin región en el DOM; abierto `"true"` con `aria-controls="login-ayuda-pin"` resolviendo a un elemento existente y visible.

**P2-5 · LA RACHA 🔥, VARIANTE SEGURA (LA ÚNICA AUTORIZADA).** El significado del 🔥 vivía **solo** en un `title=""`, que no existe con teclado ni en táctil —y un niño de 7 años no hace hover—, y para un lector de pantalla el chip sonaba «fuego 3». Ahora lleva `role="img"` + `aria-label` con singular/plural correcto, el **mismo patrón que ya usaba el ✓ de misión** (SPEC-018 F6.3); el árbol de accesibilidad lo confirma como `img "Racha: 1 día seguido jugando"`. **Restricción de Fabrizio cumplida y medida: cero texto visible nuevo y layout intacto** — el chip mide **47,78 px a 1280 y 47,78 px a 320**, idéntico, con `right` 226,4 < 320, barra de XP en 141,09 px (el valor que fijó SPEC-018 F6.3), **0 elementos fuera del viewport y 0 scroll horizontal**.

**VERIFICACIÓN FUNCIONAL, CONTRA EL BUILD DE DISTRIBUCIÓN.** Arrancado con `instalador/iniciar.ps1`: MySQL portable 3308, backend 3001 y **`dist/` servido en 5173** — el artefacto real, no el servidor de desarrollo. Los tres estados de P2-8 se ejercitaron **de verdad** parcheando `fetch` (retardo de 5 s y caída selectiva de `/api/docente/`, `/api/retos` y `/api/materias`, dejando pasar el login) y remontando el panel con un ciclo de logout/login: **cargando** → 3 esqueletos, `role="status"`, `aria-label="Buscando tus materias"`; **listo** → «Hoy tienes 2 materias y 3 estudiantes» con los conteos reales por materia; **error** → «No pudimos cargar tus materias» + «Intentar de nuevo», y **el botón recupera los datos sin recargar**, probado en 1280 y en 375. **El esqueleto reproduce el alto real medido:** 134 px frente a 134,41 px de la tarjeta a 1280, y 170 px frente a 169,8 px a 375, donde el detalle pasa a dos líneas (de ahí la media query añadida). **Consola del navegador sin un solo mensaje** en todo el recorrido.

**NO PUDO PROBARSE:** el panel de administrador (no se usaron las credenciales de `CREDENCIALES.txt`); el comportamiento con Render en frío, que es el escenario que motiva P2-8 y solo se reprodujo simulando latencia en local; y un quiz con muchas preguntas —el caso donde SC 2.4.11 castigaría más— porque crear ese contenido exigía escribir en la BD.

**CALIDAD:** `npm run build` limpio (3,19 s). **Lint: 29 problemas (26 errores + 3 warnings) medidos con `npx eslint src server` = línea base exacta, cero nuevos.**

**PENDIENTE DE DECISIÓN: P2-6** (bienvenida de primer uso del docente, `QuickActionCard` cuando `stats.actividades === 0`) sigue **sin implementar**, aprobado solo si sobra tiempo. Estimación 30-45 min, riesgo bajo por ser aditivo y condicional.)

2026-07-30 (**FASE RC — BLOQUE P1 CERRADO: 13 MEJORAS DE PRESENTACIÓN + 2 BUGS ENCONTRADOS Y CORREGIDOS EN LA VERIFICACIÓN FUNCIONAL.** Entra la fase de Release Candidate: se cierran las auditorías y se ejecuta un backlog único. Documentos de la fase: `docs/audit/Auditoria-UX-UI-Release-Candidate-v1.md` (auditoría UI/UX) y `docs/audit/RC-Plan-Limpieza-Y-Backlog-v1.0.md` (plan de limpieza + backlog P1/P2/P3). **Cero cambios de arquitectura, BD, migraciones, permisos, XP, ranking, misiones o contratos de juegos.**

**LIMPIEZA DEL REPOSITORIO — CASI TODO ERA UN NO-OP, Y ESO SE MIDIÓ.** Se verificó con `git ls-files` cada candidato de `REPOSITORY_CLEANUP.md`: **10 de las 12 categorías de la lista «Eliminar» no están versionadas** — `.gitignore` ya cubre `.env`, `server/.env`, `CREDENCIALES.txt`, `logs/`, `.run/`, `.claude/`, `node_modules/`, `dist/`, `release/` y `runtime/`. Borrarlas del disco no mejoraría el repositorio y **destruiría el entorno justo antes de validar el instalador** (`server/.env` apunta al MySQL portable 3308; `runtime/` son los 1,25 GB de Node y MySQL portables; `release/` es el paquete a probar). **Decisión de Fabrizio: no ejecutar ninguna.** De los 8 realmente versionados se borraron **solo los 4 assets sin referencias** (`src/assets/react.svg`, `vite.svg`, `hero.png`, `public/icons.svg`; 0 referencias verificadas en `src/`, `index.html` y `public/`). **Conservados por decisión:** `asistenteIA.jsx`/`respuestaIA.jsx` (el endpoint `/api/ia/asistente` queda intacto) y `tools/build_auditoria_docx.py` + el DOCX de auditoría (evidencia de tesis). **Dos correcciones al documento de limpieza:** `.asistente-*` de `adminDashboard.css` **no es CSS huérfano** (lo usan 6 módulos del admin), y `server/lib/totalEsperado.js` se mantiene porque su nombre figura en las fórmulas congeladas de SPEC-018 §3 y `totalEsperado` es control de seguridad de `POST /api/progreso`. **Anotado sin tocar:** `CLAUDE.md §3` documenta `server/lib/iaCliente.js` como el camino de la IA cuando el real es `server/lib/ia/` (el shim tiene 0 importadores).

**LOS 13 CAMBIOS DE P1.** (1) **Estados de carga y error del estudiante**: `DashboardEstudiante.jsx` pasa a tres estados explícitos (`cargando`/`listo`/`error`) para catálogo, retos y materia. Antes una lista vacía significaba a la vez «todavía no sé» y «no hay nada», y el panel siempre elegía la segunda: afirmaba **«Todavía no hay juegos»** mientras la petición viajaba y **para siempre si fallaba la red**, porque el `.catch(() => {})` se la tragaba. Ahora hay esqueletos con la geometría de las tarjetas reales, `«Buscando…»` en las pestañas, y estado de error con **«Intentar de nuevo»**. (2) **Contraste del podio del ranking** (widget «Top estudiantes» de la vista de materia del docente, `rank-pos-1/-2`): blanco sobre oro medía **2,15:1** y sobre plata **2,56:1** — texto oscuro los lleva a **8,31:1** y **6,96:1**; el bronce se dejó en blanco porque ya cumplía (5,02:1). (3) **Tipografía autoalojada**: Inter y Poppins salen del CDN de Google y viajan en `public/fonts/` (**4 archivos, 76 kB**, subset latin; Inter es variable y un solo fichero cubre 400-700), con `@font-face` en `index.css`, `font-display: swap` y `preload` de Inter. Motivo: la app **se instala en Windows** y sin internet la tipografía caía a `system-ui`, cambiando métricas e identidad visual en plena demo. (4) **Material Icons eliminado** de `index.html`: era una hoja externa bloqueante con **0 usos** en `src/`. (5) **Shell de arranque inline** en `#root`: antes había pantalla en blanco hasta que React montaba. (6) **Región de toasts permanente**: `ToastHost` devolvía `null` sin avisos, así que la región `aria-live` nacía junto a su contenido y el primer toast podía no anunciarse. (7) **`.toast-cerrar` a 24×24** (medía ~18×18, incumplía WCAG 2.2 SC 2.5.8). (8) **`aria-pressed` en las tarjetas de perfil del login** (el perfil elegido se comunicaba solo por color; es el mismo arreglo que SPEC-018 aplicó a `.opcion`). (9) **Mostrar/ocultar el PIN en el login**, reutilizando `.login-eye`: el docente ya podía ver su contraseña y el niño no su PIN. (10) **Estados vacíos del estudiante a `EmptyState`** — cierra el ítem **35** del MASTER_PLAN. (11) **`--color-text-muted` #64748b → #5c6a80**: sobre `--color-bg`, que es el fondo real de `.contenido`, medía **4,34:1** y no alcanzaba el 4,5:1 de WCAG 1.4.3; ahora **5,01:1** (5,49:1 sobre blanco) — cierra el ítem **36**. (12) **Capitalización**: el sidebar del estudiante decía «Mis Mundos»/«Mis Premios» y los encabezados «Mis mundos»/«Mis premios», visibles a la vez. (13) **Código muerto del login retirado**: el estado `aviso` y `.login-aviso` eran inalcanzables (`setAviso` solo recibía `""`).

**MODAL DE CAMBIO DE PIN (autorizado expresamente por Fabrizio).** Nuevo `src/pages/estudiante/ModalCambiarPin.jsx` sobre `ModalPanel`, en sustitución de **dos `window.prompt` encadenados** — el único diálogo nativo que quedaba en el camino del niño tras la Fase 4 de SPEC-018. Tres campos (actual / nuevo / repetir), un solo ojo que revela los tres, contador «N de 6», aviso en vivo cuando los nuevos no coinciden y error inline con `role="alert"`. **Autenticación sin tocar:** mismo `authService.cambiarPin(actual, nuevo)`, mismo `PUT /api/auth/cambiar-pin`, mismos dos parámetros; el servidor sigue validando. Cierra los ítems **29/37** del MASTER_PLAN sin spec porque el cambio es de piel, no de comportamiento (principio de SPEC-018 §3). El campo de confirmación elimina el riesgo real de los prompts: cambiar el PIN a algo que el niño no sabe.

**BUG 1 (CRÍTICO, PREEXISTENTE) — EQUIVOCARSE AL TECLEAR EL PIN CERRABA LA SESIÓN DEL NIÑO EN SILENCIO.** Encontrado ejecutando la verificación funcional, no leyendo código. Cadena: `PUT /api/auth/cambiar-pin` responde **401** cuando el PIN actual es incorrecto (`auth.js:529`) y `authFetch` trata **cualquier** 401 como sesión caducada y llama a `logout()` (`authService.js:190`), vaciando el token. Resultado medido: el segundo intento fallaba con **«Token requerido»** y el estudiante quedaba varado. **Existía igual con los `window.prompt`**; el modal solo lo hizo visible. **Corrección contenida:** `authFetch` acepta `conservarSesionEn401` (opt-in por llamada) y solo `cambiarPin` lo usa — en ese endpoint un 401 describe la credencial del cuerpo, no el estado de la sesión, que `autenticar` ya validó para llegar al handler. **La red de seguridad de cierre por 401 sigue intacta en todas las demás peticiones.** Sin tocar el backend: cambiar el código de estado a 400 sería el arreglo semánticamente más limpio, pero altera el contrato de la API y queda anotado para post-1.0.

**BUG 2 (MEDIO, INTRODUCIDO POR MÍ EN P1) — EL ESTADO DE ERROR ERA INALCANZABLE.** Con la red caída el panel seguía mostrando el falso vacío. Causa: `obtenerRetosPublicados` y `obtenerMaterial` **capturan el fallo y devuelven `[]`**, así que la excepción nunca llegaba al `Promise.allSettled` del componente. **Corrección:** ambos servicios aceptan `propagarError` (opt-in, por defecto siguen devolviendo `[]`) y solo el panel del estudiante lo pide. El otro consumidor (`dashboard.jsx` del docente) no pasa el flag y conserva su comportamiento previo. **Tercer defecto del mismo tipo, también corregido:** la tarjeta «Mis premios» decía «Juega para ganar tu primera insignia» a un estudiante con 10 insignias mientras cargaba; ahora, sin dato, no afirma nada.

**VERIFICACIÓN FUNCIONAL EN EL ENTORNO LOCAL, CON EL FLUJO OFICIAL Y CONTRA EL BUILD DE DISTRIBUCIÓN.** Arrancado con `instalador/iniciar.ps1`: MySQL portable 3308, backend 3001 y **`dist/` servido por `vite preview` en 5173** — es decir, se probó el artefacto real, no el servidor de desarrollo. BD local `gamificapp_dev` con el seed: 4 estudiantes, 6 materias, los 7 juegos publicados. **Probado y medido:** login de estudiante y de docente; **ciclo completo de cambio de PIN** (validación en vivo → PIN actual incorrecto con sesión conservada → cambio correcto con toast → login con el PIN nuevo → **restauración del PIN del seed**); esqueletos del Home con red retardada a 4 s; `«Buscando…»` y contador oculto al abrir materia; estado de error + «Intentar de nuevo» con red caída y recuperación posterior; las **4 pestañas** de materia con `EmptyState` y **cero** `vacio-msg` residual; **fuentes con `peticionesAGoogle: []`** y 3 `.woff2` locales en 7/3/13 ms; las **3 medallas del ranking medidas en el DOM real** (8,31 / 6,96 / 5,02); `.toast-cerrar` **24×24**; `--color-text-muted` = `#5c6a80`; a11y del modal (`role=dialog`, `aria-modal`, `aria-labelledby`, foco dentro, scroll bloqueado); sidebar colapsable (contenido **750 → 959 px**, raíl visible, **relevo de foco** correcto en ambos sentidos); móvil a **375 px** (hamburguesa, `aria-expanded`, scroll de fondo bloqueado, **sin scroll horizontal**, contenido en y=63 px, 0 elementos desbordados). **Consolas: navegador cero errores** (los únicos `warn` son los fallos de red simulados a propósito); **backend 5 líneas, todas ✅, `backend-errores.log` vacío**.

**NO PUDO PROBARSE:** el recorrido de admin (no se usaron las credenciales de `CREDENCIALES.txt`); jugar los 7 juegos de punta a punta con XP (fuera del alcance de P1, que no toca juegos ni XP); el comportamiento con Render en frío, que es el escenario que motivó el cambio (1) y solo se reprodujo simulando latencia en local.

**CALIDAD:** `npm run build` limpio (3,16 s). **Lint: 29 problemas (26 errores + 3 warnings) sobre el código fuente = línea base exacta del ítem 12 del MASTER_PLAN, cero nuevos.**

**CORRECCIÓN DE UNA AFIRMACIÓN MÍA EN ESTA MISMA ENTRADA (2026-07-30, verificada después).** Durante P1 reporté una línea base de **58 problemas (52+6)** y la atribuí a «una actualización del plugin de React Hooks». **Las dos cosas eran falsas y quedan corregidas.** El utillaje es **idéntico al lock** —`eslint 10.4.1`, `eslint-plugin-react-hooks 7.1.1`, `eslint-plugin-react-refresh 0.5.2`, instalado = declarado—, así que no hubo ninguna deriva de herramientas. La causa real: **`npm run lint` ejecuta `eslint .`, y `eslint .` recorre `release/GamificApp/`, que contiene una copia completa del código fuente generada por el empaquetador.** `.gitignore` **no** se aplica al flat config de ESLint, que solo obedece a sus propios `ignores` (hoy únicamente `dist`). Cada problema se contaba **dos veces**: una en la fuente y otra en la copia empaquetada. Evidencia: el desglose por regla es el doble exacto del documentado (`set-state-in-effect` 13→26, `exhaustive-deps` 3→6, `refs` 1→2, `no-useless-assignment` 1→2), las rutas con hallazgos son **14 de la fuente + 14 de `release/`**, y **`npx eslint src server` da exactamente 29 (26 errores + 3 warnings)** — el número que esta bitácora ya había fijado el 2026-07-29 y que el MASTER_PLAN §3 repite. **La conclusión operativa de P1 no cambia: cero problemas nuevos**, porque el factor ×2 afectaba igual a la medición inicial y a la final. **La línea base sigue siendo 29 y solo se mide sobre `src` y `server`.** El «58» aparece únicamente cuando existe una carpeta `release/` en el disco, así que es un artefacto del entorno, no del código. **No se corrigió `eslint.config.js`** (P1 no toca configuración): añadir `release` a `globalIgnores` queda anotado como ítem **62** del MASTER_PLAN.

**P2 NO IMPLEMENTADO** (pendiente de autorización): pausa de toasts en hover/foco, `scroll-padding` para WCAG 2.2 SC 2.4.11, `aria-expanded` en el disclosure del PIN, `<title>` por vista, la racha 🔥 que hoy solo se explica en un `title=""`, bienvenida de primer uso del docente, densidad de filtros de la Biblioteca y estados de carga en las vistas restantes.)

2026-07-29 (**VALIDACIÓN EN PRODUCCIÓN DE SPEC-016 Y SPEC-017 — LAS DOS SPECS DEL REVISOR, AUDITADAS EN SOLO LECTURA CON UNA ÚNICA GENERACIÓN REAL DE IA AUTORIZADA.** Verificado contra Git antes y después: `main` = `origin/main` = `60d481a`, working tree limpio. **Cero cambios de código, cero escrituras en la BD de producción, cero estados de juego modificados, cero configuración de IA persistida.** Sesión de Administrador Principal abierta **manualmente por Fabrizio**; en ningún momento se pidieron ni se almacenaron sus credenciales, y ningún token aparece en este documento.

**SPEC-017 — VALIDADA EN PRODUCCIÓN.** El panel «Gestión de juegos» carga contra Aiven y devuelve los **3 estados exactos de la spec** (`activo`, `solo_jugar`, `deshabilitado`) y los **7 tipos instalados**, todos en **activo**: Quiz (6 actividades / 1 publicada), Misión Narrativa (6/1), Clasificador (7/1), Memorama (3/1), Línea del tiempo (4/1), Completar espacios (3/1) y **Verdadero o Falso (1/0)**. **Total 30 actividades y 6 publicadas: son conteos reales de la tabla `retos`, así que prueban que el endpoint consultó la BD de verdad y no un valor por defecto.** Por juego, la API expone `tipo`, `etiqueta`, `emoji`, `descripcion`, `capacidades {ia, banco, reutilizar, automatico}`, `integracion {evaluacion, ia, banco}`, `estado`, `actividades` y `publicadas`. **Lo que más pesa para la defensa: el séptimo juego, el de demostración de extensibilidad, está vivo en producción con todas sus capacidades.** No se pulsó ningún botón de estado; solo `GET`, nunca el `PUT`.

**SPEC-016 — VALIDADA FUNCIONALMENTE, CON UNA GENERACIÓN REAL Y UNA SOLA.** Dos partes. **(1) Configuración, sin contacto con el proveedor:** se evitó a propósito abrir la sección de IA del panel, porque `ModuloIA` pide el catálogo de modelos en un `useEffect` al montar (`ModuloIA.jsx:49`) y eso llamaría a Gemini sin necesidad. En su lugar se consultó solo `GET /api/admin/ia/configuracion`, que por código lee únicamente BD y registro: **200** con `proveedor: gemini`, `modelo: null` (automático), `origen: entorno`, y los dos proveedores implementados — **Gemini `configurado: true`** (la clave está puesta en Render) y **OpenAI `configurado: false`**. **(2) Generación real, el flujo del docente:** `POST /api/ia/generar`, que es el endpoint del modal «generar con IA» (`GeneradorActividadIA.jsx`) y que **por diseño NO persiste nada** —se comprobó leyendo el handler antes de ejecutarlo: ni `INSERT` ni auditoría, a diferencia de `/api/ia/sorpresa`, que sí llama a `guardarBorradorIA()` y por eso se descartó—. Una única llamada: quiz de Matemáticas, «sumas hasta 10», dificultad fácil, **3 preguntas (el mínimo del rango `[3,10]`)** → **200 en 2.254 ms**, 3 preguntas devueltas, `xp_sugerida` 300 y descripción normalizada «Quiz de Matemáticas sobre sumas hasta 10». Muestra real: *«¿Cuánto es 2 + 3?»* con `correcta: "B"`. **La actividad NO se guardó ni se publicó.**

**POR QUÉ LA ESTRUCTURA ESTÁ VALIDADA, DICHO CON PRECISIÓN.** El 200 no es una impresión: `generarActividad()` ejecuta `VALIDADORES_CONFIG[tipo]` **después** de normalizar y **lanza** si falla (`actividadesIA.js:97-101`), así que una respuesta 200 garantiza que `quiz.validarConfig` pasó — es decir, `preguntas` no vacío, cada pregunta con enunciado, con al menos las alternativas A y B, y con una `correcta` que existe entre ellas. **Nota metodológica, para que no se malinterprete el dato crudo:** una comprobación propia de estructura dio `false`, pero **el error era mío**: buscaba `opciones` como array con índice numérico cuando el esquema real del quiz es `alternativas {A,B,C,D}` con `correcta` como **letra** (`quiz.js:7-26`), que es justo lo que devolvió. No se repitió la generación para corregir la comprobación: el límite de una sola llamada se respetó.

**SEGURIDAD: NINGÚN SECRETO EXPUESTO, COMPROBADO SOBRE RESPUESTAS REALES.** El cuerpo completo de `/api/admin/ia/configuracion` mide 293 caracteres y se revisó literal: del secreto solo sale el booleano `configurado`, conforme a SPEC-016 §5.1. Los patrones de clave real (`AIza…`, `sk-…`) **no aparecen** en ninguna respuesta, tampoco en la de la generación. **Falso positivo propio, aclarado en vez de reportado como hallazgo:** un barrido genérico marcó «contiene api_key» y al extraer las coincidencias exactas resultaron ser los literales **`GEMINI_API_KEY`** y **`OPENAI_API_KEY`**, es decir los **nombres** de las variables de entorno, no sus valores. **Consola sin un solo mensaje** en todo el recorrido y estáticos en 200/304.

**MIGRACIONES 013 Y 014, CON LA CLASIFICACIÓN ESTRICTA.** **Evidencia directa:** `migrarConfiguracionIA` y `migrarTiposJuego` están encadenadas en `initDb.js` dentro de `inicializarEsquema()`, son `CREATE TABLE IF NOT EXISTS` idempotentes, el backend ha reiniciado dos veces hoy, y la BD responde con datos reales (30 actividades, 6 materias, institución). **Inferencia:** que ambas tablas existen en Aiven. **NO verificable sin escritura ni logs: la existencia de `tipos_juego` y `configuracion_ia` en Aiven no está confirmada directamente**, porque las dos funcionalidades degradan en silencio ante tabla ausente (`estados.js:42` y `config.js:37`). Se confirmó en la práctica: `origen: entorno` significa «sin fila en `configuracion_ia`» **o** «sin tabla», y no hay forma de separarlos desde fuera. Redacción oficial: **«migraciones encadenadas al arranque y presumiblemente ejecutadas; existencia de las tablas en Aiven no confirmada directamente»**. Se cerraría mirando los logs de Render, a lo que esta sesión no tiene acceso (sin CLI ni credenciales).

**DISCREPANCIA ENCONTRADA — P3, REGISTRADA Y NO CORREGIDA (ítem 61).** `registro.js:35` mapea `modelosSugeridos` en `proveedoresPublicos()`, pero **ningún adaptador lo define**, así que sale siempre `undefined` y JSON lo omite — visible en la respuesta real. Sin impacto funcional: SPEC-016 §4.5 decidió a propósito que el catálogo de modelos sea **dinámico**, así que el campo es un vestigio del contrato. No se tocó.

**LO QUE NO SE HIZO, A PROPÓSITO.** No se abrió la sección de IA del panel, no se pulsó «Probar conexión», no se cambió el proveedor ni el modelo, no se guardó configuración (que además dispararía su propia prueba real antes de persistir, `adminIA.js:112`), no se llamó al endpoint de listado de modelos, no se usó OpenAI, no se cambió el estado de ningún juego, no se guardó ni publicó la actividad generada, y no se inspeccionó en ningún momento el valor de `GEMINI_API_KEY`. **El consumo de cuota se limitó deliberadamente a una única generación.**

**LO QUE SIGUE SIN VALIDARSE DE ESTAS DOS SPECS.** De SPEC-017, cambiar un estado y ver su efecto en docente y estudiante (exige escritura). De SPEC-016, el catálogo dinámico de modelos, el cambio de proveedor y «Probar conexión» (exigen contactar al proveedor), y cualquier generación de un tipo distinto del quiz.)

2026-07-29 (**AUDITORÍA DE PRODUCCIÓN DEL COMMIT `2718897` — DESPLIEGUE CONFIRMADO, UNA INCIDENCIA DE CORS ENCONTRADA Y CERRADA POR CONFIGURACIÓN, Y LA PRIMERA MEDICIÓN REAL DEL COSTE DE SPEC-019 EN AIVEN.** Verificado contra Git antes y después: `main` = `origin/main` = `2718897`, working tree limpio. **Cero cambios de código, cero commits de aplicación y cero cuentas de producción tocadas**; lo único que se escribe es esta documentación.

**CÓMO SE DESPLIEGA, LEÍDO DEL REPOSITORIO Y NO ASUMIDO.** No existe **ninguna** configuración de despliegue versionada: sin `vercel.json`, sin `render.yaml`, sin `.github/workflows`. Ambos proveedores se configuran desde sus paneles, así que el estado se comprobó por fuera. **Vercel publica registros de despliegue en GitHub** (30 entradas, todas de `vercel[bot]`): la de `2718897` es la `5666402516`, creada el 2026-07-29 23:17:50Z, estado **`success`** → auto-deploy en push a `main` confirmado. **Render NO publica ningún registro en GitHub**, así que su estado solo se puede establecer por comportamiento.

**COMMIT REALMENTE DESPLEGADO, CON DOS NIVELES DE EVIDENCIA DISTINTOS Y ASÍ SE DICEN.** **Frontend = `2718897`, probado byte a byte:** recompilando HEAD con `VITE_API_URL=https://gamificapp.onrender.com` sale el mismo nombre con hash de contenido (`index-9E0s5i9r.js`) y **el mismo MD5, `5df100ffea38240413ab859f753241a9`**, que sirven **los dos** frontends (`www.gamificapp.com` y `gamificapp.vercel.app`). Prueba directa, no inferencia. **Backend = `2718897` por inferencia acotada:** `Access-Control-Max-Age: 86400` solo existe desde este commit (`server/server.js:83`), y Render despliega commits completos, así que `auth.js` con SPEC-019 está vivo. **No es prueba de comportamiento del middleware** y no se declara como tal.

**INCIDENCIA REAL DE CONFIGURACIÓN: `gamificapp.vercel.app` NO ESTABA EN LA ALLOWLIST DE CORS. P2, ENCONTRADA, DIAGNOSTICADA Y CERRADA.** Diagnóstico con control positivo, que es lo que lo hace concluyente: desde `https://gamificapp.vercel.app` un `fetch` en modo `cors` a `/api/health` daba **`TypeError: Failed to fetch`** mientras el mismo `fetch` en modo `no-cors` **sí devolvía respuesta opaca** → la red llegaba y el rechazo era de CORS, no aislamiento del entorno. Confirmado por `curl`: el backend no emitía `Access-Control-Allow-Origin` para ese origen **ni para el valor por defecto `http://localhost:5173`**, lo que demostraba que `CORS_ORIGIN` tenía un valor deliberado que simplemente no lo incluía. **NO era regresión de `2718897`**: ese commit solo añadió `maxAge`, sin tocar qué orígenes se aceptan. Contraste conductual limpio entre los dos dominios: en `www` la app poblaba `institucion_cache` y aplicaba el título institucional; en `vercel.app` el título se quedaba en «GamificApp» y no había caché. **Se comprobó antes de proponer nada que el código YA soportaba varios orígenes** (`server/server.js:75-78` parte `CORS_ORIGIN` por comas, hace `trim()`, quita una barra final y pasa un **array** a `cors()`, que compara con `===` entrada por entrada) → **no hacía falta tocar `server.js`**, y no se tocó. **Fabrizio corrigió `CORS_ORIGIN` en Render conservando lo existente y añadiendo `https://gamificapp.vercel.app`.**

**REVERIFICACIÓN TRAS EL CAMBIO: LOS DOS ORÍGENES LEGÍTIMOS FUNCIONAN, Y NADA MÁS.** En `https://www.gamificapp.com` y `https://gamificapp.vercel.app`, ambos: preflight **204**, `Access-Control-Allow-Origin` **con su propio origen**, `Access-Control-Max-Age` **86400**, `GET /api/health` **200 + ACAO**, `GET /api/institucion` **200 + ACAO**, preflight del login **204 + ACAO** y `POST /api/auth/login` con cuerpo `{}` → **400 «Faltan credenciales» + ACAO**, que prueba que el preflight pasa **y la petición llega al backend**, comprobado **sin usar ninguna credencial real** y sin escribir nada (el 400 sale antes de tocar la BD). En navegador, con caché limpia y recarga completa, **la propia aplicación** repobló `institucion_cache` y aplicó el título institucional desde **los dos** orígenes, con **consola sin un solo mensaje** y estáticos en 200/304. **Ocho orígenes de control, todos rechazados sin ACAO:** `evil.example.com`, `http://gamificapp.vercel.app` (esquema http), `gamificapp.vercel.app.evil.com` (sufijo), `evil-gamificapp.vercel.app` (prefijo), el apex `gamificapp.com`, `localhost:5173`, un deploy de vista previa de Vercel y el origen `null`. **Ningún comodín `*` en ninguna respuesta, sin `Access-Control-Allow-Credentials`, y sin cabecera `Origin` no se emite ACAO.** El apex **no** se añadió a propósito: hace **308 → www**, así que nunca llega a ser origen de página. Los deploys de vista previa de Vercel **siguen bloqueados** porque usan hostnames distintos: la allowlist es exacta por host.

**PRUEBA AUTENTICADA, EJECUTADA POR FABRIZIO (no se pidió ni se almacenó contraseña ni token).** Dos pasadas de 30 muestras contra producción, con **Administrador Principal** y con **Docente**:

| | login | `/api/materias` p50 | `/api/health` p50 | delta p50 | códigos |
|---|---|---|---|---|---|
| **Admin Principal** | 1.037,8 ms | **171,1 ms** (min 160,7 · p90 215,6 · p95 252 · máx 362,9) | 133,5 ms (p90 183,1 · máx 290) | **+37,6 ms** | **200 × 30** |
| **Docente** | 790,5 ms | **166,6 ms** (min 160,4 · p90 173,4 · p95 197,5 · máx 236,7) | 125,9 ms (p90 133,7 · máx 134,6) | **+40,7 ms** | **200 × 30** |

**EL RESULTADO OPERATIVO ES EL TITULAR: 60/60 PETICIONES AUTENTICADAS EN 200, CERO 401 Y CERO 503.** Cero 401 significa que el muro de SPEC-019 no expulsa a cuentas válidas en uso normal; **cero 503 significa que la rama fail-closed no se disparó ni una vez en 60 peticiones consecutivas**, es decir que la conexión a Aiven se mantuvo estable. Los dos roles se comportan igual, como corresponde a un muro que hace la misma consulta para todos. **El login no pasa por el muro** (`/api/auth` se monta en `server.js:92`, antes del `app.use('/api', autenticar)` de `:98`), así que sus ~0,8-1,0 s son coste de `bcrypt`, deliberado y ajeno a SPEC-019.

**COSTE DE SPEC-019: TRES NÚMEROS DISTINTOS QUE NO SE MEZCLAN.** **(1) Medido end-to-end:** una petición autenticada cuesta **+37,6 / +40,7 ms de mediana** sobre `/api/health`. Ese delta **NO es de SPEC-019**: contiene la consulta del muro **más el trabajo propio de `/api/materias`** (su propia consulta y una carga útil mayor). **(2) Estimado por proxy, no aislado: ~+19,8 / +20,0 ms.** `/api/institucion` es un `SELECT` por clave primaria a Aiven **sin autenticación** y `/api/health` no toca la BD, así que su diferencia es un round-trip a Aiven, que es exactamente la forma de la consulta que añade SPEC-019. Medido dos veces desde la misma máquina, **antes y después del reinicio del servicio**: p50 123,3 → 143,3 ms (**+20,0**) y p50 124,7 → 144,5 ms (**+19,8**). Reproducible. **(3) Lo que NO se puede afirmar:** aislar la cuota exacta de SPEC-019 dentro del delta medido exigiría desplegar en producción un build **sin** SPEC-019 como línea base, y **no se hizo ni se va a hacer**. Los dos números son coherentes entre sí —~20 ms de muro + ~18-21 ms de trabajo propio de la ruta ≈ los ~38-41 ms medidos— pero **eso es consistencia aritmética, no aislamiento**, y así queda dicho. Para dimensionarlo: de los ~167 ms que tarda una petición autenticada, **~125 ms son latencia base de red y proceso que existiría igual sin ninguna consulta**. Frente a los +0,35 ms de mediana medidos en loopback local, el orden de magnitud en Aiven es **~50 veces mayor en términos relativos y ~20 ms en absolutos**, tal como la spec advirtió que pasaría.

**REVOCACIÓN: NO PROBADA EN PRODUCCIÓN, Y NO SE DECLARA PROBADA.** Fabrizio **no autorizó** modificar, desactivar, eliminar ni enviar a la Papelera ninguna cuenta real para demostrarla. La revocación queda **validada funcionalmente en local** por los 9 escenarios contra MySQL real (ver la entrada siguiente de este documento), y en producción la conclusión se limita exactamente a esto: **middleware desplegado por inferencia del commit activo, sesiones válidas funcionando, 60/60 autenticadas en 200, cero 401, cero 503.** Nada más.

**LIMITACIONES DE LA PRUEBA, SIN MAQUILLAR.** (1) El commit del backend es **inferencia por cabecera**, no lectura del despliegue de Render. (2) **La revocación de una cuenta real no se probó** (no autorizada). (3) El coste de SPEC-019 **no está aislado**, solo acotado por proxy. (4) `login_ms` es **una sola muestra** por rol, no una distribución. (5) Las latencias se midieron desde la red del equipo de desarrollo en Guayaquil contra Render en `iad1`; el tramo cliente→Render se cancela en las restas, pero **los valores absolutos no son los de la escuela**. (6) La pasada del Administrador tuvo más ruido de red (`/api/health` p90 183,1 y máx 290 frente a 133,7 y 134,6 del Docente), así que la pasada del Docente es la medición más limpia de las dos. (7) El panel de red del navegador usado solo lista peticiones del propio origen y **no captura los `fetch` cruzados a la API**: la evidencia de esas llamadas son los `fetch` directos y el repoblado de `institucion_cache` por la propia app. (8) **`/api/misiones` se evitó a propósito** en todo el recorrido: su evaluación perezosa (SPEC-007 §2.3) puede liquidar recompensas y mover XP real.

**HALLAZGO DOCUMENTAL, CORREGIDO AQUÍ.** La fila **3.8 del roadmap** de `MASTER_PLAN.md` declaraba SPEC-016 «pendiente de aprobación, ninguna fase iniciada», **contradiciendo a su propia línea de actualización del 2026-07-19 y al código**: existen `server/lib/ia/` con `proveedorGemini.js`, `proveedorOpenAI.js`, `config.js`, `probar.js` y `registro.js`, la ruta `server/routes/adminIA.js`, el servicio `src/services/iaConfigService.js` y la migración **013**, que además está **enganchada en la cadena de arranque** (`migrarConfiguracionIA` en `initDb.js`, igual que `migrarTiposJuego` de SPEC-017/014). Fila corregida. **Consecuencia útil para producción:** como ambas migraciones corren en cada boot y el servicio acaba de reiniciarse por el cambio de `CORS_ORIGIN`, se han vuelto a ejecutar; **su validación FUNCIONAL en producción sigue pendiente** y no se declara hecha.

**LO QUE NO SE TOCÓ, A PROPÓSITO.** Cero código, cero commits de aplicación, cero cuentas, cero XP, cero PIN, cero reseed, cero migraciones manuales, cero escrituras en la BD de producción. Las únicas peticiones con efecto de escritura posible fueron los dos logins de Fabrizio, que en cuentas admin/docente solo ejecutan `UPDATE usuarios SET intentos_fallidos = 0` (no-op si ya estaba en 0) y **no** registran auditoría, a diferencia del login de estudiante, que sí lo hace (`auth.js:208`) y por eso se excluyó de la prueba.)

2026-07-29 (**SPEC-019 — REVOCACIÓN INMEDIATA DE ACCESO: APROBADA POR FABRIZIO, IMPLEMENTADA Y VERIFICADA.** Cierra el ítem **59**, la observación del experto de más peso para la defensa. **Un solo archivo de código: `server/middleware/auth.js`.** Cero migraciones, cero frontend, cero cambios en login, JWT, PIN, código de emergencia, XP, misiones, ranking ni permisos.

**QUÉ CAMBIA.** `autenticar` deja de creerle solo a la firma del token y le pregunta a la BD si esa persona **sigue teniendo acceso hoy** (`SELECT rol, activo, eliminado_en FROM usuarios WHERE id = ?`), que es exactamente lo que `conPermiso` y `soloAdminPrincipal` ya hacían para los administradores. Cuenta inexistente, en la Papelera o con `activo = 0` → **401**, que `authFetch` ya traduce a cerrar sesión sin escribir una línea de frontend. **La BD manda también sobre el `rol`:** a quien degraden, su token viejo no le conserva el rol viejo.

**FAIL-CLOSED, Y LA ÚNICA EXCEPCIÓN ESTÁ JUSTIFICADA CON EVIDENCIA, NO POR ANALOGÍA.** Se comprobó antes de decidir: (1) `usuarios.activo` y `usuarios.eliminado_en` **NO están en el esquema base** — los añaden las migraciones **003** y **004**; (2) `inicializarEsquema()` corre **dentro del callback de `app.listen`** (`server/server.js:126`) y **si falla el servidor sigue vivo a propósito**. O sea, la ventana de «esquema sin migrar atendiendo tráfico» **existe de verdad**, y sin tolerancia un fallo de migración dejaría a toda la escuela fuera con 401. Por eso `ER_BAD_FIELD_ERROR` —y **solo** ese— conserva el comportamiento anterior, **nunca en silencio**: emite un aviso limitado a uno por minuto. **Cualquier otro fallo (MySQL caído, timeout, conexión perdida, error SQL inesperado) NO deja continuar la petición: 503.** Se eligió 503 y no 500 (que se confundiría con un error de lógica) ni 401 (que le cerraría la sesión a todo el mundo por una caída de la BD).

**LOS 9 ESCENARIOS, TODOS POR LA API REAL (nada de SQL directo): PASAN.** Cuenta desactivada con sesión abierta: 200 → **401** → reactivar → 200. Docente a la Papelera: su token pasa a **401** y el reintento de login también; al **restaurarlo, el MISMO token vuelve a 200**. Estudiante en Papelera enviando una partida: `POST /api/progreso` **401** y **el XP no se movió (6300 → 6300)**. Estudiante restaurado: login por PIN **200**. Sin token / token roto / firma alterada: **401, 401, 401**. Administrador Principal: sin cambios. **XP intacto e idempotente:** reenvío ×2 del mismo intento → **201/201**, `xp_total` 6300 y 6300, **`xp_abonado` 0/0**.

**FAIL-CLOSED PROBADO APAGANDO MYSQL DE VERDAD.** Con la BD **detenida** y un token **válido**, las 4 rutas autenticadas probadas dieron **503 con cuerpo de error y CERO datos**, mientras `/api/health` (fuera del muro) seguía en **200** — prueba de que el proceso estaba vivo y la negativa vino del muro. El log registró `[SPEC-019] No se pudo verificar el acceso: ECONNREFUSED` en las 4. Al levantar MySQL, **el mismo token recuperó el 200**: la caída **no le cerró la sesión a nadie**. La rama tolerada (`ER_BAD_FIELD_ERROR`) **no se provocó** —habría exigido tirar columnas de la BD de desarrollo— y queda verificada por lectura de código; así se dice, sin declararla probada.

**NINGÚN 401 EN USO NORMAL.** 19 rutas por API con 0 respuestas 401, y en navegador **Docente 45/45 en 200** recorriendo sus 7 secciones y **Estudiante 13/13 en 200** (Inicio, Mis Mundos, Mis Premios y dos materias con sus 4 pestañas). **Cero 503 en todo el recorrido.**

**COSTE, MEDIDO REVIRTIENDO `auth.js` PARA TENER UN BASELINE REAL** (200 peticiones por pasada, con calentamiento). `/api/materias`, la ruta autenticada más liviana: **p50 1,479 ms sin SPEC-019 → 1,832 / 1,939 / 1,645 ms con ella** en tres pasadas ≈ **+0,35 ms de mediana**. `/api/progreso/1`, con trabajo real: **2,028 ms sin → 1,832 / 2,299 / 2,001 ms con** — es decir, **dentro del ruido de medición**, con pasadas incluso más rápidas que el baseline. **ADVERTENCIA QUE NO HAY QUE CALLARSE: esto es MySQL LOCAL por loopback. En producción la BD es Aiven, REMOTA, así que el coste real no serán 0,35 ms sino un round-trip de red más por petición autenticada.** Atenúan el golpe que la consulta sea por clave primaria y sobre una conexión ya abierta del pool, y que el ítem 57 acabe de recortar las peticiones del panel Admin. **Hay que volver a medirlo tras el deploy.**

**HALLAZGO MENOR DE LA VERIFICACIÓN, SIN RELACIÓN CON EL CAMBIO.** Tres rutas que probé al principio devolvían 404/403 y resultaron ser **nombres inventados por mí**, no defectos: la real es `/api/docente/mis-estudiantes` (no `/estudiantes`), el ranking vive en `/api/ranking` (no `/api/docente/ranking`), `/api/banco` lista el banco (no `/api/banco/preguntas`), y `/api/misiones` responde **403 a quien no es estudiante por diseño** (`server/routes/misiones.js:13`). Repetida la prueba con las rutas correctas: **19/19 sin un solo 401**.

**REGRESIÓN.** `npm run build` limpio (3,90 s). **Lint 29 problemas (26 errores + 3 warnings) = baseline exacto, cero nuevos.** **Estado final de los datos idéntico al de partida:** 1 docente, 4 estudiantes, 2 administradores, **papelera con 0 elementos**, XP del estudiante 6300, y los tres accesos (admin, docente, estudiante por PIN) funcionando. **Producción sin validar hasta el deploy**, como el resto del proyecto.)

2026-07-29 (**CIERRE DE LAS OBSERVACIONES ABIERTAS DEL EXPERTO — 3 CORREGIDAS Y MEDIDAS, 2 AUDITADAS SIN TOCAR, 1 ELEVADA A SPEC.** Verificado contra Git antes de tocar nada: `main` = `origin/main` = `aa50256`, working tree limpio. Alcance deliberado: **cero BD, cero migraciones, cero autenticación, cero XP, cero permisos, cero dependencias, cero instalador**. Tres archivos de código tocados: `server/server.js` (una línea), `src/pages/estudiante/DashboardEstudiante.jsx` y `src/pages/admin/AdminDashboard.jsx`. El instalador y SPEC-018 se trataron como congelados por decisión de Fabrizio.

**ÍTEM 55 — ABRIR UNA MATERIA PASA DE 3 PETICIONES DE RETOS A 1.** Baseline medido en navegador antes de tocar nada: `?materia_id=1&tipo=quiz`, `?materia_id=1` y `?materia_id=1&tipo=mision`, más sus 3 preflight. Verificado leyendo `server/routes/retos.js` que **el que va sin `tipo` ya devuelve todos los tipos publicados de la materia**, y que `JUEGOS_UI` **excluye** `quiz` y `mision` (`enPestanaJuegos`), así que las tres listas se derivan del mismo cuerpo sin solaparse. Los filtros nuevos son los de antes **más el `tipo` que antes ponía la query** (`r.tipo === 'quiz'`, `r.tipo === 'mision'`). **Equivalencia comprobada en dos materias con contenido distinto:** Matemáticas **1 quiz / 1 juego / 0 misiones** y Lengua y Literatura **0 / 1 / 1** — idénticos byte a byte al baseline, con los mismos títulos, número de preguntas y XP. Peticiones totales del recorrido login→abrir materia: **16 → 14**.

**ÍTEM 56 — `cors()` CON `maxAge: 86400`.** Una línea en `server/server.js`, sin cambiar qué orígenes se aceptan. Comprobado en la respuesta real: `Access-Control-Max-Age: 86400` presente en el preflight de `/api/retos?materia_id=1`. **Evidencia de comportamiento, no de configuración:** antes, `/api/retos` y `/api/misiones` pagaban **2 OPTIONS para sus 2 GET de StrictMode**, separados por milisegundos; después, **1 OPTIONS para los 2 GET**. Y navegando dos veces de ida y vuelta entre dos materias, los **8 GET posteriores generaron 0 preflight nuevos**.

**ÍTEM 57 — EL SONDEO DEL PANEL ADMIN YA SOLO PIDE LO DE LA SECCIÓN VISIBLE.** `cargar()` acepta una lista de claves; `null` = carga completa, que es lo que siguen haciendo **el montaje y toda escritura** (`ejecutar()` llama `cargar()` sin argumentos). Solo el `useAutoRefresh` de 20 s pasa `DATOS_POR_SECCION[pagina]`. Detalles que importan: `undefined` significa «no tocada en esta pasada» y **su estado se deja como estaba, no se vacía**; Misiones conserva `null` (no `[]`) cuando falta permiso, porque su módulo espera un objeto con catálogos y `[]` es *truthy*; el filtro **solo se aplica si llega un array**, así que los `onCreado={cargar}`/`onImportado={cargar}` que pasan `cargar` como manejador siguen recargando todo; y una sección que no esté en el mapa cae en la carga completa (por defecto seguro: pide de más, nunca de menos). **Medido con el admin restringido, el mismo perfil con el que el experto midió las «4 peticiones por oleada»** (permisos `materias` + `estudiantes`): en **Inicio 4 → 2** por oleada y en **Materias 4 → 1**, con 3 oleadas a **20 s exactos** en ambos casos. Para un Administrador Principal el mapa da **10 → 4 en Inicio y 10 → 1** en la mayoría de secciones; **esto es aritmética del mapa, NO está medido** y así queda dicho.

**ÍTEM 58 — AUDITADO, NO ES UN DEFECTO. NO SE TOCÓ.** Con la pestaña **visible**, la barra de XP del Home rinde exacta: `width` en línea **30 %**, `getComputedStyle` **107,594 px de 358,672 px = 30,0 %**, y `getAnimations()` **vacío** (la transición ya terminó). El síntoma de «barra vacía» solo aparece con el reloj de animaciones congelado del entorno de previsualización, **la misma forma de fallo que el falso «0/100»**. Sin impacto para el usuario final.

**ÍTEM 33 — AUDITADO Y MEDIDO, NO SE TOCÓ (fuera de alcance: no es del experto ni del revisor, y SPEC-018 está congelada).** Solo quedan **dos** usos de `color: var(--color-success)`: `.linea-evento-check` en `juegos.css`, que es **CSS muerto** (el JSX de Línea del tiempo nunca lo aplica — ítem 42), y `.opcion-correcta .opcion-icono`, que **no es texto sino un SVG de MUI** (`CheckCircleRoundedIcon`), así que le aplica SC 1.4.11 (**3:1**), no el 4.5:1 de texto. Medido en runtime: **3,001:1** sobre `--color-success-soft` → **cumple, con margen prácticamente nulo**. Su contraparte `.opcion-incorrecta .opcion-icono` mide 3,953:1. Subirlo a `--color-success-dark` daría **4,57:1** cambiando un solo token; **queda como decisión de Fabrizio, que decidió dejarlo como está.**

**ÍTEM 59 — SPEC-019 APROBADA POR FABRIZIO E IMPLEMENTADA EN LA MISMA SESIÓN.** Detalle completo en la spec y en el bloque de abajo de esta entrada.

**DECISIONES DE FABRIZIO EN ESTA SESIÓN.** Los ítems **24** (fallback automático entre proveedores de IA) y **27** (sistema formal de migraciones versionadas) **siguen fuera**, tal como los difirió en su momento. El ítem **33** se deja como está.

**REGRESIÓN.** `npm run build` limpio (3,40 s; CSS 154,23 kB). **Lint 29 problemas (26 errores + 3 warnings) = baseline exacto, cero nuevos.** Smoke en navegador con MySQL real (portable, puerto 3308; Docker no estaba levantado): **Estudiante** (Inicio con sus 6 mundos, Nivel 7 / 6300 XP, y las 4 pestañas de dos materias distintas) y **Administrador restringido** (Inicio, Estudiantes con 4, Materias con 6, Misiones con 46/46). **Consola sin errores de aplicación.** **DATOS: no se completó ninguna partida → cero `POST /api/progreso`**; no se cambió ningún PIN, no se reseedeó y no se ejecutó ninguna migración.

**INCIDENTE DE LA SESIÓN, DICHO SIN MAQUILLAR.** Un script de verificación propio recorrió `li button` en la sección Materias y **desactivó las 6 materias** y abrió dos modales. **Se revirtió por la propia aplicación** (botón «Activar» de cada fila, no por SQL): las 6 quedaron **Activa**, con **orden 1–6, nombres, iconos y colores intactos y ninguna en la Papelera**, confirmado además desde el lado del estudiante, que volvió a ver sus 6 mundos. Ninguna materia llegó a irse a la Papelera (esa confirmación se canceló). **Efecto residual: entradas extra en la tabla `auditoria` de la BD LOCAL de desarrollo**, sin ningún efecto en producción. Lección aplicada en el resto de la sesión: nunca volver a hacer clic por selector amplio sobre una lista con acciones.)

2026-07-29 (**PRUEBA DE ACEPTACIÓN DEL INSTALADOR LOCAL — EL PAQUETE DE `71db7c6` SE INSTALA, SE ABRE Y SE USA DESDE CERO. CERO CÓDIGO TOCADO.** Verificado contra Git antes de empezar: `main` = `origin/main` = `71db7c6`, working tree limpio; al terminar, **el repositorio sigue sin un solo cambio de código** (solo este documento y `MASTER_PLAN.md`). Toda la prueba se hizo **desde el paquete de distribución, nunca desde el repositorio**.

**QUÉ CLASE DE PRUEBA ES, DICHO SIN ADORNOS: CASO B — SIMULACIÓN AISLADA FUERTE, NO UN WINDOWS RECIÉN INSTALADO.** Lo que sí se aisló de verdad: paquete generado con `empaquetar.ps1` y **extraído del ZIP** en `%TEMP%\GA-Limpio`, `%LOCALAPPDATA%` **redirigido** a una carpeta desechable (datadir, `root.txt` e `instancia.json` propios), y `PATH` **saneado** quitando `C:\Program Files\nodejs`, `%APPDATA%\npm` y Docker — comprobado en el momento: `Get-Command node` y `Get-Command mysql` **no resolvían nada**. Lo que NO se puede simular aquí y **no se declara probado**: que falte el *Visual C++ Redistributable* en `System32`, un Windows sin políticas ni antivirus previos, y el arranque tras reiniciar el sistema operativo.

**PRIMERA INSTALACIÓN: 61,1 s, código de salida 0, SIN INTERNET Y SIN NADA INSTALADO.** Punto de partida real: sin `server/.env`, sin base de datos, sin datos de ejecución y sin procesos previos. Secuencia medida: Node portable v22.23.1 reconocido dentro del paquete → **datadir inicializado de cero** en el `%LOCALAPPDATA%` aislado → root protegido con contraseña aleatoria → puerto **3308** → puertos 3001 y 5173 libres → **«Dependencias de servidor/frontend incluidas en el paquete: no hace falta descargar nada»** (los `dependencias-*.sha256` hicieron su trabajo: cero `npm ci`, cero red) → `gamificapp_dev` creada, esquema base **0 → 11 tablas** → usuario `gamificapp_local` con permisos solo sobre esa base → `server/.env` generado con 15 claves (`GEMINI_API_KEY`, `OPENAI_API_KEY` e `IA_MODELO` vacías; `RESET_HABILITADO=false`) → build → backend → `/api/health` 200 → `/api/institucion` 200 → **esquema completo 18 tablas y cuenta admin lista** → datos demo → frontend 200 → `CREDENCIALES.txt`.

**LOS TRES PROCESOS SALEN DEL PAQUETE, COMPROBADO POR `ExecutablePath`**: `…\GA-Limpio\GamificApp\runtime\node\node.exe` (×2, PID 7252 backend y 30120 frontend) y `…\runtime\mysql\bin\mysqld.exe` (PID 15992 padre y 40248 hijo, dueño del socket 3308). **Y `mysqld` carga el Visual C++ APP-LOCAL, no el del sistema**: enumerando los módulos de **los dos** procesos, `VCRUNTIME140.dll`, `VCRUNTIME140_1.dll` y `MSVCP140.dll` resuelven a `…\GA-Limpio\GamificApp\runtime\mysql\bin\`, ninguna a `System32`. Es la evidencia más fuerte disponible sin una máquina limpia, y **no sustituye** a esa prueba.

**PRIMER ACCESO: HAY RUTA CLARA Y SE PROBÓ.** El mecanismo es `ADMIN_PASSWORD` aleatoria de 14 caracteres → `server/.env` → `initDb.asegurarAdmin` la sincroniza con `bcrypt` en cada arranque → el instalador la escribe en `CREDENCIALES.txt` **y la anuncia en pantalla**. Probado por el mismo `POST /api/auth/login` que usa el formulario, leyendo la contraseña del archivo como haría el revisor: **200, token de 236 caracteres, `rol=admin`, `es_principal=true`**; con contraseña incorrecta, **401**. `JWT_SECRET` no aparece en `CREDENCIALES.txt` ni en ningún log. **Sin datos demo el revisor entra igual**: la cuenta `admin` no depende del seed (comprobado en la segunda ejecución, donde `CREDENCIALES.txt` ya no traía cuentas demo y el login de admin siguió dando 200).

**SMOKE TEST DE LOS 3 ROLES EN NAVEGADOR, CON DATOS REALES DE LA INSTALACIÓN.** **Administrador** (13 secciones de Principal): Inicio (**1 docente, 4 estudiantes, 0 invitaciones** — leídos de la BD), Docentes, Estudiantes (4), Materias (6), Cursos (2), Institución, Administradores (2), Papelera (vacía, con su `EmptyState`) y Auditoría (sin actividad, con el suyo). **Docente** `docente.demo`, entrando **por el formulario real**: Inicio («2 materias y 3 estudiantes»), Materias, Biblioteca (6 actividades), Mis Estudiantes (3), Ranking (4), Misiones y Mi Perfil; se abrió **Crear actividad** hasta el selector de tipo y **no se creó nada**. **Estudiante** «Estudiante Prueba Uno» con **PIN**: Inicio (Nivel 6, 5000 XP), Mis Mundos (6), Mis Premios (Nivel 7, 6300 XP, 10/46 misiones — la evaluación perezosa de SPEC-007 §2.3 poniéndose al día, ya documentada), apertura de Matemáticas y del quiz «Suma y resta básica», que renderizó sus 2 preguntas. **No se jugó ninguna partida: cero escrituras de progreso.** **Cero errores de consola en todo el recorrido** y todas las llamadas a la API en 200/204 (incluidos los preflight CORS desde `localhost:5173`).

**DETENER → INICIAR: 3,8 s y 10,6 s, POR LOS `.cmd` DE DOBLE CLIC.** Tras detener: **0 procesos huérfanos** y 3001/5173/3308 libres, con el cierre ordenado certificado por el propio `error.log` («*Received SHUTDOWN from user root*», «*Normal shutdown*», «*Shutdown complete*»). El arranque **no reinstaló nada**, reutilizó Node y MySQL portables y encontró las **18 tablas**; después, `admin` 200 y el estudiante con PIN 200, con **4 estudiantes, 1 docente, 6 materias y 7 retos** intactos.

**SEGUNDA EJECUCIÓN DEL INSTALADOR: SEGURA, Y MEDIDA CON HUELLAS.** 12,1 s. Conservó `server/.env` (**mismo SHA-256 antes y después**), no tocó `secretos\root.txt` (**mismo SHA-256**), **no reinicializó** (`instancia.creada` e `ibdata1.CreationTime` idénticos), reutilizó la instancia ya en marcha en vez de lanzar otra (**2 procesos `mysqld` = padre e hijo de una sola instancia**), omitió los datos demo por sí solo («*ya tienes datos guardados y no se escribe encima de ellos*»), dejó el esquema en **18 → 18** tablas y los conteos sin duplicar (4/1/6/7). Los únicos avisos: detuvo sus propios procesos de 3001 y 5173 para reinstalar, que es lo correcto.

**HALLAZGO P2 — REINSTALAR BORRABA LAS CUENTAS DE DEMOSTRACIÓN DE `CREDENCIALES.txt`. CORREGIDO Y REVERIFICADO (ver el bloque de cierre al final de esta entrada).** Medido antes del arreglo: **4 líneas `•` antes, 0 después**. Causa raíz: `instalar.ps1` §13 reescribe el archivo entero y `$lineasDemo` solo se rellena si se siembra en esa misma pasada; la segunda ejecución no siembra —y hace bien— así que el bloque desaparecía. El revisor perdía el usuario del docente y el PIN del estudiante sin forma documentada de recuperarlos. Nunca rompió la instalación: la cuenta `admin` y su contraseña se conservaban intactas.

**SEGURIDAD: 18 archivos de log y estado revisados buscando los VALORES reales de `JWT_SECRET`, `ADMIN_PASSWORD`, `DB_PASSWORD` y la clave de root → 0 fugas.** `secretos\root.txt` quedó con una **única ACL, solo para la cuenta actual**. El paquete se auditó antes de entregarse (231 archivos propios revisados) y salió limpio: sin `.env`, sin `CREDENCIALES.txt`, sin `docs/`, sin `.git`, sin `empaquetar.ps1`. Integridad de formato comprobada en la copia extraída: los 3 `.cmd` con **CRLF y sin BOM**, los `.ps1` con **CRLF y BOM UTF-8**, cero finales LF sueltos.

**PAQUETE PROBADO: 750,8 MB en 61.161 archivos; ZIP de 243,6 MB en 683 s (728,5 s el empaquetado completo); extracción en 19,6 s.** Reparto sin cambios respecto a la Fase 2.3: `runtime\mysql` 398,8 MB (53,1%), `node_modules` 205,0 MB, `runtime\node` 94,9 MB, `server\node_modules` 46,8 MB.

**EL ENTORNO DE DESARROLLO NO SE TOCÓ, Y SE COMPROBÓ AL TERMINAR.** Los datos reales conservan `instancia.creada = 01:40:10` e `ibdata1` creado a las `01:40:02`, y `root.txt` su SHA-256; el contenedor Docker siguió atendiendo el 3307 durante toda la sesión. Se detuvo la instancia de desarrollo solo porque el instalador exige los puertos **3001 y 5173 exactos**, y se volvió a levantar con su propio `Iniciar GamificApp.cmd`.

**VEREDICTO: APTO PARA REVISOR**, con la limitación de arriba dicha en voz alta — falta la prueba en una máquina Windows realmente limpia y tras reiniciar el sistema operativo. Procedimiento del revisor, verificado paso a paso: extraer el ZIP entero → doble clic en `Instalar GamificApp.cmd` → responder `s` o `n` a los datos de demostración → esperar ~1 minuto → se abre solo en `http://localhost:5173` → entrar como `admin` con la contraseña de `CREDENCIALES.txt`. Después, `Iniciar` y `Detener`.

**CIERRE DE LOS TRES HALLAZGOS (misma sesión, sobre un paquete REGENERADO desde el `main` corregido).** Alcance: **cero cambios en la aplicación** — `src/**` y `server/**` intactos salvo el `bd-verificar.mjs` del instalador; ni BD, ni autenticación, ni XP, ni permisos, ni migraciones.

**P2 — CERRADO. La corrección NO copia texto a ciegas.** Dos piezas. **(1) Origen:** cuando el seed tiene éxito, `instalar.ps1` §11 guarda sus líneas `•` en `.run\datos-demo.txt` — estado de ejecución de cada equipo, ignorado por Git y fuera del paquete. **El `CREDENCIALES.txt` anterior NO se lee nunca**: es una salida para el usuario, no una fuente de verdad, y puede haberse editado a mano. **(2) Vigencia:** en una ejecución que no siembra, §11-bis extrae de cada línea el identificador (`docente.demo`, o el nombre entre comillas del estudiante) y **pregunta a la base de datos cuáles permiten entrar HOY**, con el mismo predicado que usa el login: la cuenta existe en `usuarios`, `eliminado_en IS NULL` (papelera) y `activo = 1`. Hallazgo que simplificó el diseño: **las tres clases de cuenta demo son filas de `usuarios`** —el estudiante se localiza por `nombre_norm`, igual que en `auth.js`— así que basta un único predicado. Solo sobreviven las líneas cuya cuenta responde; el resto se descarta con un aviso. Implementado en `bd-verificar.mjs` como consulta **de solo lectura** (`GA_CUENTAS`), que no altera su campo `ok` y comprueba antes en `information_schema` si `eliminado_en`, `activo` y `nombre_norm` existen, para no romper en un esquema a medio migrar.

**PRUEBAS DEL P2, TODAS SOBRE COPIAS AISLADAS RECIÉN EXTRAÍDAS DEL ZIP NUEVO.** **(A) Con demo:** primera instalación **54,0 s**, marcador creado con 4 líneas. Segunda ejecución **12,1 s** → «*Se conservan 4 cuentas de demostracion que siguen activas*», **4 líneas antes y 4 después, 0 duplicados**, y `server/.env` con **el mismo SHA-256**, `root.txt` igual, `instancia.creada` e `ibdata1.CreationTime` sin cambio, **18 → 18 tablas** y conteos idénticos (4 estudiantes, 1 docente, 6 materias, 7 retos, 2 administradores). Los cuatro accesos siguieron funcionando: admin **200**, `admin.limitado` **200**, `docente.demo` **200**, estudiante por PIN **200**; contraseña incorrecta **401**. **(B) Salvaguarda de obsolescencia, probada de verdad:** se envió `admin.limitado` a la papelera por la API real (`DELETE /api/admin/administradores/2` → **200**, y su login pasó a **401**) y se reinstaló: «*Se conservan 3 cuentas… · 1 cuenta(s) de demostracion ya no existen o estan desactivadas: no se vuelven a anunciar*». `CREDENCIALES.txt` quedó con **3** líneas, sin la cuenta borrada. **(C) Sin demo:** primera instalación con respuesta `n` → **0 líneas `•`**, **sin marcador**, admin **200** y `docente.demo` **401**. Segunda y tercera ejecución → siguen **0 líneas**, sin inventar ninguna cuenta, con `server/.env`, `root.txt` e `instancia.creada` idénticos y admin **200**.

**P3 (comentario) — CERRADO.** `Instalar GamificApp.cmd` decía «*Requisitos previos: Node.js y MySQL 8 ya instalados*», justo lo contrario de lo que hace el producto, en el archivo que el revisor abre primero. Sustituido por la realidad: no hace falta instalar nada, GamificApp trae y prepara sus runtimes portables sin tocar Windows ni pedir permisos de administrador. **Cero cambios de comportamiento**; verificado que el archivo sigue en **CRLF y sin BOM**, y los `.ps1` en **CRLF con BOM**, conforme a `.gitattributes`.

**P3 (README) — CERRADO POR EXCLUSIÓN.** `README.md` es documentación del repositorio: remite a `CLAUDE.md`, `START_HERE.md` y `docs/`, que se excluyen del paquete a propósito, y explica un flujo (npm, build manual, Docker) que el revisor no necesita. Deja de viajar en la distribución; **sigue intacto en el repositorio**. El paquete pasó de 61.161 a **61.160 archivos** y la auditoría del empaquetador de 231 a **230** archivos propios revisados. Comprobado en el ZIP final: `GamificApp/README.md` **no existe**, y los 397 `README.md` restantes están todos dentro de `node_modules` de terceros. Para que la exclusión no dejara al revisor sin información, **`LEEME.txt` se completó con lo que le faltaba**: la URL `http://localhost:5173` (antes solo decía «se abre sola»), la pregunta de datos de demostración con qué significa responder `s` o `n`, y un bloque «CÓMO ENTRAR» con dirección, usuario `admin` y dónde está la contraseña.

**REGRESIÓN Y SEGURIDAD DEL CIERRE.** `npm run build` limpio (3,33 s). **Lint 29 problemas (26 errores + 3 warnings) = baseline exacto, cero nuevos.** Los cuatro `.ps1` del instalador pasan el analizador de sintaxis de PowerShell. **0 fugas** buscando los valores reales de `JWT_SECRET`, `ADMIN_PASSWORD`, `DB_PASSWORD` y la clave de root en los 29 archivos de log y estado de las dos instalaciones de prueba. El marcador nuevo vive en `.run\` (ignorado por Git, confirmado con `git check-ignore`), va en UTF-8 sin BOM y **no contiene nada que el propio seed no imprima ya en pantalla**: son credenciales ficticias de datos ficticios.

**LO QUE SIGUE ABIERTO, SIN MAQUILLAR.** La prueba sigue siendo **CASO B**: paquete real extraído del ZIP, `%LOCALAPPDATA%` redirigido y `PATH` saneado, pero **en el equipo de desarrollo**. Falta —y no se declara hecho— ejecutar el paquete en un **Windows realmente limpio o una VM**, sin *Visual C++ Redistributable* en `System32`, y comprobar el arranque **tras reiniciar el sistema operativo**.)

2026-07-29 (**MICROFASE FINAL A — CORRECCIONES DE ALTO VALOR + CIERRE DE B3.** Verificado contra Git antes de tocar nada: `main` = `origin/main` = `b50594a`, working tree limpio. Alcance deliberado: **cero backend, cero XP, cero migraciones, cero dependencias, cero reseed**. Los 6 archivos de código tocados son 3 JSX de texto/semántica y 3 CSS.

**B3 — CERRADO: NO REPRODUCIBLE EN NAVEGADOR VISIBLE. El «0/100» era un artefacto del entorno, no un fallo de la app.** Esta vez la prueba SÍ fue válida y se demostró antes de jugar: `document.visibilityState = "visible"` y **646 frames de `requestAnimationFrame` en 11 388 ms (57 fps)** — frente a la medición del mismo día con el panel oculto, **1 frame en 5 580 ms mientras `setInterval` daba 41 ticks**. Dos partidas reales del quiz **ya existente** «Suma y resta básica» (2 preguntas, 200 XP; no se creó ninguna actividad). **(1) Partida perfecta**: `POST /api/progreso {aciertos:2,total:2}` → **201** con `calificacion:100, mejor_calificacion:100, porcentaje:100` y el overlay terminó en **100 / 100** («2 de 2 correctas»). **(2) Partida intermedia**: `{aciertos:1,total:2}` → **201** con `calificacion:50, mejor_calificacion:100, porcentaje:50` y el overlay terminó en **50 / 100** («1 de 2 correctas»). **Backend y overlay coinciden en las dos.** La prueba decisiva es la traza del contador muestreada cada 100 ms: **0 → 15 → 26 → 35 → 41 → 45 → 48 → 49 → 50** (y 10 → 39 → 60 → 75 → 86 → 93 → 97 → 99 → 100 en la perfecta), con `visibilityState` visible en **las 93 y las 229 muestras** respectivamente. **El contador arranca en 0 y sube por `requestAnimationFrame`: con el reloj congelado se queda literalmente en el 0 inicial, que es exactamente el síntoma reportado.** No se tocó `ResultadoActividad`, ni la calificación, ni el XP, ni se añadió ningún `setTimeout`, fallback o workaround.

**DATOS QUE ESCRIBIERON LAS PARTIDAS: prácticamente ninguno, y medido.** Antes y después, `GET /api/progreso/1` devuelve **exactamente lo mismo**: `xp_total 6300` y la fila del reto 1 con `porcentaje 100, calificacion 100, xp_obtenido 200, completado 1` y **el mismo `actualizado_en` (06:40:40)**. Motivo: el `ON DUPLICATE KEY UPDATE` usa `GREATEST`, así que un intento igual o peor no cambia ninguna columna (y MySQL no toca `ON UPDATE CURRENT_TIMESTAMP` si ningún valor cambia); el XP se abona solo por la mejora (`xp_abonado: 0` en ambas). `nuevas_misiones: []` las dos veces. **El único rastro son 2 filas de auditoría** (`completo-reto` y `avanzo-reto`), que es un log append-only por diseño. No hizo falta restaurar nada.

**A3 — MENSAJE DE ELIMINACIÓN DE CURSOS: CORREGIDO EL TEXTO, NO LA REGLA.** El confirm decía «Solo es posible si no tiene estudiantes» y era **falso**, comprobado en vivo: `DELETE /api/admin/cursos/2` respondió **200** con **1 estudiante en el curso** (dato restaurado después desde la Papelera). La regla real, leída en `admin.js:600`: soft-delete a la papelera, y el **único** 409 es que algún estudiante siga pendiente de activar su cuenta (SPEC-014 §13). El texto nuevo dice exactamente eso y reutiliza el patrón de Materias («Enviar a la papelera» en título, botón, `title` y toast). **Backend intacto.**

**B1 — CONTRASTE EN LA BIBLIOTECA: 3.15:1 → 4.79:1.** Medido en runtime, no deducido: color real `#16a34a` sobre el **fondo real de la vista previa `#f8fafc`** (no blanco), texto 16px/600 → umbral AA 4.5:1 → **3.15:1, falla**. Cambiado **solo ese uso** a `--color-success-dark` → **4.79:1**. Verificado en sus dos consumidores (vista previa de Quiz y de Misión). **`--color-success` global sigue en `#16a34a`.**

**B2 — MISIÓN BLOQUEADA: 2.42:1 → 5.16:1 SIN TOCAR EL ATENUADO.** El contraste se calculó **compositado de verdad** (grayscale 0.35 → opacity 0.62 sobre el fondo de página), no sobre el color declarado. Hallazgo que **invalida la hipótesis del triage**: subir la opacidad no resuelve nada — con el gris de origen, `0.75` da **3.04:1** y **ni al 100% pasa de 4.84:1**. Por eso la corrección va por color (`--color-text` en `.mision-desc`, `.mision-progreso-txt` y `.mision-hint`) y **`opacity: .62` y `grayscale(.35)` quedan intactos**: el estado «bloqueado» se ve igual. Las tarjetas no bloqueadas no se tocan (verificado: siguen en `#6b7280` con opacidad 1). Probado a 320 y 1280.

**RESPONSIVE 320 px — CORREGIDO CON MEDIA QUERY, Y NO BAJANDO EL `minmax`.** A 320 los 6 mundos se apilaban en 1 columna: rejilla **754px**, scroll **1231px**. Con `@media (max-width: 359px) { repeat(2, 1fr) }`: **427px** y **904px**, tarjetas de 137×133 (muy por encima del mínimo táctil), **cero desbordes y cero nombres cortados**. **La alternativa obvia se descartó por regresión medida**: bajar el `minmax` a 137px metía una **tercera** columna a 480px, encogiendo las tarjetas de 217 a 140px. Con la media query, **375, 480, 768 y 1280 quedan idénticos** (medido uno a uno).

**HEADINGS DEL DOCENTE — SOLO LO SEMÁNTICO.** Auditados los tres: **Mi Perfil** tenía un `<h2>` (el nombre) **dentro** de la sección `<h3>` «Mi identidad» — inversión real de rango que además se tragaba las secciones siguientes → pasado a `<h4>`, con el selector CSS actualizado y `letter-spacing` explícito (la regla global de `index.css` solo cubre h1-h3). **Renderizado idéntico, medido: 19.2px, peso 700, Poppins, `-0.192px`, 223 × 29 px, mismo `top`.** **NO se tocó `SectionCard`**: su `<h3>` compartido hace que las 3 pantallas salten de H1 a H3, pero cambiarlo es transversal a los 3 roles y queda anotado en el backlog.

**C1 — DOCUMENTACIÓN NORMATIVA ALINEADA CON EL CÓDIGO.** `SPEC-002 §2` decía «Solo si no tiene estudiantes (409 si no)» para cursos **y** «Solo si no tiene retos/materiales/docentes» para materias: **las dos filas estaban desfasadas** desde que SPEC-003 convirtió ambos borrados en soft-delete y movió la validación de dependencias a la **purga**. Corregidas ambas + nota fechada que explica el porqué. `POLITICA-ELIMINACION.md` era correcta pero omitía el 409 por pendientes de activación: añadido. **No se cambió ni una línea de backend para «adaptarlo» a la documentación vieja**, y no se reescribió ninguna bitácora histórica.

**C2 — «ESTA ACCIÓN NO SE PUEDE DESHACER» ERA FALSO EN ADMINISTRADORES.** `DELETE /api/admin/administradores/:id` hace soft-delete (`admin.js:805`) y `administrador` está en `TIPOS_PAPELERA`. Mensaje corregido al patrón de Materias/Cursos. **Probado de verdad sobre la única cuenta secundaria segura (`admin.limitado`, demo)**: enviada a la papelera → apareció en «Administradores (1)» → **restaurada**, y volvió a la tabla como `Activo` con sus «2 de 9» permisos intactos. **Nunca se tocó el Administrador Principal** (su botón está deshabilitado por diseño). Endpoint, permisos e invariante del Principal sin tocar.

**C3 — CONTRASTE DE `.mision-xp` Y `.mision-tier`: DIFERIDO CON DATOS, NO IGNORADO.** Medidos compositados y **por variante**, con el fondo propio de cada elemento: `.mision-xp` (12px/700) va de **1.54:1** (amarillo) a **2.50:1** (rojo) según el color de categoría —7 colores distintos—, y `.mision-tier` (11.52px/700, sobre su tinte al 14%) de **1.83:1** (Platino) a **2.59:1** (Diamante). **Todos fallan AA.** Se barrieron soluciones locales: oscurecer con `color-mix(… , black)` alcanza 4.5:1 **solo al 20% de matiz**, es decir dejando los 7 colores de categoría visualmente **negros** (se pierde justo la identidad que había que conservar); conservar 40-50% del matiz **exige aflojar el atenuado a 0.75-0.85**, degradando la señal de «bloqueado». Como toda vía a AA degrada uno de los dos requisitos, **se difiere la decisión de diseño a Fabrizio con la tabla completa** en vez de forzar el cambio. La información no se pierde: el tier lleva emoji (🥈🥇💠💎) y el XP vuelve a ser legible al desbloquearse.

**C4 — TÍTULO DUPLICADO ELIMINADO.** Mis Estudiantes mostraba `H1 "Mis Estudiantes"` y justo debajo `H3 "Mis estudiantes"`. La tarjeta es la **lista**, así que su rótulo pasa a **«Listado de estudiantes»**, siguiendo el patrón que ya usa Admin («Cursos» → «Cursos y paralelos»). **Ranking se dejó intacto**: `H1 "Ranking"` + `H3 "Ranking completo"` sí tienen distinción semántica. Cero cambios en `SectionCard` y cero jerarquías globales.

**C5 — XP AL ABRIR «MIS PREMIOS»: NO ES BUG, ESTÁ ESPECIFICADO.** Durante la sesión el estudiante demo pasó de 5000 XP/7 insignias a 6300/10 al abrir el panel. Investigado: `GET /api/misiones` → `obtenerMisionesEstudiante` → `evaluarMisiones`, y **`SPEC-007 §2.3` punto 2 lo documenta explícitamente** como «evaluación perezosa fuera de transacción», precisamente para cubrir misiones que dependen de terceros (ranking) y garantizar que el panel muestre el estado correcto. Es **idempotente** (SPEC §2.1 y el propio código). No hay recompensa duplicada ni contradicción con ninguna spec → **comportamiento por diseño, documentado y no corregido**. Lo que se vio fue la liquidación de recompensas ya cumplidas por los datos sembrados.

**REGRESIÓN.** `npm run build` limpio (3.45 s); CSS 154.00 → **154.23 kB** (gzip 24.11 → 24.15). **Lint 29 problemas (26 errores + 3 warnings) = baseline exacto, cero nuevos**; no se corrigió ninguno histórico. **Consola: cero mensajes** en todo el recorrido. **Smoke test de los 3 roles a 320 y 1280**: Admin 13 secciones, Docente 7, Estudiante 3 → **desbordamiento 0 en todas** y **cero botones fuera de pantalla** (los de las tablas cuelgan de `.tabla-scroll`, su scroll propio de diseño). Verificado en local contra el MySQL portable del instalador (3308); el Docker de desarrollo (3307) no se tocó.

**LO QUE NO SE HIZO, A PROPÓSITO.** No se tocó el instalador, ni autenticación, ni XP/recompensas, ni permisos, ni el backend de eliminación; no hay migraciones, reseed ni dependencias nuevas. El salto H1→H3 de `SectionCard`, el duplicado de contraste de C3 y los demás diferidos siguen en `MASTER_PLAN`.)

2026-07-29 (**FASE LOCAL 2.3 — EMPAQUETADO Y DISTRIBUCIÓN. EXISTE UN PAQUETE DE ENTREGA REAL.** Verificado contra Git antes de tocar nada: `main` = `origin/main` = `1b63d28`, working tree limpio (incluidos los no rastreados). Descripción exacta de lo que existe ahora, sin exagerar: **«Paquete de distribución para Windows x64 que se extrae y se instala con doble clic, sin Node, sin MySQL, sin npm, sin Docker y sin conexión a internet.»** Lo que **sigue sin demostrarse** es que funcione en un Windows **realmente recién instalado**: no hay VM disponible (ver «lo que no se probó» al final de esta entrada).

**HALLAZGO CRÍTICO 1 — MYSQL DEPENDE DE VISUAL C++ Y WINDOWS NO LO TRAE.** Se leyó la **tabla de importaciones PE** de los binarios, no la documentación: `mysqld.exe`, `mysql.exe`, `mysqladmin.exe` y `mysqldump.exe` importan **`vcruntime140.dll`, `vcruntime140_1.dll` y `msvcp140.dll`** de forma **estática** (directorio 1 del PE, no delay-load), así que si faltan **el proceso no llega a arrancar y no queda rastro en ningún log de MySQL**: falla el cargador de Windows. Esas tres DLL las instala el *Microsoft Visual C++ Redistributable*, **no forman parte de Windows**. Las otras 14 que importa (`api-ms-win-crt-*`) sí: son el Universal CRT. **Node quedó exonerado con la misma medición**: `node.exe` solo importa DLL del sistema (`kernel32, ws2_32, crypt32, advapi32, user32, ole32, iphlpapi, shell32, userenv, winmm, dbghelp`) y **los tres binarios nativos de `node_modules`** —`rolldown-binding`, `lightningcss` y `skia` (`@napi-rs/canvas`)— **están compilados con CRT estático**. **La única dependencia de Visual C++ de todo el paquete es MySQL.**

**DECISIÓN (aprobada por Fabrizio): despliegue APP-LOCAL de las 3 DLL, sin instalar nada.** Viajan dentro de `runtime\mysql\bin`, junto a los ejecutables; Windows resuelve las importaciones buscando primero en el directorio del ejecutable. **No se toca System32, ni el PATH global, ni el registro, ni hacen falta privilegios de administrador.** Procedencia verificada archivo por archivo antes de copiar (`Preparar-VcRuntimeParaMySql` en `instalador\runtime.ps1`): **firma Authenticode `Valid` y firmante `CN=Microsoft Windows Software Compatibility Publisher, O=Microsoft Corporation`**, **arquitectura x64 leída del campo `Machine` de la cabecera PE** (`0x8664`) y versión **14.51.36247.0**, origen `%WINDIR%\System32` de este equipo (donde las dejó el instalador de Microsoft) — **cero descargas de terceros**. SHA-256: `vcruntime140.dll` `D1F4225D…9CE7`, `vcruntime140_1.dll` `A7146C08…434E`, `msvcp140.dll` `7C26614E…89CA`. **Contrapartida dicha sin adornos: una copia app-local NO la actualiza Windows Update; si Microsoft publica un parche del runtime hay que reemplazar esos archivos y volver a empaquetar.** Forman parte del runtime distribuido.

**PRUEBA DE QUE EL APP-LOCAL FUNCIONA DE VERDAD, no por deducción.** Se enumeraron los **módulos cargados** por los procesos vivos. **Antes** de copiar las DLL, los dos `mysqld.exe` cargaban `C:\WINDOWS\SYSTEM32\VCRUNTIME140.dll`, `…\MSVCP140.dll` y `…\VCRUNTIME140_1.dll`. **Después**, los mismos procesos cargan **`…\runtime\mysql\bin\VCRUNTIME140.dll`** y sus compañeras. Con `mysql.exe` y `mysqladmin.exe` hizo falta mantenerlos vivos para poder inspeccionarlos (`--execute=SELECT SLEEP(4)…` y `--sleep=1 --count=4 status`): **ambos cargan también las copias del paquete**, y devolvieron datos reales (`8.0.44`, `@@datadir` correcto, `Uptime/Threads/Questions`). La contraseña viajó por `MYSQL_PWD`, nunca como argumento.

**COMPROBACIÓN PREVIA EN EL INSTALADOR** (`Test-VcRuntimeMySql`, `instalador\mysql.ps1`), llamada desde `Preparar-MySqlPortable` y desde `Asegurar-MySqlPortableEnMarcha` **antes de intentar arrancar nada**: si las 3 DLL no están junto a `mysqld.exe` pero sí en el sistema, **avisa** (funciona aquí, pero esa copia no serviría en otro equipo); si no están en ninguno de los dos sitios, **se detiene con un mensaje que explica qué falta y por qué**, en vez de dejar que el cargador falle en silencio.

**HALLAZGO CRÍTICO 2 — LOS `-debug.dll` DE MYSQL NO PUEDEN CARGAR EN NINGÚN SITIO.** `libprotobuf-debug.dll`, `libprotobuf-lite-debug.dll`, `abseil_dll-debug.dll` y los **30 plugins de `lib\plugin\debug`** importan `msvcp140d.dll`, `vcruntime140d.dll`, `vcruntime140_1d.dll` y `ucrtbased.dll` — el **CRT de depuración de Visual Studio, que Microsoft no redistribuye** y que no existe en ninguna máquina sin Visual Studio. Y `mysqld.exe` importa la versión de publicación (`libprotobuf-lite.dll`), no la de depuración. Son **90 MB que no pueden ejecutarse en ninguna parte**.

**PODA DE `runtime\mysql`: SOLO LO DEMOSTRADO. 924,6 MB → 398,0 MB (92 archivos, 526,6 MB fuera).** Decisión explícita de Fabrizio: *fiabilidad > tamaño*, y **no se elimina nada cuya necesidad no esté demostrada**. Las cuatro reglas, declaradas como datos en `$PodaMySql` y aplicadas **sobre la copia**, nunca sobre `runtime\mysql` del repositorio (que sigue siendo el árbol oficial íntegro, así que la poda es **reproducible y reversible**): **(1) `*.pdb`** — 446,7 MB en 35 archivos; símbolos de depuración que el cargador de Windows nunca abre. **(2) `*.lib`** — 55,1 MB en 7; bibliotecas para **compilar** en C contra MySQL. **(3) `*-debug.dll`** — 14,8 MB en 3 (hallazgo 2). **(4) `lib\plugin\debug\`** — 30 DLL de depuración + sus 30 `.pdb`. Más **`include\`** (0,4 MB de cabeceras `.h`/`.c`). **SE CONSERVA TODO LO DEMÁS**, incluido lo que hoy no se usa: los **28 ejecutables cliente**, **`lib\mecab`** (130 MB de diccionario japonés), `lib\plugin` completo, `lib\private` (datos ICU del motor de expresiones regulares) y **`share\` entero** con los charsets, las collations y los mensajes de error en los 25 idiomas. El empaquetador **cuenta los archivos esperados y aborta** si la copia no coincide.

**QUÉ SE IMPLEMENTÓ.** Un archivo nuevo, `instalador\empaquetar.ps1` (**no viaja en el paquete**: es herramienta del equipo de desarrollo), y cambios acotados en `runtime.ps1` (`Preparar-VcRuntimeParaMySql`, `Obtener-ArquitecturaPE`), `mysql.ps1` (`Test-VcRuntimeMySql` + 2 llamadas), `instalar.ps1` (`Instalar-Dependencias` reconoce el marcador del paquete) y `.gitignore` (`release/`, `instalador/dependencias-*.sha256`). **Cero archivos de la aplicación tocados: `src/**` y `server/**` intactos.** El empaquetado es reproducible (`-Salida`, `-Zip`, `-SaltarBuild`, `-SinDependencias`), **construye la web antes de copiar y aborta si el build falla**, y **solo borra una salida anterior si encuentra su propia marca `PAQUETE.json`** — que ahora se escribe **al crear la carpeta**, para que un fallo a mitad no deje una carpeta que el siguiente intento no se atreva a limpiar.

**EL PAQUETE VIAJA CON `node_modules` YA INSTALADO: LA INSTALACIÓN NO NECESITA INTERNET.** Es el cambio que más fragilidad quita (npm, proxy, antivirus, registro caído) a cambio de 251,8 MB. El mecanismo es explícito: el empaquetador escribe `instalador\dependencias-{frontend,servidor}.sha256` con el **SHA-256 del `package-lock.json`** con el que se instalaron, e `Instalar-Dependencias` se salta `npm ci` **solo si ese hash coincide exactamente**; si no coincide, avisa y reinstala. **Medido en la prueba real: «Dependencias de servidor/frontend incluidas en el paquete: no hace falta descargar nada.»**

**PAQUETE FINAL: 750,8 MB en 61.161 archivos, generado en 55,6 s.** Reparto: `runtime\mysql` **398,8 MB (53,1%)** · `node_modules` **205,0 MB (27,3%)** · `runtime\node` **94,9 MB (12,6%)** · `server\node_modules` **46,8 MB (6,2%)** · `dist` 3,3 MB · `src` 1,1 MB · `instalador` 140 KB · `database` 52 KB · `public` 14 KB. **MySQL sigue siendo la mitad del paquete y es la respuesta honesta a «por qué pesa tanto»**: es un motor de base de datos completo. Se genera en `release\GamificApp` (ignorado por Git) o donde diga `-Salida`.

**ZIP: 243,6 MB (255.384.213 bytes) en 393 s — ratio 3,08:1 sobre los 750,8 MB.** Verificado: **63.361 entradas**, con los tres `.cmd`, `LEEME.txt`, `INVENTARIO.txt`, `PAQUETE.json`, `mysqld.exe` y las tres DLL de Visual C++ dentro; el único archivo que casa con los patrones prohibidos es `server/.env.example`, que es una plantilla. **Hallazgo de rendimiento que cambió la implementación:** `System.IO.Compression` con `CompressionLevel.Optimal` resultó **impracticable** con 61.000 entradas — medido: **2 MB comprimidos en 6 minutos**, es decir horas para el paquete entero. Se sustituyó por el **`tar.exe` de Windows (bsdtar 3.8.4 / libarchive)**, presente desde Windows 10 1803, que hace el mismo ZIP en **6,5 minutos**; si no existiera, se cae a `System.IO.Compression` con `Fastest`.

**PRUEBAS: TODAS EJECUTADAS DESDE EL PAQUETE, NUNCA DESDE EL REPOSITORIO, Y CONTRA UN `%LOCALAPPDATA%` AISLADO** (`$env:LOCALAPPDATA` redirigido a una carpeta temporal) **para no tocar los datos reales**. Comprobado al terminar: la instancia real conserva `instancia.creada = 01:40:10` e `ibdata1` de `01:40:02`, las de la Fase 2.2. **(A) Instalación desde cero en la ruta más incómoda posible** —`…\GA-Entrega\GamificApp Prueba ÁÉ`, con **espacios y acentos**—: **51,1 s**, datadir inicializado de cero, `0 → 11 → 18` tablas, `server/.env` generado, build correcto, `/api/health` **200**, `/api/institucion` **200**, frontend **200**. **Los tres procesos corren desde el paquete** (`ExecutablePath` comprobado: `…\GamificApp Prueba ÁÉ\runtime\node\node.exe` ×2 y `…\runtime\mysql\bin\mysqld.exe`) y **mysqld carga el Visual C++ del paquete** en esa misma ruta acentuada. **Resultado nuevo y relevante: el `my.ini` con `basedir` acentuado y con espacios funcionó tal cual**; el camino de respaldo a ruta corta 8.3 sigue sin ejercitarse porque no hizo falta. **Login real** por el mismo `POST /api/auth/login` que envía el formulario: **admin 200 con JWT de 236 caracteres**, **contraseña incorrecta 401**, y `GET /api/admin/docentes` con el token **200**. La contraseña se leyó de `CREDENCIALES.txt`, como haría el usuario. **(B) Detener → Iniciar**: **4,1 s** y **12,1 s**; el testigo de persistencia «TESTIGO PAQUETE 2.3», creado **por la API real** (`POST /api/admin/materias` → **201**), **seguía ahí**. El cierre ordenado lo certifica el propio `error.log`: «*Received SHUTDOWN from user root*», «*Normal shutdown*», «*Shutdown complete*», con **0 `mysqld` vivos** y 3001/3308/5173 libres. **(C) Reemplazar la carpeta del proyecto**, ahora desde un paquete: segunda copia intacta (sin `server/.env`, sin `.run`, sin `CREDENCIALES.txt`) apuntando a los mismos datos → «*Se encontraron datos de una instalación anterior: se conservan*», `18 → 18` tablas, **datos demo omitidos automáticamente**, `server/.env` reconstruido desde `root.txt` — y **prueba de que no hubo `initialize`: `ibdata1` conserva su fecha de creación, `instancia.creada` no cambia y el SHA-256 de `root.txt` es idéntico**. El testigo seguía en su sitio. **(D) Docker de desarrollo intacto**: `gamificapp-mysql-dev` `Up (healthy)` en el 3307 durante todo el trabajo, y `gamificapp_dev` del contenedor sin un solo cambio.

**AUDITORÍA DE SECRETOS DEL PAQUETE, EN DOS CAPAS.** **(1) La del propio empaquetador**, que **aborta la generación** si encuentra algo: nombres prohibidos (`.env`, `CREDENCIALES.txt`, `root.txt`, `instancia.json`, `my.ini`, `*.pid`, `*.log`), carpetas prohibidas (`.git`, `.claude`, `.run`, `logs`, `server\backups`, `runtime\descargas`, `docs`) y —lo importante— los **valores** secretos reales de este equipo leídos de `server/.env` y de `secretos\root.txt`, más el marcador de pruebas, `%USERPROFILE%` y el principal `EQUIPO\usuario`. **No busca nombres de variable**: `JWT_SECRET=` en `.env.example` es legítimo y no debe dar falso positivo. **(2) Una auditoría independiente**, escrita aparte y sin reutilizar esa lógica, que recorrió **los 61.164 archivos y los 750,8 MB byte a byte —incluidos `node_modules` y `runtime`—** buscando 9 agujas: **0 secretos reales, 0 nombres o extensiones prohibidos**, y **solo 3 archivos `.env*`, los tres plantillas `.example`**. Encontró **2 coincidencias benignas** que aun así se corrigieron excluyéndolas del paquete: el propio `empaquetar.ps1` (contiene por fuerza las cadenas que busca) y `server/.env.development.example` (plantilla del MySQL de Docker, con credenciales ficticias que el revisor no necesita).

**RESIDUO DE PRUEBAS ELIMINADO, DEMOSTRANDO ANTES QUE ERA SOLO ESO.** «MARCA PERSISTENCIA 2026» **existía únicamente en la instancia portable** (`%LOCALAPPDATA%\GamificApp\mysql\datos`, materia **id 7**) y **no en la base de Docker**, que conservaba exactamente las 6 materias semilla. Antes de borrar se barrieron **las 5 columnas del esquema que pueden referenciar una materia** (`auditoria.materia`, `banco_preguntas.materia_id`, `docente_materia.materia_id`, `materiales.materia_id`, `retos.materia_id`): **0 filas dependientes**. `DELETE` de **1 fila**; la instancia queda con las **6 materias semilla**, idéntica a la de Docker. El testigo de esta fase («TESTIGO PAQUETE 2.3») vivió y murió con el `%LOCALAPPDATA%` aislado, que se borró entero.

**REGRESIÓN.** `npm run build` limpio y **lint 29 problemas (26 errores + 3 warnings) = baseline exacto medido sobre el HEAD inicial `1b63d28`, cero nuevos** (no se corrigió ninguno). Los cambios son `.ps1`, `.gitignore` y documentación: ESLint solo cubre `**/*.{js,jsx}`, así que ni siquiera entran.

**LO QUE NO SE PROBÓ, Y NO SE DECLARA RESUELTO.** **(1) WINDOWS LIMPIO: SIGUE SIN VALIDAR.** El despliegue app-local del Visual C++ **quita la causa conocida** del fallo y está demostrado que Windows carga esas copias, pero **no se ha ejecutado el paquete en un equipo recién instalado**: no hay VM disponible. Es la última prueba obligatoria antes de enseñárselo al revisor, y hay que hacerla con el paquete, no con el repositorio. **(2) Instalación DESDE EL ZIP**: el ZIP se generó y se inspeccionó su índice, pero **no se extrajo para reinstalar desde la extracción**; las pruebas de instalación se hicieron sobre la carpeta. **(3) Reinicio real de Windows**: sigue sin probarse. **(4) Rutas no representables en ANSI**: la prueba con espacios y acentos pasó, así que el respaldo a ruta corta 8.3 **sigue sin ejercitarse**. **(5) Antivirus restrictivos y proxy corporativo**: sin probar (aunque el paquete ya no necesita red). **(6) Windows ARM64 o de 32 bits**: fuera de alcance; el instalador aborta a propósito.)

2026-07-29 (**FASE LOCAL 2.2 — MYSQL PORTABLE INTEGRADO EN EL CICLO INSTALAR / INICIAR / DETENER.** Verificado contra Git antes de tocar nada: `main` = `origin/main` = `7fe5eb6`, working tree limpio. **MySQL deja de ser un requisito previo de GamificApp.** Descripción exacta de lo que existe ahora, sin exagerar: **«Instalación local automatizada para Windows que no exige ni Node ni MySQL instalados; la base de datos la pone la propia aplicación.»**

**HALLAZGO DE ARRANQUE: `runtime/mysql` NO existía.** La prueba de viabilidad de la sesión anterior borró sus artefactos, así que lo primero fue reponer el binario. **MySQL 8.0.44 Community Server win-x64** descargado de `https://cdn.mysql.com/Downloads/MySQL-8.0/mysql-8.0.44-winx64.zip` (244.529.072 bytes, 233,2 MB). **Integridad SÍ verificada, y sin inventar nada**: el **MD5 del archivo descargado = `F5E62718C0E77122AEE3476386F48051`, idéntico al MD5 que Oracle publica** para `mysql-8.0.44-winx64.zip` en su página oficial de archivos (`downloads.mysql.com/archives/community/`). Como MySQL **no publica un `SHASUMS256.txt` al estilo de Node** (HALLAZGO 2 de la viabilidad), se deja constancia del SHA-256 **calculado sobre ese archivo ya verificado por MD5**: `4D9316D2955B0BD2B484E08BDA67C18D7979E0622B84F2E9146D97F04ED9A2FC`. La cadena es honesta: MD5 anclado a la fuente oficial → SHA-256 derivado de ese archivo. **Decisión aprobada por Fabrizio: en esta fase el instalador NO descarga MySQL.** Exige `runtime\mysql` ya presente y jamás usa la red para la base de datos; la estrategia de distribución e integridad se diseña en la fase de empaquetado. `runtime\mysql` ocupa **925 MB** sin podar (la poda es explícitamente de la fase siguiente).

**HALLAZGO 1 (doble proceso) MEDIDO CONTRA ESTE BINARIO, ANTES DE ESCRIBIR LA LÓGICA.** Sonda aislada en el puerto 3316 con datadir desechable: `Start-Process -PassThru` devolvió **PID 38988** (padre, hijo de PowerShell) con la ruta del `.ini` **entrecomillada**; el **dueño real del socket fue el PID 29532**, hijo del anterior, con la ruta del `.ini` **sin comillas**; **ambos** con `ExecutablePath` = nuestro `runtime\mysql\bin\mysqld.exe`; el **`pid-file` que escribe mysqld contenía 29532**, es decir el dueño del socket y no el de `Start-Process`; y **`mysqladmin shutdown` cerró los dos** (0 `mysqld.exe` vivos después). **Consecuencia de diseño: la identificación NUNCA se apoya en el PID registrado.** Un mysqld es nuestro solo si cumple **las dos** condiciones a la vez: (1) `ExecutablePath` == exactamente `runtime\mysql\bin\mysqld.exe` y (2) su línea de comandos **menciona nuestro `my.ini`** — la ruta se busca como **subcadena** precisamente porque el padre la lleva entrecomillada y el hijo no, así el mismo predicado vale para los dos. Además existe una prueba **independiente de los procesos**: conectarse al puerto y comparar **`@@datadir`** con el nuestro (`bd-verificar.mjs` se amplió para devolver `datadir` y `puerto`). **Comprobado en vivo durante la instalación real: socket 3308 → PID 12184, hijo de 40780, y `mysqld.pid` contenía 12184.**

**QUÉ SE IMPLEMENTÓ (aditivo; cero archivos de la aplicación tocados — `src/**` y `server/**` intactos).** Un módulo nuevo `instalador/mysql.ps1` (ciclo de vida completo), un script nuevo `instalador/mysql-clave-root.mjs` (protección de root) y cambios acotados en `instalar.ps1`, `iniciar.ps1`, `detener.ps1` y `bd-verificar.mjs`. **Separación esencial: `runtime\mysql` = binarios REEMPLAZABLES; `%LOCALAPPDATA%\GamificApp\` = datos PERMANENTES.** El datadir **jamás** vive dentro de `runtime\mysql`: si viviera, actualizar los binarios borraría los datos de la escuela. Estructura creada: `mysql\datos\`, `mysql\my.ini`, `mysql\error.log`, `mysql\mysqld.pid`, `secretos\root.txt` e `instancia.json`. **`instancia.json` (formato v1, 502 B) contiene solo lo necesario para reconstruir la conexión** — versión, motor y versión de MySQL, ruta del runtime (informativa), host, puerto, datadir, `dbName`, `dbUser`, y fechas de creación y actualización — **y ninguna contraseña ni el `JWT_SECRET`**. **Puerto 3308 preferido con reserva 3309-3315; 3306 y 3307 quedan estructuralmente fuera de la lista** para no acercarse ni a un MySQL instalado ni al contenedor Docker de desarrollo. `bind-address=127.0.0.1`, `mysqlx=0` (33060 nunca se abre), `utf8mb4` / `utf8mb4_spanish_ci`.

**PROTECCIÓN DE ROOT.** Sobre un datadir nuevo se ejecuta `mysqld --initialize-insecure` (root sin contraseña) y **en los segundos siguientes** se le pone una contraseña aleatoria de 24 caracteres generada con el RNG criptográfico; `mysql-clave-root.mjs` no se limita a lanzar el `ALTER USER`: **abre una conexión NUEVA con la clave nueva para demostrar que la instancia quedó realmente protegida**, y si algo falla la instalación **se detiene** en vez de dejar una base sin contraseña. El secreto se guarda en `%LOCALAPPDATA%\GamificApp\secretos\root.txt` (**24 caracteres**, ACL con **herencia cortada y una única regla `FABRIZIO\extun → FullControl`**, verificado), se registra en el sistema de censura de logs y **viaja siempre por variables de entorno, nunca por argumentos** — ni en los `.mjs` (patrón ya existente) ni en `mysqladmin shutdown`, que usa `MYSQL_PWD` porque en Windows la línea de comandos de cualquier proceso es legible pero su entorno no.

**PRUEBAS EJECUTADAS (todas reales, con evidencia).** **(A) Primera instalación portable** sobre datadir nuevo: **42,0 s** de principio a fin, `0 → 11 → 18` tablas, admin listo, `/api/health` **200**, `/api/institucion` **200**, frontend **200**, y **login real de los 3 roles** (`admin` 200 con JWT, contraseña incorrecta **401**, `docente.demo` 200, estudiante por nombre+PIN 200). Datos de demostración sembrados con `seedDev.js` **sin tocar ninguna de sus barreras**. **(B) Persistencia**: se creó un marcador inequívoco **por la API real** (`POST /api/admin/materias` → **201**, materia «MARCA PERSISTENCIA 2026», id 7) → `Detener` → `Iniciar` (**10,1 s**) → **el marcador seguía ahí**. **(C) Segunda instalación** (13,2 s): **`server/.env` con SHA-256 idéntico**, **`root.txt` con SHA-256 idéntico**, `instancia.creada` sin cambiar, `18 → 18` tablas, **datos demo omitidos automáticamente** («ya tienes datos guardados y no se escribe encima de ellos») y **2 procesos mysqld**, los mismos, sin duplicar infraestructura. **(D) Puerto ocupado, probado en los dos sentidos**: con un proceso **ajeno** (un `TcpListener` de PowerShell) en el **3308**, el instalador registró «*Puerto 3308 ocupado por powershell.exe (PID 20060), que NO es de GamificApp: se prueba el siguiente*» y arrancó en el **3309**; repetida la prueba con el ajeno en el **3309**, saltó a **3308**, y esta vez se comprobó explícitamente que **el proceso ajeno seguía vivo y seguía siendo el dueño de su puerto después del instalador**. **Los datos sobrevivieron a los dos cambios de puerto.** **(E) Doble `Iniciar`**: «La base de datos ya estaba en marcha (puerto 3308). No se inicia otra vez», **mismos PIDs antes y después** y **un solo socket** en el 3308. **(F) `Detener`** por el punto de entrada real `Detener GamificApp.cmd` (3,9 s): frontend y backend detenidos por PID registrado, base de datos con **cierre ordenado** —el propio `error.log` lo certifica: «*Received SHUTDOWN from user root*», «*Normal shutdown*», «*Shutdown complete*»—, **0 mysqld vivos** (ni nuestros ni de nadie) y **3001, 3308 y 5173 libres**. **(G/H) Cero interferencia**: el contenedor `gamificapp-mysql-dev` siguió `Up (healthy)` en el 3307 durante todo el trabajo y la huella de filas de su `gamificapp_dev` —**las 18 tablas contadas una a una**— resultó **idéntica antes y después**. **(I) Secretos**: `git grep` de los 4 secretos vivos (clave de root, `DB_PASSWORD`, `JWT_SECRET`, `ADMIN_PASSWORD`) sobre los archivos rastreados → **0 coincidencias**; los **24 archivos de `logs/`** → **0 coincidencias**; la línea de comandos de **todos los procesos del sistema** → **0 coincidencias**; `my.ini` **no contiene contraseñas**; y `CREDENCIALES.txt` **sigue sin incluir el `JWT_SECRET`**. **(J) Regresión**: `npm run build` **limpio (5,67 s)** y **lint 29 problemas (26 errores + 3 warnings) = baseline exacto, cero nuevos** (no se corrigió ninguno).

**RECONSTRUCCIÓN TRAS REEMPLAZAR LA CARPETA DEL PROYECTO — PROBADA DE VERDAD.** Se simuló el escenario completo: se borraron **`server/.env`, `.run/` y `CREDENCIALES.txt`** dejando intacto `%LOCALAPPDATA%`. El instalador **reconoció los datos anteriores y NO inicializó nada** («Se encontraron datos de una instalación anterior: se conservan», `18 → 18` tablas), **regeneró las credenciales de aplicación y un `server/.env` nuevo** a partir de la credencial administrativa persistente, **omitió los datos demo automáticamente** y dejó la aplicación funcionando. **Prueba de que no hubo `initialize`: `ibdata1` conserva su fecha de creación original (01:40:02, la primera instalación) e `instancia.creada` sigue siendo la de entonces**, con el marcador «MARCA PERSISTENCIA 2026» en su sitio. También se respetó la regla inversa: **con un `server/.env` válido presente, el `JWT_SECRET` no se regenera** (SHA-256 idéntico en la prueba C).

**LO QUE NO SE PROBÓ, Y NO SE DECLARA RESUELTO.** **(1) WINDOWS LIMPIO: PENDIENTE DE VALIDACIÓN.** No hay VM disponible, así que **no se ha demostrado que `mysqld.exe` arranque en un equipo sin el Visual C++ Redistributable** (`VCRUNTIME140.dll`, `VCRUNTIME140_1.dll`, `MSVCP140.dll`). La viabilidad solo demostró que esos DLL **pueden convivir** junto al ejecutable, no que basten. **Sigue siendo requisito obligatorio antes de enseñárselo al revisor.** **(2) Reinicio real de Windows**: la persistencia se probó contra `Detener`/`Iniciar`, reinstalación y reemplazo de la carpeta, **no contra un reinicio del sistema operativo**. **(3) Rutas no ASCII**: el `my.ini` se escribe en la codificación ANSI del sistema (como lo lee mysqld) con una comprobación de ida y vuelta y, solo si de verdad se pierden caracteres, se recurre a la ruta corta 8.3; **ese camino de respaldo no se ha ejercitado** porque la ruta real (`C:\Users\extun\...`) es ASCII pura. **(4)** El tamaño de `runtime\mysql` (925 MB) no se ha podado: es trabajo de la fase de empaquetado.

**NOTA DE MÉTODO, ÚTIL PARA LA PRÓXIMA SESIÓN.** Capturar la salida del instalador con una tubería (`| Select-String`, `| Out-File`) **hace que el comando parezca colgado**: el backend y el frontend heredan el handle de salida y sobreviven al script, así que la tubería nunca ve el fin de archivo. **La instalación termina bien igualmente** (lo confirma `logs/instalador.log`); es un artefacto de la captura, no del instalador — y es la otra cara del comportamiento deseado ya documentado en la Fase 1: «los servidores sobreviven al cierre de la ventana».)

2026-07-29 (**FASE LOCAL 2.1 — NODE PORTABLE. CERRADA.** Verificado contra Git antes de tocar nada: `main` = `origin/main` = `4dcd24a`, working tree limpio. **Node.js deja de ser un requisito previo de GamificApp.** Descripción exacta de lo que existe ahora, sin exagerar: **«Instalación local automatizada para Windows que ya no exige Node instalado; MySQL 8 sigue siendo requisito previo.»** La Fase Local 2.2 (MySQL portable) **no se ha integrado** en ningún script.

**QUÉ SE IMPLEMENTÓ (aditivo; cero archivos de la aplicación tocados).** Un archivo nuevo `instalador/runtime.ps1` y cambios en `comun.ps1`, `instalar.ps1`, `iniciar.ps1` y `.gitignore`. **Resolución de Node con prioridad fija: `runtime\node\node.exe` (portable) → Node del equipo si es compatible → descarga verificada → error.** El portable **gana siempre** cuando existe, para que el revisor ejecute exactamente el binario que se probó. **Versión clavada: `v22.23.1`** (rama 22 LTS «Jod», npm 10.9.8), **no «latest»**, descargada de `https://nodejs.org/dist/v22.23.1/node-v22.23.1-win-x64.zip` y **verificada por SHA-256 `7DF0BC…09C29` contra la línea `node-v22.23.1-win-x64.zip` de `https://nodejs.org/dist/v22.23.1/SHASUMS256.txt`** (fuente oficial de integridad de Node.js). Si el hash no coincide, **el archivo se borra sin abrirlo y no se ejecuta ni un byte**. **Nada se instala en el sistema**: sin `setx`, sin registro de Windows, sin MSI, sin Program Files, sin servicios y sin privilegios de administrador; todo vive en `runtime\node\` (95 MB), que está en `.gitignore` — **el repositorio no contiene ni contendrá binarios**.

**HALLAZGO R6 CONFIRMADO Y RESUELTO, con evidencia de las dos direcciones.** El lanzador `node_modules\.bin\vite.cmd` que genera npm hace `IF EXIST "%dp0%\node.exe"` y, al no encontrarlo, **cae a `node` a secas resuelto por PATH** — leído en el propio shim, no supuesto. Medido sin Node global: **llamarlo tal cual devuelve `""node"" no se reconoce como un comando interno o externo` y código de salida 1**; **con la inyección de PATH de `Anteponer-NodeAlPath` devuelve `vite/8.0.16 win32-x64 node-v22.23.1` y código 0**, y el PATH queda restaurado al salir. La inyección es **solo del proceso actual y sus hijos**: comprobado que ni el PATH de usuario ni el de máquina contienen `runtime` después de todas las pruebas. Además **npm se invoca como `node.exe node_modules\npm\bin\npm-cli.js`, nunca por `npm.cmd`**, precisamente porque ese `.cmd` resuelve `node` por PATH.

**PRUEBA CRÍTICA — TODO EJECUTADO SIN NODE GLOBAL.** Se retiraron del PATH del proceso las dos carpetas que contienen `node.exe`/`npm.cmd` (`C:\Program Files\nodejs\` y `C:\Program Files\nodejs`) y se comprobó primero que la simulación era real: `Get-Command node`, `Get-Command npm` y `Get-Command npm.cmd` **no encuentran nada**, y **`node -v` a secas falla**. A partir de ahí, usando únicamente `runtime\node`: **`node --version` → `v22.23.1`**; **`npm --version` → `10.9.8`**; **`npm ci` en `server/` → exit 0 en 9,1 s (128 paquetes)**; **`npm ci` en la raíz → exit 0 en 69,7 s**; **`dist/` borrada y `npm run build` → exit 0 en 14,2 s**, regenerando 7 archivos (3,3 MB) con **CSS 154.00 kB / gzip 24.11 kB = el mismo tamaño del baseline**; **backend arrancado (PID 38220) con `/api/health` 200 y `/api/institucion` 200** (esta sí consulta MySQL, así que prueba la pila completa); **frontend arrancado (PID 14456)** sirviendo `<title>GamificApp`. **Prueba definitiva de que ambos procesos corren sobre el portable: `ExecutablePath` de los dos PID = `…\runtime\node\node.exe`**, leído de `Win32_Process`. Parada limpia con el mecanismo de siempre (`detenido`/`detenido`) y **puertos 3001 y 5173 libres después**. **Ruta de respaldo también comprobada en ambos sentidos**: renombrando `runtime\node` la resolución cae a `origen=global v24.15.0`; restaurándolo vuelve a `origen=portable v22.23.1`.

**REGRESIÓN.** `npm run build` limpio (4,23 s) y **lint 29 problemas (26 errores + 3 warnings) = baseline exacto, cero nuevos** (no se corrigió ninguno: son todos patrón preexistente). **DATOS: cero cambios.** No se tocó `server/.env` (mismo SHA-256 al terminar), no se ejecutó ninguna migración, no se sembró nada, no se jugó ninguna partida y no se tocó el MySQL de Docker (3307) ni ningún otro. La única BD consultada fue `gamificapp_dev` en modo lectura a través de `/api/institucion`.

**LO QUE NO SE PROBÓ.** Un equipo/usuario Windows **realmente limpio** (aquí se simuló la ausencia de Node depurando el PATH del proceso, que es fiel para todo lo que hacen los scripts, pero no sustituye a una máquina sin Node instalado); la descarga bajo un proxy corporativo o con antivirus restrictivo; y Windows ARM64 o de 32 bits, donde el instalador **aborta a propósito** con un mensaje explícito en vez de intentarlo.)

2026-07-29 (**FASE LOCAL 1 — CERRADA (aprobada por Fabrizio y commiteada).** Descripción exacta de lo que existe hoy, sin exagerar: **«Instalación local automatizada para Windows con Node compatible y MySQL 8 previamente disponibles.»** **GamificApp NO tiene todavía instalación portable**: Node y MySQL deben estar ya instalados en el equipo. **Sigue pendiente y es obligatoria la prueba en un equipo/usuario Windows limpio** antes de enseñárselo al revisor.

**VERIFICACIÓN DE CIERRE (posterior al informe de implementación).** **(a) Pregunta de datos demo probada de forma INTERACTIVA REAL**, en una consola de verdad con pulsaciones de teclado (sin `stdin` redirigido, que era la limitación admitida en el informe anterior): **`n` → no ejecuta `seedDev.js`**; **Enter a secas → No** (mismo resultado, `datos-demo.log` sin tocar en ambos); **`s` → sí lo ejecuta**, y el propio log del seed confirma el destino: «Sembrando datos de desarrollo en **gamificapp_dev**». Los tres casos comparten el mismo mecanismo de teclado, y como `s` produce un resultado **distinto** de `n`/Enter, queda demostrado que los caracteres se entregan y se interpretan de verdad. **(b) Barreras de `seedDev.js` ejercitadas en vivo, sin modificarlas**: sin `DEV_SEED` → aborta; `NODE_ENV=production` → aborta; `DB_HOST=mysql-xxxx.aivencloud.com` → aborta («jamás siembra un host remoto»); `DB_NAME=mysql` (base existente sin «dev») → aborta con `ER_DBACCESS_DENIED_ERROR` **antes de escribir nada**. *Matiz honesto*: en ese cuarto caso el corte lo da el privilegio de MySQL, no la comprobación de nombre, porque `seedDev.js` abre la conexión (`:34`) **antes** de validar `DB_NAME` (`:43`); sobre una base no-dev accesible sería la comprobación de nombre la que abortaría. En ambos caminos: cero escrituras. El instalador añade además **dos puertas propias** (`instalar.ps1:405-409` para ofrecer y `:514` para ejecutar): exige `DB_NAME` **exactamente** `gamificapp_dev` y servidor local. **(c) Hallazgo de codificación corregido antes del commit**: los tres `.cmd` tenían finales de línea **LF**, y contienen un bloque `if (...)` de varias líneas — justo el caso en que `cmd.exe` puede fallar al interpretarlo. Se normalizaron a **CRLF** y se añadió un **`.gitattributes`** (`*.cmd` y `*.ps1` → `text eol=crlf`) para que Git no dependa del `core.autocrlf` de cada equipo; el **BOM UTF-8 de los `.ps1`** (imprescindible: PowerShell 5.1 lee como ANSI los archivos sin BOM y rompería todos los acentos) es contenido del archivo y Git lo conserva. **Camino de error del `.cmd` probado con CRLF**: con el 5173 ocupado, el bloque `if` se ejecuta y muestra «GamificApp no se pudo iniciar…». **(d) Datos**: tras las tres instalaciones interactivas, `gamificapp_dev` con **cero diferencias en las 18 tablas**, `server/.env` con el **mismo SHA-256** del inicio de la sesión, y el servidor MySQL sin bases ni usuarios sobrantes. **(e) Build** limpio (3,22 s) y **lint 29 (26 errores + 3 warnings) = baseline exacto**, cero nuevos.

**Detalle de la implementación y de las pruebas A–N:**

2026-07-29 (**FASE LOCAL 1 — INSTALADOR BASE FUNCIONAL PARA WINDOWS.** Responde al requisito del revisor de poder instalar y ejecutar GamificApp en local sin conocimientos técnicos. Verificado contra Git antes de tocar nada: `main` = `origin/main` = `ef008c8`, working tree limpio. **Alcance deliberado: NO descarga Node ni MySQL portables (eso es Fase Local 2), no toca Docker, no sirve `dist` desde Express, no cambia lógica de negocio.**

**HALLAZGOS VERIFICADOS CONTRA EL CÓDIGO** (la auditoría previa no había implementado nada; sus conclusiones se comprobaron una a una): **(1)** el requisito real de Node es **`^20.19.0 || >=22.12.0`** — leído de `node_modules/vite/package.json` (Vite 8.0.16), así que **Node 18 y 21 NO sirven**, ni 22.0–22.11; `README.md` y `START_HERE.md` decían «Node 18+» y estaban **mal**. **(2)** `initDb.js` **no puede inicializar una base vacía**: ejecuta `migrarColumnasMaterias` (`ALTER TABLE materias`) en `initDb.js:36` **antes** de `conn.query(sql)` en `:37`, de modo que sobre una base recién creada falla con `Table 'materias' doesn't exist`. **(3) Hallazgo nuevo, no anticipado por la auditoría previa: el SQL base tampoco basta por sí solo.** `database/produccion_defaultdb.sql` crea **11 tablas** (materias, cursos, institucion, estudiantes, retos, progreso_estudiante, usuarios, retroalimentaciones, materiales, docente_materia, invitaciones_estudiante); las **7 restantes** (auditoria, misiones, mision_estudiante, docente_curso, banco_preguntas, configuracion_ia, tipos_juego) las crean **solo** las migraciones de `initDb.js`. **Hacen falta los dos pasos, en ese orden** — medido en vivo sobre una base vacía: 0 → 11 tablas tras el SQL, 11 → 18 tras el primer arranque. Corolario: `seedDev.js` (que escribe en `tipos_juego`) **debe correr después** de que el backend haya arrancado al menos una vez. **(4) `JWT_SECRET` es obligatorio y tumba el arranque** (`middleware/auth.js:12-15` lanza al importar). **(5)** `/api/institucion` es público y sí consulta la BD → sirve de health check real de base de datos, a diferencia de `/api/health`, que no la toca. **(6) Hallazgo propio corregido durante el desarrollo: `server.js` empieza a escuchar ANTES de terminar `inicializarEsquema()`** (el callback de `app.listen` es asíncrono), así que responder `/api/health` **no** prueba que las migraciones hayan acabado — la primera versión del instalador daba el esquema por completo con **11 de 18** tablas creadas. Corregido con una espera activa a que existan las 7 tablas de migraciones **y** la cuenta `admin`.

**QUÉ SE IMPLEMENTÓ (aditivo; cero archivos existentes de la aplicación modificados).** Tres puntos de entrada de doble clic en la raíz — `Instalar GamificApp.cmd`, `Iniciar GamificApp.cmd`, `Detener GamificApp.cmd` — sobre `instalador/` (`comun.ps1`, `instalar.ps1`, `iniciar.ps1`, `detener.ps1`, `bd-verificar.mjs`, `bd-preparar.mjs`). **Instalar**: comprueba Node/npm con el predicado real de Vite, detecta MySQL (sonda TCP en 3306 y 3307 + handshake real con `mysql2` y exigencia de versión ≥ 8), verifica los puertos **3001 y 5173**, `npm ci` en `server/` y raíz (con marcador por hash del `package-lock.json` para no repetirlo), crea la base con `utf8mb4_spanish_ci`, crea un **usuario de aplicación dedicado** con permisos solo sobre esa base, carga el SQL base, genera `server/.env`, `npm run build`, arranca ambos procesos y espera **health checks reales** (nunca `sleep` fijos), y abre el navegador. **El frontend se sirve con `vite preview --port 5173 --strictPort`**: no puede saltar al 5174 y romper CORS. **Credenciales**: `JWT_SECRET` (48 bytes hex) y `ADMIN_PASSWORD`/clave de BD (alfabeto sin caracteres ambiguos) se generan con `RNGCryptoServiceProvider`; **si `server/.env` ya existe se conserva intacto** y solo se completan claves obligatorias ausentes. Resumen legible en `CREDENCIALES.txt` (ignorado por Git) que **nunca** incluye el `JWT_SECRET`. **Datos demo**: pregunta explícita con **predeterminado NO**, solo sobre la base `gamificapp_dev` y en servidor local, ejecutando `server/scripts/seedDev.js` **sin tocar ninguna de sus barreras**; las credenciales demo se extraen de la salida del propio seed (no se duplican constantes). **Detener** cierra **solo** por PID registrado en `.run/` y verificando la línea de comandos — **nunca `taskkill /IM node.exe`**. Logs en `logs/` con censura activa de secretos.

**PRUEBAS EJECUTADAS (todas reales, con evidencia).** **(A) Primera instalación desde cero**: clon limpio del repo (sin `node_modules`, sin `.env`, sin `dist`) contra una base **nueva y vacía** creada para la prueba — completada en ~90 s, 0→11→18 tablas. **(B) Segunda ejecución**: `server/.env` **byte a byte idéntico** (SHA-256 igual), **cero diferencias de filas en las 18 tablas**, fila marcadora sobrevivió, `npm ci` omitido, 12,6 s. *Matiz honesto*: el hash bcrypt de `admin` **sí cambia** en cada arranque porque `asegurarAdmin` re-hashea `ADMIN_PASSWORD` con sal nueva — **la contraseña no cambia**, comprobado con login real posterior. **(C) Iniciar→Detener→Iniciar**: arranque en **9,2 s**. **(D) Iniciar estando ya en marcha**: «Ya estaba en marcha», **no duplica procesos**. **(E) Puerto ocupado**: con un `node.exe` ajeno en el 5173, error claro con nombre y PID, salida 1, y **el intruso no fue tocado**; `Detener` lo reportó como ajeno y tampoco lo cerró. **(F) Fallo de backend** (se quitó `JWT_SECRET`): detectado en **8,9 s por muerte del proceso**, no por agotar el tiempo, mostrando el error real de Node. **(G) Instalación sin datos demo** y **(H) con datos demo** sobre `gamificapp_dev`: el seed corrió por `seedDev.js` y resultó **idempotente (cero filas nuevas)**. **(I/J/K) Autenticación real**: `POST /api/auth/login` → **admin 200 con JWT** (con la contraseña que generó el instalador en el clon, y con la preexistente en el repo principal), **contraseña incorrecta 401**, **docente.demo 200**, **admin.limitado 200**, **estudiante por nombre+PIN 200**, y `GET /api/admin/docentes` 200 con el token. **Navegador**: la página del build servido en :5173 renderiza el login con el nombre institucional real, `GET http://localhost:3001/api/institucion` **200 OK** (CORS correcto) y **consola sin ningún mensaje**. **Doble clic real**: `Iniciar GamificApp.cmd` y `Detener GamificApp.cmd` lanzados como los lanzaría el usuario; **los servidores sobreviven al cierre de la ventana** (verificado: sus procesos padre están muertos y ambos siguen respondiendo 200). **(L) Build** limpio en 3,05 s. **(M) Lint: 29 problemas (26 errores + 3 warnings) = baseline exacto, cero nuevos** — ESLint solo cubre `**/*.{js,jsx}`, así que los `.mjs` del instalador no entran. **(N) Consola del navegador: cero mensajes.**

**LO QUE NO SE PROBÓ (y por qué).** El **login pulsando en el formulario del navegador** no se ejecutó: se validó el mismo `POST /api/auth/login` que envía ese formulario, para los tres roles. **La prueba en un equipo/usuario Windows limpio sigue siendo requisito obligatorio** antes de dar la fase por cerrada: aquí Node y MySQL ya estaban instalados, y el MySQL disponible es el **contenedor Docker en el puerto 3307**, no un MySQL 8 nativo en el 3306 — la rama de detección del 3306 y los mensajes de «instala MySQL» **no se han ejercitado en un equipo sin MySQL**. Tampoco se probó con rutas que contengan espacios distintas de la actual, ni con antivirus restrictivos, ni la generación de un `.exe` (fuera de alcance).

**DATOS.** Ninguna base se borró salvo la creada por las pruebas (`gamificapp_dev_prueba`, inventariada antes de borrarla: 55 filas, todas del esquema semilla) y su usuario MySQL de prueba; el servidor MySQL quedó **exactamente** con las bases y usuarios previos. `gamificapp_dev` conserva sus 18 tablas y sus filas sin un solo cambio. No se ejecutó ninguna migración nueva, no se cambió ningún PIN y no se jugó ninguna partida.)

2026-07-29 (**MICROFASE DE CIERRE — auditoría de las recomendaciones del experto + 2 correcciones P1.** Sesión en dos partes: **auditoría (solo lectura)** de las 12 observaciones del experto contra el código real, y **microfase de implementación** con alcance aprobado y acotado a dos hallazgos. Verificado contra Git antes de tocar nada: `main` = `origin/main` = `3f90372`, working tree limpio.

**AUDITORÍA — resultado por observación del experto.** Ya resueltas y **verificadas en runtime, no solo en documentación**: sidebar colapsable (medido: contenido 504→**713px** a 768, raíl de restauración, foco relevado en ambos sentidos), menú alineado a la izquierda (`text-align:left`, `justify-content:flex-start`, `padding-left:16px` uniforme), responsive del panel del **estudiante** (320/375/768/1280 con desbordamiento **0** en Inicio, Mis Mundos, Mis Premios, detalle de materia con sus 4 pestañas y un reproductor abierto), contenido dinámico (XP 1350, Nivel 2, racha, 6 mundos de BD, 46 misiones/18 completadas, actividad reciente), **caché** (tras logout el `localStorage` queda **solo** con `institucion_cache` y `materias_cache`; inventario de claves confirmado en vivo, sin ninguna clave privada no documentada) y **JWT** (secreto obligatorio que aborta el arranque si falta, expiración 8h, firma verificada, payload mínimo sin PII, bcrypt, rate limiting por cuenta **y** por IP, mensajes de error idénticos, cabeceras de seguridad, CORS con lista blanca, y autorización de admin releída desde BD en cada petición) → **sin P0 ni P1 de seguridad; se mantiene la decisión de NO introducir Zod ni reestructurar JWT**. Emojis: inventario semántico (262 en 56 archivos) muestra que funcionan como **marcadores consistentes** (✨=IA, ⭐=XP, 🔥=racha, 🔒=bloqueado, 📊=stats), ya en la proporción «uno por segmento» que pedía el experto y con valor pedagógico para 6-9 años → **no se toca**.

**CONFIRMACIÓN INDEPENDIENTE DEL FALSO «0/100» (refuerza el cierre del ítem 30).** Reproducido el mecanismo con evidencia nueva y distinta: la barra de XP del Home tenía `width: 35%` **en línea** pero `getComputedStyle().width` = **0px**, con `getAnimations()[0].currentTime` clavado en **0** y `playState: running` bajo `visibilityState: 'hidden'`; al forzar `.finish()` midió **47px = exactamente el 35% de 133px**. **El reloj de animaciones congelado del entorno de previsualización explica el síntoma; no hay fallo de cálculo.**

**DOS HALLAZGOS NUEVOS P1 — IMPLEMENTADOS (3 reglas CSS, 3 archivos, cero JSX, cero backend).**
- **(N1) Las acciones de cabecera no hacían wrap y quedaban FUERA DE PANTALLA.** Medido antes: Docente → Mis Estudiantes «📥 Importar desde Excel» en `right: 392` y «Ver ranking completo» en `right: 496` con viewport de 320; a **768px (el ancho de las tablets de la escuela) «Ver ranking completo» seguía fuera**. Admin → Estudiantes: `.contenido` con **102px** de desbordamiento. Causa: **dos** niveles sin `flex-wrap` — `.card-head` (`admin/dashboard.css`, título vs. bloque de acciones) y `.section-head-extra` (`dashboardWidgets.css`, las acciones entre sí). Corrección **aditiva**: `flex-wrap: wrap` en ambos + `gap: 10px` en `.card-head` (que no tenía ninguno) para que las dos filas no se toquen al plegarse. **Resultado medido: los 10 botones de acción de Admin y Docente quedan dentro del viewport y son alcanzables por `elementFromPoint` en 320/375/768/1280.** Admin → Estudiantes pasó de 102px de desbordamiento a **0**; Admin → Misiones de 3px a **0**. **Escritorio intacto**: a 1280 todas las `.card-head` siguen en **una sola fila** (`mismaFila: true`, alto 36px, separación título↔acciones de 222-599px), por lo que ni el `wrap` ni el `gap` tienen efecto visible.
- **(N2) Tres `<select>` con apariencia nativa** en el modal «Adaptar con IA» (Docente → Biblioteca). Es el **espejo exacto** del bug que la Fase 8 corrigió en los textareas: `docentePanel.css` tematizaba `.perfil-form input` pero **no `.perfil-form select`**. Medido antes: **Arial 13.33px, borde `rgb(118,118,118)`, `border-radius: 0`, `padding: 0`** junto a inputs ya correctos. Corrección: **se amplió el selector existente** (no se creó regla duplicada) añadiendo `select`, más `background: var(--color-surface)` explícito porque el fondo por defecto de un `<select>` lo decide el sistema — y `--color-surface` es `#ffffff`, **idéntico al que ya tenían los inputs, así que para ellos es un no-op visual**. Medido después: los 3 selects rinden **Inter 13.6px, borde `rgb(226,232,240)` (`--color-border`), radio 10px (`--radius-sm`), padding 9px 12px, fondo #fff** = **coincidencia exacta con los inputs vecinos** (`coherentes: true`), y el tratamiento de foco es **idéntico** al de los inputs (`mismoTratamiento: true`), sin introducir estados nuevos. Diferencia residual inocua: el select mide 38px de alto y el input 36px (la flecha nativa); van en filas distintas de un `grid`, nunca se comparan lado a lado.

- **(N1-bis / ítem 54) La tabla de Docente → Mis Estudiantes no estaba envuelta en `.tabla-scroll`.** Detectado al validar N1 y **aprobado como ampliación del alcance en la misma tanda**. Es un hallazgo **distinto** de N1: aunque los botones ya quedaban dentro tras el `flex-wrap`, la tabla seguía ensanchando el panel (240px a 320, 185px a 375, 80px a 768, 0 a 1280). Verificado que sus ancestros hasta `.contenido` eran todos `overflow-x: visible`, a diferencia de la de Admin y la de la Biblioteca. **Antes de tocar nada se comparó con el patrón de la casa** (`DashboardWidgets.jsx:241`, componente `TablaPro`): `<div className="tabla-scroll"><table className="admin-tabla …">`, con `.tabla-scroll { overflow-x: auto }` en `adminDashboard.css:48` — el mismo patrón que ya usan `ImportarEstudiantes` (con su variante `.imp-tabla-scroll`) y el Libro de Calificaciones. **Corrección: se envolvió la tabla en ese mismo `.tabla-scroll` existente. Cero CSS nuevo, cero segundo sistema de scroll, cero columnas tocadas.** Medido después: desbordamiento del contenedor de contenido **0 en los cuatro anchos**; la tabla sigue **completa y accesible por su propio scroll** (a 320: 238px visibles de 519 totales; a 375: 293 de 519; a 768 expandido: 374 de 519); **las 5 columnas siguen presentes** (Nombre, Curso, XP, Acceso, Acciones) en todos los anchos; **exactamente un contenedor con scroll horizontal** (`.tabla-scroll`), sin doble barra; y con el **sidebar colapsado a 768 la tabla cabe entera** (583 de 583) por lo que **no aparece ninguna barra innecesaria**. **A 1280 no hay regresión**: la tabla mide 886px en un contenedor de 886px (`puedeScrollear: false`, cero barras) y la cabecera sigue en una sola fila. El diff de este cambio, ignorando espacios (`git diff -w`), es **exclusivamente el `<div>` envolvente más su comentario**: el resto son líneas re-indentadas.

**CORRECCIÓN DE BASELINE DOCUMENTAL: el lint real es 29 problemas (26 errores + 3 warnings), no 28.** Investigado como pedía la orden: es **determinista** (3 ejecuciones seguidas → 29), **no hay deriva de herramientas** (`package-lock.json` fija eslint `10.4.1` y lo instalado es `10.4.1`), y **el commit `3f90372` no aportó ninguno** — de sus 10 archivos solo 2 son linteables (`SidebarLayout.jsx`, `authService.js`) y **ambos linteados por separado dan exit 0**; como todas las reglas activas son por archivo, no pueden sumar al total. El «28» de la entrada de la Fase 8 fue un **error de medición de aquella sesión** (la propia entrada admitía que el número «ha oscilado entre 28 y 29», y el ítem 12 del `MASTER_PLAN`, muy anterior, ya decía 29). **No se corrigió ninguno de los 29**; son todos patrón preexistente (`react-hooks/set-state-in-effect` ×13, `react-refresh/only-export-components` ×10, `exhaustive-deps` ×3 warn, `react-hooks/refs` ×1, `no-useless-assignment` ×1 en server).

**REGRESIÓN.** `npm run build` limpio (4.45s); el CSS pasa de 153.91 kB a **154.00 kB** (+90 bytes, gzip **24.11 kB sin cambio**). **Lint 29 (26+3) = baseline exacto, cero nuevos.** **Consola: cero errores** en todo el recorrido de los 3 roles. **Smoke test de los 3 roles** con el sidebar **expandido y colapsado** donde aplica: **Estudiante** (Inicio, Mis Mundos, Mis Premios, detalle de materia) sin desbordamiento a 320 ni a 1280 — **cero regresión pese a que `.card-head` es CSS compartido**; **Docente** (7 secciones) y **Admin** (4 secciones del admin restringido) con todos los botones dentro del viewport en los 4 anchos. **DATOS: no se completó ninguna partida → cero `POST /api/progreso`**; no se cambió ningún PIN, no se reseedeó, no se ejecutó ninguna migración y no se tocó la BD. Verificado en local Docker/MySQL (`gamificapp-mysql-dev`:3307, backend :3001, Vite :5173). **Local ≠ producción**: IA, concurrencia real y migraciones sobre datos productivos siguen sin validar.

**DIFERIDOS CONSCIENTEMENTE en esta microfase** (auditados, con evidencia, **no implementados**; detalle en `MASTER_PLAN` §3): **N3** abrir una materia dispara 3 `GET /api/retos` a la misma materia cuando el que va sin `tipo` ya devuelve todo; **N4** `cors()` sin `maxAge` → preflight en casi cada URL distinta; **N5** el panel Admin sondea cada 20s (medido: 2 oleadas de 4 peticiones **exactamente a 20s** con el admin restringido; un Principal dispara 10 llamadas paralelas ≈ 30 req/min) aunque la sección visible sea otra — **la guarda `document.hidden` sí funciona** (0 peticiones con la pestaña oculta); **N6** la barra de XP anima `width`, patrón que con el reloj congelado se ve vacía (usuario real con pestaña visible o `reduced-motion`: correcto); **N7** un docente/estudiante desactivado conserva acceso hasta que expire su token (≤8h) porque `autenticar` solo verifica la firma — **los caminos de admin sí revalidan contra BD** —, y cerrarlo toca §10 y exige spec. **Deuda técnica explícitamente fuera de este cierre:** Zod, reestructuración de JWT, token en `localStorage`, code splitting (bundle de 1.6 MB), base64 en MySQL, ausencia de pruebas automatizadas, Clean Architecture estricta y los 29 de lint.

**LOGO INSTITUCIONAL (N8): sigue pendiente de DATO, no de código.** Confirmado hoy contra la API: `GET /api/institucion` devuelve `logo_data` de **longitud 0**. El soporte en `SidebarLayout` + `dashboard.css` ya está completo (ver entrada de la Fase 8); se sube desde **Administración → Institución**. **No se generó ningún asset ni se metió base64 temporal.**)

2026-07-29 (**SPEC-018 Fase 8 — AUDITORÍA FINAL Y CIERRE DE AJUSTES.** Dos etapas: la 1 fue **solo lectura** (chat anterior) y la 2 implementó lo accionable. Verificado contra Git antes de tocar nada: `main` = `origin/main` = `83708b0`, working tree limpio, Fases 1-7 cerradas.

**ETAPA 1 — Auditoría (sin cambios de código).**
- **DIAGNÓSTICO DEFINITIVO DEL FALSO «0/100» (P0 del triage, ítem 30 de MASTER_PLAN): NO EXISTE UN FALLO DE CALIFICACIÓN.** La causa es el contador visual de `ResultadoActividad.jsx` (hook `useContador`, basado en `requestAnimationFrame`): el entorno de preview mantiene la pestaña con `document.visibilityState === 'hidden'` y el reloj de animaciones **no avanza**, así que el número se queda clavado en el 0 con el que arranca. Se recorrió el camino completo (reproductor → `POST /api/progreso` → backend → MySQL → respuesta → overlay → Libro de Calificaciones) en Quiz, Verdadero/Falso y Misión Narrativa: el backend devolvió 50, la BD guardó la mejor calificación, V/F pasó de 67 a 100, Misión calculó 67 y el Libro mostró los valores correctos; XP y calificación están desacoplados (con `xp_abonado: 0` la calificación siguió siendo correcta). **Reconfirmado en la Etapa 2 con evidencia nueva:** partida real de *Completa la frase* con 1/2 aciertos → `POST /api/progreso → 201` con cuerpo **`{"calificacion":50,"mejor_calificacion":50,"porcentaje":50,"xp_abonado":100,"xp_total":450}`** mientras el overlay mostraba «0 / 100» y, **en el mismo overlay**, la retroalimentación de rango medio («💪 ¡Buen esfuerzo!») que corresponde a 50, no a 0 — el dato correcto ya estaba en pantalla al lado del contador congelado. Medición directa del congelamiento: `document.visibilityState = 'hidden'` y `element.getAnimations()[0].currentTime` **clavado en 0 tras 1,6 s**. **CONSECUENCIA: no se tocó nada de calificación, backend de progreso, BD, Libro, XP, recompensas ni `ResultadoActividad`.** Es una **limitación del entorno de preview**, no un bug de la app. El ítem 30 del backlog queda **cerrado como falso positivo**.
- **Requests:** el único patrón raro es `GET /api/progreso/:id` **×2** en el montaje inicial, atribuible a **React StrictMode** (solo desarrollo). Durante la navegación normal no aparecieron refetches redundantes. **No se optimizó** (el ítem 46 del backlog se mantiene, reclasificado como ruido de desarrollo).
- **JWT/validación:** auditados el secreto obligatorio, la expiración, la validación del token, la relectura de permisos/estado del usuario desde BD, las múltiples validaciones server-side y el rechazo de payloads malformados. **Sin vulnerabilidad nueva → no se introdujo Zod, no se reestructuró JWT y no se añadió ninguna librería** (ver «Decisiones deliberadas» abajo).
- **Cuatro hallazgos accionables** que son el alcance de la Etapa 2: textareas con apariencia nativa, caché `edu_xpTotal` que sobrevive al logout, sidebar no colapsable en escritorio/tablet y huecos finales de accesibilidad.

**ETAPA 2 — Implementación (7 archivos, +314/-17, cero backend).** Los cuatro hallazgos se **re-verificaron en código antes de tocarlos**; los cuatro seguían siendo ciertos.
- **(A) Textareas con estilo del navegador — 2 reglas, 2 archivos.** Medido el baseline de un `<textarea>` sin tematizar: `border-radius 0`, borde `solid 1px #767676`, `padding 2px`, fuente **monospace 13.33px**. (1) `adminDashboard.css`: `.asistente-campo input, select` **no incluía `textarea`** → afectaba a **«Editar materia» → Competencias**; (2) `dashboard.css`: la regla solo cubría `.quiz-field textarea.campo-tema`, no `textarea` a secas → afectaba a **Introducción y Final de la Misión Narrativa**. Ambos ahora usan los mismos tokens que sus inputs vecinos (`--radius-sm` 10px, `--color-border`, padding 11/14, `font-family: inherit` → Inter 15.2px, `--color-surface`, `resize: vertical`, y `min-height: 88px` en el de Competencias), con el `:focus` extendido a `textarea`. **Comprobado en vivo:** los 3 textareas medidos coinciden **exactamente** con el input contiguo (`coherentes: true`) y **el campo Tema autoexpandible (`textarea.campo-tema`) conserva intactos sus overrides** (`resize: none`, `overflow-y: hidden`, `min-height: 44px`) porque su selector tiene más especificidad y va después. **Ningún formulario se rediseñó.**
- **(B) Fuga de caché entre estudiantes — `authService.js`.** **Inventario COMPLETO de claves persistidas** (no hay `sessionStorage` en toda la app): *(deben sobrevivir al logout)* `institucion_cache` y `materias_cache` — catálogo público e institucional, iguales para todos, y el Login los necesita **antes** de que exista sesión; *(pertenecen a la sesión/usuario)* `auth_token`, `auth_usuario`, `edu_estudianteId`, **`edu_xpTotal`** y `edu_historialRetos`; *(podían filtrar entre usuarios)* **`edu_xpTotal`** (XP/nivel del niño anterior en el primer render) y `edu_historialRetos` (títulos de borradores del docente anterior). Las tres primeras ya se borraban; **`edu_xpTotal` y `edu_historialRetos` no.** Corrección mínima y **central**: constante `CLAVES_DE_USUARIO` + helper `limpiarCacheDeUsuario()` llamado en `logout()` **y** en `guardarSesion()` — lo segundo porque cerrar la pestaña no pasa por logout. **Cero cambios de auth/JWT** más allá de esta limpieza de estado local. **PRUEBA A→B EJECUTADA:** *Estudiante Prueba Uno* (id 1) → Home con **1350 XP / Nivel 2** y `edu_xpTotal = "1350"` → **logout** → storage queda **solo** con `institucion_cache` y `materias_cache` → login *Estudiante Prueba Dos* (id 2, 350 XP) observando **desde el primer render con un `MutationObserver`**: muestra 1 (t=26570) **«⭐ 0 XP», «Nivel 1», `edu_xpTotal = null`**; muestra 3 la caché ya vale 350; muestra 4 (t=26750, +180 ms) «⭐ 350 XP». **B no vio el XP, el nivel ni la racha de A en ningún instante, ni siquiera temporalmente.**
- **(C) Sidebar colapsable en tablet/escritorio — `SidebarLayout.jsx` + `dashboard.css`.** Recomendación coincidente de dos revisores. Se **extendió el componente existente** (no se construyó un segundo sistema): estado `colapsado`, botón **«Ocultar el menú de navegación»** en la cabecera y, al ocultarse, un **raíl de 55px con «Mostrar el menú de navegación»** — la navegación **nunca queda irrecuperable**. Se eligió **ocultar/mostrar completo** en vez de un mini-sidebar de iconos por ser más simple y robusto. Detalles medidos: el aside colapsa con `width: 0` + `visibility: hidden` (lo saca del orden de tabulación **y** del árbol de accesibilidad: con el sidebar oculto el árbol solo expone el botón del raíl, sin trampas de foco invisibles) + `border-right-width: 0` (sin esto el borde de 1px sobrevive a `width: 0`); `overflow: hidden` se acota **al estado colapsado** y no a `.sidebar`, porque el panel desplegable móvil se posiciona fuera de la caja del aside y se recortaría; transición discreta de 0.18s; `aria-label` + `aria-expanded` + `aria-controls` apuntando al mismo `id` del aside en ambos botones; **el foco pasa de un botón a su relevo** al ocultar y al restaurar (verificado en ambos sentidos). **Ganancia real medida:** contenido **1016→1225px a 1280** y **504→713px a 768** (+209px, +41%). **Verificado en los 3 roles** (Estudiante 3 secciones, Docente 7, Admin restringido 4 con su rótulo de grupo) y en **320/375/480/768/1024/1280**: `overflow-x = 0` y cero elementos fuera del viewport en todos. **La experiencia móvil ≤760px no cambia**: el botón de ocultar y el raíl van a `display: none` y el aside vuelve a `width: 100%` / `visibility: visible` **aunque el usuario hubiera dejado el sidebar oculto antes de reducir la ventana** — probado ese caso exacto (colapsar a 1024 → bajar a 375): barra superior de 42px, menú desplegable abre (absolute, top 62, 375×356), ítems y logout visibles, cierra bien, `overflow-x = 0`.
- **(D) Accesibilidad final.** **(D1)** `SidebarLayout` no exponía la sección activa más que por color → **`aria-current="page"`** en el ítem activo (es navegación entre vistas del panel; no se usó ARIA incorrecta para rellenar el hueco). Verificado en los 3 roles y también dentro del menú móvil. **(D2)** `.completar-btn-siguiente` era el **único** control de los reproductores sin `:focus-visible` tras las Fases 7.1-7.7; se confirmó por grep que lo **comparten Completar Espacios y Verdadero o Falso**, se trató como infraestructura compartida y **se probaron los dos**: en Completar («Siguiente frase») y en V/F («Siguiente afirmación») el anillo medido es `solid 3px rgb(17,94,89)` = `--color-primary-dark` con `outline-offset: 3px` sobre el relleno `rgb(15,118,110)` del propio botón (se usó el tono oscuro, no `--color-primary`, para que anillo y botón no se lean como una sola mancha). **Mecánica sin tocar.** **(D3) `prefers-reduced-motion` global en `index.css`:** solo 4 archivos lo respetaban (login, Toast, overlays de resultado y parte de `juegos.css`) frente a **~17 `@keyframes` en 9 archivos** más las transiciones de hover/modales/editores. En vez de reescribir cada archivo se añadió **un único bloque de red de seguridad** (`*, *::before, *::after` con `animation-duration/delay`, `animation-iteration-count`, `transition-duration/delay` y `scroll-behavior`; `!important` es imprescindible para ganarle a 10 archivos y a los estilos en línea de MUI). **Las animaciones no se cancelan: se COMPLETAN al instante**, así que overlays, modales, toasts y tarjetas **siguen apareciendo** en su estado final visible. **EXCEPCIÓN deliberada:** `.quiz-spinner` y `.editor-ia-spinner` **siguen girando** (1.4s, infinito) porque son la única señal de «generando con IA / cargando» y congelarlos borraría información. **Verificado inyectando las mismas declaraciones** (el entorno no emula la preferencia a nivel de SO): los 4 bloques del Home quedan en `opacity: 1` con su alto real, la barra de XP salta directa a su ancho correcto (**223px = 35% de 637**) y el spinner conserva `quiz-spin 1.4s infinite`. **Animaciones cubiertas:** las 17 `@keyframes` del proyecto (`toast-in`, `editor-rise`, `editor-spin`, `modal-fade`, `modal-rise`, `quiz-spin`, `login-deriva-a/-b`, `login-aparece`, `toast-sube`, `linea-sacudida`, `canasta-feliz`, `ficha-aterriza`, `ficha-rebote`, `chip-pop`, `resultado-fondo/-pop/-sube/-brote/-confeti-cae`, `home-entra`) **más todas las transiciones**, con los 4 bloques específicos previos intactos y ahora redundantes-pero-correctos.
- **(E) Microinteracciones — exactamente 2, las dos CSS puro, exclusivas del estudiante.** **(1) Entrada del contenido del Home** (`.home-nino > *`, cascada 0.3/0.4/0.5/0.6s). **Hallazgo de diseño durante la prueba, importante:** la escritura habitual (`animation-delay` + `fill-mode: both` + fade de `opacity`) dejaba los 4 bloques en **`opacity: 0`** cuando el reloj de animaciones está congelado → **Home invisible**. Se rediseñó para que el **peor caso sea inofensivo**: se anima **solo `transform`** (nunca `opacity`) y la cascada se hace con **duraciones distintas en vez de retardos**, lo que elimina la necesidad de `fill-mode`. Medido el peor caso real: `opacity: 1`, alto completo y texto legible en los 4 bloques, apenas 10px más abajo. **(2) La barra de XP se llena** (`transition: width 0.6s` acotado a `.home-nivel-track .progress-fill`, **no** al `.progress-fill` global que comparten los juegos): el ancho lo sigue calculando el mismo `porcentajeNivel` de siempre — **cero cambios en XP, progreso, recompensas o backend**.

**LOGO INSTITUCIONAL (recomendación de un revisor) — VERIFICADO, SIN CAMBIO DE CÓDIGO.** El soporte **ya existe completo**: `SidebarLayout.jsx:36` lee `getInstitucionCache()?.logo_data` y `:106` lo pinta como `<img className="sidebar-logo" src={logo} alt="" />` (decorativo, el nombre va en el `<h2>` contiguo); el CSS está en `dashboard.css` con su variante móvil (`max-height: 48px` / 34px, `max-width: 72%` / 48px, `object-fit: contain`). El dato llega del **caché de `institucionService`**, que se llena con `GET /api/institucion` (público, SPEC-002); el mismo campo alimenta el favicon (`aplicarInstitucion`, con `favicon_data || logo_data`). **Lo único que falta es el dato:** consultado el MySQL local, la fila de `institucion` tiene **`logo_data` y `favicon_data` de longitud 0**. Se carga desde **Administración → Institución**. **No se inventó logo, no se generó ningún asset y no se metió base64 temporal.**

**DECISIONES DELIBERADAS — recomendaciones de expertos NO implementadas** (y por qué): **Zod** y cualquier librería nueva a 3 días de la entrega (la auditoría de JWT/validación no encontró vulnerabilidad: hay secreto obligatorio, expiración, validación de token, relectura de permisos desde BD y rechazo de payloads malformados; añadir un validador de esquemas sería riesgo sin beneficio demostrado); **reestructurar JWT** (misma razón); **optimizar el `×2` de `GET /api/progreso/:id`** (es StrictMode, solo desarrollo); **el falso «0/100»** (no es un bug, ver Etapa 1); y todo lo excluido por alcance: endpoints nuevos, cambios de SQL/esquema, de calificación, XP, recompensas, IA, permisos, SPEC-013/016/017, limpieza general de CSS muerto y arrastre de errores históricos de lint.

**REGRESIÓN.** `npm run build` limpio (3.67s). **Lint: 28 problemas (25 errores + 3 warnings) = baseline EXACTO de HEAD `83708b0`, cero nuevos** — medido con `git stash` sobre el HEAD limpio. **CORRECCIÓN DOCUMENTAL: el baseline real es 28, no 29.** Varias entradas de esta bitácora dicen «29 = baseline» y la propia orden de la Fase 8 esperaba «29 (26 errores + 3 warnings)»; el número ha oscilado entre 28 y 29 en distintos momentos y **al día de hoy, sobre `83708b0`, es 28 (25+3) verificado con stash**. **Consola:** cero errores de aplicación en todo el recorrido de los 3 roles (validado con una sonda de control `console.error` para confirmar que la captura funcionaba; lo único emitido fue el aviso de React DevTools y los mensajes de HMR de Vite). **Red:** ninguna petición nueva ni endpoint nuevo; se mantiene el patrón `×2` de StrictMode ya documentado y **exactamente un `POST /api/progreso` por partida** (2 partidas → 2 POST). **Datos:** no se alteró ni el PIN, ni la BD, ni se reseedeó; las dos partidas jugadas escribieron progreso legítimo del estudiante de prueba en la BD **local** (Completar 1/2 → calificación 50, XP 350→450; V/F 0/3 → calificación 0). El modal «Editar materia» se abrió y se **cerró sin guardar**. **Verificado en local Docker/MySQL** (`gamificapp-mysql-dev` :3307) con backend propio en :3002 y Vite en :5174 para no interferir con otra sesión que ocupaba :3001/:5173. **Un falso positivo descartado:** a 1280 la Biblioteca del docente muestra 159 elementos más allá del borde del viewport, pero el **100%** están dentro de `.tabla-scroll` (`overflow-x: auto`, el patrón correcto) y el documento tiene `overflow-x = 0` — preexistente, no regresión; de hecho colapsar el sidebar lo reduce a 0.

**PENDIENTES REALES POSTERIORES** (nada de esto se implementó): del triage de 21 diferidos siguen abiertos los ítems **32** (contraste de `.mision-card.mision-bloqueada`), **33** (decisión global sobre `--color-success`), **35** (estados vacíos inconsistentes), **36** (`--color-text-soft`/`-muted` como tokens globales), **37-41** (P2: `window.prompt` del PIN, títulos duplicados del docente, `aria-label` del menú «Agregar preguntas», hex decorativos, `.home-mundos-grid` a 1 columna en 320px) y **42-50** (P3: CSS muerto, ubicación de `LogroToast`, divergencia Quiz ↔ `useRecompensa`, breakpoints históricos, `pointerEvents:none` heredado, nomenclatura `.mision-progreso`, fallbacks de `.memorama-carta-dorso`). Se **cierran** el **30** (falso positivo) y el **31**, **34** y el `prefers-reduced-motion` (implementados aquí). Además: **cargar el logo institucional real** y **validar en producción** SPEC-016/SPEC-017.

**Estado SPEC-018: las 8 fases CERRADAS.**)

2026-07-28 (**SPEC-018 — CIERRE DE FASES 6 Y 7 + sincronización documental** (solo documentación, cero código). **Auditoría contra Git (fuente de verdad, no informes previos):** `main` = `origin/main`, working tree limpio. **Fase 6 CERRADA:** 6.1 Administrador → commit **`cf5ef6e`** (2026-07-20, `adminDashboard.css`, `ModuloIA.jsx`, `ModuloJuegos.jsx`, `moduloMaterias.css` + `CURRENT_STATE`); 6.2 Docente → **`ccb18b6`** (2026-07-20, `selectorBanco.css` + `CURRENT_STATE`); 6.3 Estudiante → **`43185ac`** (2026-07-28, `DashboardEstudiante.jsx`, `PanelMisiones.jsx`, `dashboardEstudiante.css`, `misionesPanel.css` + `CURRENT_STATE`). **Fase 7 CERRADA:** 7.A+7.1 Quiz e infraestructura compartida → **`ac8d6b6`** (2026-07-21, un solo commit para ambas subfases, `dashboard.css`); 7.2 Clasificador → **`844ede7`**; 7.3 Misión Narrativa → **`25ae1c8`**; 7.4 Completar Espacios → **`07ef88f`**; 7.5 Verdadero o Falso → **`f818ade`**; 7.6 Memorama → **`85d452b`** (todas 2026-07-22); 7.7 Línea del tiempo → **`83d9ad1`** (2026-07-28). **CONTRADICCIÓN HISTÓRICA RESUELTA:** cinco entradas de esta bitácora (Fases 7.A, 7.1, 7.2, 7.3 y 7.4) declaraban «Sin commit / sin push (pendiente de revisión de Fabrizio)» porque se escribieron **antes** del commit de cada fase y nunca se actualizaron después; `git log`/`git show` demuestran que **las cinco sí se commitearon y pushearon**. Corregidas in situ con su hash, marcando explícitamente la corrección. Igualmente **falsa la afirmación posterior de que la Fase 6.1 estaba «sin commit»**: `cf5ef6e` la contiene con sus 4 archivos de código. Regla aprendida: la nota de commit de una fase se actualiza **en el mismo commit que la cierra**. **TRIAGE de hallazgos diferidos de Fases 6 y 7 (recopilados, NO implementados; entrada a Fase 8):** **P0** — (1) overlay de fin de partida muestra **`0/100`** pese a respuestas correctas, reproducido en 7.3/7.4/7.5/7.6/7.7 con XP ya maximizado; vive en backend + `ResultadoCierre` compartido; **§10 (XP/calificación) → requiere spec**; lo primero de Fase 8 es determinar si la calificación **persistida** (SPEC-015, Libro de Calificaciones del docente) también es 0 —dato incorrecto— o si es solo presentación. **P1** — (2) `SidebarLayout` (los 3 roles) no expone `aria-current`/`aria-selected`: la sección activa se distingue solo por color → transversal, requiere regresión en Admin/Docente; (3) contraste del contenido bloqueado en Mis Premios: `.mision-card.mision-bloqueada { opacity:.62 }` deja el texto que explica cómo desbloquear en ≈2.4:1; (4) `--color-success` **#16a34a** global falla AA sobre blanco (3.30:1) allí donde aún se usa como texto —ya corregido puntualmente en 7.4 y 6.3—; (5) `.completar-btn-siguiente` (compartido Completar+V/F) sigue sin `:focus-visible` temático, único hueco tras 7.1-7.7 y 6.3; (6) estados vacíos inconsistentes: el Home del estudiante usa `EmptyState` pero las 4 pestañas de materia usan `<p class="vacio-msg">`, clase compartida con 6 reproductores y la Biblioteca del docente; (7) formalizar `--color-text-soft`/`--color-text-muted` como tokens globales (diferido en 6.1 **y** 6.2). **P2** — (8) `window.prompt` ×2 del cambio de PIN del estudiante (MASTER_PLAN §3.29; su reemplazo correcto es el modal de SPEC-001 y toca autenticación §10); (9) títulos duplicados H1/H3 en Mis Estudiantes y Ranking + jerarquía de headings en Mi Perfil (6.2); (10) `aria-label` en los íconos del menú «Agregar preguntas» (6.2, área SPEC-013); (11) hex decorativos sueltos: ✕ `#ef4444` inline en la revisión de Completar y V/F, `#f0fdf4` en `.linea-evento.is-correcto`, violet/rose de las canastas del Clasificador (hex de identidad, decisión de Fase 2); (12) `.home-mundos-grid` cae a 1 columna a 320px (6 mundos ≈760px de scroll; a 375px ya son 2). **P3** — (13) CSS muerto en juegos: `.quiz-puntaje` (`quizInteractivo.css`, el marcador migró a `ResultadoOverlay`) y `.linea-evento.is-correcto`/`.linea-evento-check`/`.linea-lista.is-sacudida`+`@keyframes linea-sacudida` (nunca aplicados por `LineaTiempo.jsx`); (14) CSS muerto en el panel del estudiante: `.home-mundo-2/-4`, `.logros-grid`+`.logro-card/-icon/-badge/-bloqueado` (retirados por SPEC-011; contienen los 2 únicos `!important` del archivo) y `.rank-item-yo` — ~70 líneas; (15) **`LogroToast` se define dentro de `src/components/quiz/QuizInteractivo.jsx`** y `juegosComunes.jsx` solo lo re-exporta (`:11`/`:171`), de modo que los 7 juegos dependen del reproductor del Quiz para su toast — deuda de ubicación, verificada en código, **no estaba registrada en docs**; (16) **divergencia Quiz ↔ `useRecompensa`**: Clasificador, Completar, Memorama, Línea, V/F y Misión Narrativa usan el hook compartido, pero `QuizInteractivo.jsx` **no lo usa** (0 ocurrencias) y mantiene su propio camino de recompensa — verificada en código, **no estaba registrada en docs**, toca §10; (17) `GET /api/progreso/:id` se dispara 4-6 veces al entrar al Home del estudiante (StrictMode + dependencias `pagina`/`refrescar`) — ruido de red, sin impacto funcional; (18) consolidar los breakpoints históricos 560/720/900 y las cabeceras de módulo compartidas (diferido en 6.1); (19) `style={{ pointerEvents:'none' }}` en los `h1`/`p` de «Mis mundos» y «Mis premios» (hack heredado sin causa documentada); (20) `.mision-progreso` sigue siendo un nombre genérico en el panel de Premios (deuda de nomenclatura tras el rename de 7.3); (21) `.memorama-carta-dorso` declara fallbacks hex divergentes del token real — inofensivo, los tokens existen. **NO se implementó ninguno de los 21.** **Estado SPEC-018: Fases 1-7 cerradas, Fase 8 (auditoría visual y funcional final) pendiente.**)

2026-07-28 (SPEC-018 **Fase 6.3 — Pulido visual y UX del panel Estudiante** (fase pendiente desde F6.1/F6.2; misma metodología de las fases de reproductores: inventario real por grep → clasificar A exclusivo / B compartido con Admin-Docente / C compartido con juegos / D infraestructura global → navegación real como estudiante en local Docker/MySQL → solo P0/P1 y P2 pequeños EXCLUSIVOS → regresión → build/lint/consola → parar antes de commit). **Alcance:** experiencia del estudiante FUERA de los reproductores (los 7 juegos ya se auditaron en Fase 7). **Superficie inventariada:** `src/pages/estudiante/DashboardEstudiante.jsx` + `dashboardEstudiante.css` (Home, Mis Mundos, detalle de materia con sus 4 pestañas, listados de material/quizzes/juegos/misiones) y `PanelMisiones.jsx` + `misionesPanel.css` (Mis Premios). **Inventario clasificado por búsqueda real (NO por nombre de archivo):** *(A. exclusivo del estudiante — verificado por grep en todo `src/`)* `.home-nino`, `.home-saludo(-meta)`, `.home-avatar`, `.home-nivel(-badge/-track/-xp/-racha)`, `.home-hero(-emoji/-texto/-flecha)`, `.home-mundos(-grid)`, `.home-mundo(-emoji)`, `.home-logros(-emoji/-texto/-flecha)`, `.materia-panel-est`, `.quiz-disponible-lista/-item/-icon/-meta/-tema/-sub/-cta`, `.misiones-panel/-resumen/-stat/-filtros/-grid`, `.mision-chip/-card/-tier/-check/-lock/-titulo/-desc/-progreso(-txt)/-card-foot/-recompensa/-xp/-horizonte/-hint`; *(B. COMPARTIDO con Admin/Docente, NO tocado)* `.opcion`/`.opcion-activa`, `.materia-panel`, `.back-btn(-inline)`, `.materia-hero(-emoji/-meta)` (también en `dashboard.jsx` del admin y `.materia-doc` del docente), `.card`/`.card-head`/`.card-tag`, `.contenido-sub`; *(C. compartido con juegos, NO tocado)* `.vacio-msg` (6 reproductores + Biblioteca del docente), `.progress-track/-fill`, `.materia-subvista`; *(D. infraestructura global, NO tocada)* `SidebarLayout` (Fase 5, los 3 roles), `ModalPanel`/`ConfirmDialog`/`Toast` (Fases 3-4), `EmptyState`, `FileChip`/`FilePreviewModal`, `useGuardiaActividad`, `LogroToast`. **HALLAZGOS IMPLEMENTADOS (4 archivos, +62/-4, TODOS exclusivos del estudiante; cero cambios de mecánica/evaluación/XP/progreso/recompensas/permisos/backend):** **(P1-1, responsive real medido)** la barra de nivel (`.home-nivel`) era un único `flex` sin `wrap` en el que la barra de XP era el único hijo que podía encoger (los otros tres tienen `flex-shrink:0`): **a 320px la barra medía 0px de ancho (desaparecía por completo)**, a 375px 35px y a 768px 101px, y **el chip de racha 🔥 se salía del contenedor** (`right: 324 > 320`) → `.home-nivel { flex-wrap: wrap; row-gap: 8px }` + `.home-nivel-track { flex: 1 1 120px; min-width: 120px }` (fix en la regla base, no en media query, para cubrir también el caso 768 con sidebar lateral); **(P1-2, responsive real medido)** en los listados de quizzes/juegos/misiones el chip CTA "Empezar/Jugar" (`flex-shrink:0`, 87px) y el icono (44px) dejaban el título en **una columna de 45px a 320px** (item de 161px de alto) → media query ≤480: `.quiz-disponible-item { flex-wrap: wrap }` + `.quiz-disponible-meta { flex-basis: calc(100% - 58px) }` + CTA a su propia línea; **(P1-3, accesibilidad)** ningún control del panel tenía `:focus-visible` temático (con Tab real: `auto 1px rgb(229,151,0)`, outline por defecto del navegador), inconsistente con los 7 reproductores ya pulidos en Fases 7.1-7.7 → añadido `outline: 3px solid var(--color-primary); outline-offset: 2px` a `.home-mundo`, `.home-logros`, `.quiz-disponible-item` y `.mision-chip`, y `--color-primary-dark` con offset 3px a `.home-hero` (su fondo ES el degradado `--color-primary`, el anillo se dibuja sobre el blanco de la página); **(P2-4, accesibilidad)** el estado activo de las 4 pestañas de materia y de los 9 chips de filtro de Premios se comunicaba **solo por color** → `aria-pressed` en ambos grupos (solo en el JSX del estudiante; `.opcion` es CSS compartido y NO se tocó); **(P2-5, contraste)** el ✓ de misión completada usaba `#10b981` = **2.28:1 sobre blanco** → `var(--color-success-dark)` #15803d = 5.02:1 (mismo criterio de la Fase 7.4), y su `aria-label` colgaba de un `<span>` sin rol (nombre no expuesto de forma fiable) → `role="img"`; **(P2-6, microcopy)** "🔥 1 días" → singular/plural correcto; **(P2-7, responsive)** el Home del estudiante no tenía **ninguna** media query propia (el del docente sí, a 720px) → bloque `@media (max-width:480px)` con la misma escala que ya usa el docente (avatar 64→52px, h1 1.6→1.3rem, hero padding 22/24→18). **Verificado en local Docker/MySQL (`gamificapp-mysql-dev` :3307, backend :3001 + Vite :5173, `Estudiante Prueba Uno` / PIN 111111):** login OK; Home, Mis Mundos, detalle de materia (4 pestañas), listados y Mis Premios (46 misiones, 18 completadas) navegados; **regresión de flujo completa post-cambio** — Home → hero "¡Seguir jugando!" → materia *Ciencias Sociales* → pestaña Juegos (`aria-pressed` = `[false,false,true,false]`) → abrir *El día de Ana* (reproductor monta) → "← Otros juegos" (vuelve a la lista) → "← Volver a mis mundos" (6 mundos); filtro de Premios: 46 → 5 tarjetas con "🔥 Constancia" y 46 al volver a "Todas"; **foco de teclado real (Tab/Shift+Tab)** medido en `.home-hero` (`:focus-visible`=true, `solid 3px rgb(17,94,89)`, offset 3px), `.home-mundo`, `.home-logros`, `.quiz-disponible-item` y `.mision-chip` (los cuatro `solid 3px rgb(15,118,110)` = `--color-primary`, offset 2px; baseline `auto 1px` naranja del UA); ✓ de misión medido en `rgb(21,128,61)`. **Responsive 320/375/480/768/1024/1280 con `overflow-x = 0` y cero elementos fuera del viewport en todos:** 320 → barra de XP 0→**140px**, racha `right` 324→**222**, título de actividad 45→**146px**, item 161→140px, avatar 52, h1 20.8px, hero 180→141px; 375 → barra 35→**195px**; 480 → barra 152px, la fila de nivel no necesita saltar (26px); 768 → barra 101→**159px** (salta a 2 filas, 58px); **1024 y 1280 idénticos al baseline** (barra 372/628px, fila 26px, avatar 64px) → **cero regresión de escritorio**. **Build** limpio (3.12s); **lint 29 problemas (26 errores + 3 warnings) = baseline exacto**, cero nuevos; **consola sin errores** en todo el recorrido. **DATOS: no se alteró ninguno** — inspección de red del recorrido completo: **solo `GET /api/progreso/1 → 200`, cero `POST /api/progreso`** (no se completó ninguna partida); no se cambió el PIN, no se reseedeó, no se tocó la BD. **Re-verificación de cierre (sesión de commit, HEAD `83d9ad1` + working tree):** revisión semántica del diff completo (5 archivos, +65/-5) — cero lógica de negocio, cero endpoints/payloads, cero hex nuevos (el único hex del diff **desaparece**: `#10b981` → token), cero `!important` nuevos, cero componentes nuevos; **exclusividad reconfirmada por grep de tokens exactos en todo `src/` + `server/`** — `home-avatar`, `home-saludo-meta`, `home-hero`, `home-nivel(-track)`, `home-mundo`, `home-logros`, `quiz-disponible-*`, `mision-chip`, `mision-check` **solo se emiten en `DashboardEstudiante.jsx`/`PanelMisiones.jsx`**; el docente escribe `.home-doc .home-avatar`/`.home-saludo-meta h1` en `admin/dashboard.css` (@720px) pero **ningún JSX de Admin/Docente emite esas clases** (regla huérfana preexistente) y además su especificidad (0,2,0) gana a la del bloque nuevo (0,1,0) **con valores idénticos** → cero riesgo de sangrado a Admin/Docente/reproductores. Build limpio (3.05s); **lint 29 problemas (26 errores + 3 warnings) = baseline exacto**, cero nuevos; **smoke test dirigido** en local Docker/MySQL (`gamificapp-mysql-dev` :3307, backend :3001 + Vite :5173, `Estudiante Prueba Uno` / PIN 111111): login OK; **a 320px** barra de XP **140px** (baseline 0px), racha `right` **222** (baseline 324), avatar 52px, h1 20.8px, hero 141px, `overflow-x = 0`, **cero elementos fuera del viewport**; materia *Ciencias Sociales* → pestaña Juegos `aria-pressed = [false,false,true,false]`, título de actividad **146px** (baseline 45px), item 137px; Mis Premios: 9 chips con `aria-pressed` (`Todas`=true), 46 tarjetas, ✓ medido en `rgb(21,128,61)` con `role="img"` + `aria-label="Completada"`, "Racha 🔥 **1 día**" (singular correcto); **1280 idéntico al baseline** (barra 628px, fila 26px, avatar 64px) → cero regresión de escritorio; consola **sin errores**; **cero `POST /api/progreso`** (no se completó ninguna partida, no se alteró ningún dato). **Fase 6.3 CERRADA — commit en `main` (pusheado).** **Hallazgos DIFERIDOS (documentados, NO implementados):** (a) **P3 CSS muerto en `dashboardEstudiante.css`** — `.home-mundo-2/-4` (los colores vienen de `uiMateria(...).estilo` inline desde SPEC-002), `.logros-grid` + `.logro-card/-icon(-gold/-fire/-accent/-primary)/-badge/-bloqueado` (retirados por SPEC-011 al sustituir los logros de `localStorage` por Misiones; incluyen los 2 únicos `!important` del archivo) y `.rank-item-yo` (el estudiante ya no tiene sección de ranking): ~70 líneas sin consumidor en el JSX, verificado por grep; es limpieza, no pulido → candidata a fase de limpieza; (b) **P3 cambio de PIN por `window.prompt` ×2** (`handleCambiarPin`) — ya diferido en MASTER_PLAN §3 ítem 29 y además toca autenticación (§10, requiere spec); (c) **P2 transversal NO aplicado** — los ítems del sidebar (`SidebarLayout`, `<div role="button">` de MUI) no exponen `aria-current`/`aria-selected`: la sección activa se distingue solo por color, igual que las pestañas, pero el componente lo comparten **los 3 roles** → cambio transversal, requiere autorización y regresión en Admin y Docente; (d) **P2/P3 estados vacíos inconsistentes** — el Home usa el componente `EmptyState` pero las 4 pestañas de materia usan `<p class="vacio-msg">`; `.vacio-msg` es **compartida con 6 reproductores y la Biblioteca del docente** → unificar es transversal; (e) **P2 `.mision-card.mision-bloqueada { opacity: .62 }`** baja el texto descriptivo a ≈2.4:1 efectivo (justo el texto que explica cómo desbloquear la misión) → subir a ~0.75 es un cambio visual del panel de Premios, se deja a decisión; (f) **P3** `.home-mundos-grid` cae a **1 sola columna a 320px** (`minmax(150px,1fr)`) → 6 mundos apilados ≈760px de scroll; a 375px ya son 2 columnas, así que se descartó tocarlo para no mover el layout de 480px; (g) **P3** `GET /api/progreso/:id` se dispara 4-6 veces al entrar al Home (StrictMode + dependencias `pagina`/`refrescar`) — ruido de red, sin impacto funcional; (h) **P3** `style={{ pointerEvents: 'none' }}` en los `h1`/`p` de "Mis mundos" y "Mis premios" (hack heredado sin causa documentada); (i) el hallazgo transversal de calificación **`0/100`** en los overlays sigue **sin tocar**, como pide el alcance de esta fase.)

2026-07-22 (SPEC-018 **Fase 7.7 — Auditoría y pulido de Línea del tiempo** (misma metodología: inventario con búsquedas reales en todo `src/` → clasificar A exclusivo / B compartido / C infra de juegos / D transversal → partida completa con datos reales → solo mejoras EXCLUSIVAS justificadas → regresión → build/lint/consola → parar antes de commit). **Reproductor:** `src/components/juegos/LineaTiempo.jsx` + bloque `.linea-*` de `juegos.css`. **Inventario clasificado por búsqueda real (NO por nombre/archivo):** *(A. exclusivo de Línea del tiempo, verificado por grep — solo en `LineaTiempo.jsx` + `juegos.css`)* — `.juego-linea`, `.linea-titulo-secuencia`, `.linea-lista(+ is-sacudida)`, `.linea-evento(+ is-correcto)`, `.linea-evento-num`, `.linea-evento-texto`, `.linea-evento-etiqueta`, `.linea-evento-check`, `.linea-evento-flechas(+ button)`, `.linea-btn-comprobar`, `.linea-pista`, `@keyframes linea-sacudida`; *(B/C. compartido entre varios juegos, NO tocado)* — `.juego-dnd-instruccion` (Clasificador/Completar/Memorama/V/F), `.progress-*` (no lo usa este reproductor directamente); *(D. infraestructura global, NO tocada)* — `mezclar`, `useRecompensa`, `useReporteIntento`, `PantallaFinal`, `LogroToast`, `DialogoSalida` (`juegosComunes.jsx`); *(evaluación §10 CONGELADA, NO tocada)* — `evaluarOrden` (concordancia de rangos de Kendall, escala pares n(n-1)/2 en `ordenSecuencia.js`). **HALLAZGO EXCLUSIVO IMPLEMENTADO (1 archivo `juegos.css`, +10 líneas, SOLO `.linea-evento-flechas button` y `.linea-btn-comprobar` que no usa ningún otro reproductor; cero cambios de mecánica/evaluación Kendall/XP/progreso/recompensa/backend):** accesibilidad — foco de teclado: las flechas Subir/Bajar y el botón «Comprobar orden» (todos `<button>` nativos, las flechas ya con `aria-label` "Subir/Bajar: {texto}") no tenían `:focus-visible` temático (con foco medido `outline-style: none`), inconsistente con Quiz `.opcion-quiz`, Clasificador `.juego-canasta`, Completar `.completar-opcion`, V/F `.vf-opcion` y Memorama `.memorama-carta` → añadido `.linea-evento-flechas button:focus-visible, .linea-btn-comprobar:focus-visible { outline:3px solid var(--color-primary); outline-offset:2px }`. **Verificado en local Docker/MySQL (seed `El día de Ana`, 4 eventos, Ciencias Sociales; Vite :5173 contra el contenedor `gamificapp_dev`, estudiante `estudiante prueba uno` PIN 111111 con XP ya maximizado):** partida baseline y post-cambio completas; **mecánica intacta** — orden inicial mezclado (≠ correcto); flechas Subir/Bajar reordenan (verificado por árbol de accesibilidad fresco tras cada movimiento; el `get_page_text` del preview con pestaña oculta queda congelado, solo fiables el árbol A11y y las sondas frescas); **límites** primer evento Subir `disabled`, último evento Bajar `disabled`; «Comprobar orden» evalúa una vez y cierra; **evaluación Kendall verificada y CONGELADA** — orden perfecto → **6 de 6 parejas** ("¡Resultado perfecto!"), orden imperfecto `[Almuerza,Duerme,Va,Se despierta]`=índices `[2,3,1,0]` → **1 de 6 parejas** (rango bajo "¡Sigue intentándolo!"), coincide exactamente con el cálculo manual de pares concordantes (fórmula sin tocar); foco de teclado real (Tab) sobre `.linea-evento-flechas button` → `matches(':focus-visible')`=true, outline `solid 3px rgb(15,118,110)`=`--color-primary`, offset 2px (baseline: `outline-style none`); `POST /api/progreso → 201` **exactamente una vez por partida** (2 partidas completas → 2 POST); "Jugar otra vez" reinicia la lista y **rebaraja** (`mezclar` → orden nuevo distinto del anterior y del correcto); **guardia de salida** (`DialogoSalida`) aparece al intentar salir con un evento movido ("Todavía no terminas… lo que llevas en este intento no se guardará") y "¡Seguir jugando!" la cierra; responsive 320/480/768/1024/1280 **overflow-x = 0** (320 → flechas 35×35px táctil > 24px AA, eventos dentro del viewport right 263<320, texto 16px legible, Comprobar 196×41); build limpio (3.44s); lint 28 = baseline HEAD `85d452b` (sin nuevos; el cambio es CSS, ESLint no lo evalúa); consola sin errores. **Regresión:** solo se tocaron dos selectores `:focus-visible` exclusivos → los otros 6 juegos y la evaluación Kendall fuera de alcance por construcción; no se rejugaron los demás. **Re-verificación de cierre (sesión de commit, HEAD `85d452b` + working tree):** build limpio (3.28s); lint **29 problemas = baseline actual** (el cambio es CSS, ESLint no lo evalúa); partida completa nueva en local Docker/MySQL (`gamificapp-mysql-dev`, backend :3001 + Vite :5173, `estudiante prueba uno` PIN 111111): reproductor carga con orden barajado ≠ correcto, flecha Subir reordena (verificado por DOM), límites `disabled` correctos (primer Subir / último Bajar), «Comprobar orden» evalúa una vez → **6 de 6 parejas** ("¡Resultado perfecto!"), overlay `PantallaFinal` presente, `POST /api/progreso → 201` **exactamente una vez**, "Jugar otra vez" rebaraja, foco real con Tab sobre `.linea-evento-flechas button` **y** `.linea-btn-comprobar` → `:focus-visible`=true, outline `solid 3px rgb(15,118,110)`=`--color-primary`, offset 2px; responsive 320/768/1280 **overflow-x = 0** (flecha 35×35px, evento right 263<320, Comprobar 196×41); consola sin errores. **Exclusividad reconfirmada por grep en `src/` + `server/`:** `.linea-evento-flechas` y `.linea-btn-comprobar` solo aparecen en `LineaTiempo.jsx` y `juegos.css`; sin `!important`, sin hex nuevos, solo el token `--color-primary`. **Fase 7.7 CERRADA — commit en `main` (pusheado).** **Hallazgos DIFERIDOS/documentados (NO implementados en 7.7):** (a) **CSS muerto** — `.linea-evento.is-correcto`, `.linea-evento-check` y `.linea-lista.is-sacudida`/`@keyframes linea-sacudida` **no se aplican en el JSX** (restos del refactor de evaluación diferida: durante el intento nada se marca ni se sacude); **reconfirmado en esta sesión leyendo `LineaTiempo.jsx` completo: el JSX nunca aplica `is-correcto`, `linea-evento-check` ni `is-sacudida`. NO se elimina en 7.7** (sería refactor); deuda histórica de limpieza, exclusiva de Línea del tiempo → candidata a limpieza futura; (b) calificación `0/100` en el overlay pese a Kendall correcto — mismo hallazgo transversal de Fases 7.3–7.6, vive en backend + componente compartido `ResultadoCierre` (§10 XP/calificación) + contador rAF congelado en el preview con pestaña oculta, reproducido aquí con XP ya maximizado; requiere spec; (c) `.linea-evento.is-correcto` (si se reactivara) usa fondo `#f0fdf4` hardcodeado + borde `--color-success` — hex de identidad ya documentado, no se toca.)

2026-07-22 (SPEC-018 **Fase 7.6 — Auditoría y pulido del Memorama** (misma metodología: inventario con búsquedas reales en todo `src/` → clasificar exclusivo/compartido/global → partida completa con datos reales → solo mejoras EXCLUSIVAS justificadas → regresión → build/lint/consola → parar antes de commit). **Reproductor:** `src/components/juegos/Memorama.jsx` + `juegos.css`. **Inventario clasificado por búsqueda real (NO por nombre/archivo):** *(A. exclusivo de Memorama, verificado por grep — solo en `Memorama.jsx` + `juegos.css`)* — `.memorama-tablero`, `.memorama-carta(+ is-arriba/is-resuelta)`, `.memorama-carta-interior`, `.memorama-carta-dorso`, `.memorama-carta-frente`; *(C. infraestructura compartida entre varios juegos, NO tocada)* — `.juego-dnd-instruccion/-avance` (Clasificador/Completar/Línea/V/F, en `juegoDragAndDrop.css`), `.progress-track/-fill(-accent)`; *(D. infraestructura global, NO tocada)* — `mezclar`, `useRecompensa`, `useReporteIntento`, `PantallaFinal`, `LogroToast` (`juegosComunes.jsx`); *(lógica de evaluación §10, NO tocada)* — `evaluarMemorama`/`calificacionMemorama.js`. **HALLAZGO EXCLUSIVO IMPLEMENTADO (1 archivo `juegos.css`, +9 líneas, SOLO `.memorama-carta` que no usa ningún otro reproductor; cero cambios de mecánica/evaluación/XP/progreso/recompensa/backend):** accesibilidad — foco de teclado: las cartas (`<button>` nativos) no tenían `:focus-visible` temático (con Tab real mostraban el anillo por defecto del navegador — `outline auto 1px rgb(229,151,0)`, UA), inconsistente con Quiz `.opcion-quiz`, Clasificador `.juego-canasta`, Completar `.completar-opcion` y V/F `.vf-opcion` → añadido `.memorama-carta:focus-visible { outline:3px solid var(--color-primary); outline-offset:2px }`. **Verificado en local Docker/MySQL (seed `Memorama de animales`, 3 parejas animal↔cría, Matemáticas; backend :3001 + Vite :5173 contra el contenedor `gamificapp_dev`, estudiante `Estudiante Prueba Uno` PIN 111111 con XP ya maximizado):** partida baseline y post-cambio completas; **mecánica intacta** — voltear una carta la deja boca arriba (`is-arriba`), pareja correcta queda revelada+resuelta (`is-resuelta`, fondo verde-claro + texto oscuro `rgb(15,23,42)` sobre `rgb(220,252,231)` = contraste amplio), pareja incorrecta se revela y a los 900 ms se voltea de nuevo con bloqueo (`setBloqueado`) sin marcar acierto (verificado por clase real en DOM: tras el fallo `arriba=[]`, avance sigue 0/3 y `fallos+1`); contador `n/3 parejas` + detalle "N intentos fallidos" correctos; foco de teclado real (Tab) sobre `.memorama-carta` → `matches(':focus-visible')`=true, outline `solid 3px rgb(15,118,110)`=`--color-primary`, offset 2px (antes del fix: `auto 1px` naranja UA); cartas son `<button type="button">` nativos (activación por Enter/Espacio es nativa; los eventos de teclado sintéticos del navegador CDP no disparan la activación — limitación del entorno, no de la app); `POST /api/progreso → 201` **exactamente una vez por partida** (2 partidas completas → 2 POST); "Jugar otra vez" reinicia a 0/3 y rebaraja (`mezclar`); guardia de salida (`DialogoSalida`) aparece con progreso parcial ("lo que llevas en este intento no se guardará") y "¡Seguir jugando!" la cierra; responsive 320/480/768/1024/1280 **overflow-x = 0** (320 → 2 col 99×84px táctil amplio; 480 → 3 col 117×84; 768 → 2 col 158×96; 1024 → 4 col 141×96; ninguna carta fuera del viewport); build limpio (4.39s); lint 28 = baseline HEAD `f818ade` (sin nuevos); consola sin errores. **Regresión:** solo se tocó `.memorama-carta:focus-visible` (clase exclusiva) → los otros 6 juegos fuera de alcance por construcción; no se rejugaron. **Commit `85d452b` en main** (pusheado). **Hallazgos transversales/globales DIFERIDOS (documentados, NO implementados en 7.6):** (a) calificación `0/100` en el overlay pese a 3/3 parejas — mismo hallazgo transversal de Fases 7.3/7.4/7.5, vive en backend + componente compartido `ResultadoCierre` (§10 XP/calificación) + contador rAF congelado en el preview con pestaña oculta, reproducido aquí sobre BD compartida con XP ya maximizado; requiere spec; (b) `.memorama-carta-dorso` usa fallbacks hex `var(--color-primary-soft, #e0f2fe)`/`var(--color-primary, #0284c7)` cuyos valores difieren del token real, pero los tokens existen → el fallback nunca se aplica (no es bug); (c) `.memorama-carta.is-resuelta` usa `--color-success` #16a34a solo como **borde decorativo** (el texto conserva `--color-text` oscuro con contraste amplio; el estado "resuelta" no depende solo del color: la carta queda boca arriba mostrando su texto y `disabled`) → sin fallo AA real; `--color-success` global sigue como candidato Fase 8.)

2026-07-22 (SPEC-018 **Fase 7.5 — Auditoría y pulido de Verdadero o Falso** (misma metodología: inventario con búsquedas reales → clasificar exclusivo/compartido/global → partida completa con datos reales → solo mejoras EXCLUSIVAS justificadas → regresión → build/lint/consola → parar antes de commit). **Reproductor:** `src/components/juegos/VerdaderoFalso.jsx` + `verdaderoFalso.css`. **Inventario clasificado por búsqueda real en todo `src/` (NO por nombre/archivo):** *(A. exclusivo de V/F)* — `.juego-vf`, `.vf-tarjeta`, `.vf-afirmacion`, `.vf-opciones`, `.vf-opcion(+ is-elegida/-atenuada)`, `.vf-revision-texto`, `.vf-revision-respuesta(+ is-correcta/-incorrecta)`, `.vf-revision-explicacion`; *(B. compartido con Completar, NO tocado)* — `.completar-feedback`, `.completar-btn-siguiente`, `.completar-revision`, `.completar-revision-item`, `.completar-revision-icono` (V/F los reutiliza con contenido `vf-*` propio); *(C. compartido entre varios juegos, NO tocado)* — `.juego-dnd-instruccion/-avance`, `.progress-track/-fill(-accent)` (Clasificador/Completar/Memorama/Línea/V/F); *(D. infraestructura global, NO tocada)* — `mezclar`, `useRecompensa`, `useReporteIntento`, `PantallaFinal`, `LogroToast` (`juegosComunes.jsx`). **HALLAZGO EXCLUSIVO IMPLEMENTADO (1 archivo `verdaderoFalso.css`, +8 líneas, SOLO `.vf-opcion` que no usa ningún otro reproductor; cero cambios de mecánica/evaluación/XP/progreso/recompensa/backend):** accesibilidad — foco de teclado: las opciones (`<button>` nativos) no tenían `:focus-visible` temático (outline-style computado = `none`), inconsistente con Quiz `.opcion-quiz`, Clasificador `.juego-canasta` y Completar `.completar-opcion` → añadido `.vf-opcion:focus-visible { outline:3px solid var(--color-primary); outline-offset:2px }`. **Verificado en local Docker/MySQL (seed `Verdadero o falso: la naturaleza`, 3 afirmaciones, Ciencias Naturales; backend propio en :3002 + Vite :5174 contra el mismo contenedor, porque otro chat ocupaba :3001/:5173):** partida baseline y post-cambio completas (1 incorrecta + 2 correctas cada una); **corrección diferida intacta** — elegir solo marca `vf-opcion is-elegida` (ámbar) + la otra `is-atenuada`, NUNCA `is-correcto/-incorrecto` ni verde/rojo durante el intento (verificado por clase real en DOM); verde/rojo (`is-correcta`=success-dark, `is-incorrecta`=danger-dark) solo en la revisión final; foco de teclado real (Tab) sobre `.vf-opcion` → `matches(':focus-visible')`=true, outline 3px `rgb(15,118,110)`=`--color-primary`, offset 2px; opciones son `<button>` nativos (Enter selecciona, confirmado por `is-elegida` en DOM tras keydown real); `POST /api/progreso → 201` **exactamente una vez por partida** (2 partidas → 2 POST); "Jugar otra vez" reinicia a 0/3 (rebaraja por `mezclar`); guardia de salida activa desde la primera respuesta; responsive 320/768/1280 **overflow-x = 0** (opción 82px alto/110px ancho a 320 → táctil amplio; afirmación 17.6px por media query @480); build limpio (4.17s); lint 28 = baseline HEAD (sin nuevos); consola sin errores. **Regresión de Completar:** NO se tocó ninguna clase compartida (`.vf-opcion` es exclusiva, verificado por grep: solo en `VerdaderoFalso.jsx` + `verdaderoFalso.css`) → Completar fuera de alcance por construcción. **Commit `f818ade` en main** (pusheado). **Hallazgos transversales/globales DIFERIDOS (documentados, NO implementados en 7.5):** (a) calificación `0/100` en el overlay pese a 2/3 correctas — hallazgo transversal ya registrado en Fases 7.3/7.4, vive en backend + componente compartido `ResultadoCierre` (§10 XP/calificación), reproducido aquí sobre BD compartida con XP ya maximizado; requiere spec; (b) `.completar-btn-siguiente` (compartido con Completar) tampoco tiene `:focus-visible` temático → mejora transversal Completar+V/F, requiere autorización explícita y ampliar regresión a Completar; (c) icono ✕ de la revisión usa `#ef4444` hardcodeado en el JSX (`aria-hidden`, decorativo, acompañado de texto `is-incorrecta` que ya cumple AA) tanto en V/F como en Completar → no es fallo de accesibilidad real y es compartido; consistencia transversal, no exclusiva; (d) icono ✓ de la revisión usa `var(--color-success)` inline (`aria-hidden`, decorativo) → mismo caso; (e) `--color-success` #16a34a global sigue como candidato Fase 8. **PROPUESTA TRANSVERSAL sugerida (no implementada):** unificar el `:focus-visible` temático de `.completar-btn-siguiente` (afecta a Completar y V/F por igual) en una sola fase transversal aprobada.)

2026-07-22 (SPEC-018 **Fase 7.4 — Auditoría y pulido de Completar Espacios** (misma metodología: inventario con búsquedas reales → clasificar exclusivo/compartido/global → partida completa con datos reales → solo mejoras EXCLUSIVAS justificadas → regresión → build/lint/consola → parar antes de commit). **Reproductor:** `src/components/juegos/CompletarEspacios.jsx` + `juegos.css`. **Inventario clasificado por búsqueda real (NO por nombre/archivo):** *(compartido con Verdadero/Falso, NO tocado)* — `.completar-feedback`, `.completar-btn-siguiente`, `.completar-revision`, `.completar-revision-item`, `.completar-revision-icono` (V/F los reutiliza con su propio contenido `vf-*`); *(infraestructura global compartida por varios juegos, NO tocada)* — `.juego-dnd-instruccion/-avance`, `.progress-track/-fill(-accent)`, `mezclar`, `useRecompensa`, `useReporteIntento`, `PantallaFinal`, `LogroToast`; *(exclusivo de Completar)* — `.juego-completar`, `.completar-tarjeta`, `.completar-frase`, `.completar-hueco(+ is-correcto/-incorrecto/-elegido)`, `.completar-opciones`, `.completar-opcion(+ opcion-correcta/-incorrecta/-atenuada/-elegida)`, `.completar-revision-correcta`. **HALLAZGOS y CAMBIOS (1 archivo `juegos.css`, SOLO clases exclusivas de Completar, cero cambios de mecánica/evaluación/XP/progreso/recompensa/backend):** (1) **accesibilidad — foco de teclado:** las opciones (`<button>` nativos) no tenían `:focus-visible` temático (solo el outline gris del navegador), inconsistente con Quiz `.opcion-quiz` y Clasificador `.juego-canasta` → añadido `.completar-opcion:focus-visible { outline:3px var(--color-primary); offset:2px }` (exclusivo: `.completar-opcion` no lo usa ningún otro reproductor). (2) **accesibilidad — contraste:** la palabra acertada en la revisión (`.completar-hueco.is-correcto`) usaba `--color-success` #16a34a = **3.30:1 sobre blanco (falla AA)** en texto negrita 15.2px → cambiado a `--color-success-dark` #15803d = **5.02:1 (pasa AA)** en color y borde; fix local exclusivo, mismo criterio que Fase 7.1 aplicó al Quiz (el `is-incorrecto` ya usaba `--color-danger` #dc2626 = 4.77:1). **Verificado en local Docker/MySQL (seed `Completa la frase`, 2 frases, Lengua y Literatura):** partida baseline y post-cambio completas (1 incorrecta + 1 correcta); **corrección diferida intacta** — elegir solo marca `opcion-elegida` (ámbar #d97706) + `opcion-atenuada`, NUNCA `opcion-correcta/-incorrecta` durante el intento (verificado por clase real); verde/rojo solo en la revisión (is-incorrecto #dc2626 + tachado, is-correcto ahora #15803d); `:focus-visible` renderiza outline `--color-primary` #0f766e con foco real de teclado (Tab), `matches(':focus-visible')`=true; opciones son `<button>` nativos con foco por Tab; `POST /api/progreso → 201` **exactamente una vez por partida** (2 partidas → 2 POST); "Jugar otra vez" reinicia a 0/2; guardia de salida aparece con progreso ("no se guardará"); responsive 320/768/800 **overflow-x = 0** (opción 41px alto > 24px AA, frase 16.32px por media query @480); build limpio (4.14s); lint 28 = baseline HEAD (sin nuevos); consola sin errores. **Fase 7.4 CERRADA — commit `07ef88f` en `main` (pusheado).** *(Corrección documental 2026-07-28: esta entrada decía «Sin commit / sin push»; Git demuestra que el trabajo SÍ se commiteó el 2026-07-22 y la nota quedó desfasada.)* **Hallazgos transversales/globales DIFERIDOS (no tocados):** (a) calificación `0/100` pese a 1/2 en reintento con XP ya maximizado — mismo hallazgo transversal de Fase 7.3, vive en backend + componente compartido §10, requiere spec; (b) icono ✕ de la revisión usa `#ef4444` hardcodeado en el JSX (aria-hidden, no es texto) tanto en Completar como en V/F → consistencia transversal, no exclusivo; (c) `.completar-btn-siguiente` (compartido con V/F) tampoco tiene `:focus-visible` temático → transversal, requiere ampliar regresión a V/F; (d) `--color-success` #16a34a global sigue fallando AA en fondos (candidato Fase 8, ya documentado).)

2026-07-22 (SPEC-018 **Fase 7.3 — Auditoría y pulido de Misión Narrativa** (misma metodología rigurosa que Quiz/Clasificador: inventariar → clasificar exclusivo/compartido con búsquedas reales → jugar partida completa con datos reales → solo mejoras EXCLUSIVAS justificadas → regresión → build/lint/consola → parar antes de commit). **Reproductor:** `src/components/mision/MisionNarrativa.jsx` + `misionNarrativa.css`. **Infraestructura clasificada por búsqueda real (NO por nombre/archivo):** *(compartido, NO tocado)* — clases del patrón de opción `.opcion-quiz/-letra/-texto/-correcta/-incorrecta/-atenuada`, `.pregunta-justificacion(+ p)`, `.justificacion-icono` viven en `dashboard.css` (globales, congeladas por Fase 7.A; Misión las reutiliza deliberadamente); hooks/componentes JS `useRecompensa`, `useReporteIntento`, `ResultadoCierre`/`LogroToast` (todos §-restringidos/compartidos). *(exclusivo del reproductor)* — `.mision-rpg/-escena/-portada(-icono)/-icono-oro/-texto/-narrativa/-pregunta/-opciones/-paso/-pista/-exito/-btn(-secundario)/-final-acciones`. **HALLAZGO PRINCIPAL — colisión de clase CSS (bug real, roto en TODOS los anchos):** el indicador de capítulos usaba `.mision-progreso`, nombre que **colisiona** con `.mision-progreso` de `src/pages/estudiante/misionesPanel.css:113` (`flex-direction:column; gap:.25rem`), cuya regla ganaba la cascada → los 3 pasos se apilaban en **vertical** (alto 52px, pasos tops 408/420/432 en desktop 1280 y 653/665/677 en 320) en lugar de la fila horizontal prevista. Única colisión `.mision-*` entre reproductor y panel (verificado por búsqueda: `.mision-progreso-label` del reproductor vs `.mision-progreso-txt` del panel NO colisionan; `.mision-paso` es exclusivo). **CAMBIO (2 archivos SOLO del reproductor, cero cambios de mecánica/evaluación/XP/progreso/recompensa/backend):** renombrada la clase en colisión `.mision-progreso`→`.mision-capitulos` y su hijo `-label`→`.mision-capitulos-label` en `MisionNarrativa.jsx` + `misionNarrativa.css`. Elimina la colisión SIN tocar `misionesPanel.css`/`PanelMisiones.jsx` (el panel conserva su `.mision-progreso` intacto; búsqueda post-cambio confirma separación total: `-progreso*` solo en panel, `-capitulos*` solo en reproductor). Restaura la fila horizontal (flex-direction row, gap 8px, pasos alineados, alto 16px en 1280 / 32px en 320, label a la derecha con margin-left:auto). **Verificado en local Docker/MySQL (seed `La expedición de las palabras`, 3 desafíos):** partida completa antes y después; regresión con respuesta incorrecta (pista amable + reintento + capítulo deja de puntuar), aciertos 2/3, 3 capítulos, overlay `ResultadoCierre` (calificación + retroalimentación 💪 de rango medio + XP "ya conseguido"), escena final narrativa detrás; `POST /api/progreso → 201` exactamente **una vez por partida**; responsive 320/1280 sin overflow horizontal; opciones son `<button>` reales (Enter/Espacio nativos, 54px táctil); contraste pista slate-500 `#64748b` sobre `#f8fafc` medido **4.55:1 (pasa AA)**; build limpio (4.29s); lint 28 (≤ baseline 29, sin nuevos); consola sin errores. **Fase 7.3 CERRADA — commit `25ae1c8` en `main` (pusheado).** *(Corrección documental 2026-07-28: esta entrada decía «Sin commit / sin push»; Git demuestra que el trabajo SÍ se commiteó el 2026-07-22 y la nota quedó desfasada.)* **Hallazgos transversales DIFERIDOS (no tocados):** (a) la calificación mostrada en `ResultadoCierre` salió `0/100` pese a 2/3 al primer intento en un reintento con XP ya maximizado — vive en backend + componente compartido §10 (XP/calificación), común a todos los juegos, requiere spec; (b) el nombre `.mision-progreso` sigue siendo genérico en el panel (deuda de nomenclatura, sin impacto tras el rename del reproductor).)

2026-07-20 (SPEC-018 **Fase 7.2 — Auditoría y pulido del Clasificador** (misma metodología que Quiz: auditar → clasificar compartido/exclusivo → solo mejoras justificadas → jugar → build/lint/consola → parar antes de commit). **Auditoría de infraestructura:** aunque vivan en `juegoDragAndDrop.css`, las clases `.juego-dnd-instruccion/-avance/-btn(-ghost)/-final*/-estrellas/-final-trofeo` y `.progress-track/-fill/-accent` son **compartidas** (las reutilizan Completar/Memorama/VF/Línea vía `PantallaFinal`) → NO tocadas; hooks `useRecompensa`/`useReporteIntento`/`PantallaFinal`/`LogroToast` y `COLORES_CATEGORIA` (compartido con el editor) tampoco. **Exclusivo del Clasificador** (`.juego-canasta*`, `.juego-ficha*`, `.juego-dnd-canastas`). **Cambios (2 archivos del Clasificador, cero cambios de mecánica/evaluación/XP):** (1) **accesibilidad de teclado** — las canastas (zonas de drop) eran `<div>` sin `role`/`tabindex`/teclado, así que el flujo inclusivo "tocar ficha → tocar canasta" fallaba con teclado (WCAG 2.1.1); ahora `role="button"` + `tabIndex=0` + `aria-label` + `onKeyDown` (Enter/Espacio replican EXACTAMENTE el `onClick`) + `.juego-canasta:focus-visible`; (2) grid `minmax(220px,1fr)` → `minmax(min(100%,220px),1fr)` para no forzar overflow en contenedores <220px. **Verificado en local Docker/MySQL:** partida completa por TECLADO (seleccionar ficha + Enter en canasta clasifica; 4/4 al primer intento, confeti, overlay, `POST /api/progreso` 201) y por ratón/tap (error→rebote, 3/4, 201); responsive 320 (sin overflow, canastas apiladas, fichas 53px táctiles) y 1280; build limpio (3.14s); lint 29 = baseline; consola sin errores. **Fase 7.2 CERRADA — commit `844ede7` en `main` (pusheado).** *(Corrección documental 2026-07-28: esta entrada decía «Sin commit»; Git demuestra que el trabajo SÍ se commiteó el 2026-07-22.)* **Anotado, no tocado:** violet/rose de canastas son hex de identidad (sin token, decisión Fase 2); "Suelta aquí" usa `--color-text-muted`.)

2026-07-20 (SPEC-018 **Fase 7.1 — Pulido EXCLUSIVO del reproductor del Quiz** (tras 7.A). Solo clases Quiz-only (Misión Narrativa NO usa ninguna → intacta por construcción); **solo presentación, 1 archivo (`dashboard.css`), cero cambios de comportamiento**: (1) `.pregunta-card` gana `box-shadow: var(--shadow-sm)` — el resto de cards del sistema ya la usan, las del Quiz eran las únicas planas; (2) media query `@media (max-width:480px)` Quiz-only (`.quiz-interactivo` gap 18→14/margin-top 22→16, `.pregunta-card` padding 20→16, `.pregunta-enunciado` margin-bottom 16→12) para más ancho útil en dispositivos pequeños de la escuela; (3) `.opcion-correcta-no-elegida .opcion-letra` texto `--color-success` → `--color-success-dark` (texto normal 13.6px/700; contraste WCAG medido 3.00→**4.57:1, pasa AA**; `--color-success-dark` es el verde más oscuro del Design System). Verificado: sondas de elementos frescos a 375 (padding 16, sombra, gap 14, letra success-dark) y 1280 (padding 20/gap 18, media query no aplica); partida completa del Quiz (2/2 → `opcion-correcta`, overlay con 3 acciones, justificaciones, `POST /api/progreso` 201); build limpio (3.15s); lint 29 = baseline; consola sin errores. **Anotado (no tocado):** `.quiz-puntaje` en `quizInteractivo.css` es código muerto (no se usa en JSX desde que el marcador migró a `ResultadoOverlay`) — candidato de limpieza, no de pulido. **Fase 7.1 CERRADA — commit `ac8d6b6` en `main` (pusheado, 2026-07-21; el mismo commit incluye la Fase 7.A).** *(Corrección documental 2026-07-28: esta entrada decía «Sin commit».)*)

2026-07-20 (SPEC-018 **Fase 7.A — Consolidación de infraestructura compartida de reproductores** (subfase previa a 7.1, aprobada por Fabrizio). Se implementaron los 3 hallazgos auditados como **infraestructura común** (afectan a Quiz Y Misión Narrativa, que reutilizan el patrón de opción), **solo presentación, 1 archivo (`dashboard.css`), sin mover CSS, sin tocar componentes/`LogroToast`/`useRecompensa`/`ResultadoOverlay`, cero cambios de comportamiento**: (H1) `.opcion-texto` → `min-width:0; overflow-wrap:anywhere` (fix ~51px→0 de desbordamiento con opción larga + chip a 320px); (H2) rojo "incorrecto" `#ef4444` (blanco = 3.74:1, falla AA) → `var(--color-danger)` #dc2626 (4.77:1, pasa AA; unifica con el token dominante y Completar) en borde/letra/ícono/chip; (H3) `:focus-visible` temático (3px `--color-primary` + offset) en `.opcion-quiz`. **Verificado jugando ambos juegos completos en local Docker/MySQL:** Quiz (1 incorrecta + 1 correcta → `opcion-incorrecta` roja, `opcion-correcta-no-elegida` verde hueco, overlay con sus 3 acciones, justificaciones, `POST /api/progreso` 201) y Misión Narrativa (respuesta incorrecta → `opcion-incorrecta` roja con pista+reintento intactos, 3 capítulos, overlay con SUS acciones "Jugar de nuevo/Ver el final/Otras misiones", retroalimentación 💪 de rango medio 2/3, `POST` 201). Sondas de elementos frescos confirmaron los 3 estilos (el preview congela rAF/paint con pestaña oculta → contador de nota a 0 y estilos obsoletos en elementos mutados; solo fiables los recién creados). Build limpio (4.33s); lint 29 = baseline; consola sin errores. **Fase 7.A CERRADA — commit `ac8d6b6` en `main` (pusheado, 2026-07-21; el mismo commit incluye la Fase 7.1).** *(Corrección documental 2026-07-28: esta entrada decía «Sin commit».)* Pendiente 7.1: pulido EXCLUSIVO del Quiz (`.pregunta-card`, `.opcion-tu-respuesta`, `.pregunta-num`, layout apilado) que no afecta a otros juegos.)

2026-07-20 (SPEC-018 Fase 7.1 — auditoría del **reproductor del Quiz** hecha; **implementación EN PAUSA para revisión**, working tree revertido a HEAD, cero cambios de código aplicados). Se auditó jugando en local Docker/MySQL como estudiante real + sondas de elementos frescos (el preview trata la pestaña como `hidden`: congela rAF/paint —screenshots y contador de nota a 0— y devuelve estilos computados obsoletos en elementos mutados por interacción; los elementos recién creados sí reflejan el CSS real, por eso se usaron para verificar). **Hallazgo de proceso clave:** las clases del reproductor (`.opcion-quiz`, `.opcion-incorrecta`, `.opcion-texto`, `.opcion-letra`, `.opcion-atenuada`, `.opcion-correcta`, `.pregunta-justificacion`) **NO son exclusivas del Quiz: `MisionNarrativa.jsx` las reutiliza deliberadamente** (lo dice su propio CSS). Por tanto cualquier retoque en ellas es **transversal Quiz + Misión Narrativa**, no "solo Quiz". (Completar Espacios NO se ve afectado: usa markup `.completar-opcion` con overrides propios de mayor especificidad.) Siguiendo la directiva de Fabrizio (no convertir mejoras en cambios transversales sin revisión), los 3 arreglos detectados quedan **documentados y pendientes de aprobación** antes de tocar código:
- **H1 (bug responsive, transversal):** `.opcion-texto { flex:1 }` sin `min-width:0` → una opción larga junto al chip "Tu respuesta" (`flex-shrink:0`) desborda ~51px dentro del botón a 320px (verificado 51→0 con `min-width:0` + `overflow-wrap:anywhere`). Fix sin regresión, beneficia también a Misión.
- **H2 (accesibilidad + consistencia, transversal):** rojo "incorrecto" hardcodeado `#ef4444` → blanco encima = **3.74:1, falla WCAG AA**; `var(--color-danger)` #dc2626 da 4.77:1 (pasa) y unifica con el token dominante y con Completar. Cambia visiblemente el color de error **en Quiz y en Misión Narrativa** → requiere decisión de diseño.
- **H3 (accesibilidad, transversal):** opciones sin `:focus-visible` temático (usan el outline gris del navegador); añadir uno con `--color-primary` afecta también a Misión.
- **Fuera de alcance / candidatos globales (Fase 8):** blanco sobre `--color-success` #16a34a = 3.21:1 (falla AA) — token global de los 6 juegos/19 usos; plural "1 quizzes" en la lista de selección (`DashboardEstudiante`, no el reproductor); no hay indicador "pregunta X de Y" (apilado a propósito para niños 6-9). Overlays/tarjeta de resultado (`ResultadoActividad.jsx`) son COMPARTIDOS por los 6 juegos.
- **Recomendación de scoping:** si se quiere pulir SOLO el Quiz sin tocar Misión, envolver el reproductor con una clase contenedora y prefijar las reglas nuevas; decisión pendiente de Fabrizio.)

2026-07-20 (SPEC-018 Fase 6.2 completada: pulido visual del panel Docente — auditoría completa en local de todos los módulos y flujos del docente (Inicio, Materias con sus 5 pestañas, los 7 generadores/editores, flujo completo de creación/edición de actividad con Vista previa y Deshacer, Biblioteca con filtros y modal de Estadísticas, Mis Estudiantes + Ficha, Ranking, Misiones, Mi Perfil, modales). Panel ya muy pulido tras Fases 1-5; ÚNICO hallazgo corregido: filtros del modal "Añadir del banco de preguntas" (`SelectorBanco.jsx`) con buscador + selects nativos del navegador → 1 solo archivo (`selectorBanco.css`, +11 líneas) replicando el patrón `.bib-filtros` con tokens del Design System; cubre también los `banco-filtros` de BancoPreguntas.jsx. Sin cambios funcionales. Baseline de lint corregido a 29 (verificado que HEAD ya daba 29, no regresión). Candidato pendiente: formalizar `--color-text-soft`/`--color-text-muted` como tokens globales al cierre de Fase 6.)

2026-07-20 (SPEC-018 Fase 6.1 completada: pulido visual del panel Administrador — 13 módulos administrativos auditados en local; botones sin estilo (clases `btn-primario`/`btn-secundario` inexistentes en ModuloIA y ModuloJuegos) corregidos reutilizando `preview-action`/`preview-action-primary` del sistema visual existente; coherencia de H1 del Inicio (1.9rem), radios (`--radius-sm`), neutros → tokens y chip de estado de materias (0.75rem, mismo cuerpo que curso-estado/rol-chip); responsive validado. Diferidos al cierre global de Fase 6: tokens `--color-text-soft`/`--color-text-muted` globales, cabeceras compartidas y consolidación de breakpoints 560/720/900.)

2026-07-20 (SPEC-018 Fase 5 completada: sidebar responsive compartido por los 3 roles — en móvil barra superior compacta + menú desplegable accesible (aria-expanded/Escape/foco/clic fuera/cierre al navegar/scroll-lock); el contenido móvil empieza en y≈63px en vez de ≈460px; jerarquía de z-index validada (menú 30 < modales 50 < overlays 55/60 < toast 70); breakpoints auditados y validado en Docker/MySQL a 320/375/480/768/1024/1280. Pulido visual de módulos diferido a Fase 6.)

2026-07-20 (SPEC-018 Fase 4 completada: `ConfirmDialog` reutilizable + sistema `Toast` global; 12 `window.confirm` destructivos/administrativos y 3 `window.alert` migrados con equivalencia funcional verificada por red en local; feedback educativo e inline conservados; confirms de editores y prompts de PIN diferidos con registro en MASTER_PLAN §3.)

2026-07-20 (SPEC-018 Fase 3 completada: `ModalPanel` endurecido — foco inicial, focus trap, Escape, restauración de foco, scroll-lock, ARIA y `--z-modal` — cubriendo sus 20 instancias en 16 consumidores con contrato retrocompatible; verificado con pruebas reales de teclado/foco en los 3 roles y regresión de overlays de juegos.)

2026-07-20 (SPEC-018 Fases 1 y 2 completadas: tokens semánticos/z-index/breakpoints en `src/index.css` + migración de ~60 hex semánticos a tokens en 15 archivos, validada en entorno local Docker/MySQL con los 7 juegos y los 3 paneles; cero regresiones funcionales.)

2026-07-20 (Entorno local de desarrollo Docker+MySQL `gamificapp_dev` validado y versionado — los 3 roles y los 7 juegos jugables/comprobables en local con `seedDev.js`. SPEC-018 (pulido integral UI/UX) redactada y aprobada como base oficial tras auditoría runtime.)

2026-07-19 (SPEC-017 completa en codigo: registro central, migracion 014, Gestion de juegos y septimo juego Verdadero o Falso como prueba de extensibilidad. Pendiente de validacion en produccion)

# Responsable

Fabrizio Zurita (Extun)

## Estado por módulo

| Módulo | Estado |
|--------|--------|
| Autenticación (login nombre+PIN, registro, emergencia, rate limiting) | ✅ |
| Gestión de docentes (admin) | ✅ |
| Gestión de estudiantes (alta manual + carga masiva Excel + activación por código) | ✅ SPEC-014 completa, e2e confirmado en producción |
| Invitaciones de estudiante | ⚪ Legacy — códigos existentes siguen funcionando; ya no se generan nuevos desde la UI |
| Asignación de cursos a docentes (SPEC-009) | ✅ |
| Material de estudio (base64 en MySQL, preview PDF/docx) | ✅ |
| Quiz / Clasificador / Misión Narrativa | ✅ (crear con/sin IA y jugar) |
| Memorama / Línea del tiempo / Completar espacios | ✅ (100% IA, SPEC-006) |
| Verdadero o Falso | 🟡 SPEC-017 Fase 7 en código — prueba de la arquitectura extensible |
| Actividad sorpresa / Adaptar con IA / Biblioteca IA (papelera, estadísticas) | ✅ |
| XP / niveles / ranking | ✅ (transaccional, idempotente) |
| Misiones (reemplaza logros de `localStorage`) | ✅ Fases 1 y 2 (SPEC-007), e2e confirmado en producción |
| Dashboards de los 3 roles con datos 100% reales | ✅ |
| Libro de Calificaciones | ✅ (muestra calificación /100 — mejor resultado) |
| Calificación académica /100 + retroalimentación por rangos (los 6 juegos) | ✅ en código — revisión visual e2e en producción tras deploy |
| Sistema de resultados (reintentar, guardia de salida, corrección diferida, XP confirmado por backend) — los 6 juegos | ✅ en código — validación e2e en producción tras deploy |
| Sistema RESET (SPEC-008) | ✅ (activo, protegido por `RESET_HABILITADO`) |
| Permisos / Auditoría / Papelera (SPEC-003) | ✅ |
| Asistente IA | ⚪ Retirado del menú — endpoint sigue existiendo sin entrada en la UI |
| Navegación del estudiante (shell con rutas anidadas) | 🔴 Épica 1 / SPEC-001 — nada implementado, ver `MASTER_PLAN.md` |
| Proveedores de IA conmutables (Gemini/OpenAI) | 🟢 SPEC-016 desplegada (2026-07-19). **Gemini validado en producción y en uso.** **OpenAI implementado y verificado estructuralmente, SIN validación funcional: no hay `OPENAI_API_KEY` disponible** — no presentarlo como probado contra su API real (ver `SPEC-016 §16`). Incluye sanitización de logs (`lib/ia/errores.js`) y validación previa al guardado |
| Gestión de tipos de juego por el administrador | 🟢 SPEC-017 — registro central (backend + frontend), 3 estados, migración 014, `ModuloJuegos`. **Validado en entorno local (2026-07-20): cambiar activo/solo_jugar/deshabilitado altera la visibilidad del estudiante sin borrar retos/progreso/XP/calificaciones.** Pendiente de validación en producción |
| Extensibilidad: añadir un juego nuevo | 🟢 **Demostrada**: Verdadero o Falso se incorporó con 4 archivos propios + 4 líneas en los 2 índices de registro, **0 archivos centrales con lógica del tipo y 0 modificaciones a los 6 juegos existentes**. Guía y evidencia en `docs/COMO-AGREGAR-UN-JUEGO.md` |
| Verdadero o Falso (7º juego) | 🟢 **Jugado a partida completa en entorno local (2026-07-20)** — reproductor, corrección diferida (selección no revela hasta finalizar), overlay, IA/banco/editor. Pendiente de validación en producción |
| Entorno local de desarrollo/QA (Docker + MySQL `gamificapp_dev`) | 🟢 **Validado y versionado (2026-07-20, commit `local dbxd`).** `docker-compose.dev.yml` + `server/scripts/seedDev.js` (triple barrera de seguridad) + `docs/DEV-ENTORNO-LOCAL.md`. Los 3 roles y los 7 juegos jugables/comprobables en local. Hallazgo abierto: `initDb.js` no inicializa una BD vacía desde cero (deuda técnica separada, `DEV-ENTORNO-LOCAL.md §8-bis`) |
| Instalador local para Windows (Fase Local 1) | 🟢 **CERRADA (2026-07-29, commiteada).** «Instalación local automatizada para Windows con Node compatible y MySQL 8 previamente disponibles» — **no es portable todavía**. `Instalar/Iniciar/Detener GamificApp.cmd` + `instalador/` (PowerShell + 2 scripts Node de BD) + `.gitattributes` que fija CRLF. Idempotente: conserva `server/.env`, `JWT_SECRET` y `ADMIN_PASSWORD` entre ejecuciones. Datos demo **opcionales** (predeterminado NO, probado interactivamente `n`/Enter/`s`) vía `seedDev.js` sin tocar sus barreras. Health checks reales que esperan a que el esquema esté completo, `--strictPort` en 5173, parada solo por PID propio. **Pendientes explícitos: (1) prueba obligatoria en un equipo/usuario Windows limpio** — aquí MySQL es el contenedor en :3307, no un MySQL nativo en :3306, así que esa rama de detección y los mensajes de «instala MySQL» no se han ejercitado sin MySQL; **(2) Fase Local 2 (Node portable + MySQL portable), no iniciada** |
| Pulido integral UI/UX (SPEC-018) | 🟢 **COMPLETA — las 8 fases cerradas y pusheadas (2026-07-29).** F1: tokens semánticos + z-index + breakpoints. F2: ~60 hex semánticos → tokens. F3: `ModalPanel` accesible (20 instancias/16 consumidores). F4: `ConfirmDialog` + `Toast` global (12 confirms + 3 alerts migrados 1:1; diferidos en MASTER_PLAN §3.28-29). F5: sidebar responsive compartido (barra + menú desplegable accesible en ≤760px, contenido móvil en y≈63 en vez de ≈460, z-index validado, 320-1280 probados). F6.1: pulido visual del panel Administrador (13 módulos auditados; botones crudos → `preview-action`; H1/radios/neutros/chips coherentes; responsive validado). F6.2: pulido visual del panel Docente (auditoría completa; único fix = filtros nativos del modal "Añadir del banco" → `selectorBanco.css` con patrón `.bib-filtros`). F7.A+7.1 (`ac8d6b6`), F7.2 (`844ede7`), F7.3 (`25ae1c8`), F7.4 (`07ef88f`), F7.5 (`f818ade`), F7.6 (`85d452b`) y F7.7 (`83d9ad1`): auditoría y pulido de los 7 reproductores, **todas commiteadas y pusheadas** (verificado en Git 2026-07-28). **F6.3 (`43185ac`, 2026-07-28, pusheada): pulido del panel Estudiante** — barra de XP que se anulaba a 0px en móvil, título de actividad aplastado a 45px a 320px, `:focus-visible` temático ausente en los 5 controles del panel, `aria-pressed` en pestañas y chips, ✓ de misión 2.28:1 → 5.02:1, media query móvil que faltaba; 4 archivos exclusivos del estudiante, cero cambios funcionales, escritorio idéntico al baseline. **F8 (2026-07-29): auditoría final y cierre de ajustes** — Etapa 1 (solo lectura) demostró que el «0/100» **no es un bug** sino el contador rAF congelado con la pestaña oculta (backend devolvió `calificacion: 50` en la misma partida), auditó requests (`×2` = StrictMode) y JWT/validación (sin vulnerabilidad → sin Zod, sin librerías nuevas); Etapa 2 implementó 4 hallazgos en 7 archivos (+314/-17, cero backend): textareas nativos tematizados (Editar materia + Misión), fuga de caché `edu_xpTotal`/`edu_historialRetos` entre usuarios cerrada en el logout/login central, sidebar colapsable en tablet/escritorio con raíl de restauración (+209px de contenido a 768 y 1280), `aria-current="page"`, `:focus-visible` en `.completar-btn-siguiente` (Completar + V/F) y `prefers-reduced-motion` global con excepción para los spinners de carga; más 2 microinteracciones CSS del Home del estudiante. **SPEC-018 COMPLETA (8/8 fases).** Solo presentación; áreas §10/SPEC-013/016/017 congeladas |

## Último sprint

**SPEC-018 Fase 6.2 — Pulido visual del panel Docente (2026-07-20, validada en entorno local Docker/MySQL `gamificapp_dev`).** Un solo archivo (`selectorBanco.css`, +11 líneas); cero cambios de comportamiento.

- **Auditoría completa** navegando como `docente.demo`: Inicio, Materias (Matemáticas con sus 5 pestañas: Resumen, Crear actividad con los 7 tipos, Actividades, Material, Calificaciones), **flujo completo de creación/edición** (editor de Quiz: acordeón, alternativas, menú Agregar preguntas manual/IA/banco, Configuración, Vista previa en reproductor real, Deshacer verificado, Publicar), Biblioteca (filtros + modal Estadísticas + Vista previa), Mis Estudiantes + Ficha, Ranking, Misiones, Mi Perfil. El panel ya estaba muy pulido tras Fases 1-5.
- **Único hallazgo corregido**: el modal "Añadir del banco de preguntas" (`SelectorBanco.jsx`, compartido por quiz/memorama/línea/completar) tenía el `input[type=search]` y los `select` de filtros con estilo nativo del navegador (border-radius 0, apariencia por defecto) — mismo tipo de bug que ModuloIA en F6.1. Corregido en `selectorBanco.css` replicando el patrón `.bib-filtros` de la Biblioteca (border/radius/padding/background con tokens). Cubre también los `.banco-filtros` de `BancoPreguntas.jsx`. Cero JSX tocado.
- **Diferido a propósito**: títulos duplicados H1/H3 en Mis Estudiantes y Ranking, jerarquía de headings en Mi Perfil, `aria-label` en íconos del menú "Agregar preguntas" (SPEC-013), y formalización de `--color-text-soft`/`--color-text-muted` como tokens globales (cierre de Fase 6).
- **Verificado en navegador local**: cero overflow-x en 320/375/480/768/1024/escritorio recorriendo las 7 secciones + editor + Libro + modal del banco; foco/Escape en Vista previa, Estadísticas, Ficha y Banco; ConfirmDialog de papelera cancela con 0 peticiones de red; editar+Deshacer restaura el borrador; build limpio; **lint 29 = baseline real de HEAD** (el "28" documentado antes estaba desfasado, verificado con stash); consola sin errores.

**SPEC-018 Fase 6.1 — Pulido visual del panel Administrador (2026-07-20, validada en entorno local Docker/MySQL `gamificapp_dev`).** Solo 4 archivos (`ModuloIA.jsx`, `ModuloJuegos.jsx`, `adminDashboard.css`, `moduloMaterias.css`); cero cambios de comportamiento.

- **Auditoría completa de los 13 módulos administrativos** navegando como admin real; el hallazgo principal fueron **botones crudos del navegador**: ModuloIA (Guardar/Probar conexión) y ModuloJuegos (diálogo de confirmación) usaban las clases `btn-primario`/`btn-secundario`, que no existen en ningún CSS — se sustituyeron por `preview-action`/`preview-action-primary`, ya definidas en `dashboard.css` y usadas por el resto del panel. Sin cambios de JSX estructural ni de handlers.
- **Coherencia menor**: H1 del Inicio a 1.9rem (igual que el resto de módulos), radios crudos → `--radius-sm`, neutros hardcodeados de `.admin-acciones button` → tokens (`--color-border`/`--color-surface` y `--color-text-soft` con fallback), chip de estado de materias a 0.75rem (mismo cuerpo que `curso-estado` y `rol-chip`).
- **Diferido deliberadamente al cierre global de Fase 6**: formalizar `--color-text-soft`/`--color-text-muted` como tokens globales, cabeceras de módulo compartidas y consolidación de breakpoints 560/720/900.
- **Verificado**: build limpio, lint = baseline, responsive de los módulos tocados y regresión visual del panel admin en local.

**SPEC-018 Fase 5 — Navegación responsive (2026-07-20, validada en entorno local Docker/MySQL `gamificapp_dev`).** Solo 2 archivos: `SidebarLayout.jsx` y el bloque `@media (max-width: 760px)` de `dashboard.css`; los 3 roles heredan el mecanismo del componente compartido.

- **Móvil (≤760px)**: barra superior compacta (logo + título + botón hamburguesa) y navegación + footer colapsados en un **panel desplegable superpuesto** al contenido (ya no lo empuja: el contenido empieza en y≈63px, antes ≈460 con los 12 ítems del admin apilados — cierre del hallazgo P1 de la auditoría runtime). Accesibilidad: `aria-expanded`/`aria-controls`/`aria-label` dinámico, foco al panel al abrir, **Escape cierra y devuelve el foco al botón**, clic fuera cierra, **elegir sección navega y cierra**, scroll-lock del fondo (guardar/restaurar), cierre al agrandar por doble vía (`resize` + `matchMedia`). Ítems táctiles de 42px.
- **Escritorio/tablet (≥761px)**: cero cambios visuales — sidebar lateral de 264px idéntico (verificado en los 3 roles); el envoltorio `.sidebar-menu` es transparente al layout.
- **Jerarquía z-index validada en vivo**: menú móvil `--z-dropdown` (30) < `ModalPanel`/`ConfirmDialog` `--z-modal` (50) < overlays/guardia de juegos (55/60) < `Toast` (70) — la guardia de salida aparece SOBRE el menú al navegar con partida a medias.
- **Breakpoints auditados** (480/520/560/640/720/760/900/1024): solo se reescribió el bloque de 760; la consolidación de 560/720/900 queda para Fase 6.
- **Verificado en navegador local**: 320 (menú y contenido dentro del viewport), 375 (los 12 módulos admin navegados por el menú, cierre en todos, cero overflow-x; docente Biblioteca + ConfirmDialog; estudiante quiz completo con guardia y overlay), 480, 768/1024 (sidebar lateral intacto), 1280 (regresión escritorio en los 3 roles). Tablas sin romper el viewport en ningún módulo. Build limpio; lint 28 = baseline; consola sin errores. Limitación del entorno: la emulación del preview no emite `resize`/`matchMedia change` al cambiar el viewport — el cierre-al-agrandar se verificó disparando el evento manualmente (en navegador real esos eventos sí se emiten).

**SPEC-018 Fase 4 — Feedback: ConfirmDialog + Toast (2026-07-20, validada en entorno local Docker/MySQL `gamificapp_dev`).** Sustituye el feedback nativo por componentes coherentes, con **equivalencia funcional estricta** (mismo servicio, endpoint, payload y refresh; verificado inspeccionando la red en vivo).

- **Nuevos**: `ConfirmDialog.jsx` (sobre el `ModalPanel` de Fase 3; variantes danger/warning/neutral con tokens, `detalle` opcional, prop `procesando`, **anti doble-confirmación por ref** — triple clic = 1 sola petición, comprobado por red; semántica exacta de `window.confirm`: cierra y DESPUÉS corre la acción), `hooks/useConfirmacion.jsx` (patrón `useGuardiaActividad`: `pedirConfirmacion` + `dialogoConfirmacion`), `toastBus.js` (bus sin React, separado por la regla `react-refresh` del lint) y `Toast.jsx` (`ToastHost` único montado en `App.jsx` fuera de `Routes` — sobrevive la navegación SPA sin duplicarse; éxito/error/aviso/info con tokens, `--z-toast`, `role="alert"` solo errores y `status` para el resto, cierre manual + automático 4.5-8s, anti-duplicados por tipo+mensaje, apilado, `prefers-reduced-motion`, responsive `min(480px, 100vw-24px)`).
- **Migrados (12 confirms + 3 alerts)**: AdminDashboard (eliminar docente/estudiante/invitación), ModuloAdministradores, ModuloCursos, ModuloPapelera (purgar, con `detalle`), ModuloMaterias (papelera + 2 avisos warning de materia protegida), Biblioteca (purgar/papelera/archivar); alerts del cambio de PIN del estudiante → `toast.exito/error` y aviso del login de emergencia → `toast.aviso` (12 s). Cada diálogo conserva el texto original del mensaje (vía `aria-describedby`).
- **Conservados a propósito**: avisos inline ok/error de `ejecutar()` en los paneles, errores de formulario, feedback educativo completo de los 7 juegos (cero dependencias de Toast/ConfirmDialog en reproductores, verificado por grep); 4 confirms no destructivos de editores y los 2 `window.prompt` del PIN diferidos → MASTER_PLAN §3 ítems 28-29.
- **Verificado en local**: cancelar = 0 peticiones (clic y Escape, foco restaurado); confirmar = 1 petición exacta + refresh; error real de backend (purgar actividad jugada → rechazo del servidor mostrado por el canal inline de siempre, estado recuperable); toast real del backend en el flujo de emergencia; responsive 320/768/escritorio (diálogo 272-460px, toast 296px, sin overflow); juego completo post-cambios sin interferencias; build limpio; lint 28 = baseline; consola sin errores.
- **Hallazgo PREEXISTENTE (no corregido, registrado en MASTER_PLAN §3 ítem 27)**: el confirm de eliminar curso dice "solo es posible si no tiene estudiantes", pero el backend responde 200 (papelera) con estudiantes **activados** — el 409 de SPEC-014 §13 solo cubre pendientes de activación. Inconsistencia mensaje ↔ regla; sin decidir cuál lado es el correcto.

**SPEC-018 Fase 3 — Sistema unificado de modales (2026-07-20, validada en entorno local Docker/MySQL `gamificapp_dev`).** `ModalPanel` (`DashboardWidgets.jsx`) endurecido con el mismo patrón de foco que ya usaban los overlays de juego: foco inicial en el panel (`tabIndex={-1}`), focus trap Tab/Shift+Tab, **Escape → `onCerrar`** (la misma función que backdrop y ✕, por lo que hereda las guardas `!guardando && cerrar` de cada consumidor), restauración del foco al elemento que abrió el modal (protegida con `isConnected`/`!disabled`), bloqueo de scroll del body con contador (soporta modales consecutivos/anidados), `aria-labelledby`/`aria-describedby` vía `useId`, y `.preview-backdrop` con `z-index: var(--z-modal)`. **Contrato de props idéntico: cero cambios en los 16 consumidores (20 instancias).** Deliberadamente FUERA: overlays de resultados y `DialogoSalida` de la guardia (ya tenían a11y propia y lógica estabilizada), el modal manual de asignaciones (`AdminDashboard.jsx` `.modal-materias`) y `FilePreviewModal` (`ArchivoChip.jsx`) — candidatos a unificar en Fases 4/6. Verificado en navegador con teclado real: foco inicial, ciclo Tab/Shift+Tab en ambos sentidos, clic dentro no cierra, backdrop cierra, Escape cierra (también en modales con guarda), foco restaurado al abridor exacto, overflow restaurado; regresión de juegos (overlay único, reabrir, guardia) sin cambios. Build limpio; lint 28 = baseline; consola sin errores.

**SPEC-018 Fases 1 y 2 — Design system semántico y consolidación de color (2026-07-20, validadas en entorno local Docker/MySQL `gamificapp_dev`).** Solo presentación; cero cambios de comportamiento.

- **Fase 1 (commit `finalistas v3xdxd`)**: tokens puramente aditivos en `src/index.css` — colores semánticos (`--color-success/-soft/-dark`, `--color-danger/…`, `--color-warning/…`, `--color-info/-soft`; sin `info-dark` por no existir valor justificado), escala de z-index (`--z-base/sticky/dropdown/modal/overlay/toast`) y breakpoints documentados (480/768/1024 — `var()` no funciona en `@media`, son referencia).
- **Fase 2**: **~60 hex semánticos migrados a los tokens en 15 archivos** (juegos.css, verdaderoFalso.css + iconos de revisión en VerdaderoFalso/CompletarEspacios.jsx, editorClasificador, editorQuiz, dashboard.css con reemplazos puntuales por semántica mezclada, adminDashboard, docentePanel, dashboardEstudiante, login, moduloJuegos/IA/Materias). Cada sustitución usa el token de **valor idéntico** al hex previo → cero cambio visual. Bonus: dos reglas referenciaban `var(--color-exito, …)`, variable que nunca existió; ahora usan `--color-success`.
- **Conservados deliberadamente (~50 hex)**: identidad visual (iconos Excel/PDF/Word, medallas de ranking, fuego de racha, paletas de materias/mundos, chips por tipo en Papelera, categorías/tiers de `misionesService.js` — el mismo `#16a34a` significa "correcto" en el Quiz e "Excel" en los chips de archivo, por eso no hubo replace_all global) y tonos sin token exacto cuya sustitución cambiaría el aspecto (`#ef4444` del Quiz incorrecto, rebote suave del Clasificador `#f87171/#fef2f2` — es un aviso amable a propósito —, `#fecaca/#fca5a5/#f0fdf4/#86efac/#166534/#991b1b`, ámbares `#fffbeb/#92400e` del V/F). No se inventó ningún token nuevo.
- **Hallazgo retroactivo (no regresión funcional)**: `importarEstudiantes.css` ya referenciaba los tokens semánticos ANTES de que existieran, con fallbacks de valores distintos (p. ej. `var(--color-success-soft, #f0fdf4)`); al definirlos la Fase 1 cambió sutilmente los tonos de ese módulo (green-50→green-100, etc.). Es el estado final deseado, pero Fase 1 no fue 100 % "cero cambio visual" ahí.
- **Inconsistencias detectadas y APLAZADAS a Fase 7** (unificarlas es un cambio visual perceptible que requiere decisión): rojo "incorrecto" difiere entre Quiz (`#ef4444`) y Completar (`#dc2626`); check de misiones en emerald `#10b981`; aviso ok del admin en emerald `#ecfdf5/#065f46`.
- **Verificado**: build limpio; lint 28 = baseline exacto (cero nuevos); consola sin errores en los 3 roles; partidas reales de los 7 juegos en local (Quiz/Completar/V-F: selección ámbar → revisión verde/rojo con corrección diferida intacta; Memorama/Línea/Clasificador/Misión: mecánicas intactas) y navegación de los 3 paneles midiendo estilos computados. Limitación del preview anotada: con pestaña oculta las transiciones CSS se congelan y el contador de nota (rAF) muestra 0 — no es un bug de la app.

**Auditoría de actividades y cierre de seguridad del progreso (2026-07-19, en código; sin migración; pendiente de validación e2e en producción).** Ronda de auditoría sobre los 6 juegos a partir de pruebas reales en producción como estudiante y docente. Cambios aprobados uno a uno por Fabrizio.

- **Resultado duplicado eliminado (los 5 juegos que no eran Quiz).** Causa raíz: `PantallaFinal` (`juegosComunes.jsx`) montaba a la vez `ResultadoCierre` (overlay) **y** `ResultadoActividad` (tarjeta incrustada), ambas alimentadas por el mismo `CuerpoResultado`; idéntico patrón en `MisionNarrativa`. Ahora la calificación, la retroalimentación y el XP viven **solo** en el overlay; la pantalla inferior queda como estado final de la actividad (trofeo, estrellas, resumen corto del intento, revisión de Completar, narrativa final de Misión) y se reabre con "Ver mi resultado". `ResultadoActividad` sigue exportado: lo usa el Libro de Calificaciones.
- **Línea del tiempo — mecánica y evaluación nuevas.** Durante el intento el estudiante reordena **libremente**: nada se marca en verde, nada se bloquea y nada revela la solución (antes, "Comprobar" fijaba las posiciones correctas, lo que revelaba parte de la respuesta e impedía mover el resto). Un único botón **"Comprobar orden"** evalúa la secuencia completa y cierra el intento. La nota ya **no** es posición absoluta sino **concordancia de rangos de Kendall**: pares de eventos en orden relativo correcto sobre `n(n-1)/2` (`src/components/juegos/ordenSecuencia.js`, puro). Motivo: mover un evento desplaza a todos los siguientes, así que la posición absoluta daba 0 a intentos que sí demostraban comprender la secuencia. **Se probó y descartó la adyacencia** (pares consecutivos): intercambiar dos vecinos —el error más típico a los 6-9 años— rompe tres pares a la vez y hundía la nota a 0/100 con 3 eventos, peor que el criterio viejo.
- **Memorama — nueva fórmula de calificación** (`src/components/juegos/calificacionMemorama.js`, puro). El criterio anterior (una pareja solo puntuaba si se encontraba sin ningún fallo previo) castigaba justo la mecánica del juego: descubrir dónde está cada carta ES jugar, así que el niño empezaba con casi todas las parejas descalificadas. Ahora: **una vuelta completa de exploración es gratis y después la nota mide la eficiencia** — `f <= n` da 100; `f > n` da `100*n/(n+(f-n))`, con `n` = parejas y `f` = **intentos fallidos** de formar pareja (no cartas sueltas; el `Set` anterior deduplicaba y era incapaz de contar intentos, ahora es un contador). La nota viaja sobre **base 100** (`aciertos` = nota, `total` = 100): con `total` = nº de parejas quedaba cuantizada en 7 escalones y se perdía la curva. `useRecompensa` acepta un `puntosObtenidos` opcional para que el XP optimista de la caché local siga siendo coherente.
- **Dificultad y Curso en Quiz y Clasificador** (antes solo los tenían Misión/Memorama/Línea/Completar). Ambos campos llegan al payload y persisten en `retos.dificultad`/`retos.curso_id`, en generar, añadir con IA, guardar borrador, publicar y al reabrir del historial. Bloque compartido en `CamposActividad.jsx` + `metadatosActividad.js` (fuente única de `DIFICULTADES_UI`, re-exportada desde `GeneradorActividadIA` para no tocar a sus consumidores). **Hizo falta un cambio de servidor**: `/api/ia/quiz` (endpoint legacy) ignoraba `dificultad` y `curso_id`, así que en Quiz habrían sido campos decorativos; ahora los acepta como opcionales y retrocompatibles.
- **Campo Tema autoexpandible + placeholders por materia** (`CampoTema.jsx`, usado en los 3 sitios donde se escribe un tema: `GeneradorActividadIA` —cubre memorama/línea/completar—, `GeneradorQuiz` y `EditorClasificador`). Textarea de una fila con altura de input que crece hasta ~5 líneas y luego hace scroll interno; Enter sigue enviando el formulario. El placeholder se deriva del nombre de materia normalizado contra una lista corta de palabras clave, con fallback genérico para materias personalizadas (antes salía "Ej. Las partes de la planta" en Matemáticas).
- **Banco de preguntas oculto del dashboard (opción B, tras auditoría).** El pool de contenido vive hoy en cada actividad, así que la página global perdió sentido. Se comentó **solo** la entrada del menú: tabla `banco_preguntas`, endpoints, `bancoService`, `_banco_id` incrustado en configuraciones publicadas, snapshots, el modal "Reutilizar" dentro de los editores y el guardado automático siguen **intactos**. Sin migración ni borrado. Reactivar = descomentar una línea. Recomendación registrada: **mantener el guardado automático**, porque `Reutilizar` sigue existiendo dentro de los editores y sin él el banco se vaciaría de contenido nuevo dejando esa función muerta en silencio.
- **Endurecimiento de `POST /api/progreso` (cierre de seguridad).** Modelo de confianza completo en `docs/specifications/SPEC-015-Calificacion-Academica-Persistida.md`. Cerrado:
  1. Un **estudiante podía crear retos publicados** por la ruta `materia_id + reto_titulo` **y fijarles él mismo el `xp_recompensa`** que luego cobraba (XP ilimitado en una petición). Esa ruta queda reservada a docente/admin.
  2. `aciertos:1, total:1` daba 100/100 y toda la recompensa de cualquier actividad sin jugarla. Nuevo `server/lib/totalEsperado.js` (puro) deriva del `tipo` + `configuracion_json` el `total` que un intento legítimo puede reportar; si no coincide, **400** — nunca fallback silencioso al flujo histórico (ese fallback era el agujero).
  3. El servidor carga de BD `tipo`, `estado`, `configuracion_json`, `xp_recompensa`, `materia_id` y `curso_id`: la recompensa sale **siempre** de la fila persistida.
  4. El estudiante solo registra progreso sobre retos **publicados, no eliminados y de materia viva y activa** — exactamente el mismo criterio que usa `GET /api/retos` para listarle actividades, para que no haya nada visible que no sea enviable ni viceversa.
  5. El estudiante debe enviar siempre `aciertos/total`; la ruta legacy de solo `puntos_obtenidos` queda reservada al **ajuste manual de XP del Libro de Calificaciones** (docente/admin), su único cliente.
  6. `aciertos`/`total` deben ser números enteros exactos: se rechazan decimales, negativos, `aciertos > total`, strings, `null`/`NaN` y valores desproporcionados. (Este último caso lo encontró el arnés de pruebas, no la revisión de código: `NaN` viaja como `null` en JSON y `Number(null)` es `0`, así que se colaba como "0 aciertos".)
- **Retos legacy sin configuración derivable**: las filas creadas por la vieja ruta `materia_id + reto_titulo` nacían con `configuracion_json = NULL` y `tipo = 'quiz'` por defecto. Para esas, `totalEsperado` devuelve `null` y el intento se rechaza con 400. No bloquea ningún juego real (todos los reproductores exigen configuración válida), y el XP histórico ya acreditado no se toca. **Pendiente**: ejecutar en producción, SOLO LECTURA, el diagnóstico preparado para confirmar cuántas filas existen y si alguna tiene intentos registrados.
- **Verificado**: `npm run build` limpio; `node --check` en los archivos de servidor; lint **33 problemas vs 34 en `main`** (comparado por archivo y por regla: cero nuevos, uno menos); pruebas aisladas con `node` de `ordenSecuencia` (3/5/8 eventos: perfecto, un desplazado, vecinos intercambiados, varios intercambios, inverso), de `calificacionMemorama` (barrido exhaustivo n=2..12 x f=0..500 + 4000 partidas simuladas por perfil de memoria en tableros de 4/6/10) y de `totalEsperado` (23 asserts). Los **13 escenarios de seguridad** se ejercitaron contra el **router real** montado en Express con la BD stubbeada (30 asserts verdes), incluyendo XP incremental 60-80-100, repetición al 100% sin duplicar XP y los 3 flujos legítimos de docente/admin. Arnés retirado del repo antes del commit. **Sin MySQL local: e2e completo (curva de Memorama, nota de Línea del tiempo persistida, Dificultad/Curso, concurrencia real de `FOR UPDATE`) queda para producción tras el deploy.**

**Sistema de resultados extendido a los 6 juegos (2026-07-18, en código; solo frontend, sin migración; pendiente de validación e2e en producción).** Cierra el piloto del Quiz (entrada anterior de este sprint) extendiendo el mismo sistema UX a Misión Narrativa, Clasificador, Memorama, Línea del tiempo y Completar Espacios, adaptando acciones y retroalimentación al funcionamiento real de cada uno (no se copió literalmente la UX del Quiz).

- **Corrección visual del estado "seleccionado" (fix pedido tras probar el piloto):** durante el intento, la opción elegida se veía verde/turquesa con un ✓, indistinguible de "correcta" aunque la corrección todavía no se revelaba. Ahora usa **ámbar del tema** (`--color-accent`) + un **chip textual "Tu respuesta"** (así el estado se entiende sin depender solo del color) — nunca verde, nunca ✓ mientras el intento sigue en curso. En la revisión final: correcta elegida = verde sólido + ✓; incorrecta elegida = rojo + ✕; correcta NO elegida = verde "hueco" (borde discontinuo, sin relleno, visualmente distinta de la elegida); el resto, neutro. Contraste del chip ajustado a `#b45309` (~5:1 sobre blanco). Aplica a Quiz (`.opcion-quiz`/`.opcion-elegida`/`.opcion-correcta-no-elegida` en `dashboard.css`) y a Completar Espacios (`.completar-opcion.opcion-elegida` en `juegos.css`).
- **Infraestructura compartida (evita duplicar el sistema 6 veces):**
  - `useRecompensa` (`juegosComunes.jsx`) ahora envía `total` al backend (antes solo Quiz lo hacía) y expone `xpIntento` con el mismo contrato de 4 estados que ya tenía el Quiz (`ganado`/`sinCambio`/`completo`/`sinConfirmar`) — el overlay de cada juego muestra el XP REAL acreditado por el servidor, nunca una estimación ni "+0 XP".
  - `ResultadoCierre` (nuevo, en `ResultadoActividad.jsx`): overlay autocontenido que se abre al montarse y, al cerrarse (Revisar / clic fuera / Escape), deja el botón "🏅 Ver mi resultado" para reabrirlo — mismo patrón que ya tenía el Quiz, ahora reutilizable.
  - `PantallaFinal` (`juegosComunes.jsx`) monta `ResultadoCierre` sobre el trofeo/estrellas de siempre; acepta `xp`, `detalle` y `etiquetaRevisar`/`etiquetaSalir` para que cada juego adapte el texto sin duplicar el componente.
  - `useReporteIntento` (nuevo, en `useGuardiaActividad.jsx`): lado "reproductor" del contrato de guardia — cada juego calcula su propia condición de "hay progreso real sin terminar" y la reporta con este hook; reemplaza el código a mano que tenía el Quiz y es el que usan los 5 juegos nuevos.
  - **Clasificador dejó de duplicar** su propia `useEffect` de recompensa y su propia pantalla final a mano (~60 líneas): ahora reutiliza `useRecompensa` + `PantallaFinal` igual que memorama/línea/completar.
- **Por juego (comportamiento durante el intento sin cambios salvo donde se indica; todos ganan overlay+reintento+guardia):**
  - **Quiz**: reintento re-muestrea el pool y re-baraja (SPEC-013 §mezclar); guardia desde la 1.ª respuesta; corrección diferida (ya documentada arriba).
  - **Misión Narrativa**: la pista+reintento del capítulo (mecánica ya existente) no cambia; ahora usa `useRecompensa`/`useReporteIntento` en vez de su propio bloque de XP; overlay (`ResultadoCierre`) se monta SOBRE la escena final narrativa ("¡Misión cumplida!" + texto de cierre siguen visibles debajo); acciones "Jugar de nuevo" / "Ver el final de la historia" / "Otras misiones"; guardia desde la 1.ª respuesta de un desafío (la portada/introducción no protege — confirmar la salida ahí no pierde nada).
  - **Clasificador**: el rebote visual del error (mecánica ya existente) no cambia; reintento re-baraja las fichas; guardia desde la 1.ª ficha colocada o el 1.er error.
  - **Memorama**: voltear cartas no cambia; reintento re-baraja el tablero (todas boca abajo); guardia desde la 1.ª pareja resuelta o fallada (voltear una sola carta sin resolver aún NO protege).
  - **Línea del tiempo**: mover/comprobar (mecánica ya existente) no cambia; reintento re-baraja el orden inicial; guardia desde el 1.er movimiento o la 1.ª comprobación.
  - **Completar Espacios**: gana **corrección diferida** (antes revelaba correcto/incorrecto+justificación al elegir, igual que el Quiz pre-fix): ahora la palabra elegida solo se marca en ámbar ("¡Respuesta guardada!") y avanza sin revelar; nueva sección "Así respondiste" en la pantalla final con cada frase, la palabra elegida en verde/rojo y la correcta cuando falló (nuevas clases `.completar-revision*` en `juegos.css`); reintento re-baraja frases y opciones; guardia desde la 1.ª palabra elegida.
- **Común a los 6**: overlay se cierra con clic fuera / Escape / "Revisar" (mismo efecto); reabre con "Ver mi resultado"; XP mostrado = el que confirma el backend (nunca estimado); actividad terminada u overlay abierto → navegar no advierte; `soloPrueba` (vista previa docente) se comporta igual pero sin XP/progreso y sin guardia; jerarquía de botones sin ambigüedad (primaria sólida + secundarias ghost).
- Verificado: `npm run build` limpio + arnés de prueba aislado en el navegador para los 6 juegos (estados visuales del Quiz uno por uno, corrección diferida de Completar, guardia con/sin progreso en cada juego, clic-fuera, reintento limpio con re-aleatorización, responsive móvil sin scroll horizontal) — arnés retirado antes del commit. **Sin MySQL local: validación e2e completa (XP incremental real entre reintentos en los 5 juegos que ahora envían `total`, calificación en el Libro para los 6, flujo en móvil real) queda para producción tras el deploy.**

**Quiz — Reintentar, guardia de salida y retroalimentación diferida (2026-07-18, piloto único: solo Quiz; solo frontend, sin migración; en código, pendiente de validación e2e en producción).** Última mejora del flujo del Quiz antes de extender el sistema de resultados a los otros 5 juegos. (1) **"Jugar otra vez"** en el overlay de resultado (`ResultadoOverlay`, acción primaria — motiva a mejorar la nota; "Revisar respuestas" y "Otros quizzes" quedan como secundarias): `QuizInteractivo.jsx` incorpora un contador `intento` que compone la clave de reinicio (antes solo dependía de `reto.id`+preguntas, así que repetir el MISMO quiz no re-sorteaba nada); al reintentar se re-ejecuta `muestrear()` sobre el pool y `barajarQuiz()` respetando `mezclar_preguntas`/`mezclar_respuestas` del reto, se remontan las preguntas (respuestas y estados visuales limpios) y se reinician aciertos/recompensa/overlay. El XP sigue siendo 100% lo que acredita el backend por intento (`xp_abonado`, SPEC-015): nunca se duplica. (2) **Guardia de salida reutilizable** (`src/hooks/useGuardiaActividad.jsx`, nuevo): contrato `onEstadoIntento(enProgreso)` que reporta el reproductor + `proteger(fn)` que envuelve cualquier navegación del contenedor; el Quiz reporta "en progreso" cuando hay ≥1 respuesta y el intento no terminó (nunca en `soloPrueba`). Diálogo amigable ("¿Quieres salir del juego? … no se guardará", "¡Seguir jugando!" primaria / "Salir sin terminar" secundaria) reutilizando los estilos del overlay de resultado; `beforeunload` nativo solo mientras hay intento iniciado sin terminar. Cableado en `DashboardEstudiante.jsx` sobre las navegaciones que desmontan el Quiz (sidebar, volver a mundos, pestañas de materia, "Otros quizzes", cerrar sesión). Pensado para reutilizarse en los 5 juegos restantes sin lógica de navegación dispersa por la app. **Sin autoguardado ni restauración de intentos**: confirmar la salida descarta el progreso local; XP/calificación de intentos anteriores YA completados quedan intactos. (3) **Clic fuera del overlay** = exactamente "Revisar respuestas" (cierra el popup, muestra la revisión, no navega, no reinicia); los clics dentro de la tarjeta no cierran por propagación (`target === currentTarget`). (4) **Retroalimentación diferida**: nuevo prop `revelar` en `PreguntaCard` — durante el intento solo se marca la opción elegida (nuevo estilo neutro `.opcion-elegida` en `dashboard.css`, sin verde/rojo), sin corrección, respuesta correcta ni justificación; al completar el intento se revela todo. La captura de la respuesta y el cálculo de `aciertos` no cambiaron (mismo `onResponder`); cada opción queda deshabilitada al responder, así que no se puede alterar el resultado ya registrado durante la revisión. Auditoría previa sin conflictos con `soloPrueba`, XP incremental, `mezclar_preguntas/respuestas`, navegación de `DashboardEstudiante` ni misiones (no montan el Quiz). Verificado: `npm run build` limpio + arnés de prueba aislado en el navegador (reintento re-sortea y re-baraja, XP-cero no revela pistas, clic-fuera = revisar sin navegar, guardia aparece solo con ≥1 respuesta sin terminar y respeta overlay/soloPrueba/tras-reintento, `beforeunload` armado solo con intento en curso) — arnés retirado antes del commit. **Sin MySQL local: validación e2e completa (XP incremental real entre reintentos, flujo en móvil) queda para producción tras el deploy.** No extendido todavía a los otros 5 juegos: pendiente de validación en producción y aprobación explícita antes de adaptarlo caso por caso (los otros juegos no necesariamente comparten las mismas acciones finales del overlay).

**SPEC-015 — Calificación académica persistida + XP proporcional al desempeño (2026-07-18, en código; pendiente de commit/deploy).** Cierra la auditoría del sistema de resultados (C1/C2/C3): (1) **BD/backend**: nueva columna `progreso_estudiante.calificacion` (migración 010 idempotente con backfill único desde `porcentaje`); `POST /api/progreso` acepta `aciertos`+`total` opcionales y con ellos el SERVIDOR calcula la nota (`round(a/t×100)`, nunca confía en el cliente) y el XP proporcional (`round(a/t × xp_recompensa)`) — editar la recompensa XP ya no distorsiona la nota del Libro; sin esos campos el flujo histórico queda intacto (juegos no migrados y ajuste manual del docente NO tocan la calificación). Acreditación incremental intacta (`delta = max(0, nuevo−previo)`, `FOR UPDATE`, `GREATEST`): nunca se duplica ni se resta XP; `porcentaje` conserva su semántica (misiones/ranking usan `=100` como perfecto, que sigue ocurriendo solo con intento perfecto). Respuesta ampliada: `calificacion` (intento), `mejor_calificacion`, `xp_obtenido_total`, `xp_recompensa` (+ `xp_abonado` existente = XP real del intento). (2) **Overlay del Quiz**: muestra el XP que el backend realmente acreditó, con 3 estados — "+N XP", "sin mejora" (mensaje amable, nunca "+0 XP") y "recompensa completa"; la nota mostrada es la del intento actual, el Libro conserva la mejor. (3) C2: foco restaurado al cerrar el overlay (botón "Ver mi resultado"); C3: la clave del resultado incluye `reto.id`. Verificado: build limpio, `node --check`, 2 suites aisladas (nota/rangos + 25 escenarios de reintentos/XP incremental/legacy). **Sin MySQL local: e2e (migración 010, reintentos reales, Libro) en producción tras el deploy.** Detalle: `docs/specifications/SPEC-015-Calificacion-Academica-Persistida.md`.

**Calificación académica /100 + retroalimentación por rangos (2026-07-18, requisito nº 4 del revisor de tesis; solo frontend, sin migración).** Toda actividad muestra al terminar una **calificación sobre 100** separada del XP: `calificacion = round(aciertos / total_evaluado × 100)`, calculada SIEMPRE desde los aciertos y el total realmente jugado en el intento (en el quiz con banco, la muestra presentada — nunca el pool), jamás desde el XP. Retroalimentación estándar por rango (0–40 🌱 motivación / 41–70 💪 esfuerzo / 71–100 🏆 felicitación, lenguaje neutral para niños) que depende SOLO de la nota. Nuevo `src/components/juegos/calificacion.js` (`calificacionDe`, `retroalimentacionDe`, puro) + componente compartido `ResultadoActividad.jsx` (+ css) integrado en las 4 pantallas finales que cubren los 6 juegos (PantallaFinal de juegosComunes → memorama/línea/completar; QuizInteractivo; JuegoDragAndDrop; MisionNarrativa): "Tu calificación X/100" con jerarquía principal, mensaje del rango y "+XX XP" como chip separado. **XP, progreso, ranking, niveles y misiones intactos** (§10 sin tocar; cero cambios de backend/BD). Libro de Calificaciones: nueva columna "Calificación" que presenta el `porcentaje` persistido como **nota X/100 con emoji del rango y tooltip "Mejor resultado"** (el upsert usa GREATEST: lo guardado es el mejor intento); verificado que los flujos actuales publican `xp_recompensa = total_items × 100` (quiz con banco: muestra × 100), por lo que `porcentaje` equivale a aciertos/total sin migración. Convención a mantener: si un flujo futuro publica `xp_recompensa` distinto de total×100, `porcentaje` dejaría de ser la nota (habría que persistir `calificacion` propia). Verificado: build limpio + prueba aislada de nota/rangos (16 casos borde). **Sin MySQL local: revisión visual e2e (pantallas finales de los 6 juegos, incl. móvil, y Libro) en producción tras el deploy.**

**SPEC-014 — Carga masiva de estudiantes: COMPLETADA (2026-07-18, e2e validado en producción por Fabrizio).** Requisito nº 3 del revisor de tesis cerrado. Modelo único de alta: el docente registra (➕ Añadir estudiante o 📥 Importar desde Excel — ambos por la misma lógica de servidor) y el estudiante activa su acceso (Curso → Nombre → Código individual de un solo uso), y desde entonces entra con Nombre + PIN. Incluye estado de acceso Pendiente/Activado en los paneles, regeneración de código, edición de nombres/apellidos y fecha de nacimiento, homónimos resueltos por `nombre_norm`, criterio único de aula del docente (`docente_curso` OR invitación legacy) y las invitaciones antiguas en modo legacy. Detalle completo en el historial más abajo. **Cerrado: no se hacen más cambios en este módulo salvo bugs; las mejoras no indispensables sobre estudiantes quedan fuera de alcance hasta terminar el resto de puntos del revisor.**

## Próximo sprint

**SPEC-018 — Pulido integral de UI/UX y accesibilidad (Fases 1-5 completadas; siguiente: Fase 6 Pulido por rol).** Solo presentación. Hecho: design system, color a tokens, `ModalPanel` accesible, `ConfirmDialog`/`Toast`, sidebar responsive compartido. Pendiente: pulido por rol (6: Admin → Docente → Estudiante), juegos (7) y auditoría final (8). **Áreas §10 y SPEC-013/016/017 congeladas (solo se cambia su piel).** Verificación obligatoria por fase (build + lint vs baseline 28 + funcional + navegación real + responsive + consola + regresiones) usando el entorno local `gamificapp_dev`; punto de parada antes de cada commit. Detalle en `docs/specifications/SPEC-018-Pulido-Integral-UI-UX.md`.

**SPEC-013 — Editor de actividades universal (aprobada 2026-07-17, diseño congelado).** Rediseño UX de los editores del docente: botón único "➕ Agregar" con menú por acciones (Escribir / Generarlas automáticamente / Reutilizar / Seleccionar automáticamente), lenguaje docente sin términos técnicos, toggles "Mezclar" configurables en el quiz, selección aleatoria desde preguntas guardadas, autoguardado unificado en los 6 editores y modal de generación IA único. Solo frontend, sin migraciones. Fases 1-7 por implementar en orden; detalle en `docs/specifications/SPEC-013-Editor-Actividades-Universal.md`.

**Épica 1 — Rediseño de la experiencia del estudiante.** SPEC-001 (Student Shell: rutas anidadas, sin sidebar, nuevo Inicio, menú de avatar, modal de PIN) redactada y pendiente de aprobación; nada implementado. Specs 2–5 (vista de materia, motor único de retos, login infantil, vitrina de premios) por redactar, dependen de SPEC-001. Insumos: `docs/audit/Auditoria-UX-Estudiante-v1.md`, `docs/specifications/SPEC-001-Student-Shell-Plan.md`. Roadmap y backlog completo: `MASTER_PLAN.md`.

## Riesgos operativos vigentes

- **Render plan free** (cold start): mitigado con keep-alive vía `/api/health` cada 14 min.
- **Archivos base64 en LONGTEXT** crecen sin límite: aceptado hasta post-tesis.
- **Caché localStorage vs BD**: el servidor siempre pisa la caché al leer; mantener esa regla.
- **Riesgo residual de seguridad ACEPTADO (2026-07-19)**: las validaciones de `POST /api/progreso` cierran la manipulación trivial y el XP arbitrario, pero el sistema **no** es server-authoritative. Alguien con conocimientos técnicos puede enviar un resultado estructuralmente válido pero no jugado (p. ej. `5/5` en un quiz que sí tiene 5 preguntas). El daño está acotado a la recompensa de esa actividad (`xp_recompensa` nunca se excede). Cerrarlo exige sesiones de intento firmadas o validar respuestas en backend; queda en backlog, fuera del alcance de la tesis.
- **Decisión pendiente: `curso_id` como frontera de acceso (2026-07-19)**: hoy `curso_id` **no** delimita el acceso. `GET /api/retos` deja ver a cualquier estudiante las actividades publicadas de cualquier materia activa, sin filtrar por curso, y `POST /api/progreso` aplica deliberadamente el mismo criterio para no crear incoherencias (actividades visibles pero rechazadas al enviarlas). Convertir el curso en frontera real es una decisión de producto que debe cambiar **ambos** endpoints a la vez.
- **⚠️ No existe concepto de "intento iniciado" en el backend (auditado 2026-07-19)**: `POST /api/progreso` es la única escritura y ocurre al TERMINAR. El servidor **no distingue** entre un estudiante que ya estaba jugando cuando se deshabilitó un tipo y otro que intenta enviar un resultado después: acepta ambos. El comportamiento deseado (que el niño pueda terminar y cobrar su XP) se cumple **por ausencia de comprobación, no por diseño**. Riesgo residual idéntico al ya aceptado en `MASTER_PLAN.md` §3 ítem 20; la solución (token de intento firmado) cerraría ambos y queda fuera del alcance de la tesis. Detalle en `SPEC-017 §5.0-bis`.
- **Migraciones: los `.sql` son documentación, la ejecución real vive en `initDb.js` (auditado 2026-07-19)**. `database/migraciones/*.sql` funciona como **referencia y versionado documental**; las migraciones automáticas de producción se ejecutan mediante **funciones idempotentes de `server/initDb.js`** (`initDb.js:36-52`), guardadas con `faltaColumna()` contra `information_schema`. No hay tabla de migraciones aplicadas. **Escribir solo el `.sql` no aplica nada**: hace falta la función JS. Mecanismo completo en `MASTER_PLAN.md` §6; evolución a un sistema formal versionado en §3 ítem 27 (post-tesis). Afirmaciones contrarias en la bitácora histórica de este archivo (línea ~198) quedan superadas por esta entrada.
- **Notas históricas de Línea del tiempo no son comparables (2026-07-19)**: cambiaron el criterio y el denominador (de `n` posiciones a `n(n-1)/2` pares). `GREATEST` impide que nadie pierda nota, pero el Libro mezclará dos criterios en actividades anteriores al cambio. Conviene mencionarlo en la tesis.

---

## Historial detallado (bitácora, no hace falta leer para trabajar — consultar solo para contexto de un cambio pasado)

> **SPEC-014 — Edición de nombres/apellidos de estudiantes (2026-07-18):** nueva acción "Editar" en las tablas de estudiantes de docente y admin (modal único `src/components/EditarEstudiante.jsx`). Backend: `PUT /api/estudiantes/:usuarioId` en `estudiantesImport.js`, con helper compartido `puedeGestionarEstudiante` (admin con permiso `estudiantes` verificado en BD; docente solo sobre sus estudiantes — `docente_curso` O invitación suya — el mismo criterio de `mis-estudiantes`; ahora también lo usa `regenerar-codigo`). El endpoint valida con `validarNombrePropio` (ahora exportado del validador puro), aplica la regla anti-homónimos de SPEC-014 (rechaza mismo `nombre_norm` + misma fecha de nacimiento con el mensaje de la importación, excluyendo al propio estudiante), y actualiza en UNA transacción `estudiantes.nombres/apellidos` + `usuarios.nombre_completo/nombre_norm`. El `username` interno (sufijo `~N` invisible) NO se toca — el login y la emergencia localizan por `nombre_norm`, así que el nombre nuevo entra al instante con el PIN actual y el anterior deja de localizar; código de activación, PIN, XP y progreso intactos (pendientes conservan su código y aparecen renombrados en `/registro`, que lee `nombre_completo`). Choque exacto en el mismo curso (índice único) → 409 claro; BD sin migración 012 → 409 explicativo. Auditado (`edito-estudiante`, antes/después). Decisión: editar fecha de nacimiento y curso NO se implementó (fecha exige política de re-derivar PIN; curso tiene efectos en visibilidad/registro/índice — pendiente de aprobación). Verificado: build limpio, `node --check`, prueba aislada del validador. **E2e en producción tras deploy.**

> **SPEC-014 — Auditoría del sistema de importaciones + 4 fixes (2026-07-18):** (1) Regenerar código ahora limpia `intentos_fallidos`/`bloqueado_hasta` (un niño bloqueado por 5 intentos fallidos podía seguir bloqueado 15 min con el código nuevo en la mano). (2) La creación de estudiantes (importación/alta manual/edición) usa `bcrypt.hash` asíncrono: los 120 `hashSync` de una importación de 60 filas congelaban el event loop (toda la app) ~8-10 s en Render. (3) Implementado el bloqueo de SPEC-014 §13 que faltaba: desactivar o mandar a papelera un curso con estudiantes pendientes de activación se rechaza con 409 y mensaje claro (esos niños quedarían sin poder entrar; el chequeo tolera BD sin migración 012). (4) `GET /api/auth/curso/:id/estudiantes-pendientes` exige curso activo y no eliminado (evita enumerar nombres de cursos ocultos con un id arbitrario). Revisado sin hallazgos: transaccionalidad/rollback de importar y alta, revalidación server-side en confirmar, doble activación (UPDATE condicional), fuerza bruta de /activar (contador por cuenta + limitador IP), inyección vía filas del Excel (validación de caracteres + parámetros preparados), choque de usernames concurrentes (ER_DUP_ENTRY→409), XSS del contenido del Excel (React escapa), fechas serial/futuras (rango de edad 4-15). Verificado: build + `node --check`. **E2e en producción tras deploy.**

> **SPEC-014 — Consolidación del alta de estudiantes (2026-07-18, aprobado por Fabrizio):** (1) **Criterio único de aula del docente**: nuevo `sqlAulaDocente()`/`esDelAulaDocente()` en `server/lib/estudiantes.js` (curso asignado vía `docente_curso` O invitación usada legacy) aplicado a los 5 puntos que aún filtraban solo por invitaciones — registro de progreso y libro de calificaciones (`progreso.js`, cambio autorizado §10: SOLO el chequeo de pertenencia, la lógica de XP/calificación intacta), stats del Home, Centro de Actividad y panel de misiones (`docente.js`) — y unificado también en mis-estudiantes/ficha/resetear-pin/gestión. (2) **Legacy**: el formulario "Generar invitaciones" se retiró de la UI del docente (backend, tabla y `/registro` por código intactos); la sección de códigos solo aparece si quedan invitaciones `pendiente` (solo esas filas, con expiración) y desaparece sola al consumirse/expirar. (3) **Edición**: `PUT /api/estudiantes/:usuarioId` acepta ahora `fecha_nacimiento` — pendiente: re-deriva el PIN inicial (aviso de que las credenciales descargadas quedan obsoletas; el código de activación no cambia); activado: PIN intacto, un reseteo futuro usa la fecha nueva; mismas validaciones de homónimos/edad con los datos NUEVOS. `EditarEstudiante.jsx` con campo de fecha y aviso contextual. **Cambio de curso: fuera de alcance (mejora futura anotada aquí, no implementar sin decisión).** Verificado: build limpio, `node --check`, prueba aislada de helpers (edad/PIN/parseo/placeholders). **E2e en producción tras deploy.**

> **SPEC-014 — Alta manual de estudiantes, unificada con la importación (2026-07-18):** nuevo `POST /api/estudiantes` (alta individual) que reutiliza EXACTAMENTE la misma cadena que el Excel: `puedeImportarCurso` (permisos) → `validarImportacion` con una sola fila (mismas reglas: nombre, edad 4–15, duplicado en curso, choque nombre+fecha) → nuevo helper común `crearEstudiantePendiente()` (username interno desambiguado + `nombre_norm` + PIN inicial DDMMAA + código de emergencia + código de activación hasheado). `importar/confirmar` se refactorizó para usar el mismo helper, así que **no hay dos caminos de creación**: el resultado es idéntico (Pendiente → código individual → activación → Activado). Frontend: `AgregarEstudiante.jsx` (modal de 2 pasos: formulario → código en claro una sola vez), botón "➕ Añadir estudiante" junto a "📥 Importar desde Excel" en los paneles docente y admin; `SectionCard` acepta ahora una o varias acciones. **Sistema legacy de invitaciones intacto** (generación, `/registro` por código y datos históricos): la consolidación de la UI queda pendiente de decisión de Fabrizio. Verificado: build limpio + `node --check`. **E2e en producción tras el deploy.**

> **SPEC-014 Fase 6 — Estado de acceso y regeneración en los paneles (2026-07-18):** `GET /api/docente/mis-estudiantes` ahora devuelve TODOS los estudiantes del docente — los de sus cursos asignados (`docente_curso`, incluye importados por Excel aún pendientes) **más** los registrados con sus invitaciones (unión, no sustitución: nadie desaparece si cambian las asignaciones) — con `pendiente` (derivado: `codigo_acceso_hash` presente y sin usar) y `codigo_acceso_pista`. Los chequeos de propiedad del docente (`resetear-pin`, `estudianteDelDocente`/ficha/retroalimentación) usan el mismo criterio doble. `GET /api/admin/estudiantes` añade los mismos campos (aditivo). UI docente ("Mis Estudiantes"): la lista principal pasa a llamarse **"Mis estudiantes"** y va primero; columna **"Acceso"** con badge Pendiente (con pista `ABC…`) / Activado; pendientes solo ofrecen "Regenerar código" (sin ficha/restablecer, que no aplican antes de activar); el código nuevo se muestra UNA vez en un modal; "Mis códigos emitidos" → "Códigos de invitación emitidos". UI admin: columna "Acceso" + botón regenerar (🔑) en pendientes con el mismo modal. `estudiantesService.regenerarCodigo()` consume el endpoint ya existente de F2. Los estudiantes pre-SPEC-014 (sin `codigo_acceso_hash`) aparecen como Activado. El refresco tras importar ya estaba cableado (`onImportado`). Verificado: `npm run build` limpio + `node --check` en rutas. **Sin MySQL local: e2e (importar→Pendiente, activar→Activado, regenerar invalida el código anterior) en producción tras el deploy.**

> **Acordeón en los editores de Misión/Memorama/Línea/Completar (2026-07-18, solo frontend; feedback de Fabrizio — campos de edición demasiado grandes):** los 4 editores adoptan el **mismo acordeón del quiz** (clases `editor-item*` de `editorQuiz.css`, ahora compartidas): cada desafío/pareja/evento/frase se colapsa a una fila (número o ✓ de completo + resumen del texto + chevron) y **solo se expande el que se edita** (single-open, "Escribir" abre el ítem nuevo). Misión: narrativa/pregunta/opciones A-B-C con el layout `editor-alt-row` del quiz + pista + Eliminar en el cuerpo. Línea del tiempo: Subir/Bajar como botones con texto en el cuerpo (el acordeón sigue al ítem movido). Completar: frase + opciones con radio numerado estilo quiz. Sin cambios de datos ni de lógica: mismos handlers, mismo `configuracion_json`. Verificado: `npm run build` limpio. **Sin MySQL local: revisión visual/e2e de los 4 editores (incl. móvil) en producción tras el deploy.**

> **SPEC-013 — Configuración en popup (2026-07-17, solo frontend; feedback de Fabrizio sobre la ubicación):** la configuración deja de vivir incrustada en el editor: nuevo botón **"⚙ Configuración"** en la barra de acciones (junto a "Vista previa") que abre un popup (`ModalConfigActividad`, wrapper de `ModalPanel`). Quiz: mezclas + preguntas por intento (se quitó el `<details>` de dentro del editor). Memorama/Línea/Completar y Misión: dificultad + curso (los mismos valores del formulario de generación — siguen también ahí porque la IA los usa al generar; el popup permite ajustarlos después sin volver arriba). Clasificador: sin botón (no tiene configuración — coherente con "lo que no aplica no aparece"). CSS `.config-actividad-*` reutilizando las clases `quiz-config-opcion/select/ayuda`. Verificado: build limpio, lint sin errores nuevos. **Revisión visual en producción tras deploy.**

> **SPEC-013 — "Preguntas por intento" (mini banco por quiz) (2026-07-17, solo frontend; ampliación aprobada de la spec §6):** un quiz puede guardar un pool grande (p. ej. 30 preguntas) y mostrar una **muestra aleatoria** por intento (p. ej. 5): campo aditivo `preguntas_por_intento` en `configuracion_json` (0/ausente = todas — quizzes viejos intactos). `QuizInteractivo.jsx`: nueva `muestrear()` (índices barajados + orden relativo conservado, para respetar `mezclar_preguntas` apagado); el marcador y el XP del intento se calculan sobre la muestra, no sobre el pool. `GeneradorQuiz.jsx`: helper `xpRecompensaDe()` — **`xp_recompensa` se publica según las preguntas mostradas por intento** (el servidor capa el abono con ese valor; sin esto un pool de 30 mostrando 5 regalaría XP por 30) — aplicado en borrador/PATCH/publicar. `EditorQuiz.jsx`: selector "Preguntas por intento" (Todas / 3 / 5 / 10 / 15 / 20, filtrado por el tamaño del pool) dentro de "⚙ Configuración" + texto de publicar que dice "N guardadas · cada intento muestra M al azar · recompensa M×100 XP". Decisión de producto: el Banco de Preguntas global NO se conecta al juego; es autoría (llena el pool). Verificado: build limpio; prueba aislada (300 muestras 5-de-30: tamaño/duplicados/orden ok, 299 combinaciones distintas; XP correcto en 5 casos borde incl. por-intento &gt; pool y quiz viejo sin campo). **E2e (repetir quiz como estudiante y ver preguntas distintas) en producción tras deploy.**

> **SPEC-013 Fase 2.1 — Correcciones tras primera prueba en producción (2026-07-17, solo frontend; feedback de Fabrizio):**
> - **"Añadir con IA" ahora AÑADE de verdad (no reemplaza):** en memorama/línea/completar, la entrada 🤖 del menú pide una tanda al endpoint existente `/api/ia/generar` sobre el mismo tema (tema del formulario o título de la actividad), descarta duplicados por texto (`firmaItem`) y ANEXA hasta N ítems (sub-paso "¿Cuántas más?" 1/2/3, respetando `MAX_ITEMS`), guardándolos también en el banco. En el Clasificador, 🤖 FUSIONA: a las categorías con el mismo nombre les suma elementos nuevos (sin duplicar) y agrega categorías desconocidas enteras. Nada de lo escrito se pierde.
> - **Confirmación antes de regenerar:** el formulario superior "Generar con IA" (que sí reemplaza el contenido abierto) ahora pide confirmación con `window.confirm` cuando hay contenido en el editor (genéricos, clasificador y misión), explicando que lo actual queda en "Últimos generados".
> - **Botón "✕ Cerrar" en los 4 editores** (quiz, genéricos, clasificador, misión): cabecera "Editando: <título>" + cerrar sin perder nada (el borrador vive en BD); en clasificador resetea a editor en blanco con confirmación si no hay título (único caso sin respaldo). CSS `.editor-abierto-head`/`.editor-cerrar-btn` en `dashboard.css`.
> - **"⚙ Configuración" del quiz reposicionada:** el acordeón de los toggles "Mezclar" se movió de después del botón Publicar (fuera del editor) a DENTRO de `EditorQuiz`, entre la barra Agregar y la barra de publicar (props nuevos `mezclarPreguntas`/`mezclarRespuestas`/`onCambiarMezcla`/`onCerrar`).
> - Verificado: `npm run build` limpio; lint sin errores nuevos. **E2e de "Añadir con IA" incremental (calidad/deduplicación con IA real) en producción tras deploy.**

> **SPEC-013 Fase 2.5 — "Deshacer cambios" + cerrar por re-clic en el historial (2026-07-17, solo frontend; pedido de Fabrizio tras probar en producción):** (1) Los 4 componentes de creación (Quiz, Clasificador, Misión, genéricos) guardan una **foto (snapshot en estado React)** de la actividad tal como quedó al generarla, abrirla del historial o guardarla/publicarla; el botón nuevo **"Deshacer cambios"** (en la barra de acciones, junto a Configuración/Vista previa; deshabilitado con motivo cuando no hay cambios) restaura esa foto y **re-sincroniza el borrador en BD** (el autoguardado ya había persistido las ediciones). Tras publicar, lo publicado pasa a ser la nueva base del deshacer. En Clasificador, `snapshot=null` representa el editor en blanco (comparación semántica, sin efecto de montaje). (2) `HistorialActividades` acepta `onCerrar`: **volver a hacer clic en la entrada ya abierta la cierra** (toggle), con tooltip "Clic para cerrar este editor"; cableado en los 4 editores hacia su `cerrarEditor` existente — complementa al botón "✕ Cerrar" que Fabrizio consideraba poco visible. Verificado: `npm run build` limpio; lint sin errores nuevos (solo los preexistentes de MASTER_PLAN §3.12). **Sin MySQL local: e2e (editar→deshacer→ver el borrador restaurado en BD; toggle del historial) queda para producción tras el deploy.**

> **SPEC-013 Fase 2 — Botón único "➕ Agregar…" con menú por acciones (2026-07-17, solo frontend):** `BarraAccionesEditor` gana el prop `agregar` (aditivo; la API `acciones` de SPEC-012 sigue igual y la usa "Vista previa"): un botón primario "➕ Agregar preguntas/parejas/eventos/frases/categorías/desafíos" abre un menú con encabezado "¿Cómo deseas agregarlas?" y entradas ricas (emoji + título + subtítulo) según el tipo — **lo que no aplica ya NO aparece** (fin de los botones deshabilitados con tooltip de SPEC-012 §F3). Quiz: Escribir / Generarlas automáticamente (sub-paso "¿Cuántas preguntas?" 1/3/5, misma `añadirConIA`) / Reutilizar (SelectorBanco). Genéricos: Escribir / Generar de nuevo / Reutilizar (con tope MAX_ITEMS deshabilitando entradas con motivo en el subtítulo). Clasificador y Misión: solo Escribir + Generar (sin banco, SPEC-013 §3); la entrada "Generar" hace scroll suave + focus al formulario de IA superior (acción real en vez de botón muerto; la Fase 7 lo convertirá en modal). Sin ningún cambio de lógica de negocio: mismos handlers de siempre detrás del nuevo menú. CSS nuevo `.barra-agregar-*` (tokens, menú `min(320px, 86vw)` para móvil). Verificado: `npm run build` limpio; lint sin errores nuevos (solo los 2 `react-refresh` preexistentes, MASTER_PLAN §3.12). **Sin MySQL local: la revisión visual/e2e de los 6 editores (menú, sub-paso, móvil) queda para producción tras el deploy.**

> **SPEC-013 Fase 1 — Toggles "Mezclar" del Quiz (2026-07-17, solo frontend):** el barajado del quiz (implementado el mismo día como comportamiento fijo) pasa a ser configurable por quiz: dos flags aditivos en `configuracion_json` (`mezclar_preguntas`, `mezclar_respuestas`), **ausentes = true** (los quizzes publicados antes de los flags siguen barajando igual — regla §6.12 intacta). `QuizInteractivo.jsx` los lee de `reto.configuracion` (cubre estudiante y vista previa); `GeneradorQuiz.jsx` gana la sección colapsada "⚙ Configuración" (`<details>` nativo) con los 2 checkboxes y el copy congelado de la SPEC-013 §4.10, persiste los flags en cada escritura (borrador nuevo, PATCH del debounce, publicar, reabrir del historial, vista previa) vía el helper `configuracionDe()`. CSS con tokens en `dashboard.css` (`.quiz-config*`). Verificado: `npm run build` limpio + prueba aislada de los 4 modos de mezcla (flags apagados = salida idéntica 100/100; cada flag actúa solo sobre lo suyo; lectura de flags correcta en 6 combinaciones; original sin mutar). **Sin MySQL local: e2e (toggle → publicar → jugar como estudiante) queda para producción tras el deploy.**

> **Quiz: preguntas y alternativas barajadas por partida (2026-07-17, solo frontend):** `QuizInteractivo.jsx` ahora baraja el orden de las preguntas y, dentro de cada una, el orden de las alternativas remapeando las letras (la correcta apunta siempre a su nueva letra) en cada montaje del reproductor — mismo patrón `mezclar` (Fisher–Yates) que ya usaban memorama/línea/completar/clasificador, definido localmente para evitar un import circular con `juegosComunes.jsx`. La configuración del reto NUNCA se muta (copias nuevas); el editor del docente y `configuracion_json` conservan el orden canónico. Cubre estudiante, misiones y vista previa del docente (todos montan el mismo reproductor). Casos borde manejados: alternativas con letras faltantes o vacías se compactan a letras contiguas desde A; preguntas con <2 alternativas reales no se barajan; `correcta` en minúscula/con espacios se normaliza como ya hacía `PreguntaCard`. Verificado: `npm run build` limpio + prueba aislada de la lógica (500 corridas: sin pérdida de alternativas/justificación/`_banco_id`, correcta siempre remapeada, original sin mutar). **Sin MySQL local: e2e (jugar quiz como estudiante, repetirlo y ver otro orden) queda para producción tras el deploy.**

> **SPEC-012 — Editores de juegos consistentes (2026-07-16, Fases 1-3 en código; solo frontend, sin migración):**
> - **Fase 1 — Misión narrativa editable:** `GeneradorMision.jsx` pasó de vista de solo lectura a editor completo — título, introducción, final y, por desafío: narrativa, pregunta, alternativas A/B/C con radio de correcta y pista; añadir/quitar desafíos (respetando el mínimo de 3 del validador); selectores de **dificultad** y **curso** (como los juegos genéricos); botón **Guardar borrador**. Los borradores se sincronizan con PATCH (SPEC-011); un reto publicado no se edita en caliente. `_tema`/`_tematica` siguen como metadatos aditivos en la configuración.
> - **Fase 2 — Vista previa como estudiante:** nuevo `PreviewJuegoModal` (compartido) monta el reproductor REAL del tipo dado con la configuración del borrador EN MEMORIA (sin publicar). Bandera nueva `soloPrueba` en los 6 reproductores (QuizInteractivo, MisionNarrativa, JuegoDragAndDrop y `useRecompensa` para memorama/línea/completar): el juego se vive igual, pero NO se otorga XP ni se llama `POST /api/progreso` (puntaje simulado + toast "Modo prueba"). Sin la bandera, comportamiento idéntico al de siempre (aditiva, default false).
> - **Fase 3 — Barra de acciones unificada:** nuevo `BarraAccionesEditor` (+ css) con el set estándar **[+ Manual] [+ Del banco] [+ Con IA] [Vista previa]** en los 6 editores; lo que no aplica NO desaparece: se deshabilita con tooltip que explica por qué (misión/clasificador + banco, IA incremental en genéricos/misión/clasificador). Bonus: los juegos genéricos ganaron "+ ítem manual" (pareja/evento/frase vacía editable en la lista), que antes no existía. "Guardar borrador" explícito queda en misión/genéricos; quiz/clasificador autoguardan (SPEC-011).
> - Verificado: `npm run build` limpio tras cada fase; lint sin errores nuevos (los `set-state-in-effect` señalados están dentro de los efectos de recompensa preexistentes, patrón documentado en MASTER_PLAN §3.12). **Sin MySQL local: e2e (editar misión, vista previa sin XP, barra en los 6 editores, responsive móvil) queda para producción tras el deploy.** Spec: `docs/specifications/SPEC-012-Editores-Consistentes.md`.

> **"Mi Banco de Preguntas" (SPEC-010 Fase 3) + auditoría de sincronización (2026-07-16, solo frontend; endpoints ya existían):** nueva entrada **"Banco de preguntas"** en el sidebar del docente (grupo Enseñanza): `src/pages/docente/BancoPreguntas.jsx`. StatCards reales (total/activas/archivadas/IA), filtros por estado/materia/tipo + búsqueda (TablaPro), y por fila: **editar** (modal con campos por tipo — quiz/memorama/línea/completar — y metadatos tema/dificultad/etiquetas, con validación cliente espejo de los validadores del servidor), **duplicar**, **archivar/reactivar** y **eliminar** (el servidor archiva si tiene uso registrado; la UI lo explica). Tras cada escritura se relee la lista de la API (§6.11). Editar una pregunta NUNCA toca actividades ya creadas (snapshot) — está explícito en la UI. **Auditoría de sincronización entre apartados (escenarios reales verificados en código):** archivar/eliminar en el banco → desaparece de "Añadir del banco" (ese modal solo lista `aprobada` y re-consulta al abrirse); publicar un borrador INCOMPLETO desde la Biblioteca → el server ahora lo rechaza con mensaje claro (validación en PATCH borrador→publicado); eliminar desde la Biblioteca un borrador abierto en un generador → el PATCH del debounce falla silencioso y Publicar crea fila nueva vía upsert (solo re-usa filas vivas); borradores jamás visibles para estudiantes (`GET /api/retos` filtra `publicado`); insertar del banco registra uso (`POST /api/banco/uso`) y el contador se refleja en la vista de gestión. Verificado: `npm run build` limpio; lint sin errores nuevos. **Sin MySQL local: e2e en producción tras deploy.**

> **SPEC-011 — localStorage deja de ser fuente de verdad (2026-07-16, Fases 1 y 2 en código; sin migración de BD):**
> - **Auditoría completa de localStorage** (inventario en la spec): sesión JWT y cachés de lectura (materias/institución/XP) se quedan — son usos correctos; los infractores eran los historiales de borradores (única copia local) y el sistema viejo de logros.
> - **Fase 1 — borradores en BD:** los 6 generadores (quiz, misión, clasificador, memorama, línea, completar) ahora crean el reto como `borrador` en `retos` AL GENERAR; el historial "Últimos generados" se lee de `GET /api/retos/gestion` (badge Publicado/Borrador ya no puede mentir; los borradores sobreviven a cambios de navegador y aparecen también en la Biblioteca); abrir una entrada trae el detalle con `GET /api/retos/:id`; eliminar = Papelera (recuperable). Ediciones de borradores se sincronizan con `PATCH` (debounce 1.5 s); un reto PUBLICADO nunca se edita en caliente (los cambios quedan en memoria hasta re-publicar vía el upsert del POST, semántica de siempre). **Server:** `POST /api/retos` ya no exige configuración completa para `estado='borrador'` (se valida SIEMPRE al publicar, incluido `PATCH borrador→publicado`); `PATCH /api/retos/:id` acepta `titulo` (con chequeo de unicidad viva) y `configuracion` SOLO en borradores. Degradación sin red: se edita en memoria y localStorage queda como caché offline del historial (`edu_historialRetos`). La misión guarda `_tema`/`_tematica` como metadatos aditivos en su configuración para reabrirse con parámetros. El clasificador crea su borrador recién cuando hay título (sin filas basura).
> - **Fase 2 — logros viejos retirados:** `verificarLogros`/`getLogros`/`CATALOGO_LOGROS` y las claves `edu_logrosObtenidos`/`edu_actividades` se eliminaron de `gamificationService`; los 4 reproductores ya no muestran el toast de fallback (el toast real viene de `nuevas_misiones` del servidor, SPEC-007 en producción); el conteo de premios del Home usa solo el servidor. El login limpia las claves huérfanas (logros e historiales locales viejos). No se tocó el motor de misiones ni `POST /api/progreso` (§10 intactos).
> - Verificado: `npm run build` limpio; `node --check` de retos.js OK; lint sin errores nuevos (solo los patrones preexistentes de MASTER_PLAN §3.12). **Sin MySQL local: la verificación e2e (generar→borrador en BD, sincronización, abrir desde otro navegador, publicar, papelera, validación al publicar borradores incompletos) queda para producción tras el deploy.** Spec: `docs/specifications/SPEC-011-Adios-LocalStorage-Como-Verdad.md`.

> **Historial "Últimos generados" con paginación en todos los juegos (2026-07-16, solo frontend; sin migración):** el historial local de borradores (localStorage) guardaba solo las últimas **3** entradas por materia; ahora conserva **15** y se muestra **paginado de a 5** (controles ‹ › + "Página X de Y", solo visibles si hay más de una página; la página se auto-corrige si la lista encoge al eliminar). El cambio vive en el componente/hook compartido `HistorialActividades`, así que aplica de una vez a los 6 flujos (quiz, clasificador, misión narrativa, memorama, línea del tiempo, completar). **De paso, el GeneradorQuiz se migró a ese hook compartido** (tenía ~50 líneas duplicadas de la misma lógica con tope 3 propio); su clave `edu_historialQuizzes` se conserva, así que los historiales existentes de los docentes no se pierden. Auditoría del apartado: lint limpio en los archivos tocados (se corrigió un `_banco_id` sin uso; los `only-export-components` restantes son el patrón preexistente de MASTER_PLAN §3.12); verificado que los 4 consumidores del hook usan la misma API y que las entradas viejas del quiz (forma `tema`/`cantidad`) se renderizan igual con el componente compartido. Verificado: `npm run build` limpio.

> **Banco de Preguntas extendido a memorama/línea del tiempo/completar + fix contador de uso (2026-07-16, frontend + backend; el backfill de initDb.js ahora cubre los 4 tipos):**
> - **Selector genérico:** `SelectorBancoQuiz` se generalizó a `src/components/juegos/SelectorBanco.jsx` (prop `tipo`; textos por tipo; estilos propios en `selectorBanco.css`). El quiz lo usa con `tipo="quiz"`; el filtro de dificultad solo aparece en los juegos genéricos (los quiz no la usan; los otros sí la guardan).
> - **Los 3 juegos genéricos** (`GeneradorActividadIA`: memorama, línea del tiempo, completar) ahora alimentan el banco automáticamente (ítems de IA al generarse + al publicar los que falten, mismo patrón que el quiz, con `dificultad` y `tema`) y tienen botón **"Añadir del banco"** con inserción en lote. La inserción respeta el **máximo de ítems del validador del servidor** (memorama 10, línea 8, completar 8): si no caben todos, inserta los primeros y avisa cuántos quedaron fuera; el botón se deshabilita al llegar al tope.
> - **Bug corregido — `veces_utilizada` nunca se incrementaba:** la UI mostraba "usada 0 veces" siempre y el DELETE decidía archivar/borrar según un contador muerto. Nuevo `POST /api/banco/uso` (ids en lote; el docente solo afecta sus materias) llamado al insertar del banco; fallo no bloqueante.
> - **Backfill ampliado:** `migrarBackfillBanco` ahora extrae también parejas/eventos/frases de retos existentes (validación y enunciado por tipo, dedupe por materia+tipo+enunciado, `dificultad` heredada del reto). Mismo candado `origen='backfill'`.
> - **Fuera del banco a propósito:** Clasificador (sus elementos dependen de las categorías de cada actividad, no son ítems autónomos) y Misión Narrativa (historia completa, no colección de ítems). Anotado como posible mejora futura en MASTER_PLAN si se pide.
> - Verificado: `npm run build` limpio; `node --check` de initDb.js y bancoPreguntas.js OK; los reproductores acceden a los ítems por campos nombrados, así que el metadato `_banco_id` es inocuo. **Sin MySQL local: backfill, contador de uso e inserción e2e quedan para verificarse en producción tras el deploy.**

> **Banco de Preguntas del Quiz: guardado siempre automático + backfill histórico (2026-07-16, iteración sobre el punto anterior; migración 012 se aplica sola vía initDb.js):** tras probar el primer refinamiento, se simplificó más: **se quitó el toggle** "Guardar también en mi Banco" — ahora el guardado en el banco es SIEMPRE automático (IA al generarse, manuales al publicar), sin decisión extra del docente ("MVP first": menos UI, mismo resultado). Se quitó también el **filtro de dificultad** del modal "Añadir del banco" (los quiz no se clasifican por dificultad; ese campo nunca se llenaba y el filtro no filtraba nada real). Se agregó un checkbox maestro **"Seleccionar todo (N)"** sobre la lista, para marcar/desmarcar de un clic todas las preguntas visibles bajo los filtros activos, sin perder el control de revisión antes de insertar (se decidió explícitamente NO pre-seleccionar todo por defecto: riesgo de insertar preguntas sin que el docente se dé cuenta). **Backfill (migración `initDb.js` → `migrarBackfillBancoQuiz`, idempotente vía marca `origen='backfill'`):** las preguntas de quizzes publicados ANTES de que este sistema existiera solo vivían en `retos.configuracion_json` y nunca entraron al banco; el backfill las copia una sola vez a `banco_preguntas` (dedupe por `materia_id` + enunciado, solo ítems con forma válida). Verificado: `npm run build` limpio, `node --check server/initDb.js` OK. **Sin MySQL local: el backfill y el flujo e2e completo quedan para verificarse en producción tras el deploy.**

> **Refinamiento UX del Banco de Preguntas en el editor de Quiz (2026-07-16, solo frontend; sin migración):** el banco pasa de "lugar donde también se crean preguntas" a **repositorio de reutilización**. (1) El modal "Añadir del banco" (`SelectorBancoQuiz`) ahora carga de entrada TODAS las preguntas aprobadas de quiz del docente (todas sus materias, no solo la actual) y las filtra localmente por búsqueda, **materia** (por defecto la actual), **tema** y dificultad — opciones de materia/tema derivadas de lo cargado, sin fetch extra; selección múltiple con checkboxes e inserción en lote ya existían y se conservan. (2) Nuevo toggle **"Guardar también en mi Banco"** (activado por defecto) en la barra del editor: las preguntas generadas con IA (generación inicial y "Añadir con IA") se guardan en el banco al llegar, y las manuales al publicar el quiz (momento en que ya están completas); las que ya tienen `_banco_id` no se re-guardan (sin duplicados), y un fallo al guardar en el banco nunca bloquea generar/publicar. Verificado: `npm run build` limpio. **Nota: este toggle se retiró en la iteración siguiente (ver entrada arriba) — el guardado quedó siempre automático.**

> **Fix barra de XP del estudiante (2026-07-10, solo frontend; sin migración):** el Home del estudiante mostraba el XP/nivel/premios leyendo `gamificationService.getResumen()` (caché de `localStorage`) en cada render y el resumen de misiones se cargaba **una sola vez al montar** — React no re-renderiza cuando cambia `localStorage`, así que tras completar una actividad la barra no reflejaba el cambio hasta recargar la página. Ahora la cabecera del Home usa la **verdad del servidor** (`resumen` de `GET /api/misiones`: XP, nivel, racha y premios reales; la caché local solo se usa como respaldo si el servidor aún no responde o falta la migración 009) y ese resumen + el progreso se **releen al entrar al Home** (`pagina === ''`) y **cada vez que se completa una actividad**: los 6 reproductores (Quiz, Clasificador, Misión narrativa, Memorama, Línea del tiempo, Completar) reciben un nuevo prop `onCompletado` que dispara `refrescarProgreso()` cuando el servidor confirma el `POST /api/progreso`, cerrando la carrera si el niño vuelve al Home antes de que el guardado termine. Ajuste UX: chip de racha 🔥 en la cabecera cuando `racha_actual > 0`. Verificado: `npm run build` limpio; lint sin errores nuevos (los `set-state-in-effect` restantes son los ya documentados en MASTER_PLAN §3). e2e (evaluación real de XP/racha/misiones) queda para producción por no haber MySQL local.

> **SPEC-009 — Asignación de cursos a docentes (2026-07-10, migración 010 se aplica sola vía initDb.js):** antes cualquier docente elegía cualquier curso al invitar; ahora el **admin asigna** qué curso(s) maneja cada docente (tabla `docente_curso`, muchos-a-muchos como `docente_materia`) y el docente solo puede invitar a SUS cursos. "Empezar en limpio": sin precarga (los docentes existentes quedan sin cursos hasta que el admin los asigne). Backend: `GET /api/admin/docentes` devuelve `cursos`; `POST/PUT /api/admin/docentes` aceptan `curso_ids`; `GET /api/cursos` filtra por docente; `POST /api/docente/invitaciones` exige curso_id asignado (403 si no) — se eliminó el texto libre; conteo "Docentes" del módulo Cursos = asignados; RESET vacía también `docente_curso`. Frontend: asistente de alta con paso "Cursos que gestionará" (`SelectorCursos`), ficha con `ChipsCursos`, modal "Editar asignaciones" (materias + cursos). Riesgo: tras el deploy los docentes actuales no podrán invitar hasta que el admin los asigne. Spec: `docs/specifications/SPEC-009-Asignacion-Cursos-Docente.md`.

> **Fix "Generar códigos" del docente (2026-07-10):** la página "Mis Estudiantes" nunca renderizaba `errorMaterial`, así que si el docente no elegía curso (o la API fallaba) el botón parecía muerto. Ahora muestra el error; y si no hay cursos disponibles, un mensaje lo explica en vez de un desplegable vacío.

> **Auditoría integral pre-tesis (2026-07-10, solo frontend + doc; sin migración):**
> - **Consistencia XP/premios del estudiante:** la tarjeta "Mis premios" del Home usaba `gami.totalLogros` (contador viejo de `localStorage`, máx 5, local al dispositivo) mientras la página "Mis Premios" usa las misiones del servidor (SPEC-007). Ahora el Home lee `resumen.completadas` de `GET /api/misiones` y cae al conteo en caché solo si el servidor no responde (pre-migración 009): un solo número consistente. El sistema viejo de logros (`gamificationService.verificarLogros`) se **conserva a propósito** como fallback de toast en los 4 reproductores hasta que la migración 009 despliegue `nuevas_misiones`.
> - **Nombre institucional centralizado:** el literal por defecto estaba duplicado en Login, Home admin y Home estudiante. Nuevos `NOMBRE_INSTITUCION_DEFECTO` + `nombreInstitucion()` en `institucionService`; los 3 consumidores los usan.
> - **Código muerto / lint:** eliminado `materiaSugerida` (useMemo sin uso) y comentados 3 `catch {}` vacíos. Errores de ESLint 33 → 29 (los 29 restantes son los patrones `set-state-in-effect` ya documentados en MASTER_PLAN §3).
> - **SPEC-008 — Sistema RESET ("Restablecer aplicación", 2026-07-10, activado a petición explícita):** `server/routes/adminReset.js` montado en `server.js` (`/api/admin/reset`) + botón real en el módulo Institución ("Zona peligrosa", visible solo si `authService.esPrincipal()`). Solo Administrador Principal (`soloAdminPrincipal` revalida en servidor), modal de doble confirmación (advertencia → escribir la palabra RESET), backup JSON automático antes de borrar (`server/backups/`, en `.gitignore`), transacción con `DELETE` (no `TRUNCATE`) y rollback ante error, auditoría del propio reset. **Sigue protegido por la variable de entorno `RESET_HABILITADO`** (default `false` en `.env.example`): sin definirla en `true` en Render, el botón existe pero el servidor responde 403 — es la salvaguarda final antes de un borrado real. Activado sin poder probarse contra una BD de pruebas (no hay MySQL local); riesgo aceptado explícitamente por Fabrizio tras confirmar que las migraciones 002–009 ya estaban en Aiven. Spec con el paso exacto de habilitación: `docs/specifications/SPEC-008-Sistema-Reset.md`.
> - **Pospuesto (documentado):** perfiles públicos + banners equipables + récords (feature grande con cambios de BD, fuera de alcance pre-tesis).
> - Verificado: `npm run build` limpio; `node --check` del router de reset OK.
> - **Corrección post-auditoría (confirmado en producción real):** las migraciones 002–009 **SÍ están desplegadas en Aiven** (Render ya reinició el proceso tras los últimos push, y `initDb.js` las aplicó solas al arrancar, como está diseñado). La nota "pendiente de deploy" que arrastraban las specs anteriores estaba desactualizada — confirmado viendo el panel "Mis Premios" en vivo con las 46 misiones reales cargadas. Se corrige aquí y se marca cada spec abajo.

> **SPEC-007 — Sistema de Misiones y Progresión / Fase 5.5 (2026-07-10, Fase 1 implementada en código; migración 009 a Aiven se aplica sola vía initDb.js al deploy):**
> - **Reemplaza los logros de `localStorage`** (5 hardcodeados, no compartidos entre dispositivos) por un sistema **server-backed y escalable a cientos de misiones desde BD** (nada hardcodeado en el front).
> - **BD (migración `009-sistema-misiones.sql` + reversa, idempotente en `initDb.js`):** racha en `estudiantes` (`racha_actual`/`racha_maxima`/`ultima_fecha_actividad`, actualizada en la transacción de `POST /api/progreso` con fecha local UTC-5); catálogo `misiones` (categoría, tier, `tipo_objetivo`, `objetivo_meta`, `objetivo_filtro` JSON, `requiere_mision_id` para desbloqueo, recompensas XP/insignia/banner, horizonte, `activa`); progreso `mision_estudiante`.
> - **Motor `server/lib/misiones.js`:** `evaluarMisiones()` con **registro de evaluadores** por `tipo_objetivo` (11 tipos: actividades_completadas/perfectas/ia, mision_narrativa, tipos_jugados, materias_distintas, xp_total, nivel_alcanzado, racha_dias, ranking_top, insignias_categoria). Todo se calcula desde datos REALES (`progreso_estudiante`/`retos`/`estudiantes`); idempotente; otorga XP de recompensa una sola vez. Se evalúa al ESCRIBIR (dentro de la transacción de progreso → `nuevas_misiones` en la respuesta para el `LogroToast`) y al LEER (`GET /api/misiones`, cubre ranking).
> - **Seed único `server/lib/misionesSeed.js`:** 46 misiones en 8 categorías (📚🏆🔥🤝🎯🚀⭐🤖), cadenas Bronce→Diamante con umbrales/horizontes escalonados (5 min → 6 meses). Precisión = "actividades perfectas" (porcentaje=100). Aplicado idempotente por `initDb.js` (`ON DUPLICATE KEY UPDATE` por `clave`; cadena `requiere` resuelta por clave). Agregar cientos más = agregar filas, sin tocar el motor ni migrar (salvo un `tipo_objetivo` nuevo → una entrada en el registro).
> - **Frontend:** `src/services/misionesService.js` (identidad visual de categorías/tiers, presentación) + `src/pages/estudiante/PanelMisiones.jsx` (+ `misionesPanel.css`): la página **Mis Premios** del estudiante pasó de los 5 logros de `localStorage` al panel de misiones moderno (resumen nivel/XP/racha/completadas, filtros por categoría, tarjetas con barra de progreso, %, siguiente recompensa, tiempo estimado, tier y estado bloqueada/disponible/completada). Los 6 reproductores anuncian "¡Misión completada!" vía `nuevas_misiones` (Quiz, juegosComunes → Memorama/Línea/Completar, Clasificador, Misión narrativa).
> - **Progreso 100% automático** (sin botón de "reclamar"): se calcula al jugar y al abrir el panel. Los logros viejos de `localStorage` no se migran; el progreso real se **recalcula desde `progreso_estudiante`**, así un estudiante con historial recupera sus misiones.
> - **Fase 2 (2026-07-10, también en código):** **Docente** — `GET /api/docente/misiones` (estadísticas AGREGADAS de solo lectura de sus estudiantes: avance por categoría, top de misiones, promedio; el admin ve el total institucional), nuevo apartado "Misiones" en el panel docente (`src/pages/docente/MisionesDocente.jsx`, StatCards + barras por categoría + top, nunca modifica). **Admin** — router `server/routes/adminMisiones.js` (`GET/POST/PUT /api/admin/misiones` + `PATCH /:id/activa`) protegido con el permiso `materias` (contenido académico; sin migrar el modelo de permisos), validación de `tipo_objetivo` contra el registro del motor, auditoría en cada cambio; módulo `ModuloMisiones` en el panel admin (TablaPro + ModalPanel: activar/desactivar, editar recompensas/umbral/textos, crear misiones nuevas con clave-slug, filtro JSON y cadena de desbloqueo). Entrada "Misiones" en el sidebar admin (Gestión Académica) y docente (Mi aula).
> - Verificado: `npm run build` limpio (Fase 1 y 2), sintaxis de servidor OK, seed validado (46 misiones, claves únicas, cadenas íntegras, 11 tipos cubiertos). **Sin MySQL local: la verificación end-to-end (evaluación, racha, desbloqueo, toast, CRUD admin, stats docente) queda para el deploy con la migración 009.**
> - Spec: `docs/specifications/SPEC-007-Sistema-Misiones.md`.

> **Consistencia global de entidades / Papelera (2026-07-09, implementada en código; migración 007 se aplica sola vía initDb.js al deploy):**
> - **Bug raíz corregido:** eliminar una materia (soft-delete) la dejaba visible en el panel Docentes (JOIN sin filtro de papelera) y su nombre bloqueado por el `UNIQUE` físico ("ya existe" al recrearla).
> - **BD (migración `007-unicidad-papelera.sql` + reversa, idempotente en `initDb.js`):** el UNIQUE de `materias.nombre` y `cursos(nombre, paralelo)` se reemplaza por índices únicos funcionales sobre `IF(eliminado_en IS NULL, …, NULL)`: las filas en papelera no reservan el nombre; los duplicados activos siguen prohibidos.
> - **Backend:** todo JOIN/subquery contra `materias` filtra `eliminado_en IS NULL` (docentes del admin, resumen/detalle docente, progreso del estudiante, listado de retos para todos los roles); `puedeGestionarMateria` exige materia viva (nadie publica retos/material en una materia en papelera); la purga definitiva de estudiante (cuenta + ficha) ahora es transaccional.
> - **Política documentada:** `docs/architecture/POLITICA-ELIMINACION.md` (qué significa eliminar/desactivar/purgar por entidad y las reglas para entidades futuras).
>
> **SPEC-002 Fase 1 — Centro de Administración Institucional (2026-07-09, implementada en código; migración aplicada en Aiven):**
> - **Materias dinámicas:** catálogo en BD (`color`, `icono`, `activa`), CRUD admin (`/api/admin/materias`), módulo Materias en el panel admin. `src/constants/materias.js` **eliminado**; toda la app consume `src/services/materiasService.js` (caché memoria+localStorage, la API siempre pisa). Colores/emoji de los "mundos" de estudiante y docente vienen de la BD (style inline; las clases de tonos `-1..-5` se eliminaron del CSS).
> - **Cursos:** tabla `cursos` + `curso_id` en `estudiantes`/`invitaciones_estudiante` (el VARCHAR `curso` se conserva denormalizado), CRUD admin + `GET /api/cursos` para docentes, módulo Cursos, y el docente elige el curso de un `<select>` al generar invitaciones (ya no texto libre).
> - **Institución:** tabla singleton `institucion`, `GET /api/institucion` público + `PUT /api/admin/institucion`, módulo Institución (datos, logo con redimensionado en canvas y favicon autogenerado, colores con derivados, año lectivo, escala XP). `institucionService.iniciarInstitucion()` (main.jsx) inyecta colores en `:root` y aplica título/favicon; logo y nombre en Login y en los sidebars de los 3 paneles.
> - **TablaPro** (`DashboardWidgets`): búsqueda en cliente + paginación (10/25/50/100), aplicada a Estudiantes, Invitaciones (pendientes e historial) y Cursos.
> - **useAutoRefresh** (`src/hooks/`): polling 20 s en el panel admin, pausado con modal abierto y con la pestaña oculta.
> - Migraciones versionadas en `database/migraciones/002-admin-center.sql` (+ reversa). `initDb.js` las aplica de forma idempotente al arrancar.
> - Editores (Quiz/Clasificador) con candado anti doble publicación (botón "Publicado" hasta editar algo).

> **SPEC-006 — Centro de Trabajo Docente / Materias + IA (2026-07-09, implementada en código; migración 008 aplicada en Aiven):**
> - **BD (migración `008-centro-docente.sql` + reversa, idempotente en `initDb.js`):** `retos` gana `origen` ('manual'|'ia'), `favorito`, `dificultad`, `curso_id` (FK a cursos) y papelera propia (`eliminado_en`/`eliminado_por`); `progreso_estudiante` gana `observacion` y `revisado`.
> - **Arquitectura de IA reutilizable:** `server/lib/iaCliente.js` (cliente Gemini + reintentos, `generarJSON`), `server/lib/actividadesIA.js` (registro por tipo: schema + prompt con contexto REAL de BD —materia/nivel/competencias, curso, institución, dificultad— + normalización) y `server/lib/validadoresRetos.js` (validadores compartidos por `retos.js` y la IA). Cero prompts duplicados; agregar un juego = una entrada en el registro + validador + reproductor.
> - **Endpoints IA:** `POST /api/ia/generar` (genérico, 6 tipos, valida y NO guarda), `POST /api/ia/sorpresa` (la IA decide juego/tema/dificultad/cantidad/objetivo y guarda BORRADOR), `POST /api/ia/adaptar` (transforma una actividad existente —materia/curso/dificultad/temática/tema— manteniendo el formato; guarda copia en borrador). `/api/ia/quiz` y `/api/ia/mision` se conservan por compatibilidad delegando en el registro. Todo con `soloDocente` + materia asignada + auditoría.
> - **3 juegos nuevos 100% genéricos** (mismo pipeline `retos` → `configuracion_json` → reproductor → `POST /api/progreso`): **Memorama** (`parejas`), **Línea del tiempo** (`eventos` a ordenar; sirve para procesos/algoritmos) y **Completar espacios** (`frases` con `___` y opciones). Reproductores en `src/components/juegos/` con registro frontend `registroJuegos.jsx` (`JUEGOS_UI`): la pestaña Juegos del estudiante lista TODOS los tipos jugables y despacha por `tipo` (ya no solo clasificador).
> - **Creación docente:** componente único `GeneradorActividadIA` (tema + cantidad + dificultad + curso → vista previa editable → borrador o publicar) para los 3 juegos; el Clasificador ganó «✨ Generar con IA» que llena su editor; botón «✨ Actividad sorpresa» en la pestaña Crear.
> - **Biblioteca IA (`BibliotecaActividades` ampliada):** pestañas Todas/Borradores/Publicadas/Archivadas/**Papelera** (restaurar/purgar; la purga se rechaza si hay progreso), filtros por tipo/materia/origen ✨IA-manual/curso/dificultad/favoritas, orden «más utilizadas», ⭐ favorito, 👁 vista previa (GET `/api/retos/:id`, render de la configuración por tipo), 📊 estadísticas reales (GET `/api/retos/:id/estadisticas`: intentos, completados, promedio, mejor/peor, XP entregada — sin datos por pregunta ni tiempos: la BD no los registra), ✨ Adaptar con IA. La pestaña **Actividades** de la vista de materia reutiliza este mismo componente con `materiaId` (cero duplicados). `DELETE /api/retos/:id` es soft-delete a papelera.
> - **Libro de Calificaciones real:** TablaPro por materia con detalle por intento (`ModalPanel`): observación + revisado (`PATCH /api/progreso/:est/:reto`, docente solo sobre SUS estudiantes) y ajuste manual de XP reutilizando `POST /api/progreso` (transacción `FOR UPDATE` e idempotencia intactas: solo abona mejoras). Todo ajuste queda en Auditoría (`ajusto-progreso`).
> - Verificado: `npm run build` limpio, lint con los mismos patrones React documentados (MASTER_PLAN §3.12), app cargando en navegador.
> - Spec: `docs/specifications/SPEC-006-Centro-Trabajo-Docente.md` (la numeración SPEC-003 ya estaba ocupada por Panel Admin Fase 2).

> **Historial "Últimas actividades generadas" en todos los generadores (2026-07-10):** el patrón del GeneradorQuiz (últimas 3 entradas por materia en `localStorage`, borrador → editar → publicar) se extrajo a `src/components/juegos/HistorialActividades.jsx` (`useHistorialActividades` + `nuevaEntradaHistorial` + lista visual con clases `quiz-historial-*`) y se conectó a `GeneradorActividadIA` (memorama/línea del tiempo/completar; clave `edu_historialActividades_<tipo>`), `EditorClasificador` (la entrada se crea sola al empezar a editar, cubre también juegos manuales) y `GeneradorMision`. Toda edición devuelve la entrada a estado borrador; publicar la marca "Publicado". El GeneradorQuiz conserva su implementación original (`edu_historialQuizzes`). Fix relacionado: el `LEFT JOIN cursos` de `GET /api/retos/gestion` ahora excluye cursos en papelera (`c.eliminado_en IS NULL`) para no mostrar cursos eliminados en las actividades.

> **Endurecimiento por auditoría externa (2026-07-09):** correcciones puntuales sin cambios de esquema ni de arquitectura: (1) un docente solo registra XP (`POST /api/progreso`) de estudiantes que él invitó (misma regla que resetear PIN); (2) los estudiantes ya no acceden a retos ni material de materias desactivadas por ID directo; (3) el DELETE de curso también rechaza cursos con invitaciones pendientes vigentes; (4) nombre + paralelo del curso se validan a máx. 19 caracteres en total (límite de las columnas VARCHAR(20) denormalizadas); (5) el PUT de cursos sincroniza catálogo y denormalizados en una transacción; (6) los textos institucionales hardcodeados en Home admin y Registro ahora usan `institucionService`; (7) ESLint con globals de Node para `server/` (lint: 38 → 15 errores; los 15 restantes son patrones de React documentados en MASTER_PLAN §3). Los hallazgos que requieren migración de BD quedaron en el backlog (MASTER_PLAN §3).

> **Módulo Administradores (2026-07-09, adelantado de la Fase 2 de SPEC-002; migración aplicada en Aiven):**
> - Roles de admin: columnas `es_principal` y `activo` en `usuarios` (migración `003-administradores.sql` + reversa; `initDb.js` la aplica idempotente y garantiza que siempre exista ≥1 Principal activo, promoviendo al admin más antiguo si hace falta).
> - **Administrador Principal**: todo el sistema, incluida la institución y la gestión de administradores. **Administrador**: operación diaria (docentes, estudiantes, cursos, materias, invitaciones) sin institución ni administradores.
> - Backend: CRUD `/api/admin/administradores` + middleware `soloAdminPrincipal` (verifica rol contra la BD, no contra el token, para efecto inmediato; tolera la columna ausente pre-migración). `PUT /api/admin/institucion` ahora exige Principal. El login bloquea cuentas con `activo = FALSE` y la sesión incluye `es_principal`.
> - Invariantes en servidor: no se puede quitar el rol, desactivar ni eliminar al último Principal activo (409); nadie puede eliminarse a sí mismo.
> - Frontend: `ModuloAdministradores` (TablaPro + ModalPanel, chips de rol/estado, "(tú)" en la cuenta propia), entradas "Administradores" e "Institución" del sidebar visibles solo para el Principal (la UI oculta, el servidor protege).

> **SPEC-003 — Panel Admin Fase 2 (2026-07-09, sprint final del panel; migración aplicada en Aiven):**
> - **Permisos por administrador:** columna `usuarios.permisos` (JSON, migración `004-admin-fase2.sql` + reversa, idempotente en `initDb.js`). 9 claves en 3 grupos (Gestión Académica: docentes/estudiantes/materias/cursos; Institucional: invitaciones/institucion; Seguridad: administradores/auditoria/papelera). El Principal SIEMPRE tiene todas; `permisos = NULL` ⇒ conjunto operativo (compatibilidad con admins previos). Middleware `conPermiso(clave)` verifica contra la BD en cada endpoint `/api/admin/*`; lo estructural (rol Principal, repartir permisos, tocar Principales) sigue exigiendo Principal. El login devuelve `permisos` y la UI (sidebar, stats, accesos del Inicio) solo oculta.
> - **Auditoría:** tabla `auditoria` (sin FK, nombre denormalizado) + `registrarAuditoria()` fire-and-forget (`server/lib/auditoria.js`). Instrumentado: docente (publicar/editar quiz-clasificador-misión, subir/eliminar material, generar invitaciones, resetear PIN), estudiante (login, registro, emergencia, cambio de PIN, completar retos/XP) y todas las escrituras del panel admin. `GET /api/admin/auditoria` (permiso `auditoria`) + `GET /api/admin/auditoria/reciente` (cualquier admin). Módulo Auditoría: 3 tarjetas (Docente/Estudiantes/Administradores) con TablaPro y modal "Más detalles" (campos ausentes = "No registrado"). La "Actividad reciente" del Inicio consume los últimos 5 eventos reales (se eliminó la lista calculada de altas).
> - **Papelera:** soft-delete (`eliminado_en`/`eliminado_por` en `usuarios`, `materias`, `cursos`). Los DELETE del panel marcan (nunca borran); login, listados, ranking y vistas de docente/estudiante filtran eliminados. Restaurar devuelve el estado exacto (409 amigable si un homónimo ocupa el UNIQUE); la eliminación definitiva (`DELETE /api/admin/papelera/:tipo/:id`) aplica las validaciones de integridad que antes tenían los DELETE directos. Módulo Papelera con pestañas (Todos/Docentes/Estudiantes/Cursos/Materias/Administradores), restaurar y purga con confirmación.
> - **Sidebar agrupado:** `SidebarLayout` acepta `grupo` por ítem (rótulo + separador); el admin queda en Inicio · Gestión Académica · Gestión Institucional · Seguridad. Docente y Estudiante no cambian. En móvil (≤760px) los rótulos se ocultan.
> - Verificado: build y lint limpios, navegador en 375/768/1024/1366/1600/1920/2560 sin scroll horizontal y con footer siempre visible.

> **Rediseño UX del Home del Docente (2026-07-09, solo frontend):** el Home dejó de ser un dashboard de métricas y pasó a ser un centro de trabajo. Hero compacto (`.doc-saludo`: saludo según la hora + resumen de materias/estudiantes en una línea, sin chips). Se eliminaron las 7 StatCards, las Acciones rápidas, el banner "Crea una actividad hoy" y el acceso "Mi aula" (duplicaban navegación). Jerarquía nueva: **Tus materias** (protagonista, con aviso ámbar "Sin actividades · crea la primera" dentro de la tarjeta) → **Centro de actividad** (timeline ligera con puntos de color: verde = estudiante, azul = docente, ámbar = materia sin actividades, clicable) → **Actividad reciente** (retos recientes con filtros píldora Todo/Quiz/Misión/Clasificador) → dos tarjetas de resumen al final ("Actividad creada" y "Estado del aula"). Se retiró el aviso de borrador de quiz pendiente (vivía en el banner eliminado). Sin cambios de backend/APIs; CSS nuevo en `docentePanel.css` (se borraron `.doc-hero*` y `.doc-rapida*` huérfanos). Verificado en navegador con datos simulados y en 375px sin scroll horizontal.

> **SPEC-005 — Catálogo Inteligente de materias (2026-07-09, implementada en código; migración 006 aplicada en Aiven):**
> - **BD (migración `006-catalogo-inteligente.sql` + reversa, idempotente en `initDb.js`):** columnas nuevas en `materias`: `orden` (orden institucional, backfill = id), `descripcion`, `banner_data`, `competencias`, `nivel`, `protegida`. `activa` pasa a leerse como **Activa/Oculta** (oculta conserva actividades, ranking, materiales y progreso — garantizado desde SPEC-002/003).
> - **API:** `GET /api/materias` y `mis-materias` devuelven la identidad completa y ordenan por `orden, id` (también progreso y detalle de estudiante). `POST /api/admin/materias` acepta identidad completa + `asignar: 'todos' | [docenteIds]` (asignación al crear). `PUT /api/admin/materias/orden` reordena el catálogo en transacción. `POST /api/admin/materias/:id/asignacion` asigna/quita en masa (misma tabla `docente_materia`; el modelo de permisos no cambia). `DELETE` responde **409 si `protegida`** (solo puede ocultarse).
> - **UI (`ModuloMaterias` reescrito + `moduloMaterias.css`):** lista ordenada con flechas ▲▼ persistentes, banner (subido con canvas o autogenerado con color+emoji), chips Activa/Oculta y 🔒 Protegida, botón "Docentes" (asignación masiva con marcar/desmarcar todos), formulario con descripción/competencias/nivel/banner/protegida y asignación al crear (todos / específicos / no asignar). Advertencia al editar u ocultar una protegida; eliminar deshabilitado. `uiMateria()` expone `descripcion`/`banner`; el hero de materia del docente muestra la descripción.
> - Spec: `docs/specifications/SPEC-005-Catalogo-Inteligente.md`.
> - **Sincronización global (2026-07-09):** al crear una materia, la opción "Asignar automáticamente a todos los docentes" viene **activada por defecto** (backend ya existente: `asignar: 'todos'` con `INSERT IGNORE` sobre docentes activos, transaccional). El panel Docente re-consulta `mis-materias` al cambiar de sección (dep `pagina` en `dashboard.jsx`), así una materia recién creada/asignada aparece en Home y Materias sin recargar la página. Fuente única confirmada: `materias` + `docente_materia`; sin listas locales ni caché permanente (ver `docs/audit/Auditoria-Sincronizacion-Global-v1.md`).

> **SPEC-004 — Rediseño del Panel Docente / Épica 3 (2026-07-09, implementada en código; migración 005 aplicada en Aiven):**
> - **BD (migración `005-panel-docente.sql` + reversa, idempotente en `initDb.js`):** `retos.docente_id` (autoría, NULL en filas previas), `usuarios.foto_data` (foto de perfil) y tabla `retroalimentaciones` (observaciones privadas docente→estudiante).
> - **API nueva:** `GET /api/retos/gestion` (Biblioteca: todos los retos de sus materias con `veces_jugado`), `PATCH /api/retos/:id` (estado/descripcion/xp; archivar es soft y el estudiante solo ve `publicado`), `POST /api/retos/:id/duplicar` (copia en borrador); `GET /api/docente/resumen` (stats reales + cronología de auditoría del docente y SUS estudiantes), `GET /api/docente/estudiantes/:id/detalle`, CRUD de retroalimentaciones, `GET|PUT /api/docente/perfil` + `PUT /perfil/password` (exige la actual); `GET /api/ranking/completo` (docente/admin). `POST /api/retos` guarda `docente_id`. Todo con permisos en servidor (materia asignada / estudiante invitado por él).
> - **UI (src/pages/admin/dashboard.jsx + módulos en `src/pages/docente/`):** sidebar agrupado (Inicio · Enseñanza: Materias, Biblioteca · Mi aula: Mis Estudiantes, Ranking · Cuenta: Mi Perfil). Home = hero de bienvenida con datos del aula + StatCards (actividades/quizzes/misiones/clasificadores/materiales/XP/promedio) + acciones rápidas + Centro de Actividad (auditoría real). Vista de materia en pestañas píldora (Resumen · Crear actividad · Actividades · Material · Calificaciones) sin tocar generadores. `BibliotecaActividades` (buscar/filtrar/ordenar/duplicar/editar desc-XP/archivar/restaurar; nunca borra físico), `RankingCompleto` (posición/XP/nivel/insignias reales/última actividad, filtro por curso), `PerfilDocente` (identidad, stats, reconocimientos preparados vacíos, actividad propia; edita foto/nombre/contraseña), `FichaEstudiante` (modal desde Mis Estudiantes con retroalimentación). CSS nuevo en `src/pages/docente/docentePanel.css` con tokens existentes.
> - Insignias mostradas al docente = solo las 2 con regla real derivable de la BD (Primer Quiz, Maestro de la Materia); nada inventado.
> - Verificado: build y lint limpios, navegador en 375 y 1366 sin scroll horizontal y con estados vacíos correctos.
> - Spec: `docs/specifications/SPEC-004-Panel-Docente.md`.

> **Sidebar unificado (2026-07-09):** los tres paneles (Admin, Docente, Estudiante) usan el componente base `src/components/dashboard/SidebarLayout.jsx` (header con logo institucional + nombre, navegación `flex:1` con scroll propio, footer sticky con usuario y acciones siempre visible). El sidebar ocupa `100dvh`; el único scroll vertical del panel vive en `.contenido` (flex + sticky, sin `position: fixed`). En ≤760px pasa a barra superior con navegación horizontal. Estilos compartidos en `dashboard.css` (`.sidebar-header/.sidebar-nav/.sidebar-footer`, `.nav-item-activo` ahora global). Sin cambios de lógica, APIs ni BD.

> Polish Sprint (2026-07-07/08): identidad visual unificada en Login, Home y materias de ambos roles (tarjetas pastel por materia, héroes con gradiente, pestañas píldora, tablas con acentos de la paleta). Nombre institucional actualizado a **Unidad Educativa Fiscal Clemencia Coronel de Pincay** en toda la UI. Sin cambios de lógica, APIs ni BD.

### Backlog secundario resuelto (histórico)

1. ~~Libro de Calificaciones real~~ ✅ Hecho (2026-07-09, SPEC-006).
2. ~~UI de edición de docente~~ ✅ Hecho (2026-07-09, rediseño panel admin).
3. ~~Unificar fuente de materias~~ ✅ Hecho (2026-07-09, SPEC-002 Fase 1).
4. ~~Roles granulares y auditoría de acciones~~ ✅ Hecho (2026-07-09, SPEC-003).
